"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["1900"],{3170(e,t,n){n.r(t),n.d(t,{metadata:()=>o,default:()=>u,frontMatter:()=>r,contentTitle:()=>l,toc:()=>h,assets:()=>c});var o=JSON.parse('{"id":"m4-functions-and-modules/quiz","title":"Quiz: Functions, Modules and Reusable Automation","description":"<Quiz questions={[","source":"@site/docs/m4-functions-and-modules/quiz.mdx","sourceDirName":"m4-functions-and-modules","slug":"/m4-functions-and-modules/quiz","permalink":"/307-aipython-site/docs/m4-functions-and-modules/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Functions, Modules and Reusable Automation"},"sidebar":"courseSidebar","previous":{"title":"Lab: Functions, Modules and Reusable Automation","permalink":"/307-aipython-site/docs/m4-functions-and-modules/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m4-functions-and-modules/deep-dive"}}'),a=n(4848),i=n(8453),s=n(7517);let r={sidebar_position:3,title:"Quiz: Functions, Modules and Reusable Automation"},l="Functions, Modules and Reusable Automation \u2014 Quiz",c={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,i.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(t.header,{children:(0,a.jsx)(t.h1,{id:"functions-modules-and-reusable-automation--quiz",children:"Functions, Modules and Reusable Automation \u2014 Quiz"})}),"\n",(0,a.jsx)(s.A,{questions:[{prompt:"A function has no `return` statement anywhere in its body. What happens when you call it and try to use the result?",options:[{text:"The function raises a SyntaxError, since every function must return something",correct:!1,explanation:"Python does not require a return statement. A function with none still runs fine -- it just hands back a default value."},{text:"You get `None` back, even if the function printed something to the screen",correct:!0,explanation:"A function with no return statement returns None. Printed output is not the same as a returned value -- if you use the result of a call like this, you get None, not whatever was printed."},{text:"The function re-runs itself until it finds a value to return",correct:!1,explanation:"Python does not retry function calls looking for a return value. It runs once and hands back None if there is no return."},{text:"The last variable assigned inside the function is returned automatically",correct:!1,explanation:"Python never auto-returns a variable just because it was the last one assigned. Only an explicit return statement sends a value back."}]},{prompt:"Why prefer `find_low_memory_prod(inventory, min_memory_gb=8)` over `find_low_memory_prod(inventory, 8)` once a function has more than one or two arguments?",options:[{text:"The keyword form runs faster",correct:!1,explanation:"Keyword arguments cost nothing extra at runtime -- the benefit is entirely about readability and safety, not speed."},{text:"It names the value at the call site, so a reader does not need to check the function definition to know what `8` means",correct:!0,explanation:"This is the whole benefit: `min_memory_gb=8` documents itself. Positional-only calls force a reader to go look up the signature to know what each value means."},{text:"Positional arguments are deprecated in modern Python",correct:!1,explanation:"Positional arguments are completely normal and still widely used, especially for a function's first argument or two. Keyword arguments are a readability choice, not a requirement."},{text:"It lets you pass arguments in any order without the function needing named parameters",correct:!1,explanation:"You can already reorder keyword arguments freely -- but the function still needs its parameters named in its own definition (which they always are); nothing extra is required for this to work."}]},{prompt:"What is wrong with this function?\n\n```python\ndef add_finding(name, findings=[]):\n    findings.append(name)\n    return findings\n```",multiSelect:!0,options:[{text:"The default empty list is created once, when Python reads the `def` line -- not fresh on every call",correct:!0,explanation:"This is the core of the trap. Every call that relies on the default shares that one list object across calls."},{text:"Two calls that both rely on the default will silently share and accumulate into the same list",correct:!0,explanation:'Call add_finding("a") then add_finding("b") and the second call\'s result contains both "a" and "b" -- surprising if you expected a fresh list each time.'},{text:"The function will raise a TypeError the first time it is called",correct:!1,explanation:"It runs without error every time -- that is exactly what makes this trap dangerous. There is no crash to alert you; the data quietly leaks between calls."},{text:"The fix is to default `findings` to `None` and build a new list inside the function when it is `None`",correct:!0,explanation:"This is the standard fix: `def add_finding(name, findings=None): if findings is None: findings = []`. Now a fresh list is built on every call that needs one."}]},{prompt:"A module-level constant like `MIN_PROD_MEMORY_GB = 8` sits at the top of `data.py`, outside any function. Which statement about it is correct?",options:[{text:"Any function in that file can read it, but a function that reassigns it without saying so lets the change leak into every other function reading that same name",correct:!0,explanation:"Reading a module-level constant is fine and common. Writing to it from inside a function, without it being an explicit parameter or return value, is the trap -- the change is invisible from the call site."},{text:"It is only visible to the one function directly below it in the file",correct:!1,explanation:"Module-level names are visible to every function in that file (and to anything that imports the module), not just the function written directly beneath them."},{text:"It must be passed explicitly into every function that needs it, or Python raises a NameError",correct:!1,explanation:"A function can read a module-level name without it being passed in as a parameter. Python only raises a NameError if the name genuinely does not exist anywhere visible to that scope."},{text:"Changing its value from inside a function updates it for that function call only, then reverts",correct:!1,explanation:"There is no automatic revert. If a function reassigns a module-level name, that change persists for every later read of that name, in every function, until something changes it again."}]},{prompt:"What turns a folder of `.py` files into an importable Python package rather than just a folder that happens to contain some scripts?",options:[{text:"Naming the folder in all lowercase letters",correct:!1,explanation:"Folder casing has nothing to do with whether Python treats it as a package. Lowercase is a naming convention, not a requirement."},{text:"An `__init__.py` file inside the folder",correct:!0,explanation:"An __init__.py file is what makes a folder a proper Python package with a defined public surface, rather than an implicit namespace package."},{text:"A `pyproject.toml` file listing the folder as a package",correct:!1,explanation:"pyproject.toml configures how a project is built and installed, but it is not what makes Python's import system treat a specific folder as a package at import time."},{text:"At least one function inside the folder named `main`",correct:!1,explanation:"A function named main has no special meaning to Python's import system. It is a convention some projects use for an entry point, not a packaging requirement."}]},{prompt:"In the M4 lab, `uv run python -m platformops.inventory` had to keep working after `inventory.py` became a package (a folder). What made that possible?",options:[{text:"Renaming `report.py` to `inventory.py`",correct:!1,explanation:"Renaming a file inside the package does not give -m a way to know which file is the entry point -- Python needs a specific, exactly-named file for that."},{text:"Adding a `__main__.py` file, which Python runs when a package (not a single file) is invoked with `-m`",correct:!0,explanation:"This is exactly it. A single module file runs itself under -m automatically; a package has no single file to run, so Python looks specifically for __main__.py inside it."},{text:"Adding `dependencies = []` to `pyproject.toml`",correct:!1,explanation:"The dependencies list in pyproject.toml has nothing to do with how python -m resolves an entry point inside a package."},{text:"Deleting `__init__.py` so Python treats the folder as a script directory",correct:!1,explanation:"Deleting __init__.py does not help here -- and it would break every re-exported import your tests rely on, turning the folder into a namespace package with no defined public surface."}]},{prompt:"Why is `find_missing_owner(inventory)` easy to unit test with no server, no file and no mocking required?",options:[{text:"Because it is short -- fewer than ten lines of code",correct:!1,explanation:"Line count is not what makes a function easy to test. A ten-line function that reads a file or calls an API is much harder to test than this one."},{text:"Because it is a pure function: it only takes data in and returns data out, with no side effects like printing, file I/O or network calls",correct:!0,explanation:"This is the testability argument from the lesson. A test just calls the function with sample data and checks the return value -- nothing external needs to be set up or faked."},{text:"Because it has a docstring",correct:!1,explanation:"A docstring documents a function -- it does not change whether the function is easy to test. A function with no docstring but no side effects is exactly as testable."},{text:"Because it does not use a for loop",correct:!1,explanation:"Loops have nothing to do with testability. This function does use a for loop internally and is still trivially testable, because it has no side effects."}]},{prompt:"Why is `print_report()` the one function in this module's lab you cannot write a simple return-value test for?",options:[{text:"Because it calls other functions internally",correct:!1,explanation:"Calling other functions is completely normal and does not by itself make something hard to test -- the issue is what print_report does with its result, not that it calls things."},{text:"Because it has no parameters",correct:!1,explanation:"Having no parameters does not prevent testing. The real issue is what the function does (prints to the terminal), not its signature."},{text:"Because its job is almost entirely output -- printing to the terminal -- rather than returning a value a test can check",correct:!0,explanation:"print_report is nearly all side effect (writing to stdout) and has no meaningful return value. That is exactly why the lesson separates input/processing (testable, pure) from output (thin, and largely untested directly)."},{text:"Because Python cannot run functions that use f-strings inside a test",correct:!1,explanation:"f-strings work identically inside and outside of tests. This is unrelated to why print_report is hard to test directly."}]},{prompt:'Reading just the signature and docstring below, before looking at the body, what do you already know?\n\n```python\ndef find_low_memory_prod(inventory, min_memory_gb=MIN_PROD_MEMORY_GB):\n    """Return the names of prod servers with less than `min_memory_gb` memory."""\n```',multiSelect:!0,options:[{text:"It takes a collection of servers and returns a list of names, not full server records",correct:!0,explanation:'The docstring states this directly: it returns "the names" -- not the server dicts themselves.'},{text:"It has one tunable threshold with a sensible default, so most callers can omit it",correct:!0,explanation:"min_memory_gb=MIN_PROD_MEMORY_GB gives it a default, meaning a caller can just write find_low_memory_prod(inventory) in the common case."},{text:"It must be checking every environment, not just prod, because the parameter is named `inventory`",correct:!1,explanation:'The docstring explicitly scopes this to prod servers ("Return the names of prod servers..."). The parameter name inventory just describes what is passed in, not which environments get checked.'},{text:"It modifies the inventory list it is given, in place",correct:!1,explanation:"Nothing in the signature or docstring suggests mutation -- and nothing here should lead you to assume it. A trustworthy docstring would say so if it mutated its input; this one describes a return value instead."}]},{prompt:"This audience (engineers coming from Bash, Ansible and Terraform) tends to over-use Python classes. According to this module's lesson, when does a class actually earn its place over a module of plain functions?",options:[{text:"As soon as you have more than two related functions in one file",correct:!1,explanation:"Function count alone is not the deciding factor. rules.py in this lab's package has three related functions and stays a module of plain functions -- no class needed."},{text:"When you have data and behavior that must travel together and vary per object -- not just a fixed shared dataset with functions that operate on it",correct:!0,explanation:"This is the lesson's rule of thumb. INVENTORY plus functions that read it does not need a class; something like a ServiceDefinition with its own fields and its own validation, introduced in a later module, does."},{text:"Whenever you want to organize code into a single file",correct:!1,explanation:"Organizing related functions into one file is exactly what a module already does -- a class is not required, or even helpful, just to group functions together."},{text:"Only when the project needs to run faster",correct:!1,explanation:"Classes are not a performance feature in Python, and choosing a class over plain functions is not a speed decision here."}]},{prompt:"In the Phase B review, you are looking at a coding agent's diff that deduplicates the environment-filtering logic between `rules.py` and `summary.py`. Which findings should make you push back before accepting it?",multiSelect:!0,options:[{text:'The agent wrapped both files in a new `InventoryRules` class "for organization," even though the spec said not to add classes',correct:!0,explanation:"The spec explicitly ruled out new classes. A class here adds structure nobody asked for and this audience does not need -- push back and ask for the plain-function version."},{text:"The diff adds a new dependency to `pyproject.toml`",correct:!0,explanation:"Nothing about deduplicating a repeated loop needs a third-party package. A new dependency here is unrequested scope -- reject that part of the change."},{text:"The new shared helper function is named starting with an underscore, like `_servers_in_environment`",correct:!1,explanation:'This is expected, not a problem: a leading underscore signals "internal helper, not part of the package\'s public surface" -- exactly the right naming choice here, not something to push back on.'},{text:"All existing tests in `tests/test_inventory.py` still pass, unchanged",correct:!1,explanation:"This is the acceptance bar being met, not a red flag -- tests passing unchanged is exactly the proof you want that behavior did not change."}]},{prompt:"The Phase B dedup keeps the shared `_servers_in_environment` helper inside `rules.py`, and has `summary.py` import it from there -- never the other direction. Why does the direction of that import matter?",options:[{text:"It does not matter -- Python allows imports in either direction with no consequence",correct:!1,explanation:"It does matter. summary.py already imports count_by_environment from rules.py; if rules.py also imported something from summary.py, the two files would depend on each other in both directions -- a circular import."},{text:"Keeping the dependency one-directional (summary.py depends on rules.py, never the reverse) avoids creating a circular import between the two files",correct:!0,explanation:"This is exactly the reasoning. summary.py already depends on rules.py one way; adding a dependency back from rules.py to summary.py would close the loop and risk the circular-import error the Deep Dive walks through."},{text:"It only matters for files longer than 100 lines",correct:!1,explanation:"File length has nothing to do with whether a circular import occurs -- the issue is the direction and timing of imports between two files, at any size."},{text:"It affects how fast `uv run pytest` runs, not correctness",correct:!1,explanation:"This is a correctness issue (whether the import succeeds at all under certain execution paths), not a performance one."}]},{prompt:"You delete `src/platformops/inventory.py` and create a `src/platformops/inventory/` folder with `data.py`, `rules.py`, `summary.py` and `report.py` inside it, but you forget to add `__init__.py`. What is the most likely symptom?",options:[{text:"Everything works exactly as before, since Python does not require `__init__.py` for a folder to act as a package",correct:!1,explanation:"While Python 3 can treat such a folder as an implicit namespace package, that is not the same as your working package: without __init__.py, none of the re-exported names (INVENTORY, find_missing_owner, and so on) resolve the way your code and tests expect."},{text:"`from platformops.inventory import INVENTORY` raises an ImportError, because there is no `__init__.py` to re-export that name",correct:!0,explanation:"This matches the exact error the lab's Troubleshooting section names: ImportError: cannot import name 'INVENTORY' from 'platformops.inventory' (unknown location) -- the folder is found, but nothing inside it re-exports the name at the package level."},{text:"`uv run pytest` refuses to start at all, with a configuration error",correct:!1,explanation:"pytest itself starts fine -- the failure shows up as an import error inside the specific test that tries to import from platformops.inventory, not as a pytest configuration failure."},{text:"Ruff automatically creates the missing `__init__.py` file for you",correct:!1,explanation:"Ruff is a linter and formatter -- it checks and reformats code, it does not create missing package files on your behalf."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,a.jsx)(t,{...e,children:(0,a.jsx)(d,{...e})}):d(e)}},7517(e,t,n){n.d(t,{A:()=>s});var o=n(4848),a=n(6540);function i({q:e,state:t,onToggle:n}){return(0,o.jsxs)("div",{className:"question_QpiT",children:[(0,o.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,o.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,o.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,a)=>{let i=t.selected.has(a),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":i&&(s+=" incorrect_kRFc"):i&&(s+=" chosen_H3WG"),(0,o.jsxs)("li",{children:[(0,o.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>n(a),children:[(0,o.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,o.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},a)})})]})}function s({questions:e}){let[t,n]=(0,a.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,n)=>{var o,a;let i;return o=e[n],a=t.selected,i=o.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),o.multiSelect?a.size===i.length&&[...a].every(e=>o.options[e].correct):1===a.size&&i.includes([...a][0])}).length;return(0,o.jsxs)("div",{className:"quiz_I014",children:[e.map((a,s)=>(0,o.jsx)(i,{q:a,state:t[s],onToggle:t=>n(n=>n.map((n,o)=>{if(o!==s)return n;let a=new Set(n.selected);return e[s].multiSelect?a.has(t)?a.delete(t):a.add(t):(a.clear(),a.add(t)),{...n,selected:a}}))},s)),(0,o.jsxs)("div",{className:"actions_XUwt",children:[(0,o.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,o.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,o.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>s,x:()=>r});var o=n(6540);let a={},i=o.createContext(a);function s(e){let t=o.useContext(i);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:s(e.components),o.createElement(i.Provider,{value:t},e.children)}}}]);