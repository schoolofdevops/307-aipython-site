"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["5592"],{9879(e,t,n){n.r(t),n.d(t,{metadata:()=>a,default:()=>d,frontMatter:()=>s,contentTitle:()=>c,toc:()=>h,assets:()=>l});var a=JSON.parse('{"id":"m13-project-structure-quality/quiz","title":"Quiz: Structuring and Maintaining Python Projects","description":"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read","source":"@site/docs/m13-project-structure-quality/quiz.mdx","sourceDirName":"m13-project-structure-quality","slug":"/m13-project-structure-quality/quiz","permalink":"/307-aipython-site/docs/m13-project-structure-quality/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Structuring and Maintaining Python Projects"},"sidebar":"courseSidebar","previous":{"title":"Lab: Structuring and Maintaining Python Projects","permalink":"/307-aipython-site/docs/m13-project-structure-quality/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m13-project-structure-quality/deep-dive"}}'),i=n(4848),o=n(8453),r=n(7517);let s={sidebar_position:3,title:"Quiz: Structuring and Maintaining Python Projects"},c="Structuring and Maintaining Python Projects \u2014 Quiz",l={},h=[];function p(e){let t={admonition:"admonition",h1:"h1",header:"header",p:"p",...(0,o.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.header,{children:(0,i.jsx)(t.h1,{id:"structuring-and-maintaining-python-projects--quiz",children:"Structuring and Maintaining Python Projects \u2014 Quiz"})}),"\n",(0,i.jsx)(t.admonition,{title:"This quiz covers the full module",type:"info",children:(0,i.jsx)(t.p,{children:"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read\nthe Deep Dive yet, come back to those questions after you finish it."})}),"\n",(0,i.jsx)(r.A,{questions:[{prompt:"What is the main purpose of a CHANGELOG.md file?",options:[{text:'A short, human-readable summary of what changed in each release, so someone can answer "what changed" fast',correct:!0,explanation:"A changelog is written for a person who needs the answer quickly during an incident or a review -- not a replacement for git log, but a fast summary of it."},{text:"A complete, line-by-line copy of every git commit message across the project's history",correct:!1,explanation:"That is closer to what git log already gives you -- a changelog is a curated, human-written summary, not a full commit dump."},{text:"A file mypy reads to decide which modules to type-check",correct:!1,explanation:"Which files mypy checks is controlled by the files setting in [tool.mypy] in pyproject.toml, not by CHANGELOG.md."},{text:"A machine-readable manifest that uv reads before resolving dependencies",correct:!1,explanation:"uv reads pyproject.toml and uv.lock to resolve dependencies -- CHANGELOG.md plays no role in that process."}]},{prompt:"Under semantic versioning, a release adds a new CLI command but changes nothing about how any existing command behaves. Which version number changes?",options:[{text:"MINOR",correct:!0,explanation:"A MINOR bump means something new was added while everything that worked before still works -- exactly what platformops v1.1 does over v1.0."},{text:"MAJOR",correct:!1,explanation:"MAJOR is reserved for a release where something that used to work may no longer work the same way -- a new command with no behavior change does not qualify."},{text:"PATCH",correct:!1,explanation:"PATCH is for a fix inside existing behavior, with nothing new added and no change to how anything is called."},{text:"None -- new commands do not require a version bump",correct:!1,explanation:"Semantic versioning exists precisely so a version number reflects real changes -- a new command is exactly the kind of change a MINOR bump exists to signal."}]},{prompt:"In scripts/check-gates.sh, why does ruff format --check run before mypy and pytest?",options:[{text:"Formatting and linting are near-instant, so cheap checks fail fast before the slower type checker and test suite run",correct:!0,explanation:"Ordering cheap, fast checks before expensive ones is a general pipeline principle -- there is no reason to wait on a slow test suite if a fast formatting check already fails."},{text:"ruff format must run before mypy or the type checker will crash",correct:!1,explanation:"mypy does not depend on code being pre-formatted -- it type-checks regardless of formatting. The ordering is about speed, not a hard dependency."},{text:"pytest requires the code to already be linted or it refuses to collect tests",correct:!1,explanation:"pytest collects and runs tests regardless of whether ruff has been run first -- the two tools are independent."},{text:"Alphabetical order -- the gates just happen to be listed a-to-z",correct:!1,explanation:'The order is deliberate (fast checks first), not alphabetical -- "mypy" would sort before "pytest" either way, but "ruff" would not sort first alphabetically.'}]},{prompt:"A function used to return a plain dict, but a recent change makes it sometimes return None instead. A caller downstream still does result.get(...) without checking for None first. Why might pytest miss this while mypy catches it?",options:[{text:"pytest only reports a problem if a test actually exercises the exact input that triggers the None case; mypy checks the mismatch from the declared types alone, on every call site",correct:!0,explanation:"This is the core difference the lesson draws: mypy reads your whole codebase without running any of it, so a type mismatch is visible from the signatures alone, regardless of which specific inputs any test happens to use."},{text:"pytest cannot check functions that return None under any circumstance",correct:!1,explanation:"pytest can absolutely test a function that returns None -- the issue is only that a specific untested input path would need its own test case to catch this particular bug."},{text:"mypy actually runs the code to observe the None return value at runtime",correct:!1,explanation:"mypy never executes your code -- it reads the declared types statically and reasons about the mismatch without running anything."},{text:"There is no real difference -- either tool would catch this equally reliably",correct:!1,explanation:"The two tools genuinely catch different things: pytest depends on which specific inputs a test uses, while mypy checks every call site against the declared type regardless of input."}]},{prompt:"Why did the lab change config.py's load_yaml_dict() return type from a bare dict to dict[str, Any]?",options:[{text:"dict[str, Any] tells a type checker the keys are strings and the values can be anything, which is more specific than a bare dict while still being honest about not knowing every field",correct:!0,explanation:'A bare dict only says "this is a dict" -- dict[str, Any] is specific enough for mypy to catch a real mistake (like passing a list where this code expects a dict) without forcing a full field-by-field model.'},{text:"A bare dict is invalid Python syntax and would fail to import",correct:!1,explanation:"A bare dict is valid Python and always has been -- the change is about being more specific for the type checker, not about fixing a syntax error."},{text:"It changes what load_yaml_dict() actually returns at runtime",correct:!1,explanation:"Type hints are erased before Python runs the code -- this change affects only what mypy checks, not the function's actual runtime behavior."},{text:"It was required so pytest could continue to import the module",correct:!1,explanation:"pytest does not care about the specificity of a return type hint -- the existing test suite passes with either a bare dict or dict[str, Any]."}]},{prompt:"A secret was accidentally committed to git two commits ago, and the working file has since been fixed to remove it. Is the secret still a problem?",options:[{text:"Yes -- the secret is still present in git history even though the current working file no longer shows it; rotating the credential is the only real fix",correct:!0,explanation:"Deleting a line in a later commit does not remove it from earlier commits in git history -- anyone with access to the repository's history can still find it. Issuing a new credential and revoking the old one is the actual fix."},{text:"No -- once the working file is fixed, the secret is no longer accessible to anyone",correct:!1,explanation:"git history keeps every prior commit's content by default -- fixing the current file does not erase what an earlier commit recorded."},{text:"It depends only on whether the secret scanner is run again after the fix",correct:!1,explanation:"Re-running the scanner checks the current working tree, which would now look clean -- but that says nothing about whether the secret is still recoverable from git history."},{text:"It is only a problem if the repository is public",correct:!1,explanation:"Even a private repository has more than one person with access in most real teams -- rotation is the correct response regardless of the repository's visibility."}]},{prompt:"Why does scripts/check-deps.sh run pip-audit through uvx instead of adding pip-audit as a dev dependency in pyproject.toml?",options:[{text:"A security scanner is a tool you run, not something the project itself needs installed -- uvx runs it as a throwaway invocation without adding it to the project's own dependency graph",correct:!0,explanation:"This mirrors a real distinction: dev dependencies like ruff, mypy and pytest are used by every contributor constantly, while an audit tool is invoked occasionally and does not need to be a permanent part of what the project installs."},{text:"pip-audit cannot be installed as a dev dependency under uv for technical reasons",correct:!1,explanation:"pip-audit can be added as a dev dependency like any other package -- the choice to use uvx instead is a design decision, not a technical limitation."},{text:"uvx is required because pip-audit only works when run outside of a project's own virtual environment",correct:!1,explanation:"pip-audit works fine inside a project's own environment too -- uvx is chosen here to keep it out of the permanent dependency list, not because of where it can run."},{text:"Dev dependencies cannot include any package related to security scanning",correct:!1,explanation:"There is no such restriction -- a project could add pip-audit as a dev dependency if it wanted every contributor to have it installed by default."}]},{prompt:"What does py.typed (PEP 561) actually do?",options:[{text:"It signals to tools outside the project that this package ships real type information that can be trusted and checked against",correct:!0,explanation:"Without py.typed, an external tool checking a project that imports platformops has no signal that platformops's own type hints are meant to be relied on."},{text:"It enables type hints inside platformops's own source files for the first time",correct:!1,explanation:"platformops already had type hints on most functions before py.typed was added -- the marker is about signaling to consumers outside the project, not enabling hints internally."},{text:"It makes mypy run faster by skipping files that do not need checking",correct:!1,explanation:"py.typed has nothing to do with mypy's performance -- it is a packaging-level signal read by tools that consume this package from the outside."},{text:"It is required before ruff format or ruff check will run",correct:!1,explanation:"ruff has no dependency on py.typed -- format and lint checks run regardless of whether the marker file exists."}]},{prompt:'uv add "sphinx==7.0.0" "jinja2==2.11.3" in a scratch project fails immediately, even though they are two different packages. Why?',options:[{text:"sphinx 7.0.0 depends on jinja2>=3.0 internally, so pinning jinja2==2.11.3 at the same time creates a contradiction no single version of jinja2 can satisfy",correct:!0,explanation:"This is a transitive conflict: the two packages look independent in your command, but one of them depends on the other with a version range that clashes with your explicit pin. The resolver walks both constraints and reports the exact pair that disagrees."},{text:"uv always rejects adding two packages in one command",correct:!1,explanation:"uv handles multi-package adds routinely -- the failure here is specifically that the two version constraints on jinja2 (one explicit, one from sphinx) contradict each other."},{text:"sphinx 7.0.0 and jinja2 2.11.3 have been removed from PyPI",correct:!1,explanation:"Both versions exist on PyPI -- the failure is a resolution conflict between their version requirements, not a missing-package error."},{text:"uv only supports resolving one dependency at a time",correct:!1,explanation:"uv routinely resolves large dependency graphs with many packages at once -- this particular failure is about a contradiction between a direct pin and a transitive requirement."}]},{multiSelect:!0,prompt:"Select every statement that is true about what a lockfile (uv.lock) guarantees.",options:[{text:"It guarantees every install reproduces the exact same package versions, verified by a cryptographic hash",correct:!0,explanation:"This is the sealed-jar guarantee -- the exact same file, verified by hash, every time it is installed from this lock."},{text:"uv sync --locked fails if pyproject.toml and uv.lock have drifted apart, instead of silently re-resolving",correct:!0,explanation:"This is exactly the flag worth using in CI -- it catches a developer who edited pyproject.toml but never re-ran uv lock, rather than quietly installing something the lock never recorded."},{text:"It guarantees every locked version is still free of known security vulnerabilities today",correct:!1,explanation:"This is the gap the deep dive names directly: the hash guarantees reproducibility, not safety. A CVE disclosed after locking does not un-pin the vulnerable version -- that is what a recurring dependency audit is for."},{text:"It guarantees the locked versions will never need to be updated again",correct:!1,explanation:"A lockfile freezes a point-in-time solution -- it says nothing about whether that solution should stay frozen forever. Re-resolving (uv lock --upgrade) is a normal, expected operation over a project's life."}]},{prompt:"A grep-based check for the exact string shell=True works reliably in check-security. Why does ruff need to parse an AST instead of just grepping for patterns like an unused variable?",options:[{text:"Detecting whether a variable is genuinely unused requires knowing which names are actually referenced later in the same scope -- something text matching cannot determine on its own",correct:!0,explanation:'A grep for a variable\'s name finds every appearance of that exact text, including its own assignment line, but has no concept of "read" versus "write" -- an AST lets ruff track that distinction precisely.'},{text:"grep cannot search Python files at all",correct:!1,explanation:"grep searches any text file including .py files just fine -- the limitation is not about file type, it is about grep having no understanding of code structure."},{text:"shell=True and an unused variable are actually the same category of problem, so both tools work identically on either",correct:!1,explanation:'They are different categories: shell=True is one fixed literal string a grep can match reliably, while "unused" depends on tracking usage across a whole function -- a job text matching cannot do on its own.'},{text:"ruff does not actually use an AST -- it just has a larger, more complete list of regex patterns than a hand-written grep script",correct:!1,explanation:"ruff genuinely parses source into an Abstract Syntax Tree and reasons about structure (scope, control flow, name usage) -- this is a different mechanism from pattern matching, not just a bigger pattern list."}]},{prompt:"What does reveal_type() do when used in a file that mypy checks?",options:[{text:"It reports, at check time only, what mypy currently believes the named expression's type is at that exact point in the code",correct:!0,explanation:"reveal_type() is not a real function -- calling it in code that actually runs would raise NameError. It exists purely to ask mypy what it has inferred at that specific line."},{text:"It prints the runtime type of a value while the program is executing, similar to calling type(x)",correct:!1,explanation:"reveal_type() is a static, check-time-only construct -- it has no runtime behavior and would error if the code actually ran with it present."},{text:"It converts a value from one type to another at runtime",correct:!1,explanation:"reveal_type() does not convert or cast anything -- it only reports what mypy has inferred; it has no effect on the value itself."},{text:"It disables type checking for the rest of the file",correct:!1,explanation:"reveal_type() does not disable anything -- it is purely informational, reporting mypy's current understanding without changing how checking proceeds."}]},{prompt:"Inside an isinstance(result, ServiceDefinition) branch, mypy allows a call to result.to_summary() even though result's declared type is ServiceDefinition | list[dict[str, Any]]. Why?",options:[{text:"mypy narrows the type inside that branch to exactly ServiceDefinition, because the isinstance check has already ruled out the list possibility for any code inside it",correct:!0,explanation:"This is type narrowing: mypy tracks, branch by branch, which possibilities an isinstance check (or similar test) has ruled out, and only allows calls that are valid for whatever remains."},{text:"mypy allows any method call on a union type regardless of which branch it is in",correct:!1,explanation:"The opposite is true outside the isinstance branch -- calling to_summary() there would be a type error, since list[dict[str, Any]] has no such method."},{text:"to_summary() happens to exist on both ServiceDefinition and list[dict[str, Any]]",correct:!1,explanation:"to_summary() is defined only on ServiceDefinition -- the call is only valid because mypy has narrowed the type down to just that possibility inside this branch."},{text:"isinstance checks have no effect on what mypy allows -- this only works because of a runtime check, not a static one",correct:!1,explanation:"mypy performs this narrowing statically, at check time, by recognizing the isinstance() pattern -- it is not relying on anything happening at runtime."}]}]})]})}function d(e={}){let{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(p,{...e})}):p(e)}},7517(e,t,n){n.d(t,{A:()=>r});var a=n(4848),i=n(6540);function o({q:e,state:t,onToggle:n}){return(0,a.jsxs)("div",{className:"question_QpiT",children:[(0,a.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,a.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,a.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,i)=>{let o=t.selected.has(i),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":o&&(r+=" incorrect_kRFc"):o&&(r+=" chosen_H3WG"),(0,a.jsxs)("li",{children:[(0,a.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>n(i),children:[(0,a.jsx)("span",{className:"marker_vN_w",children:o?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(o||e.correct)&&e.explanation&&(0,a.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},i)})})]})}function r({questions:e}){let[t,n]=(0,i.useState)(e.map(()=>({selected:new Set,checked:!1}))),s=t.every(e=>e.checked),c=t.filter((t,n)=>{var a,i;let o;return a=e[n],i=t.selected,o=a.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),a.multiSelect?i.size===o.length&&[...i].every(e=>a.options[e].correct):1===i.size&&o.includes([...i][0])}).length;return(0,a.jsxs)("div",{className:"quiz_I014",children:[e.map((i,r)=>(0,a.jsx)(o,{q:i,state:t[r],onToggle:t=>n(n=>n.map((n,a)=>{if(a!==r)return n;let i=new Set(n.selected);return e[r].multiSelect?i.has(t)?i.delete(t):i.add(t):(i.clear(),i.add(t)),{...n,selected:i}}))},r)),(0,a.jsxs)("div",{className:"actions_XUwt",children:[(0,a.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,a.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),s&&(0,a.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>r,x:()=>s});var a=n(6540);let i={},o=a.createContext(i);function r(e){let t=a.useContext(o);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function s(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),a.createElement(o.Provider,{value:t},e.children)}}}]);