"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["9836"],{7305(e,t,n){n.r(t),n.d(t,{metadata:()=>a,default:()=>p,frontMatter:()=>s,contentTitle:()=>c,toc:()=>d,assets:()=>l});var a=JSON.parse('{"id":"m23-multi-region-inventory/quiz","title":"Quiz: Multi-Region Cloud Inventory","description":"Some questions draw on material from both the Lab and the Deep Dive page. If you have not","source":"@site/docs/m23-multi-region-inventory/quiz.mdx","sourceDirName":"m23-multi-region-inventory","slug":"/m23-multi-region-inventory/quiz","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Multi-Region Cloud Inventory"},"sidebar":"courseSidebar","previous":{"title":"Lab: Multi-Region Cloud Inventory","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/deep-dive"}}'),o=n(4848),i=n(8453),r=n(7517);let s={sidebar_position:3,title:"Quiz: Multi-Region Cloud Inventory"},c="Multi-Region Cloud Inventory \u2014 Quiz",l={},d=[];function h(e){let t={admonition:"admonition",h1:"h1",header:"header",p:"p",...(0,i.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"multi-region-cloud-inventory--quiz",children:"Multi-Region Cloud Inventory \u2014 Quiz"})}),"\n",(0,o.jsx)(t.admonition,{title:"This quiz covers the full module",type:"info",children:(0,o.jsx)(t.p,{children:"Some questions draw on material from both the Lab and the Deep Dive page. If you have not\nread the Deep Dive yet, come back to those questions after you finish it."})}),"\n",(0,o.jsx)(r.A,{questions:[{prompt:"In the lesson's health-inspector analogy, what does the small, fixed pool of inspectors represent?",options:[{text:"A bounded number of worker threads, dispatched to scan several regions at once but never more than the cap allows",correct:!0,explanation:"A small, fixed team of inspectors covering several branches at once -- never one inspector per branch, never unlimited -- is exactly the bounded thread pool scan_regions() uses."},{text:"One inspector visiting every branch sequentially, one at a time",correct:!1,explanation:"That describes the OLD, Phase 1 sequential loop this module explicitly moves away from, not the pool of inspectors."},{text:"A single inspector assigned permanently to one branch",correct:!1,explanation:"The analogy is about several inspectors covering several branches concurrently, drawn from a shared, bounded pool -- not one-to-one assignment."},{text:"An unlimited number of inspectors, one dispatched per branch regardless of how many branches exist",correct:!1,explanation:"The analogy specifically warns against this -- twenty inspectors converging on the city at once is the unbounded-concurrency mistake this module avoids."}]},{prompt:"Why does scan_regions() use a bounded ThreadPoolExecutor instead of spawning one thread per region?",options:[{text:"Unbounded thread creation grows connection and scheduling overhead with every region added, independent of how much real work each thread does -- the same discipline Module 10's httpclient.check_many() already established",correct:!0,explanation:"This is the exact reasoning the lesson gives, citing Module 10 as the precedent for bounded concurrency."},{text:"Python cannot create more than five threads in one process",correct:!1,explanation:"Python has no such hard limit -- the cap is a deliberate design choice, not a language restriction."},{text:"AWS itself rejects any account with more than five concurrent API connections",correct:!1,explanation:"AWS does not impose that limit -- the concern is about this project's own resource usage and connection overhead, not an AWS-side restriction."},{text:"ThreadPoolExecutor is required because boto3 clients are not thread-safe",correct:!1,explanation:"The lesson never makes this claim -- the reasoning is entirely about bounding concurrency, not thread-safety of the AWS client."}]},{prompt:"Why does Elastic IP discovery (list_elastic_ips()) call describe_addresses() directly instead of using a paginator?",options:[{text:'client.can_paginate("describe_addresses") returns False -- AWS never registered a paginator for this operation, a real API quirk, not a bug in this module\'s code',correct:!0,explanation:'This was confirmed directly and is stated explicitly in the lesson: calling get_paginator("describe_addresses") would raise an error, because the operation genuinely does not support it.'},{text:"Elastic IPs cannot exceed one API response page in a single account, so a paginator would be unnecessary overhead the author chose to skip",correct:!1,explanation:"The real reason is that AWS did not register a paginator for this operation at all -- not that a paginator exists but is skipped for efficiency."},{text:"This module deliberately introduces the same pagination bug for EIPs that the rigged diff introduced for EC2",correct:!1,explanation:"This is a genuine, correct behavior of the real AWS API -- not a bug or an intentional flaw planted in this module."},{text:"describe_addresses() is deprecated in favor of describe_instances() for EIP discovery",correct:!1,explanation:"describe_addresses() is the correct, current operation for Elastic IPs -- it is unrelated to describe_instances()."}]},{prompt:"Why is list_s3_buckets() called exactly once, outside the per-region scanning pool, instead of once per region?",options:[{text:"list_buckets() returns every bucket in the account regardless of which region the client was built for -- calling it once per region would return the same full list N times without ever learning which region each bucket actually lives in",correct:!0,explanation:"This matches the live demo: a client built for us-east-1 returned buckets whose real data lives in us-west-2 -- S3 is account-wide, not region-scoped."},{text:"S3 does not support being called from more than one region at a time",correct:!1,explanation:"S3 clients can be built for any region -- the issue is that list_buckets() always returns the same account-wide list no matter which region the client targets, not a restriction on which regions can call it."},{text:"Calling list_buckets() twice would raise a ThrottlingException from AWS",correct:!1,explanation:"The lesson gives no such rate-limit reasoning -- the real reason is wasted, redundant API calls that never resolve which region a bucket belongs to."},{text:"S3 buckets do not support tags, so there is no reason to scan them per region",correct:!1,explanation:"S3 buckets DO support tags -- _bucket_tags() specifically reads them (or handles their absence with NoSuchTagSet). This is unrelated to why the call happens once."}]},{prompt:'A bucket named "reports-bucket" is untagged. What happens when list_s3_buckets() tries to read its tags?',options:[{text:"get_bucket_tagging() raises a ClientError with code NoSuchTagSet, which _bucket_tags() catches by name and treats as {} -- not a crash",correct:!0,explanation:'This was confirmed directly with moto: an untagged bucket\'s get_bucket_tagging() call fails with exactly this error code, and the module handles it as "no tags," the same meaning a missing Tags key has for the other resource types.'},{text:"get_bucket_tagging() returns an empty TagSet list with no error at all",correct:!1,explanation:"Unlike EC2/EBS/SG/EIP (which can return an empty or missing Tags list with no error), an untagged S3 bucket makes get_bucket_tagging() raise a ClientError instead."},{text:"The scan crashes with an unhandled exception",correct:!1,explanation:"This is exactly the case _bucket_tags() is written to handle cleanly -- NoSuchTagSet is caught by name and treated as {}, never left to crash the scan."},{text:"The bucket is silently excluded from the report entirely",correct:!1,explanation:"The bucket still appears in the report, with tags: {} -- an untagged resource is not the same as a missing one."}]},{prompt:'What is the practical difference between an "expired-token" failure and any other ClientError in the failed_regions report?',options:[{text:"An expired token means the credentials themselves went stale mid-scan -- the fix is to re-authenticate and re-run, whereas other ClientErrors (like a permission problem) need a different fix entirely",correct:!0,explanation:'This is exactly the distinction the lesson draws: lumping these together would hide the difference between "re-authenticate" and "fix a permissions policy" from whoever reads the report.'},{text:"There is no practical difference -- both are reported identically",correct:!1,explanation:'The module deliberately reports these differently: EXPIRED_TOKEN_CODES map to a distinct "expired-token" reason, not the raw AWS error code.'},{text:"An expired token always means the whole account is compromised and every region must be treated as failed",correct:!1,explanation:"An expired token is a credentials problem for that one call, not a security compromise -- and other regions can still succeed independently."},{text:"Expired-token errors are retried automatically by scan_regions() before being reported",correct:!1,explanation:"scan_regions() does not retry -- it classifies and reports the failure once, in the failed_regions list."}]},{prompt:'Which of the following AWS error codes does _classify_client_error() map to the "expired-token" reason? Select all that apply.',multiSelect:!0,options:[{text:"ExpiredToken",correct:!0,explanation:"This is one of the three codes in EXPIRED_TOKEN_CODES."},{text:"ExpiredTokenException",correct:!0,explanation:"This is one of the three codes in EXPIRED_TOKEN_CODES."},{text:"RequestExpired",correct:!0,explanation:"This is one of the three codes in EXPIRED_TOKEN_CODES."},{text:"UnauthorizedOperation",correct:!1,explanation:'This code means the caller lacks permission for the operation -- a genuinely different problem than an expired token, and it is reported under its own raw code, not "expired-token".'}]},{prompt:"In Phase 2's reviewed diff, what specifically was wrong with the unbounded-concurrency bug?",options:[{text:"scan_regions() accepted max_workers as a parameter, but the implementation spawned one raw threading.Thread per region and started all of them immediately, never consulting max_workers at all",correct:!0,explanation:"This matches the live measurement in the lab: max_workers=3 was passed in, but the observed peak concurrency was 10 out of 10 regions -- the parameter was accepted but silently ignored."},{text:"scan_regions() forgot to accept a max_workers parameter at all",correct:!1,explanation:"The buggy version DID accept max_workers -- that is exactly what made the bug dangerous: it looked handled from the function signature alone."},{text:"ThreadPoolExecutor was used correctly, but with too high a default value",correct:!1,explanation:"The bug was not a bad default value -- it was that ThreadPoolExecutor was replaced entirely with raw, unbounded threading.Thread objects."},{text:"The bug only affected the S3 discovery pass, not the per-region EC2/EBS/SG/EIP scans",correct:!1,explanation:"The bug affected the per-region thread dispatch itself -- every region's scan ran on its own unbounded thread, all starting at once."}]},{prompt:"In Phase 2's reviewed diff, what specifically was wrong with the pagination bug, and where was it?",options:[{text:"list_ec2_instances() called client.describe_instances(MaxResults=page_size) once directly, with no paginator or NextToken handling, silently dropping every instance past the first page",correct:!0,explanation:"The live demo proved this exactly: 12 instances created, only 5 returned with page_size=5 -- seven silently missing."},{text:"list_security_groups() lost its paginator, dropping security groups past the first page",correct:!1,explanation:"The pagination bug in the reviewed diff was in list_ec2_instances(), not list_security_groups() -- moto does not even truncate describe_security_groups() results the same way, which is why the bug was placed in the EC2 lister instead."},{text:"list_s3_buckets() failed to call get_bucket_location() for buckets past the first page",correct:!1,explanation:"list_s3_buckets() was not touched by the pagination bug -- the bug was specific to list_ec2_instances()."},{text:"The bug caused a crash (an unhandled exception) rather than a silent, incomplete result",correct:!1,explanation:'This is precisely what makes it dangerous: no crash, no error -- the scan completes "successfully" and just quietly returns fewer resources than actually exist.'}]},{prompt:'Why does the lab insist on running "git apply --check" against a throwaway scratch copy before applying the rigged diff for real?',options:[{text:"An unreviewed diff should never be applied straight into a real project -- proving it applies cleanly, and observing its actual behavior, happens in a disposable copy first",correct:!0,explanation:"This mirrors the same discipline Module 14's rigged-diff review lab already established: verify and observe in a scratch copy, never the real project, before trusting a diff."},{text:"git apply --check is required because git apply itself cannot modify files without it",correct:!1,explanation:"git apply --check only verifies the diff WOULD apply cleanly -- it is a dry run, a separate step from actually applying it."},{text:"The scratch copy is required because moto cannot run inside ~/platformops directly",correct:!1,explanation:"moto works identically in any location -- the scratch copy exists for safety during code review, not because of any moto limitation."},{text:"It is not actually necessary -- the lab includes it only as an optional habit",correct:!1,explanation:"The lab treats this as a required safety step precisely because the diff contains two real, seeded bugs that should never land in the real project before being caught and fixed."}]},{prompt:"Why does scan_region() (the per-region function) deliberately exclude S3?",options:[{text:"S3 is not region-scoped -- scan_regions() discovers it once, account-wide, outside the per-region function entirely",correct:!0,explanation:"This matches the module's architecture directly: scan_region() covers EC2, EBS, security groups and Elastic IPs; S3 is handled by a separate call in scan_regions() itself."},{text:"S3 buckets cannot be scanned using get_aws_client()",correct:!1,explanation:"list_s3_buckets() uses exactly the same get_aws_client() factory as every other resource type -- the exclusion is about discovery shape, not client-building capability."},{text:"S3 is scanned by a completely separate CLI command, not multi-region-scan at all",correct:!1,explanation:"multi-region-scan's report includes S3 buckets -- they come from scan_regions()'s own account-wide pass, in the same report as everything else."},{text:"S3 buckets are considered out of scope for security review and are intentionally omitted",correct:!1,explanation:'S3 buckets ARE included in the final report -- resource_type: "s3-bucket" records appear alongside the other four types.'}]},{prompt:"Why does this module use get_aws_client() (Module 22) instead of cloudinventory.get_client() (Module 21) for scan_region()?",options:[{text:"scan_region() talks to more than one service family per call (EC2 for four resource types, S3 separately) -- get_aws_client() is the general-purpose factory built for exactly that, while get_client() only ever builds an EC2 client",correct:!0,explanation:"This is the justification given directly in the module's own docstring -- get_client() is EC2-only by design, while get_aws_client() takes a service_name argument."},{text:"get_client() has been removed from the project as of this module",correct:!1,explanation:"cloudinventory.get_client() still exists and is still used by Module 21's own inventory-scan command -- it was never removed."},{text:"get_aws_client() is required because this module talks to Floci",correct:!1,explanation:"This module never talks to Floci at all -- endpoint_url is never passed, so every call resolves to real AWS (mocked by moto in tests), the same as Module 21."},{text:"get_client() does not support setting a retry configuration",correct:!1,explanation:"Both get_client() and get_aws_client() set the same explicit retry Config -- this is not the distinguishing factor."}]},{prompt:"According to the Deep Dive, what does the lock-protected counter measurement actually prove about ThreadPoolExecutor(max_workers=...)?",options:[{text:"The observed peak concurrent region-scans never exceeds max_workers, and it climbs above 1 -- proving the cap is real AND that regions genuinely run concurrently, not sequentially",correct:!0,explanation:"Both halves matter: a peak that never exceeds the cap proves boundedness, and a peak greater than 1 proves it is not secretly running one region at a time either."},{text:"It proves that exactly max_workers regions always run at the exact same moment, every single run",correct:!1,explanation:"The Deep Dive explicitly notes the exact peak can vary run to run and machine to machine -- the guarantee is an upper bound, not an exact, fixed figure."},{text:"It proves that ThreadPoolExecutor is faster than a raw threading.Thread per region",correct:!1,explanation:"The measurement is about concurrency bounding, not raw speed comparison -- no timing/speed claim is made."},{text:"It proves that max_workers must always be set higher than the number of regions being scanned",correct:!1,explanation:"The opposite is demonstrated: max_workers can be, and normally is, smaller than the number of regions -- that is the entire point of bounding it."}]},{prompt:'A learner runs "platformops multi-region-scan --regions us-east-1 --format xml". What happens?',options:[{text:"The CLI exits with code 2 and an error explaining that --format must be markdown or csv, caught before scan_regions() is ever called",correct:!0,explanation:"This is a command-level option validation error, following the same exit-code-2 convention this project already uses for malformed input, like --tag-value without --tag-key in Module 21."},{text:"It scans successfully and prints raw XML output",correct:!1,explanation:"The command only supports markdown and csv text formats (plus --json for the full report) -- xml is not a supported value and is rejected outright."},{text:"It silently falls back to the default markdown format",correct:!1,explanation:"An invalid --format value is rejected with an explicit error and exit code 2, not silently ignored."},{text:"It raises an unhandled Python traceback",correct:!1,explanation:"The CLI validates --format explicitly before calling scan_regions(), so no traceback occurs -- the error is caught and reported cleanly."}]},{prompt:'When every requested region fails (for example, with no AWS credentials configured at all), what does "platformops multi-region-scan" print and exit with?',options:[{text:"A well-formed, empty Markdown (or CSV) table, followed by the list of failed regions on stderr, and exit code 1",correct:!0,explanation:"This was demonstrated live in the lab: total failure still produces a valid, structured report through the same failed_regions path as a partial failure -- never a crash."},{text:"The command crashes with an unhandled NoCredentialsError traceback",correct:!1,explanation:"NoCredentialsError is caught by name inside _scan_one_region() (and around the S3 pass) and turned into a failed_regions entry, not left to crash the process."},{text:"The command exits 0, since an empty report with no resources is still technically a valid report",correct:!1,explanation:"Exit code 1 signals that not every region succeeded -- the command only exits 0 when every region (and the S3 pass) completed without failure."},{text:"The command hangs indefinitely waiting for credentials",correct:!1,explanation:"NoCredentialsError is raised immediately by boto3's credential chain when nothing is configured -- it does not hang, and the module handles it as a normal, expected failure mode."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(h,{...e})}):h(e)}},7517(e,t,n){n.d(t,{A:()=>r});var a=n(4848),o=n(6540);function i({q:e,state:t,onToggle:n}){return(0,a.jsxs)("div",{className:"question_QpiT",children:[(0,a.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,a.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,a.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let i=t.selected.has(o),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":i&&(r+=" incorrect_kRFc"):i&&(r+=" chosen_H3WG"),(0,a.jsxs)("li",{children:[(0,a.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>n(o),children:[(0,a.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,a.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function r({questions:e}){let[t,n]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),s=t.every(e=>e.checked),c=t.filter((t,n)=>{var a,o;let i;return a=e[n],o=t.selected,i=a.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),a.multiSelect?o.size===i.length&&[...o].every(e=>a.options[e].correct):1===o.size&&i.includes([...o][0])}).length;return(0,a.jsxs)("div",{className:"quiz_I014",children:[e.map((o,r)=>(0,a.jsx)(i,{q:o,state:t[r],onToggle:t=>n(n=>n.map((n,a)=>{if(a!==r)return n;let o=new Set(n.selected);return e[r].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...n,selected:o}}))},r)),(0,a.jsxs)("div",{className:"actions_XUwt",children:[(0,a.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,a.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),s&&(0,a.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>r,x:()=>s});var a=n(6540);let o={},i=a.createContext(o);function r(e){let t=a.useContext(i);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function s(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:r(e.components),a.createElement(i.Provider,{value:t},e.children)}}}]);