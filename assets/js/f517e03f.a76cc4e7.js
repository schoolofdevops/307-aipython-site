"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["5477"],{4063(e,t,o){o.r(t),o.d(t,{metadata:()=>i,default:()=>p,frontMatter:()=>r,contentTitle:()=>l,toc:()=>h,assets:()=>c});var i=JSON.parse('{"id":"m22-local-aws-floci/quiz","title":"Quiz: Local AWS Automation with Floci","description":"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read","source":"@site/docs/m22-local-aws-floci/quiz.mdx","sourceDirName":"m22-local-aws-floci","slug":"/m22-local-aws-floci/quiz","permalink":"/307-aipython-site/docs/m22-local-aws-floci/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Local AWS Automation with Floci"},"sidebar":"courseSidebar","previous":{"title":"Lab: Local AWS Automation with Floci","permalink":"/307-aipython-site/docs/m22-local-aws-floci/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m22-local-aws-floci/deep-dive"}}'),a=o(4848),n=o(8453),s=o(7517);let r={sidebar_position:3,title:"Quiz: Local AWS Automation with Floci"},l="Local AWS Automation with Floci \u2014 Quiz",c={},h=[];function d(e){let t={admonition:"admonition",h1:"h1",header:"header",p:"p",...(0,n.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(t.header,{children:(0,a.jsx)(t.h1,{id:"local-aws-automation-with-floci--quiz",children:"Local AWS Automation with Floci \u2014 Quiz"})}),"\n",(0,a.jsx)(t.admonition,{title:"This quiz covers the full module",type:"info",children:(0,a.jsx)(t.p,{children:"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read\nthe Deep Dive yet, come back to those questions after you finish it."})}),"\n",(0,a.jsx)(s.A,{questions:[{prompt:'In the lesson\'s flight-simulator analogy, what does "a simulator crash does not ground the real fleet" map to?',options:[{text:"A script passing every test against Floci is not automatically proven safe against real AWS",correct:!0,explanation:"The analogy's point: Floci teaches the real controls (API shapes, common behavior) well, but a pass against it does not certify production readiness the way a real AWS sandbox check does."},{text:"Floci crashing means your laptop needs to be restarted",correct:!1,explanation:"The analogy is about what a passing test proves, not about literal software crashes."},{text:"A failed unit test always means the whole test suite is unreliable",correct:!1,explanation:'Not what the analogy addresses -- it is specifically about the gap between "works against a stand-in" and "proven safe against the real thing."'},{text:"Real AWS and Floci always behave identically for every operation",correct:!1,explanation:"The opposite is the whole point of the analogy -- Floci is a stand-in, not a guarantee of identical behavior in every respect."}]},{prompt:"What does `floci doctor` do?",options:[{text:"Prints a checklist confirming Docker, the image, the container and the endpoint are all reachable and healthy",correct:!0,explanation:"It is Floci's health-check command -- a fast way to confirm the local stand-in is actually up before running code against it."},{text:"Repairs corrupted files inside S3 buckets Floci is emulating",correct:!1,explanation:"Not what floci doctor does -- it checks environment health, not data integrity inside emulated services."},{text:"Installs Floci for the first time",correct:!1,explanation:"That is `floci start` (after installing via brew or the install script) -- `doctor` only diagnoses an existing setup."},{text:"Deletes every bucket, table and queue to reset Floci to a clean state",correct:!1,explanation:"That effect only happens from `floci stop` (which removes the container) -- `doctor` is read-only diagnostics."}]},{prompt:"This module's lab verified a real, specific behavior of `floci stop` live before writing about it. What did that test show?",options:[{text:"`floci stop` removes the container entirely -- the next `floci start` creates a brand-new, empty one with every bucket/table/queue gone",correct:!0,explanation:"Verified live: after `floci stop` then `floci start`, a bucket created moments earlier was gone -- state does not survive the stop/start cycle."},{text:"`floci stop` pauses the container, and all data is exactly as it was after the next `floci start`",correct:!1,explanation:"This was the assumption going in -- the live test disproved it. `floci stop` removes the container, it does not pause it."},{text:"`floci stop` only clears S3 data, leaving DynamoDB and SQS untouched",correct:!1,explanation:"The live test showed all three (S3 buckets, DynamoDB tables, SQS queues) were gone after the stop/start cycle -- not S3-specific."},{text:"`floci stop` cannot be run while any bucket exists",correct:!1,explanation:"Not a real restriction -- `floci stop` runs regardless of what data Floci is currently holding, and removes it all."}]},{prompt:"Why does Floci accept `AWS_ACCESS_KEY_ID=test` / `AWS_SECRET_ACCESS_KEY=test` instead of requiring real AWS credentials?",options:[{text:"Floci only checks that boto3 sent some non-empty credential value -- it never validates it against anything real",correct:!0,explanation:"This is why fake, obviously-placeholder values are the normal practice against Floci -- they satisfy boto3's requirement to send something, with no risk of being mistaken for a real leaked key."},{text:"Floci has its own separate account system with `test`/`test` as a default login",correct:!1,explanation:"Floci has no account/authentication system to log into -- it simply does not check the credential values at all."},{text:"Real AWS credentials would be rejected by Floci if you tried to use them",correct:!1,explanation:"Real credentials would also work against Floci -- Floci ignores the value either way, it just needs something present."},{text:"The values `test`/`test` are hardcoded inside the Floci server and required exactly",correct:!1,explanation:'Any non-empty string works, not specifically the literal word "test" -- that value is just the widely used, obviously-fake convention.'}]},{prompt:"What is the one thing that changes between a boto3 client that talks to real AWS and one that talks to Floci?",options:[{text:"The `endpoint_url` argument -- set, every call goes to that address instead of AWS's own endpoints",correct:!0,explanation:"Nothing else about the code changes: same method names, same request shapes, same response shapes -- only where the request is sent."},{text:"The AWS SDK itself -- Floci requires a separate, Floci-specific Python library",correct:!1,explanation:"This module uses the exact same boto3 library for both real AWS and Floci -- no separate SDK."},{text:'The service name passed to `boto3.client()`, such as "s3" vs "floci-s3"',correct:!1,explanation:'Service names stay identical (`"s3"`, `"dynamodb"`, `"sqs"`) -- only the endpoint changes.'},{text:"The HTTP method used for every request",correct:!1,explanation:"boto3 and botocore control the HTTP method internally per API operation -- this is unaffected by which endpoint the request goes to."}]},{prompt:"Why does `get_aws_client()` read `AWS_ENDPOINT_URL` itself as a fallback, instead of relying on boto3's own built-in support for that same environment variable?",options:[{text:"So the redirect decision stays local to this one function and its own clients, instead of silently affecting every boto3 client any code in the process creates",correct:!0,explanation:"Relying on boto3's global env-var handling would redirect ANY boto3 client in the process, including ones built by other libraries -- routing it through one function keeps the decision visible and testable."},{text:"boto3 does not actually support an `AWS_ENDPOINT_URL` environment variable at all",correct:!1,explanation:"boto3 does support it natively -- the lesson explicitly discusses this real, existing boto3 behavior and explains why this project chooses not to rely on it exclusively."},{text:"Reading the environment variable directly is faster at runtime than boto3's own handling",correct:!1,explanation:"Performance was not the reason -- the reasoning was about locality and testability, not speed."},{text:"It is required because Floci does not work with boto3's native environment-variable support",correct:!1,explanation:"Floci works fine with boto3's native `AWS_ENDPOINT_URL` handling too -- the design choice here is about this project's own code discipline, not a Floci limitation."}]},{prompt:"In `get_aws_client(service_name, *, region, profile=None, endpoint_url=None)`, what wins if both an explicit `endpoint_url` argument and the `AWS_ENDPOINT_URL` environment variable are set to different values?",options:[{text:"The explicit `endpoint_url` argument",correct:!0,explanation:"The function only falls back to the environment variable when `endpoint_url` is `None` -- an explicit argument always takes precedence."},{text:"The environment variable, because it applies more broadly",correct:!1,explanation:"The opposite -- explicit arguments always win in this function's precedence order."},{text:"Whichever one boto3's Session object resolves first at connection time",correct:!1,explanation:"The resolution happens in `get_aws_client()` itself, in Python, before any client is even constructed -- not inside boto3's own resolution."},{text:"Neither -- passing both raises an error",correct:!1,explanation:"No error is raised; the function has a defined precedence rule instead of treating the combination as invalid."}]},{prompt:"Which of the following are true about how this module avoids `endpoint_url` scatter, based on the Deep Dive's `grep`-based proof?",multiSelect:!0,options:[{text:"`reportstore.py` and `workqueue.py` each build a client through `get_aws_client()` inside their CLI-facing wrapper functions",correct:!0,explanation:"Confirmed by grep: `upload_report_to_bucket()`, `send_to_queue()` and `receive_from_queue()` are the call sites."},{text:"`findingsstore.py` has zero actual `get_aws_client()` call sites in its code",correct:!0,explanation:"Confirmed by grep: its only match was a docstring mention. Every function in `findingsstore.py` takes an already-built `client` as a parameter instead, the same dependency-injection style `cloudinventory.list_instances(client, ...)` uses."},{text:"Every one of the three modules calls `boto3.client()` directly at least once",correct:!1,explanation:"The grep proof showed the opposite -- none of the three modules calls `boto3.client()` directly anywhere in real code."},{text:"The `grep` check searched only comments, so it cannot actually prove anything about the real code",correct:!1,explanation:"The grep searched the whole file; the point made was that the ONE literal-looking match found was itself just a docstring sentence, which the Deep Dive explicitly calls out rather than hiding."}]},{prompt:"Why does `findingsstore.py` pick `service` as the DynamoDB partition key and `timestamp` as the sort key?",options:[{text:'So "every finding for one service, most recent first" is a single, fast query, using a key schema that is fixed at table-create time',correct:!0,explanation:"Unlike a SQL table, DynamoDB cannot add a new index on a whim later -- the partition/sort key decision has to match the access pattern you actually need, decided up front."},{text:"DynamoDB requires every table to use exactly these two attribute names",correct:!1,explanation:"DynamoDB places no naming requirement on key attributes -- `service`/`timestamp` were this module's own domain-appropriate choice."},{text:"It makes `query_findings()` scan every item in the table and filter in Python",correct:!1,explanation:"The opposite -- the composite key is exactly what lets `query_findings()` avoid a full table scan, targeting only the matching partition server-side."},{text:"A sort key is only useful for numeric values, and `timestamp` happens to be a string here by coincidence",correct:!1,explanation:"Sort keys work for any comparable attribute type, string included -- `timestamp` as a string sort key is a deliberate, correct choice, not a workaround."}]},{prompt:"What does `ScanIndexForward=False` do in `query_findings()`?",options:[{text:"Returns results in descending sort-key order (most recent timestamp first), entirely server-side",correct:!0,explanation:"DynamoDB does the reordering itself -- the function never fetches everything ascending and reverses it in Python."},{text:"Scans the entire table instead of using the partition key index",correct:!1,explanation:"This is a Query operation, not a Scan -- `ScanIndexForward` only controls sort-key ordering within a Query's matched partition."},{text:"Disables pagination for the query",correct:!1,explanation:"Pagination is a separate concern (via `Limit` and, for larger result sets, `LastEvaluatedKey`) -- unrelated to sort direction."},{text:'Forces the query to only return items with a `severity` of "high"',correct:!1,explanation:"Severity filtering is not what this parameter controls at all -- it only affects the order of the sort key."}]},{prompt:"A message is sent to a queue with `maxReceiveCount=2`. It is received (and not deleted) twice, `ApproximateReceiveCount` reaching 2. What happens on the next delivery attempt?",options:[{text:"SQS redirects the message to the dead-letter queue instead of delivering it from the main queue again",correct:!0,explanation:"This is exactly what the lab and Deep Dive both captured live: two deliveries reach the max, and the next attempt lands on the DLQ instead."},{text:"The message is permanently deleted with no record anywhere",correct:!1,explanation:"This is the exact failure mode a dead-letter queue exists to prevent -- a failing message goes to the DLQ, not into the void."},{text:"The message is delivered from the main queue a third time, and every time after that, forever",correct:!1,explanation:"This is the other failure mode a DLQ prevents -- unlimited retries. `maxReceiveCount` caps it."},{text:"SQS pauses the entire queue until someone manually intervenes",correct:!1,explanation:"Only the specific problem message is affected -- other messages in the queue continue to be delivered normally."}]},{prompt:"What does `VisibilityTimeout` control?",options:[{text:"How long a received-but-undeleted message stays hidden from other receivers before becoming visible again",correct:!0,explanation:"It is SQS's way of giving one receiver time to finish processing before the message reappears -- if that receiver never deletes it, it becomes visible again once the timeout expires."},{text:"How long a queue itself stays visible in `list_queues()` after being created",correct:!1,explanation:"Not related to queue creation or listing -- this setting is about individual message delivery behavior."},{text:"How many messages a single `receive_messages()` call can return at once",correct:!1,explanation:"That is `MaxNumberOfMessages`/`max_messages`, a separate parameter."},{text:"How long SQS retains a message before deleting it automatically, regardless of processing",correct:!1,explanation:"That is a queue's message retention period, a different setting entirely from `VisibilityTimeout`."}]},{prompt:"The Deep Dive tested attaching an IAM policy with an explicit `Deny` on `s3:PutObject`, then tried the put anyway. What did the live test against Floci show?",multiSelect:!0,options:[{text:"Floci accepted the `put_user_policy` call without error",correct:!0,explanation:"Confirmed live -- Floci's `iam` service accepts policy calls; it does not reject or validate them for enforcement purposes."},{text:"The subsequent `put_object` succeeded anyway, even with the Deny policy in place",correct:!0,explanation:"Confirmed live -- Floci's community edition emulates the S3 and IAM operations themselves but does not enforce the authorization logic between them."},{text:"The `put_object` call failed with an `AccessDenied` error, proving Floci enforces IAM exactly like real AWS",correct:!1,explanation:"This is the opposite of what the live test showed -- the put succeeded despite the Deny policy, which is exactly the simulator boundary this section demonstrates."},{text:"Floci refused to accept the Deny policy at all, returning a validation error",correct:!1,explanation:"Floci accepted the policy without complaint -- the gap is not in accepting policy documents, it is in never acting on them."}]},{prompt:"Why does the lesson recommend using S3, DynamoDB and SQS for three different pieces of this module's domain logic, instead of picking one service for everything?",options:[{text:"Each service is suited to a different storage shape: whole files (S3), fast lookups by a known key (DynamoDB), and work items a separate process picks up later (SQS)",correct:!0,explanation:'A JSON report, an individually-queryable finding record, and a "something needs doing" work item are three genuinely different problems, not the same problem solved three ways.'},{text:"AWS requires every application to use all three services together",correct:!1,explanation:"Not an AWS requirement -- the lesson chose three services because each domain problem in this module genuinely fits a different one better."},{text:"DynamoDB cannot store JSON at all, so S3 is required for anything JSON-shaped",correct:!1,explanation:"DynamoDB can absolutely store JSON-like structured data as item attributes -- the real distinction is about access pattern (whole-file storage vs. keyed lookups), not a JSON limitation."},{text:"SQS is only a backup system for when S3 or DynamoDB is unavailable",correct:!1,explanation:"SQS is not a fallback for the other two -- it solves an entirely different problem (asynchronous, queued work) that neither S3 nor DynamoDB is designed for."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,n.R)(),...e.components};return t?(0,a.jsx)(t,{...e,children:(0,a.jsx)(d,{...e})}):d(e)}},7517(e,t,o){o.d(t,{A:()=>s});var i=o(4848),a=o(6540);function n({q:e,state:t,onToggle:o}){return(0,i.jsxs)("div",{className:"question_QpiT",children:[(0,i.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,i.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,i.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,a)=>{let n=t.selected.has(a),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":n&&(s+=" incorrect_kRFc"):n&&(s+=" chosen_H3WG"),(0,i.jsxs)("li",{children:[(0,i.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>o(a),children:[(0,i.jsx)("span",{className:"marker_vN_w",children:n?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(n||e.correct)&&e.explanation&&(0,i.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},a)})})]})}function s({questions:e}){let[t,o]=(0,a.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,o)=>{var i,a;let n;return i=e[o],a=t.selected,n=i.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),i.multiSelect?a.size===n.length&&[...a].every(e=>i.options[e].correct):1===a.size&&n.includes([...a][0])}).length;return(0,i.jsxs)("div",{className:"quiz_I014",children:[e.map((a,s)=>(0,i.jsx)(n,{q:a,state:t[s],onToggle:t=>o(o=>o.map((o,i)=>{if(i!==s)return o;let a=new Set(o.selected);return e[s].multiSelect?a.has(t)?a.delete(t):a.add(t):(a.clear(),a.add(t)),{...o,selected:a}}))},s)),(0,i.jsxs)("div",{className:"actions_XUwt",children:[(0,i.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>o(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,i.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>o(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,i.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,o){o.d(t,{R:()=>s,x:()=>r});var i=o(6540);let a={},n=i.createContext(a);function s(e){let t=i.useContext(n);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:s(e.components),i.createElement(n.Provider,{value:t},e.children)}}}]);