"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["8544"],{7780(e,n,s){s.r(n),s.d(n,{metadata:()=>t,default:()=>u,frontMatter:()=>i,contentTitle:()=>a,toc:()=>l,assets:()=>c});var t=JSON.parse('{"id":"m23-multi-region-inventory/lab","title":"Lab: Multi-Region Cloud Inventory","description":"This lab has four phases. In Phase 1, you write a small, correct, sequential","source":"@site/docs/m23-multi-region-inventory/lab.md","sourceDirName":"m23-multi-region-inventory","slug":"/m23-multi-region-inventory/lab","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Multi-Region Cloud Inventory"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Multi-Region Cloud Inventory","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/lesson"},"next":{"title":"Quiz: Multi-Region Cloud Inventory","permalink":"/307-aipython-site/docs/m23-multi-region-inventory/quiz"}}'),r=s(4848),o=s(8453);let i={sidebar_position:2,title:"Lab: Multi-Region Cloud Inventory"},a="Lab \u2014 Multi-Region Cloud Inventory",c={},l=[{value:"Phase 1 \u2014 A sequential scanner first, test-first",id:"phase-1--a-sequential-scanner-first-test-first",level:2},{value:"Step 1 \u2014 Write the failing test file",id:"step-1--write-the-failing-test-file",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail",level:3},{value:"Step 3 \u2014 Write the sequential <code>multiregion.py</code>",id:"step-3--write-the-sequential-multiregionpy",level:3},{value:"Step 4 \u2014 Watch it pass",id:"step-4--watch-it-pass",level:3},{value:"Phase 2 \u2014 Review a diff, find two bugs, then apply the fix",id:"phase-2--review-a-diff-find-two-bugs-then-apply-the-fix",level:2},{value:"Step 1 \u2014 Confirm it applies cleanly",id:"step-1--confirm-it-applies-cleanly",level:3},{value:"Step 2 \u2014 Read the diff, top to bottom",id:"step-2--read-the-diff-top-to-bottom",level:3},{value:"Step 3 \u2014 Apply it in the scratch copy and see the bugs live",id:"step-3--apply-it-in-the-scratch-copy-and-see-the-bugs-live",level:3},{value:"Step 4 \u2014 Document both findings",id:"step-4--document-both-findings",level:3},{value:"Step 5 \u2014 Apply the corrected version for real",id:"step-5--apply-the-corrected-version-for-real",level:3},{value:"Step 6 \u2014 Extend the test file to match, and watch the full suite pass",id:"step-6--extend-the-test-file-to-match-and-watch-the-full-suite-pass",level:3},{value:"Step 7 \u2014 Wire the CLI",id:"step-7--wire-the-cli",level:3},{value:"Phase 3 \u2014 See it work, live, against a fake AWS account",id:"phase-3--see-it-work-live-against-a-fake-aws-account",level:2},{value:"Phase 4 \u2014 Full quality gate and version bump",id:"phase-4--full-quality-gate-and-version-bump",level:2},{value:"Teardown",id:"teardown",level:2}];function d(e){let n={a:"a",admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",ol:"ol",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,o.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"lab--multi-region-cloud-inventory",children:"Lab \u2014 Multi-Region Cloud Inventory"})}),"\n",(0,r.jsxs)(n.p,{children:["This lab has four phases. In Phase 1, you write a small, correct, ",(0,r.jsx)(n.strong,{children:"sequential"}),"\nmulti-region scanner first, test-first \u2014 EC2 and EBS only, one region at a time \u2014 to\nestablish the normalization pattern and the partial-failure handling before concurrency\nenters the picture. In Phase 2, you review a diff that extends this into the full,\n",(0,r.jsx)(n.strong,{children:"concurrent"}),", five-resource-type scanner \u2014 the diff has two seeded bugs, the same two\nbug classes this module's objectives name, and you find and fix both before applying the\ncorrected version for real. In Phase 3, you run the finished scanner live against a\ncompletely fake, in-memory AWS account using ",(0,r.jsx)(n.strong,{children:"moto"})," \u2014 no real AWS account, ever. In\nPhase 4, you run this project's full quality gate and bump the version to ",(0,r.jsx)(n.code,{children:"v1.8.0"})," \u2014 the\nMulti-Region Cloud Inventory release."]}),"\n",(0,r.jsxs)(n.admonition,{title:"Before you start",type:"info",children:[(0,r.jsxs)(n.p,{children:["You need ",(0,r.jsx)(n.code,{children:"~/platformops"})," at ",(0,r.jsx)(n.code,{children:"v1.7.0"}),", with ",(0,r.jsx)(n.code,{children:"src/platformops/awsclient.py"})," in place from\nModule 22, and the labs repo cloned at ",(0,r.jsx)(n.code,{children:"~/307-aipython-labs"}),"."]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 1.7.0\n"})}),(0,r.jsxs)(n.p,{children:["If this already shows ",(0,r.jsx)(n.code,{children:"1.8.0"}),", you have already completed this lab -- skip ahead to\n",(0,r.jsx)(n.a,{href:"#phase-4--full-quality-gate-and-version-bump",children:"Phase 4"}),", or work through the steps below\nanyway, since every step in this lab is safe to repeat. One consequence of repeating it:\nPhase 1 Step 2 overwrites ",(0,r.jsx)(n.code,{children:"tests/test_multiregion.py"})," with the Phase-1-only version while\n",(0,r.jsx)(n.code,{children:"src/platformops/multiregion.py"})," is still the finished Phase-2 version underneath -- so\ninstead of a clean ",(0,r.jsx)(n.code,{children:"ImportError"}),", you will see a handful of real, mismatched failures (extra\nresource types, wrong call order). That mismatch is expected in this specific case, not a bug\n-- Phase 1 Step 3 rewrites the module to match the tests you just wrote, exactly like a fresh\nrun would."]})]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-1--a-sequential-scanner-first-test-first",children:"Phase 1 \u2014 A sequential scanner first, test-first"}),"\n",(0,r.jsx)(n.h3,{id:"step-1--write-the-failing-test-file",children:"Step 1 \u2014 Write the failing test file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cd ~/platformops\ncat > tests/test_multiregion.py << \'PYEOF\'\nimport time\n\nimport boto3\nimport botocore.exceptions\nfrom moto import mock_aws\n\nfrom platformops import multiregion\n\n\ndef _run_instance(client, *, tags=None):\n    kwargs = {\n        "ImageId": "ami-12345678",\n        "MinCount": 1,\n        "MaxCount": 1,\n        "InstanceType": "t2.micro",\n    }\n    if tags:\n        kwargs["TagSpecifications"] = [\n            {\n                "ResourceType": "instance",\n                "Tags": [{"Key": k, "Value": v} for k, v in tags.items()],\n            }\n        ]\n    return client.run_instances(**kwargs)["Instances"][0]["InstanceId"]\n\n\n# ---------------------------------------------------------------------------\n# _tags_from_ec2_list -- missing key and empty list both normalize to {}.\n# ---------------------------------------------------------------------------\n\n\ndef test_tags_from_ec2_list_handles_missing_key_and_empty_list_and_real_tags():\n    assert multiregion._tags_from_ec2_list(None) == {}\n    assert multiregion._tags_from_ec2_list([]) == {}\n    assert multiregion._tags_from_ec2_list([{"Key": "env", "Value": "prod"}]) == {\n        "env": "prod"\n    }\n\n\n# ---------------------------------------------------------------------------\n# list_ec2_instances / list_ebs_volumes -- normalized records, paginated.\n# ---------------------------------------------------------------------------\n\n\n@mock_aws\ndef test_list_ec2_instances_returns_normalized_records():\n    client = boto3.client("ec2", region_name="us-east-1")\n    _run_instance(client, tags={"env": "prod"})\n\n    records = multiregion.list_ec2_instances(client)\n\n    assert len(records) == 1\n    record = records[0]\n    assert record.resource_type == "ec2-instance"\n    assert record.tags == {"env": "prod"}\n\n\n@mock_aws\ndef test_list_ec2_instances_walks_every_page_not_just_the_first():\n    client = boto3.client("ec2", region_name="us-east-1")\n    for _ in range(5):\n        _run_instance(client)\n\n    records = multiregion.list_ec2_instances(client, page_size=2)\n\n    assert len(records) == 5\n\n\n@mock_aws\ndef test_list_ebs_volumes_returns_normalized_records():\n    client = boto3.client("ec2", region_name="us-east-1")\n    client.create_volume(Size=8, AvailabilityZone="us-east-1a")\n\n    records = multiregion.list_ebs_volumes(client)\n\n    assert len(records) == 1\n    assert records[0].resource_type == "ebs-volume"\n    assert records[0].state == "available"\n\n\n@mock_aws\ndef test_list_ebs_volumes_walks_every_page():\n    client = boto3.client("ec2", region_name="us-east-1")\n    for _ in range(5):\n        client.create_volume(Size=8, AvailabilityZone="us-east-1a")\n\n    records = multiregion.list_ebs_volumes(client, page_size=2)\n\n    assert len(records) == 5\n\n\n# ---------------------------------------------------------------------------\n# scan_region -- combines both resource types for one region.\n# ---------------------------------------------------------------------------\n\n\n@mock_aws\ndef test_scan_region_combines_ec2_and_ebs():\n    client = boto3.client("ec2", region_name="us-east-1")\n    _run_instance(client)\n    client.create_volume(Size=8, AvailabilityZone="us-east-1a")\n\n    records = multiregion.scan_region(region="us-east-1")\n\n    resource_types = {r.resource_type for r in records}\n    assert resource_types == {"ec2-instance", "ebs-volume"}\n\n\n# ---------------------------------------------------------------------------\n# scan_regions -- partial failure handling, and (for now) strictly\n# sequential execution.\n# ---------------------------------------------------------------------------\n\n\n@mock_aws\ndef test_scan_regions_succeeds_across_multiple_regions():\n    for region in ("us-east-1", "us-west-2"):\n        client = boto3.client("ec2", region_name=region)\n        _run_instance(client)\n\n    result = multiregion.scan_regions(["us-east-1", "us-west-2"])\n\n    assert result["status"] == "ok"\n    assert sorted(result["regions_scanned"]) == ["us-east-1", "us-west-2"]\n    ec2_records = [\n        r for r in result["resources"] if r["resource_type"] == "ec2-instance"\n    ]\n    assert len(ec2_records) == 2\n\n\ndef test_scan_regions_a_client_error_lands_in_failed_regions_not_a_crash(monkeypatch):\n    def fake_scan_region(*, region, **kwargs):\n        if region == "eu-west-1":\n            raise botocore.exceptions.ClientError(\n                {"Error": {"Code": "UnauthorizedOperation", "Message": "denied"}},\n                "DescribeInstances",\n            )\n        return []\n\n    monkeypatch.setattr(multiregion, "scan_region", fake_scan_region)\n\n    result = multiregion.scan_regions(["us-east-1", "eu-west-1"])\n\n    assert result["status"] == "partial"\n    failed = {f["region"]: f for f in result["failed_regions"]}\n    assert failed["eu-west-1"]["error"] == "UnauthorizedOperation"\n    assert "us-east-1" in result["regions_scanned"]\n\n\ndef test_scan_regions_runs_strictly_sequentially(monkeypatch):\n    """Phase 2 replaces this loop with a bounded thread pool -- this test\n    captures the BEFORE behavior so the contrast is provable there."""\n    call_order = []\n\n    def fake_scan_region(*, region, **kwargs):\n        call_order.append(("start", region))\n        time.sleep(0.02)\n        call_order.append(("end", region))\n        return []\n\n    monkeypatch.setattr(multiregion, "scan_region", fake_scan_region)\n\n    multiregion.scan_regions(["us-east-1", "us-west-2", "eu-west-1"])\n\n    assert call_order == [\n        ("start", "us-east-1"),\n        ("end", "us-east-1"),\n        ("start", "us-west-2"),\n        ("end", "us-west-2"),\n        ("start", "eu-west-1"),\n        ("end", "eu-west-1"),\n    ]\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--watch-it-fail",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_multiregion.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_multiregion.py'.\n...\nE   ImportError: cannot import name 'multiregion' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_multiregion.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.09s\n"})}),"\n",(0,r.jsxs)(n.h3,{id:"step-3--write-the-sequential-multiregionpy",children:["Step 3 \u2014 Write the sequential ",(0,r.jsx)(n.code,{children:"multiregion.py"})]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/multiregion.py << \'PYEOF\'\n"""platformops.multiregion -- scan EC2 instances and EBS volumes across many regions, one at a time.\n\nModule 21 proved one region, one resource type. This is the first step\ntoward scanning several regions at once: two resource types (EC2\ninstances, EBS volumes), normalized into one shared `ResourceRecord`\nshape, and a `scan_regions()` that loops over a list of regions\nsequentially -- one region\'s scan fully finishes before the next one\nstarts. Every region\'s scan runs inside its own try/except, so one\nregion\'s failure lands in a `failed_regions` list instead of crashing the\nwhole scan.\n\nThis sequential version is deliberately not the finished module. It\nestablishes the normalization pattern and the partial-failure handling\nfirst, before concurrency gets introduced -- see the lab\'s Phase 2 for\nwhat changes next.\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import asdict, dataclass\nfrom typing import Any\n\nimport botocore.exceptions\n\nfrom platformops.awsclient import get_aws_client\n\nEXPIRED_TOKEN_CODES = {"ExpiredToken", "ExpiredTokenException", "RequestExpired"}\n\n\n@dataclass\nclass ResourceRecord:\n    """One resource of any type, already reduced to the same JSON-safe fields."""\n\n    resource_type: str\n    resource_id: str\n    region: str\n    tags: dict[str, str]\n    state: str | None\n\n\ndef _tags_from_ec2_list(tag_list: list[dict[str, str]] | None) -> dict[str, str]:\n    """Normalize an EC2-style `Tags` list into a plain dict.\n\n    A real, untagged resource can come back with `Tags` missing entirely,\n    or present as an empty list -- both mean "no tags," not an error, and\n    both must produce the same `{}` here.\n    """\n    return {tag["Key"]: tag["Value"] for tag in (tag_list or [])}\n\n\ndef list_ec2_instances(\n    client: Any, *, page_size: int | None = None\n) -> list[ResourceRecord]:\n    """List EC2 instances through a paginator -- the same pattern `cloudinventory.list_instances()` uses."""\n    paginate_kwargs: dict[str, Any] = {}\n    if page_size:\n        paginate_kwargs["PaginationConfig"] = {"PageSize": page_size}\n\n    region = client.meta.region_name\n    paginator = client.get_paginator("describe_instances")\n\n    records: list[ResourceRecord] = []\n    for page in paginator.paginate(**paginate_kwargs):\n        for reservation in page["Reservations"]:\n            for instance in reservation["Instances"]:\n                records.append(\n                    ResourceRecord(\n                        resource_type="ec2-instance",\n                        resource_id=instance["InstanceId"],\n                        region=region,\n                        tags=_tags_from_ec2_list(instance.get("Tags")),\n                        state=instance["State"]["Name"],\n                    )\n                )\n    return records\n\n\ndef list_ebs_volumes(\n    client: Any, *, page_size: int | None = None\n) -> list[ResourceRecord]:\n    """List EBS volumes through a paginator -- `describe_volumes` pages exactly like `describe_instances` does."""\n    paginate_kwargs: dict[str, Any] = {}\n    if page_size:\n        paginate_kwargs["PaginationConfig"] = {"PageSize": page_size}\n\n    region = client.meta.region_name\n    paginator = client.get_paginator("describe_volumes")\n\n    records: list[ResourceRecord] = []\n    for page in paginator.paginate(**paginate_kwargs):\n        for volume in page["Volumes"]:\n            records.append(\n                ResourceRecord(\n                    resource_type="ebs-volume",\n                    resource_id=volume["VolumeId"],\n                    region=region,\n                    tags=_tags_from_ec2_list(volume.get("Tags")),\n                    state=volume["State"],\n                )\n            )\n    return records\n\n\ndef scan_region(\n    *,\n    region: str,\n    profile: str | None = None,\n    page_size: int | None = None,\n) -> list[ResourceRecord]:\n    """Scan one region\'s EC2 instances and EBS volumes."""\n    client = get_aws_client("ec2", region=region, profile=profile)\n    records: list[ResourceRecord] = []\n    records.extend(list_ec2_instances(client, page_size=page_size))\n    records.extend(list_ebs_volumes(client, page_size=page_size))\n    return records\n\n\ndef _classify_client_error(exc: botocore.exceptions.ClientError) -> str:\n    code: str = exc.response.get("Error", {}).get("Code", "Unknown")\n    if code in EXPIRED_TOKEN_CODES:\n        return "expired-token"\n    return code\n\n\ndef scan_regions(\n    regions: list[str],\n    *,\n    profile: str | None = None,\n    page_size: int | None = None,\n) -> dict[str, Any]:\n    """Scan every region in `regions`, one at a time.\n\n    Each region\'s scan runs inside its own try/except -- a region that\n    fails (a `ClientError`, no credentials) contributes an entry to\n    `failed_regions` with the reason, and every other region\'s scan still\n    runs. This loop is sequential on purpose, for now: region 2 does not\n    start until region 1\'s scan (success or failure) has fully finished.\n    """\n    all_records: list[ResourceRecord] = []\n    failed_regions: list[dict[str, str]] = []\n\n    for region in regions:\n        try:\n            records = scan_region(region=region, profile=profile, page_size=page_size)\n            all_records.extend(records)\n        except botocore.exceptions.NoCredentialsError as exc:\n            failed_regions.append(\n                {"region": region, "error": "no-credentials", "message": str(exc)}\n            )\n        except botocore.exceptions.ClientError as exc:\n            failed_regions.append(\n                {\n                    "region": region,\n                    "error": _classify_client_error(exc),\n                    "message": str(exc),\n                }\n            )\n\n    failed_names = {failure["region"] for failure in failed_regions}\n    regions_scanned = [region for region in regions if region not in failed_names]\n\n    return {\n        "status": "ok" if not failed_regions else "partial",\n        "regions_scanned": regions_scanned,\n        "failed_regions": failed_regions,\n        "count": len(all_records),\n        "resources": [asdict(record) for record in all_records],\n    }\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4--watch-it-pass",children:"Step 4 \u2014 Watch it pass"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_multiregion.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"collecting ... collected 9 items\n\ntests/test_multiregion.py::test_tags_from_ec2_list_handles_missing_key_and_empty_list_and_real_tags PASSED [ 11%]\ntests/test_multiregion.py::test_list_ec2_instances_returns_normalized_records PASSED [ 22%]\ntests/test_multiregion.py::test_list_ec2_instances_walks_every_page_not_just_the_first PASSED [ 33%]\ntests/test_multiregion.py::test_list_ebs_volumes_returns_normalized_records PASSED [ 44%]\ntests/test_multiregion.py::test_list_ebs_volumes_walks_every_page PASSED [ 55%]\ntests/test_multiregion.py::test_scan_region_combines_ec2_and_ebs PASSED  [ 66%]\ntests/test_multiregion.py::test_scan_regions_succeeds_across_multiple_regions PASSED [ 77%]\ntests/test_multiregion.py::test_scan_regions_a_client_error_lands_in_failed_regions_not_a_crash PASSED [ 88%]\ntests/test_multiregion.py::test_scan_regions_runs_strictly_sequentially PASSED [100%]\n\n============================== 9 passed in 4.90s ===============================\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Nine tests, and the last one, ",(0,r.jsx)(n.code,{children:"test_scan_regions_runs_strictly_sequentially"}),", proves\ntoday's real behavior on purpose: each region's ",(0,r.jsx)(n.code,{children:'("start", "end")'})," pair appears together,\nwith no other region's ",(0,r.jsx)(n.code,{children:'"start"'})," in between. Phase 2 changes exactly that."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-2--review-a-diff-find-two-bugs-then-apply-the-fix",children:"Phase 2 \u2014 Review a diff, find two bugs, then apply the fix"}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"~/307-aipython-labs/labs/m23/rigged-diff.patch"})," is a diff that extends this exact file\ninto a concurrent, five-resource-type scanner \u2014 the shape the module actually needs to\nship. It adds security groups, Elastic IPs, S3 buckets, Markdown/CSV formatters, and swaps\nthe sequential loop for a thread-based one. It also contains ",(0,r.jsx)(n.strong,{children:"two seeded flaws"}),", the\nsame two bug classes this module's objectives name: an unbounded-concurrency bug and a\npagination bug."]}),"\n",(0,r.jsx)(n.h3,{id:"step-1--confirm-it-applies-cleanly",children:"Step 1 \u2014 Confirm it applies cleanly"}),"\n",(0,r.jsxs)(n.p,{children:["Never apply an unreviewed diff straight into your real project. Prove it applies against a\n",(0,r.jsx)(n.strong,{children:"throwaway copy"})," first:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cp -r ~/platformops /tmp/m23-scratch\ncd /tmp/m23-scratch\nrm -rf .venv\nuv sync\ngit apply --check ~/307-aipython-labs/labs/m23/rigged-diff.patch && echo "applies cleanly"\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"applies cleanly\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"rm -rf .venv && uv sync"})," step matters -- copying a ",(0,r.jsx)(n.code,{children:"uv"}),"-managed virtual environment with\n",(0,r.jsx)(n.code,{children:"cp -r"})," breaks the internal symlinks that point at your Python install, so any ",(0,r.jsx)(n.code,{children:"uv run"})," command\ninside the copy fails with a ",(0,r.jsx)(n.code,{children:"dyld"}),"/library-loading error. Removing the copied ",(0,r.jsx)(n.code,{children:".venv"})," and\nrebuilding it takes a few seconds and gives you a real, working environment for the rest of this\nphase."]}),"\n",(0,r.jsx)(n.h3,{id:"step-2--read-the-diff-top-to-bottom",children:"Step 2 \u2014 Read the diff, top to bottom"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cat ~/307-aipython-labs/labs/m23/rigged-diff.patch\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Do not skim. For the hunks that touch ",(0,r.jsx)(n.code,{children:"list_ec2_instances"})," and ",(0,r.jsx)(n.code,{children:"scan_regions"}),", ask:"]}),"\n",(0,r.jsxs)(n.ol,{children:["\n",(0,r.jsx)(n.li,{children:"Does this still walk every page a real account could return, or only the first one?"}),"\n",(0,r.jsx)(n.li,{children:"Does this still cap how many regions scan at once, or did the cap quietly stop doing\nanything?"}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"step-3--apply-it-in-the-scratch-copy-and-see-the-bugs-live",children:"Step 3 \u2014 Apply it in the scratch copy and see the bugs live"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd /tmp/m23-scratch\ngit apply ~/307-aipython-labs/labs/m23/rigged-diff.patch\nuv run python -c \"\nimport boto3\nfrom moto import mock_aws\nfrom platformops.multiregion import list_ec2_instances\n\n@mock_aws\ndef demo():\n    client = boto3.client('ec2', region_name='us-east-1')\n    for _ in range(12):\n        client.run_instances(ImageId='ami-12345678', MinCount=1, MaxCount=1, InstanceType='t2.micro')\n    records = list_ec2_instances(client, page_size=5)\n    print(f'created 12 instances, list_ec2_instances(page_size=5) returned: {len(records)}')\n\ndemo()\n\"\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"created 12 instances, list_ec2_instances(page_size=5) returned: 5\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Seven instances silently missing. ",(0,r.jsx)(n.code,{children:"list_ec2_instances()"})," in the diff calls\n",(0,r.jsx)(n.code,{children:"client.describe_instances(MaxResults=page_size)"})," once, directly \u2014 no paginator, no\n",(0,r.jsx)(n.code,{children:"NextToken"})," handling \u2014 so anything past the first page never comes back. Against a small\nmoto fixture with three or four instances this would look completely correct; it only\nshows up once an account (or a deliberately forced small ",(0,r.jsx)(n.code,{children:"page_size"}),", exactly like here)\nactually spans more than one page."]}),"\n",(0,r.jsx)(n.p,{children:"Now the concurrency bug:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd /tmp/m23-scratch\nuv run python -c \"\nimport threading, time\nfrom unittest import mock\nfrom platformops.multiregion import scan_regions\n\nlock = threading.Lock()\nstate = {'current': 0, 'peak': 0}\n\ndef fake_scan_region(*, region, profile=None, page_size=None):\n    with lock:\n        state['current'] += 1\n        state['peak'] = max(state['peak'], state['current'])\n    time.sleep(0.1)\n    with lock:\n        state['current'] -= 1\n    return []\n\nwith mock.patch('platformops.multiregion.scan_region', fake_scan_region), \\\n     mock.patch('platformops.multiregion.list_s3_buckets', lambda c: []), \\\n     mock.patch('platformops.multiregion.get_aws_client', lambda *a, **k: object()):\n    scan_regions([f'region-{i}' for i in range(10)], max_workers=3)\n    print(f'max_workers=3 requested, peak concurrent region-scans observed: {state[\\\"peak\\\"]}')\n\"\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"max_workers=3 requested, peak concurrent region-scans observed: 10\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"max_workers=3"})," was passed in, and all 10 regions still ran at once. The diff's\n",(0,r.jsx)(n.code,{children:"scan_regions()"})," accepts ",(0,r.jsx)(n.code,{children:"max_workers"})," as an argument \u2014 it reads as if it is respected \u2014\nbut the implementation spawns one raw ",(0,r.jsx)(n.code,{children:"threading.Thread"})," per region and starts every one\nof them immediately, never consulting ",(0,r.jsx)(n.code,{children:"max_workers"})," at all. On an account operating in\ntwenty regions, this is twenty threads opening twenty connections at the same moment."]}),"\n",(0,r.jsx)(n.h3,{id:"step-4--document-both-findings",children:"Step 4 \u2014 Document both findings"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cp ~/307-aipython-labs/labs/m23/review-template.md review-findings.md\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Fill in ",(0,r.jsx)(n.code,{children:"review-findings.md"})," with both flaws: ",(0,r.jsx)(n.code,{children:"list_ec2_instances"}),",\n",(0,r.jsx)(n.strong,{children:"pagination-bug"}),", and ",(0,r.jsx)(n.code,{children:"scan_regions"}),", ",(0,r.jsx)(n.strong,{children:"unbounded-concurrency"})," \u2014 one row each, with\nwhat is wrong and the fix instruction (a paginator for the first, an actual bounded\n",(0,r.jsx)(n.code,{children:"ThreadPoolExecutor(max_workers=...)"})," for the second)."]}),"\n",(0,r.jsx)(n.h3,{id:"step-5--apply-the-corrected-version-for-real",children:"Step 5 \u2014 Apply the corrected version for real"}),"\n",(0,r.jsxs)(n.p,{children:["With both bugs identified, write the fixed, full module into your real project \u2014 not the\nscratch copy. This is the version that actually ships: a bounded thread pool\n(",(0,r.jsx)(n.code,{children:"ThreadPoolExecutor(max_workers=...)"}),", never a raw thread per region), a real paginator\nfor every resource type AWS itself paginates, security groups, Elastic IPs and S3 buckets\nadded, and the Markdown/CSV formatters."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cd ~/platformops\ncat > src/platformops/multiregion.py << \'PYEOF\'\n"""platformops.multiregion -- scan five AWS resource types across many regions at once.\n\n`cloudinventory.py` (Module 21) proved one region, one resource type: EC2\ninstances, one paginator, one client. A real account inventory needs more\nthan that on two axes at once. First, more regions -- most accounts run\nresources in several regions, and scanning them one at a time, waiting for\neach to finish before starting the next, is slow in a way that gets worse\nevery time a region is added. Second, more resource types -- EC2 instances,\nEBS volumes, security groups and Elastic IPs, each with its own nested\nresponse shape, plus S3 buckets, which are not region-scoped at all.\n\nThis module answers both. `scan_regions()` fans out across regions with a\nsmall, bounded thread pool -- never one thread per region, the same\ndiscipline `httpclient.check_many()` (Module 10) already established for\nconcurrent HTTP checks -- and every region\'s scan runs inside its own\ntry/except, so one region\'s failure (a `ClientError`, an expired session)\nlands in a `failed_regions` list instead of losing every other region\'s\nresults. S3 gets one separate, account-wide pass, because `list_buckets()`\nalready returns every bucket regardless of which region it was created in\n-- looping it once per region would either miss nothing (S3 truly is\nglobal for this call) or repeat the exact same list N times, depending on\nhow it were wired; neither is worth the extra API calls.\n\nEvery resource type this module discovers -- no matter how differently its\nown AWS API nests the response -- gets flattened into the same\n`ResourceRecord` shape. `to_markdown()` and `to_csv()` both build their\noutput from that one normalized list; neither one re-runs any discovery\nlogic of its own.\n"""\n\nfrom __future__ import annotations\n\nimport csv\nimport io\nfrom collections.abc import Callable\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\nfrom dataclasses import asdict, dataclass\nfrom typing import Any\n\nimport botocore.exceptions\n\nfrom platformops.awsclient import get_aws_client\n\n# A small, explicit, configurable cap -- never one thread per region. See\n# Module 10 for why unbounded thread creation is a real production risk\n# (thread creation and context-switching overhead grows with count, and\n# every request-per-thread also opens its own network connection) rather\n# than just a style preference.\nDEFAULT_MAX_WORKERS = 5\n\n# The AWS-side error codes that specifically mean "your session token\n# expired mid-scan" -- a genuinely different problem from "this account\n# doesn\'t have permission" (UnauthorizedOperation) or "this region doesn\'t\n# support this resource type." Reported as its own "expired-token" reason\n# so an operator does not have to parse the raw AWS error code to tell the\n# two apart.\nEXPIRED_TOKEN_CODES = {"ExpiredToken", "ExpiredTokenException", "RequestExpired"}\n\n\n@dataclass\nclass ResourceRecord:\n    """One resource of any type, already reduced to the same JSON-safe fields.\n\n    EC2\'s `Reservations[].Instances[]`, EBS\'s flatter `Volumes[]`,\n    security groups\' `SecurityGroups[]`, EIPs\' `Addresses[]`, and S3\'s\n    bucket dicts each nest their response completely differently -- every\n    lister function in this module ends by building this one shape, so\n    nothing downstream (the report, the formatters) needs to know which\n    AWS API a given record came from.\n    """\n\n    resource_type: str\n    resource_id: str\n    region: str\n    tags: dict[str, str]\n    state: str | None\n\n\ndef _tags_from_ec2_list(tag_list: list[dict[str, str]] | None) -> dict[str, str]:\n    """Normalize an EC2-style `Tags` list into a plain dict.\n\n    A real, untagged resource can come back with `Tags` missing entirely,\n    or present as an empty list -- both mean "no tags," not an error, and\n    both must produce the same `{}` here.\n    """\n    return {tag["Key"]: tag["Value"] for tag in (tag_list or [])}\n\n\ndef list_ec2_instances(\n    client: Any, *, page_size: int | None = None\n) -> list[ResourceRecord]:\n    """List EC2 instances through a paginator -- the same pattern `cloudinventory.list_instances()` uses."""\n    paginate_kwargs: dict[str, Any] = {}\n    if page_size:\n        paginate_kwargs["PaginationConfig"] = {"PageSize": page_size}\n\n    region = client.meta.region_name\n    paginator = client.get_paginator("describe_instances")\n\n    records: list[ResourceRecord] = []\n    for page in paginator.paginate(**paginate_kwargs):\n        for reservation in page["Reservations"]:\n            for instance in reservation["Instances"]:\n                records.append(\n                    ResourceRecord(\n                        resource_type="ec2-instance",\n                        resource_id=instance["InstanceId"],\n                        region=region,\n                        tags=_tags_from_ec2_list(instance.get("Tags")),\n                        state=instance["State"]["Name"],\n                    )\n                )\n    return records\n\n\ndef list_ebs_volumes(\n    client: Any, *, page_size: int | None = None\n) -> list[ResourceRecord]:\n    """List EBS volumes through a paginator -- `describe_volumes` pages exactly like `describe_instances` does."""\n    paginate_kwargs: dict[str, Any] = {}\n    if page_size:\n        paginate_kwargs["PaginationConfig"] = {"PageSize": page_size}\n\n    region = client.meta.region_name\n    paginator = client.get_paginator("describe_volumes")\n\n    records: list[ResourceRecord] = []\n    for page in paginator.paginate(**paginate_kwargs):\n        for volume in page["Volumes"]:\n            records.append(\n                ResourceRecord(\n                    resource_type="ebs-volume",\n                    resource_id=volume["VolumeId"],\n                    region=region,\n                    tags=_tags_from_ec2_list(volume.get("Tags")),\n                    state=volume["State"],\n                )\n            )\n    return records\n\n\ndef list_security_groups(\n    client: Any, *, page_size: int | None = None\n) -> list[ResourceRecord]:\n    """List security groups through a paginator.\n\n    A security group has no natural "state" -- it exists or it does not.\n    `state` is always `None` here; that is a real, expected value for this\n    resource type, not a missing field.\n    """\n    paginate_kwargs: dict[str, Any] = {}\n    if page_size:\n        paginate_kwargs["PaginationConfig"] = {"PageSize": page_size}\n\n    region = client.meta.region_name\n    paginator = client.get_paginator("describe_security_groups")\n\n    records: list[ResourceRecord] = []\n    for page in paginator.paginate(**paginate_kwargs):\n        for group in page["SecurityGroups"]:\n            records.append(\n                ResourceRecord(\n                    resource_type="security-group",\n                    resource_id=group["GroupId"],\n                    region=region,\n                    tags=_tags_from_ec2_list(group.get("Tags")),\n                    state=None,\n                )\n            )\n    return records\n\n\ndef list_elastic_ips(client: Any) -> list[ResourceRecord]:\n    """List Elastic IPs with one direct `describe_addresses()` call -- no paginator exists for it.\n\n    `client.can_paginate("describe_addresses")` returns `False`: AWS never\n    registered a paginator for this operation, because one account\'s\n    Elastic IPs never span more than one API response page in practice.\n    Calling `get_paginator("describe_addresses")` here would raise\n    `OperationNotPageableError` -- this is a real API quirk to know about,\n    not a bug to route around.\n    """\n    region = client.meta.region_name\n    response = client.describe_addresses()\n\n    records: list[ResourceRecord] = []\n    for address in response["Addresses"]:\n        resource_id = address.get("AllocationId") or address["PublicIp"]\n        records.append(\n            ResourceRecord(\n                resource_type="elastic-ip",\n                resource_id=resource_id,\n                region=region,\n                tags=_tags_from_ec2_list(address.get("Tags")),\n                state="associated" if address.get("AssociationId") else "unassociated",\n            )\n        )\n    return records\n\n\ndef _bucket_region(client: Any, bucket_name: str) -> str:\n    """`get_bucket_location()` returns `None`/empty for `us-east-1` specifically -- every other region names itself."""\n    response = client.get_bucket_location(Bucket=bucket_name)\n    location = response.get("LocationConstraint")\n    return location or "us-east-1"\n\n\ndef _bucket_tags(client: Any, bucket_name: str) -> dict[str, str]:\n    """Best-effort tag lookup -- an untagged bucket\'s `get_bucket_tagging()` call fails with `NoSuchTagSet`, not an empty result.\n\n    That failure is expected, not an error: it means "this bucket has no\n    tags," the same meaning a missing `Tags` key has for every other\n    resource type in this module. Anything else is a real problem and is\n    left to raise.\n    """\n    try:\n        response = client.get_bucket_tagging(Bucket=bucket_name)\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "")\n        if code == "NoSuchTagSet":\n            return {}\n        raise\n    return {tag["Key"]: tag["Value"] for tag in response.get("TagSet", [])}\n\n\ndef list_s3_buckets(client: Any) -> list[ResourceRecord]:\n    """List every bucket in the account in one call, then resolve each bucket\'s real region separately.\n\n    S3 is not region-scoped the way EC2, EBS, security groups and EIPs\n    are: `list_buckets()` always returns every bucket in the account,\n    regardless of which region the client was built for. Looping this the\n    same way as the other four resource types -- once per region in\n    `scan_regions()`\'s per-region loop -- would call `list_buckets()`\n    once per region and return the exact same full list every time, N\n    times over, while never actually learning which region each bucket\n    lives in. This function is called exactly once, account-wide; the\n    bucket\'s own region comes from a second, per-bucket call.\n    """\n    response = client.list_buckets()\n\n    records: list[ResourceRecord] = []\n    for bucket in response["Buckets"]:\n        name = bucket["Name"]\n        records.append(\n            ResourceRecord(\n                resource_type="s3-bucket",\n                resource_id=name,\n                region=_bucket_region(client, name),\n                tags=_bucket_tags(client, name),\n                state=None,\n            )\n        )\n    return records\n\n\ndef scan_region(\n    *,\n    region: str,\n    profile: str | None = None,\n    page_size: int | None = None,\n) -> list[ResourceRecord]:\n    """Scan one region\'s EC2 instances, EBS volumes, security groups and Elastic IPs.\n\n    S3 is deliberately excluded here -- it is not region-scoped, and\n    `scan_regions()` discovers it once, account-wide, outside this\n    per-region function. `get_aws_client()` (Module 22) is used instead of\n    `cloudinventory.get_client()` because this function talks to more than\n    one service family per call; `endpoint_url` is never passed, so this\n    always resolves to real AWS, never Floci -- this module has no local\n    stand-in, unlike Module 22\'s project.\n    """\n    client = get_aws_client("ec2", region=region, profile=profile)\n    records: list[ResourceRecord] = []\n    records.extend(list_ec2_instances(client, page_size=page_size))\n    records.extend(list_ebs_volumes(client, page_size=page_size))\n    records.extend(list_security_groups(client, page_size=page_size))\n    records.extend(list_elastic_ips(client))\n    return records\n\n\ndef _classify_client_error(exc: botocore.exceptions.ClientError) -> str:\n    code: str = exc.response.get("Error", {}).get("Code", "Unknown")\n    if code in EXPIRED_TOKEN_CODES:\n        return "expired-token"\n    return code\n\n\ndef _scan_one_region(\n    region: str,\n    *,\n    profile: str | None,\n    page_size: int | None,\n    on_start: Callable[[], None] | None,\n    on_end: Callable[[], None] | None,\n) -> tuple[str, list[ResourceRecord] | None, dict[str, str] | None]:\n    """Run inside one worker thread: scan one region, never let its failure escape as an exception.\n\n    `on_start`/`on_end` exist purely so a test (or the Deep Dive) can\n    measure how many of these are actually running at once -- production\n    callers never need to pass them.\n    """\n    if on_start is not None:\n        on_start()\n    try:\n        records = scan_region(region=region, profile=profile, page_size=page_size)\n        return region, records, None\n    except botocore.exceptions.NoCredentialsError as exc:\n        return region, None, {"error": "no-credentials", "message": str(exc)}\n    except botocore.exceptions.ClientError as exc:\n        return (\n            region,\n            None,\n            {\n                "error": _classify_client_error(exc),\n                "message": str(exc),\n            },\n        )\n    finally:\n        if on_end is not None:\n            on_end()\n\n\ndef scan_regions(\n    regions: list[str],\n    *,\n    profile: str | None = None,\n    page_size: int | None = None,\n    max_workers: int = DEFAULT_MAX_WORKERS,\n    on_region_start: Callable[[], None] | None = None,\n    on_region_end: Callable[[], None] | None = None,\n) -> dict[str, Any]:\n    """Scan every region concurrently, bounded by `max_workers`, plus one account-wide S3 pass.\n\n    Each region\'s scan runs inside `_scan_one_region()`\'s own try/except --\n    a region that fails (a `ClientError`, an expired session, no\n    credentials) contributes an entry to `failed_regions` with the reason,\n    and every other region\'s scan still completes. The report always\n    returns whatever succeeded, plus an honest account of what did not.\n\n    `max_workers` bounds the thread pool itself -- `ThreadPoolExecutor`\n    never starts more than `max_workers` region-scans at once, no matter\n    how many regions are passed in. See the Deep Dive for a live\n    measurement proving this, not just asserting it.\n    """\n    all_records: list[ResourceRecord] = []\n    failed_regions: list[dict[str, str]] = []\n\n    with ThreadPoolExecutor(max_workers=max_workers) as executor:\n        futures = {\n            executor.submit(\n                _scan_one_region,\n                region,\n                profile=profile,\n                page_size=page_size,\n                on_start=on_region_start,\n                on_end=on_region_end,\n            ): region\n            for region in regions\n        }\n        for future in as_completed(futures):\n            region, records, failure = future.result()\n            if failure is not None:\n                failed_regions.append({"region": region, **failure})\n            else:\n                assert records is not None\n                all_records.extend(records)\n\n    s3_region = regions[0] if regions else "us-east-1"\n    try:\n        s3_client = get_aws_client("s3", region=s3_region, profile=profile)\n        all_records.extend(list_s3_buckets(s3_client))\n    except botocore.exceptions.NoCredentialsError as exc:\n        failed_regions.append(\n            {"region": "global/s3", "error": "no-credentials", "message": str(exc)}\n        )\n    except botocore.exceptions.ClientError as exc:\n        failed_regions.append(\n            {\n                "region": "global/s3",\n                "error": _classify_client_error(exc),\n                "message": str(exc),\n            }\n        )\n\n    failed_names = {failure["region"] for failure in failed_regions}\n    regions_scanned = [region for region in regions if region not in failed_names]\n\n    return {\n        "status": "ok" if not failed_regions else "partial",\n        "regions_scanned": regions_scanned,\n        "failed_regions": failed_regions,\n        "count": len(all_records),\n        "resources": [asdict(record) for record in all_records],\n    }\n\n\ndef to_markdown(records: list[ResourceRecord]) -> str:\n    """Render every record as one human-readable Markdown table, grouped by resource type.\n\n    Builds directly from the same normalized `list[ResourceRecord]`\n    `to_csv()` also reads -- neither formatter repeats any discovery\n    logic, they only differ in how they render the same records.\n    """\n    header = "| Resource Type | Resource ID | Region | State | Tags |"\n    separator = "| --- | --- | --- | --- | --- |"\n    lines = [header, separator]\n    for record in sorted(\n        records, key=lambda r: (r.resource_type, r.region, r.resource_id)\n    ):\n        tags = ", ".join(f"{k}={v}" for k, v in sorted(record.tags.items())) or "-"\n        state = record.state or "-"\n        lines.append(\n            f"| {record.resource_type} | {record.resource_id} | {record.region} "\n            f"| {state} | {tags} |"\n        )\n    return "\\n".join(lines)\n\n\ndef to_csv(records: list[ResourceRecord]) -> str:\n    """Render one row per resource -- machine-readable, same normalized records `to_markdown()` uses."""\n    buffer = io.StringIO()\n    writer = csv.writer(buffer)\n    writer.writerow(["resource_type", "resource_id", "region", "state", "tags"])\n    for record in records:\n        tags = ";".join(f"{k}={v}" for k, v in sorted(record.tags.items()))\n        writer.writerow(\n            [\n                record.resource_type,\n                record.resource_id,\n                record.region,\n                record.state or "",\n                tags,\n            ]\n        )\n    return buffer.getvalue()\n\n\n__all__ = [\n    "DEFAULT_MAX_WORKERS",\n    "ResourceRecord",\n    "list_ec2_instances",\n    "list_ebs_volumes",\n    "list_security_groups",\n    "list_elastic_ips",\n    "list_s3_buckets",\n    "scan_region",\n    "scan_regions",\n    "to_markdown",\n    "to_csv",\n]\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-6--extend-the-test-file-to-match-and-watch-the-full-suite-pass",children:"Step 6 \u2014 Extend the test file to match, and watch the full suite pass"}),"\n",(0,r.jsxs)(n.p,{children:["Two of the tests you wrote in Step 1 (",(0,r.jsx)(n.code,{children:"test_scan_region_combines_ec2_and_ebs"})," and\n",(0,r.jsx)(n.code,{children:"test_scan_regions_runs_strictly_sequentially"}),") no longer make sense: ",(0,r.jsx)(n.code,{children:"scan_region"})," now\nreturns four resource types, not two, and ",(0,r.jsx)(n.code,{children:"scan_regions"})," now runs concurrently, not\nsequentially. Delete both."]}),"\n",(0,r.jsxs)(n.p,{children:["In their place, write the 23 tests named in this step's own Expected output below -- that\nlist, in order, is the exact, complete set of test names to write, covering\n",(0,r.jsx)(n.code,{children:"list_security_groups"}),", ",(0,r.jsx)(n.code,{children:"list_elastic_ips"}),", ",(0,r.jsx)(n.code,{children:"list_s3_buckets"}),", the bounded concurrency\nbehavior (",(0,r.jsx)(n.code,{children:"max_workers"})," actually caps concurrent scans -- the direct opposite of what the\ndiff's bug did), the pagination-completeness of the whole scan, and both formatters. Then\nrun the full file:"]}),"\n",(0,r.jsx)(n.admonition,{title:"A real API quirk you'll hit here",type:"info",children:(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"describe_security_groups"}),"'s ",(0,r.jsx)(n.code,{children:"MaxResults"})," parameter has a minimum value of ",(0,r.jsx)(n.code,{children:"5"})," -- unlike\n",(0,r.jsx)(n.code,{children:"describe_instances"}),"/",(0,r.jsx)(n.code,{children:"describe_volumes"}),", you cannot force a small page size like ",(0,r.jsx)(n.code,{children:"page_size=2"}),"\nto test pagination. Use ",(0,r.jsx)(n.code,{children:"page_size=5"})," (or higher) for the security-groups pagination test, and\ncreate at least 6 mocked security groups so the test still proves a second page is walked."]})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_multiregion.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"collecting ... collected 23 items\n\ntests/test_multiregion.py::test_tags_from_ec2_list_handles_missing_key_and_empty_list_and_real_tags PASSED [  4%]\ntests/test_multiregion.py::test_list_ec2_instances_returns_normalized_records PASSED [  8%]\ntests/test_multiregion.py::test_list_ec2_instances_walks_every_page_not_just_the_first PASSED [ 13%]\ntests/test_multiregion.py::test_list_ebs_volumes_returns_normalized_records PASSED [ 17%]\ntests/test_multiregion.py::test_list_ebs_volumes_walks_every_page PASSED [ 21%]\ntests/test_multiregion.py::test_list_security_groups_returns_normalized_records_with_no_state PASSED [ 26%]\ntests/test_multiregion.py::test_list_security_groups_walks_every_page PASSED [ 30%]\ntests/test_multiregion.py::test_list_elastic_ips_returns_normalized_records PASSED [ 34%]\ntests/test_multiregion.py::test_describe_addresses_has_no_paginator_confirming_the_api_quirk PASSED [ 39%]\ntests/test_multiregion.py::test_list_s3_buckets_discovers_each_buckets_real_region PASSED [ 43%]\ntests/test_multiregion.py::test_list_s3_buckets_handles_an_untagged_bucket_without_crashing PASSED [ 47%]\ntests/test_multiregion.py::test_list_s3_buckets_reads_real_tags_when_present PASSED [ 52%]\ntests/test_multiregion.py::test_scan_region_combines_all_four_resource_types PASSED [ 56%]\ntests/test_multiregion.py::test_scan_regions_succeeds_across_multiple_regions PASSED [ 60%]\ntests/test_multiregion.py::test_scan_regions_pagination_completeness_across_the_whole_scan PASSED [ 65%]\ntests/test_multiregion.py::test_scan_regions_a_client_error_lands_in_failed_regions_not_a_crash PASSED [ 69%]\ntests/test_multiregion.py::test_scan_regions_reports_an_expired_token_distinctly_from_other_client_errors PASSED [ 73%]\ntests/test_multiregion.py::test_scan_regions_a_region_failure_does_not_abort_other_regions PASSED [ 78%]\ntests/test_multiregion.py::test_scan_regions_never_exceeds_max_workers_concurrent_scans PASSED [ 82%]\ntests/test_multiregion.py::test_scan_regions_default_max_workers_is_a_small_bounded_number PASSED [ 86%]\ntests/test_multiregion.py::test_to_markdown_renders_a_table_with_every_record PASSED [ 91%]\ntests/test_multiregion.py::test_to_csv_renders_one_row_per_resource PASSED [ 95%]\ntests/test_multiregion.py::test_formatters_use_the_same_records_no_separate_discovery PASSED [100%]\n\n============================== 23 passed in 17.39s ===============================\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"test_scan_regions_never_exceeds_max_workers_concurrent_scans"})," is the test that would have\ncaught the diff's concurrency bug directly: it drives ",(0,r.jsx)(n.code,{children:"scan_regions()"})," with a slow fake\n",(0,r.jsx)(n.code,{children:"scan_region()"})," and a lock-protected counter, and asserts the observed peak concurrency\nnever exceeds ",(0,r.jsx)(n.code,{children:"max_workers"})," \u2014 the exact measurement Step 3 just did by hand.\n",(0,r.jsx)(n.code,{children:"test_scan_regions_pagination_completeness_across_the_whole_scan"})," is the equivalent for\nthe pagination bug: twelve mocked instances, a forced small ",(0,r.jsx)(n.code,{children:"page_size"}),", and an assertion\nthat all twelve come back."]}),"\n",(0,r.jsx)(n.h3,{id:"step-7--wire-the-cli",children:"Step 7 \u2014 Wire the CLI"}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"multi-region-scan"})," command to ",(0,r.jsx)(n.code,{children:"src/platformops/cli.py"}),", following the same\n",(0,r.jsx)(n.code,{children:"--json"}),"/plain-text pattern every other command uses, plus ",(0,r.jsx)(n.code,{children:"--format markdown|csv"})," for the\ntwo required human/machine output shapes and ",(0,r.jsx)(n.code,{children:"--max-workers"})," to expose the concurrency\ncap. Import ",(0,r.jsx)(n.code,{children:"scan_regions"}),", ",(0,r.jsx)(n.code,{children:"ResourceRecord"}),", ",(0,r.jsx)(n.code,{children:"to_csv"})," and ",(0,r.jsx)(n.code,{children:"to_markdown"})," next to the other\n",(0,r.jsx)(n.code,{children:"platformops.*"})," imports."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run platformops multi-region-scan --help\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:" Usage: platformops multi-region-scan [OPTIONS]\n\n Scan EC2 instances, EBS volumes, security groups, Elastic IPs and S3 buckets\n across many regions (the M23 project).\n\n Regions are scanned concurrently, bounded by `--max-workers` -- never\n one thread per region. A region that fails (a `ClientError`, an\n expired session, missing credentials) is reported in the failed\n region list instead of aborting the scan; the command still exits 0\n only if every region succeeded.\n\n\u256D\u2500 Options \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 *  --regions            <str>  comma-separated AWS regions, e.g.             \u2502\n\u2502                                us-east-1,eu-west-1                           \u2502\n\u2502                                [required]                                    \u2502\n\u2502    --profile            <str>  named AWS profile to use (defaults to the     \u2502\n\u2502                                normal credential chain)                      \u2502\n\u2502    --max-workers        <int>  maximum number of regions scanned             \u2502\n\u2502                                concurrently                                  \u2502\n\u2502                                [default: 5]                                  \u2502\n\u2502    --format             <str>  markdown or csv (ignored with --json)         \u2502\n\u2502                                [default: markdown]                           \u2502\n\u2502    --json                      print a machine-readable JSON report instead  \u2502\n\u2502                                of markdown/csv                               \u2502\n\u2502    --help                      Show this message and exit.                   \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n"})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-3--see-it-work-live-against-a-fake-aws-account",children:"Phase 3 \u2014 See it work, live, against a fake AWS account"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/multiregion_demo.py << \'PYEOF\'\nimport json\n\nimport boto3\nfrom moto import mock_aws\n\nfrom platformops.multiregion import scan_regions, to_csv, to_markdown, ResourceRecord\n\n\n@mock_aws\ndef demo():\n    for region, az in [("us-east-1", "us-east-1a"), ("eu-west-1", "eu-west-1a")]:\n        ec2 = boto3.client("ec2", region_name=region)\n        ec2.run_instances(\n            ImageId="ami-12345678", MinCount=1, MaxCount=1, InstanceType="t2.micro",\n            TagSpecifications=[{"ResourceType": "instance", "Tags": [\n                {"Key": "env", "Value": "prod"}, {"Key": "team", "Value": "platform"},\n            ]}],\n        )\n        ec2.create_volume(Size=8, AvailabilityZone=az)\n        ec2.allocate_address(Domain="vpc")\n\n    s3 = boto3.client("s3", region_name="us-east-1")\n    s3.create_bucket(Bucket="platformops-reports-east")\n    s3.put_bucket_tagging(\n        Bucket="platformops-reports-east",\n        Tagging={"TagSet": [{"Key": "team", "Value": "platform"}]},\n    )\n    s3.create_bucket(\n        Bucket="platformops-reports-west",\n        CreateBucketConfiguration={"LocationConstraint": "us-west-2"},\n    )\n\n    result = scan_regions(["us-east-1", "eu-west-1"])\n    records = [ResourceRecord(**r) for r in result["resources"]]\n\n    print("--- as Markdown ---")\n    print(to_markdown(records))\n    print()\n    print("--- as CSV ---")\n    print(to_csv(records))\n\n\ndemo()\nPYEOF\nuv run python /tmp/multiregion_demo.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"--- as Markdown ---\n| Resource Type | Resource ID | Region | State | Tags |\n| --- | --- | --- | --- | --- |\n| ebs-volume | vol-018372babd0cb5979 | eu-west-1 | available | - |\n| ebs-volume | vol-a9c82ffed05c9af16 | eu-west-1 | in-use | - |\n| ebs-volume | vol-7ad2bad4b603934f8 | us-east-1 | in-use | - |\n| ebs-volume | vol-886cb93693521ff36 | us-east-1 | available | - |\n| ec2-instance | i-2050865a1fd73856b | eu-west-1 | running | env=prod, team=platform |\n| ec2-instance | i-81d6fb9098bc47a16 | us-east-1 | running | env=prod, team=platform |\n| elastic-ip | eipalloc-024e480e2c4642a25 | eu-west-1 | unassociated | - |\n| elastic-ip | eipalloc-9b77c5f7a9bfa1fa1 | us-east-1 | unassociated | - |\n| s3-bucket | platformops-reports-east | us-east-1 | - | team=platform |\n| s3-bucket | platformops-reports-west | us-west-2 | - | - |\n| security-group | sg-cf1c610d93a23aae2 | eu-west-1 | - | - |\n| security-group | sg-bb196733a15c93108 | us-east-1 | - | - |\n\n--- as CSV ---\nresource_type,resource_id,region,state,tags\nec2-instance,i-2050865a1fd73856b,eu-west-1,running,env=prod;team=platform\nebs-volume,vol-a9c82ffed05c9af16,eu-west-1,in-use,\nebs-volume,vol-018372babd0cb5979,eu-west-1,available,\nsecurity-group,sg-cf1c610d93a23aae2,eu-west-1,,\nelastic-ip,eipalloc-024e480e2c4642a25,eu-west-1,unassociated,\nec2-instance,i-81d6fb9098bc47a16,us-east-1,running,env=prod;team=platform\nebs-volume,vol-7ad2bad4b603934f8,us-east-1,in-use,\nebs-volume,vol-886cb93693521ff36,us-east-1,available,\nsecurity-group,sg-bb196733a15c93108,us-east-1,,\nelastic-ip,eipalloc-9b77c5f7a9bfa1fa1,us-east-1,unassociated,\ns3-bucket,platformops-reports-east,us-east-1,,team=platform\ns3-bucket,platformops-reports-west,us-west-2,,\n"})}),"\n",(0,r.jsx)(n.admonition,{title:"This output is point-in-time",type:"warning",children:(0,r.jsxs)(n.p,{children:["The instance, volume, security-group and EIP IDs are generated fresh by moto on every run\n\u2014 yours will differ. Notice ",(0,r.jsx)(n.code,{children:"platformops-reports-west"})," shows region ",(0,r.jsx)(n.code,{children:"us-west-2"})," in both\nformats even though the scan only asked for ",(0,r.jsx)(n.code,{children:"us-east-1"})," and ",(0,r.jsx)(n.code,{children:"eu-west-1"})," \u2014 that bucket's\nregion came from S3's own account-wide discovery pass, not from either region requested on\nthe command line."]})}),"\n",(0,r.jsxs)(n.p,{children:["Now the CLI itself, with no credentials configured at all \u2014 a real\n",(0,r.jsx)(n.code,{children:"NoCredentialsError"}),", on every region including the S3 pass, no moto involved:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'env -i PATH="$PATH" HOME="$HOME" \\\n  AWS_CONFIG_FILE=/nonexistent/config \\\n  AWS_SHARED_CREDENTIALS_FILE=/nonexistent/credentials \\\n  AWS_EC2_METADATA_DISABLED=true \\\n  uv run platformops multi-region-scan --regions us-east-1\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"| Resource Type | Resource ID | Region | State | Tags |\n| --- | --- | --- | --- | --- |\n\n2 region(s) failed:\n  us-east-1: no-credentials -- Unable to locate credentials\n  global/s3: no-credentials -- Unable to locate credentials\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Exit code ",(0,r.jsx)(n.code,{children:"1"}),". Notice the report still prints a well-formed (empty) Markdown table, not a\ncrash \u2014 even total failure goes through the same ",(0,r.jsx)(n.code,{children:"failed_regions"})," reporting path as a\npartial one."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run platformops multi-region-scan --regions us-east-1 --format xml\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"Error: --format must be markdown or csv, got 'xml'\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Exit code ",(0,r.jsx)(n.code,{children:"2"})," \u2014 invalid option, caught before ",(0,r.jsx)(n.code,{children:"scan_regions()"})," is even called."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-4--full-quality-gate-and-version-bump",children:"Phase 4 \u2014 Full quality gate and version bump"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 26%]\n........................................................................ [ 53%]\n........................................................................ [ 80%]\n.....................................................                    [100%]\n269 passed in 49.73s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["269 tests total \u2014 the 239 already in place at ",(0,r.jsx)(n.code,{children:"v1.7.0"}),", plus this module's 23\n",(0,r.jsx)(n.code,{children:"test_multiregion.py"})," tests, plus 7 new ",(0,r.jsx)(n.code,{children:"test_cli.py"})," tests for ",(0,r.jsx)(n.code,{children:"multi-region-scan"}),"."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format --check src/ tests/\nuv run mypy src/\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"All checks passed!\n39 files already formatted\nSuccess: no issues found in 20 source files\n"})}),"\n",(0,r.jsx)(n.p,{children:"Bump the version and update the changelog:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run python -c \"\npath = 'pyproject.toml'\ntext = open(path).read()\ntext = text.replace('version = \\\"1.7.0\\\"', 'version = \\\"1.8.0\\\"', 1)\nopen(path, 'w').write(text)\n\"\nuv run platformops version\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 1.8.0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"## [1.8.0] - Multi-Region Cloud Inventory"})," section to ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," above the\nprevious release, following the same ",(0,r.jsx)(n.code,{children:"### Added"})," structure every prior entry uses."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"ImportError: cannot import name 'multiregion'"})," after Phase 1 Step 3."]})," Confirm the\nfile landed at ",(0,r.jsx)(n.code,{children:"src/platformops/multiregion.py"}),", not ",(0,r.jsx)(n.code,{children:"scripts/"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"git apply --check"})," in Phase 2 Step 1 reports a conflict."]})," Confirm the scratch copy\nwas made from a clean ",(0,r.jsx)(n.code,{children:"~/platformops"})," still at Phase 1's sequential, EC2+EBS-only\n",(0,r.jsx)(n.code,{children:"multiregion.py"})," \u2014 the diff only applies against that exact starting point, not against\nthe final version you write in Step 5."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"The pagination demo in Phase 2 Step 3 returns 12, not 5."})," Confirm you are running it\ninside ",(0,r.jsx)(n.code,{children:"/tmp/m23-scratch"})," (the diff applied there), not ",(0,r.jsx)(n.code,{children:"~/platformops"})," \u2014 your real\nproject should still have the correct, paginated version at this point in the lab."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"The concurrency demo in Phase 2 Step 3 shows a peak of 3, not 10."})," Same check as\nabove \u2014 confirm you are in ",(0,r.jsx)(n.code,{children:"/tmp/m23-scratch"}),", and that ",(0,r.jsx)(n.code,{children:"git apply"})," reported no output\n(silence means success)."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"test_scan_regions_never_exceeds_max_workers_concurrent_scans"})," is flaky."]})," It depends\non ",(0,r.jsx)(n.code,{children:"time.sleep(0.05)"})," inside the fake region-scan being long enough for real thread\nscheduling overlap to show up. On a very slow or heavily loaded machine, rerun it once \u2014\na single flake here is a timing artifact, not a real bug, but a ",(0,r.jsx)(n.em,{children:"repeated"})," failure is\nworth investigating."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"uv run platformops version"})," still reports ",(0,r.jsx)(n.code,{children:"1.7.0"}),"."]})," Run ",(0,r.jsx)(n.code,{children:"uv sync"})," again after\nediting ",(0,r.jsx)(n.code,{children:"pyproject.toml"}),"."]}),"\n"]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -f /tmp/multiregion_demo.py\nrm -rf /tmp/m23-scratch review-findings.md\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"src/platformops/multiregion.py"}),", ",(0,r.jsx)(n.code,{children:"tests/test_multiregion.py"}),", the ",(0,r.jsx)(n.code,{children:"multi-region-scan"})," CLI\ncommand, the ",(0,r.jsx)(n.code,{children:"v1.8.0"})," version bump, and the ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," entry all stay \u2014 they are\npermanent additions to the project. moto's fake AWS account exists only for the duration\nof the process that ran it; there is no real AWS state to tear down."]})]})}function u(e={}){let{wrapper:n}={...(0,o.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(d,{...e})}):d(e)}},8453(e,n,s){s.d(n,{R:()=>i,x:()=>a});var t=s(6540);let r={},o=t.createContext(r);function i(e){let n=t.useContext(o);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:i(e.components),t.createElement(o.Provider,{value:n},e.children)}}}]);