"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["8345"],{2933(e,t,a){a.r(t),a.d(t,{metadata:()=>o,default:()=>p,frontMatter:()=>s,contentTitle:()=>c,toc:()=>h,assets:()=>l});var o=JSON.parse('{"id":"m7-errors-logging-debugging/quiz","title":"Quiz: Errors, Logging and Debugging","description":"<Quiz questions={[","source":"@site/docs/m7-errors-logging-debugging/quiz.mdx","sourceDirName":"m7-errors-logging-debugging","slug":"/m7-errors-logging-debugging/quiz","permalink":"/307-aipython-site/docs/m7-errors-logging-debugging/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Errors, Logging and Debugging"},"sidebar":"courseSidebar","previous":{"title":"Lab: Errors, Logging and Debugging","permalink":"/307-aipython-site/docs/m7-errors-logging-debugging/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m7-errors-logging-debugging/deep-dive"}}'),i=a(4848),n=a(8453),r=a(7517);let s={sidebar_position:3,title:"Quiz: Errors, Logging and Debugging"},c="Errors, Logging and Debugging -- Quiz",l={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,n.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.header,{children:(0,i.jsx)(t.h1,{id:"errors-logging-and-debugging----quiz",children:"Errors, Logging and Debugging -- Quiz"})}),"\n",(0,i.jsx)(r.A,{questions:[{prompt:"A traceback ends with `UnboundLocalError: cannot access local variable 'data' where it is not associated with a value`, with several frames printed above it. Which line tells you what actually broke?",options:[{text:'The first line, "Traceback (most recent call last):"',correct:!1,explanation:"That line is just a header announcing a traceback follows -- it carries no information about the actual failure."},{text:"The last line -- the error type and its message",correct:!0,explanation:"Read a traceback bottom-up, the way you would read an accident report: the last line names what actually hit you. The frames above it just show the route the program took to get there."},{text:"The middle frame, because the real error is always in the middle",correct:!1,explanation:"There is no rule that the real error sits in the middle -- the last line is always the one that names the error type and message."},{text:"Whichever frame has the most code shown next to it",correct:!1,explanation:"How much source code a frame shows depends on formatting, not on which frame explains the failure. The last line is what tells you."}]},{prompt:"Which of the following are runtime errors (exceptions) -- not syntax errors and not logic errors? (Select all that apply.)",multiSelect:!0,options:[{text:"A `FileNotFoundError` raised the moment `open()` runs on a path that does not exist",correct:!0,explanation:"The code is valid Python; the failure happens the instant that specific line executes -- the definition of a runtime error."},{text:"A missing colon after an `if` statement, caught before the program runs at all",correct:!1,explanation:"This is a syntax error -- Python refuses to even start running the program, the way a gibberish recipe step stops you before you begin cooking."},{text:"A `ZeroDivisionError` raised when a denominator turns out to be zero",correct:!0,explanation:"Same shape as the FileNotFoundError example: valid code, failure at the exact line that executes the bad operation."},{text:'A validation tool that runs start to finish with no error, but reports "OK" for a config with a genuinely bad value',correct:!1,explanation:"This is a logic error -- nothing stops the program, it just produces a wrong answer. It is the most dangerous of the three because nothing announces it."}]},{prompt:"You are writing code that loads a config file and want to handle a missing file cleanly, without hiding a genuine bug elsewhere in your program. Which is the best choice?",options:[{text:"`except FileNotFoundError:` (or a specific custom exception for that case)",correct:!0,explanation:"This is the narrow-catch rule: catch exactly the type you know how to handle. A different, unexpected exception keeps propagating and gets noticed instead of buried."},{text:"`except Exception:`",correct:!1,explanation:"This catches the missing-file case, but also catches every other bug -- a typo in your own code, an unrelated crash -- silently, which is exactly what the lesson warns against."},{text:"A bare `except:`",correct:!1,explanation:"Even broader than `except Exception:` -- it catches literally everything, including things like a keyboard interrupt, and hides all of it the same way."},{text:"No `try`/`except` at all -- let every exception propagate",correct:!1,explanation:"This means a routine, expected failure (a missing file) shows the user a raw traceback instead of a message they can act on -- the opposite of what a clean tool should do."}]},{prompt:'A CI pipeline runs a validation script and only checks its exit code -- it never reads the printed output. The broken version of this module\'s lab tool printed "something is wrong with the config" for a file with a missing field, but still exited `0`. What does the CI pipeline do?',options:[{text:'It fails the build, because it noticed the words "something is wrong" in the output',correct:!1,explanation:"The scenario states the pipeline only checks the exit code -- it never reads the printed text at all."},{text:"It treats the run as successful and continues, because exit code `0` is the only signal it reads",correct:!0,explanation:"This is exactly the danger the lesson describes: the exit code is the contract a machine reads. A tool that prints a warning but exits `0` gets waved through by anything that only checks the code."},{text:"It automatically retries the validation with `--verbose`",correct:!1,explanation:'Nothing in this scenario or this module\'s tools triggers an automatic retry -- a CI pipeline does exactly what it is told, which here is "check the exit code."'},{text:"It cannot proceed, because a non-zero exit code is required by definition",correct:!1,explanation:"The tool exited `0`, which most CI systems treat as success by default -- there is no such automatic non-zero requirement."}]},{prompt:"In this course's exit-code convention, what does exit code `2` mean, as distinct from `0` and `1`?",options:[{text:"A more severe version of `1` -- something is very wrong with the config",correct:!1,explanation:"Severity is not what separates `1` from `2` here. `1` and `2` describe two different kinds of problem, not two intensities of the same one."},{text:"The tool itself was called incorrectly (for example, with no file argument) -- a different kind of problem from anything wrong with the file being checked",correct:!0,explanation:'This is the usage-error code `argparse` returns automatically when a required argument is missing. It tells the caller "you used this tool wrong," distinct from `1`, which means "the tool ran correctly and found a real problem."'},{text:"The same thing as `1` -- most tools use them interchangeably",correct:!1,explanation:"They are deliberately different signals. Conflating them would defeat the point of having more than one non-zero exit code."},{text:"A warning that was logged but did not stop execution",correct:!1,explanation:"A logged warning does not, by itself, change a process's exit code -- exit codes are set explicitly by the code returning or calling `sys.exit()`."}]},{prompt:"`config.py` defines `ConfigNotFoundError`, `ConfigPermissionError` and `ConfigParseError` as subclasses of `ConfigError`, instead of three unrelated exception classes. What does this buy you? (Select all that apply.)",multiSelect:!0,options:[{text:'Code that only needs to know "was there any file-level problem" can catch `ConfigError` once and handle all three cases in one place',correct:!0,explanation:"This is the point of a small exception family: a broad catch on the base class still works, because Python matches subclasses against a parent-class `except`."},{text:"Code that needs to react differently to a missing file versus a permissions problem can still catch the specific subclass",correct:!0,explanation:"The family gives you both options -- catch broadly at `ConfigError`, or narrowly at a specific subclass, depending on what the calling code actually needs to do differently."},{text:"Every existing `except ConfigError:` elsewhere in the codebase keeps working unchanged, because all three new types are still `ConfigError` instances",correct:!0,explanation:"This is exactly why the lab's existing tests (written against `ConfigError` before this module) kept passing unchanged after the family was introduced."},{text:"It means the code never has to use `except Exception:` for anything, in any part of the program",correct:!1,explanation:"The exception family only covers config-file problems specifically. It says nothing about how the rest of the program should catch unrelated errors."}]},{prompt:"What does a `finally` block guarantee, that an `except` block alone does not?",options:[{text:"It only runs if no exception was raised",correct:!1,explanation:"That describes `else`, not `finally` -- `else` runs only on the success path, while `finally` runs regardless."},{text:"It runs every time -- whether the `try` block succeeded, raised a handled exception, or raised one that was never caught",correct:!0,explanation:"This is exactly why `finally` is the right place for cleanup (closing a file, releasing a lock): it runs no matter how the block exits."},{text:"It replaces the need for an `except` block entirely",correct:!1,explanation:"`finally` does not catch or handle anything -- an exception the `try` block raises still propagates after `finally` runs, unless something else catches it."},{text:"It only runs for exceptions the `except` clause did not match",correct:!1,explanation:"There is no such conditional behavior -- `finally` always runs, matched exception, unmatched exception, or no exception at all."}]},{prompt:"Why put success-only code in an `else` block, rather than simply after the `try` block's last line?",options:[{text:"It runs faster than code placed after the `try` block",correct:!1,explanation:"There is no performance difference -- this is about which errors get caught, not about speed."},{text:"Code in `else` is not covered by the `try`'s `except` clause, so an exception it happens to raise is never mistaken for the failure the `try` block was checking for",correct:!0,explanation:"This is the real reason: if success-path code were inside the `try` block (or right after it, sharing the same `except`), a bug in that code could get wrongly caught and reported as the original failure."},{text:"It is required syntax -- Python does not allow code directly after a `try`/`except` pair",correct:!1,explanation:"Python allows code directly after a `try`/`except` pair with no `else` at all -- `else` is optional, chosen for the reason above, not because it is mandatory."},{text:"It automatically retries the `try` block if the `else` code fails",correct:!1,explanation:"There is no automatic retry behavior tied to `else` -- if code in `else` raises, it propagates normally, uncaught by the preceding `except`."}]},{prompt:"Both `platformops.config` and `platformops.diagnostics` call `logging.basicConfig(level=DEBUG if args.verbose else WARNING, ...)`. What is actually hidden when `--verbose` is NOT passed?",options:[{text:"Every log line -- nothing is printed at all without `--verbose`",correct:!1,explanation:"WARNING-level messages (and above) still print by default -- only DEBUG and INFO detail is hidden."},{text:"Only `DEBUG` and `INFO` detail -- `WARNING` messages (like a config or validation problem being logged) still show by default",correct:!0,explanation:"This is the actual behavior demonstrated in the lab: a permission-denied run shows a `WARNING` log line even without `--verbose`, because `WARNING` clears the default level."},{text:"The `print()` output that reports the actual result to the user",correct:!1,explanation:"`print()` calls are unaffected by the logging level entirely -- they are the tool's report to the user, separate from the logging pipeline."},{text:"Only messages that mention a specific field name",correct:!1,explanation:"Logging levels filter by severity (DEBUG/INFO/WARNING/ERROR), not by the content of the message."}]},{prompt:"Reviewing a coding agent's diff for adding the `ConfigPermissionError` handling and the `aws_account` pattern check, which of these should make you push back or ask for changes? (Select all that apply.)",multiSelect:!0,options:[{text:"A log call includes the full parsed config dictionary, values and all, when `--verbose` is on",correct:!0,explanation:"This breaks the secrets-never-logged discipline from the lesson -- logging should carry the file path and, on a validation failure, the field name, never a field's actual value."},{text:"`pyproject.toml` gained a new line under `dependencies`",correct:!0,explanation:"The spec is explicit that no new dependency is needed -- `argparse` and `logging` are both standard library. A new dependency here is scope creep."},{text:"The new `ConfigPermissionError` is a subclass of `ConfigError`, and existing tests that catch `ConfigError` still pass unchanged",correct:!1,explanation:"This is exactly the correct, expected shape of the change -- not a reason to reject it."},{text:"The `aws_account` check uses `Field(pattern=...)`, the same style already used for `kubernetes_namespace` in the same file",correct:!1,explanation:"Reusing an existing, established pattern in the codebase is good practice, not a red flag."}]},{prompt:"You are debugging a test failure you cannot immediately explain. Which is the right first move, according to the debugging ladder from the lesson?",options:[{text:"Add `print()` statements throughout the code until something looks wrong",correct:!1,explanation:"This is step 3 on the ladder, not step 1 -- skipping straight to it, before reading the error and reproducing it reliably, is how time gets wasted changing code that was never the problem."},{text:"Read the full error message and traceback first -- it usually already names the problem",correct:!0,explanation:"This is the first rung. Most of the information you need is already printed; skimming past it to start guessing is the most common way to waste time debugging."},{text:"Rewrite the function from scratch",correct:!1,explanation:"Rewriting before understanding the actual cause risks losing correct code alongside the bug, and does not guarantee the same mistake will not happen again."},{text:"Immediately write a new test asserting the code should not fail",correct:!1,explanation:"A failing test that captures the bug comes after you understand it (the last rung) -- writing an assertion before you understand the cause does not help you find it."}]},{prompt:'`config.py` writes `raise ConfigParseError(f"invalid YAML in {path}: {exc}") from exc` when catching a `yaml.YAMLError`. What does the `from exc` accomplish?',options:[{text:"It is required syntax -- `raise` cannot appear inside an `except` block without it",correct:!1,explanation:"A plain `raise NewError(...)` inside an `except` block is valid Python without `from` -- it just chains implicitly instead of explicitly, with different wording in the traceback."},{text:"It chains the new exception to the original one, so the original `yaml.YAMLError` and its traceback stay visible instead of being discarded",correct:!0,explanation:"This is the difference between explicit chaining (`from exc`) and throwing the original away (`from None`) -- the original evidence stays attached for whoever reads the traceback next."},{text:"It automatically retries loading the file with a different YAML parser",correct:!1,explanation:"There is no retry behavior associated with exception chaining -- `from exc` only affects what information is attached to the new exception, not what code runs next."},{text:"It suppresses the new `ConfigParseError` from being raised at all",correct:!1,explanation:"The opposite is true -- `ConfigParseError` is still raised and still propagates; `from exc` only changes what is chained to it, not whether it happens."}]},{prompt:'Before this module\'s fix, the broken diagnostics tool printed "OK" for `service-badaccount.yaml`, a file with a 5-digit `aws_account` value (AWS account IDs are always 12 digits). Which category of error does this behavior fall into?',options:[{text:"A syntax error -- the YAML itself was invalid",correct:!1,explanation:"The YAML in that file parses perfectly well -- every field is present and the right Python type. The problem is entirely about the value, not the syntax."},{text:"A runtime error -- the tool crashed while checking this file",correct:!1,explanation:"The tool did not crash on this file at all -- it ran start to finish and printed a result. That rules out a runtime error for this specific case."},{text:"A logic error -- the program ran successfully and produced a wrong answer, with nothing to signal that anything was off",correct:!0,explanation:"This is precisely the lesson's definition of a logic error, and why it calls this the most dangerous of the three kinds: nothing stops, nothing crashes, and the wrong answer looks exactly like a right one until someone checks."},{text:"Not an error at all, since the tool did not raise an exception",correct:!1,explanation:'Not raising an exception does not mean nothing was wrong -- a five-digit AWS account number is a genuinely bad value, and reporting it as "OK" is exactly the failure this module exists to fix.'}]}]})]})}function p(e={}){let{wrapper:t}={...(0,n.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>r});var o=a(4848),i=a(6540);function n({q:e,state:t,onToggle:a}){return(0,o.jsxs)("div",{className:"question_QpiT",children:[(0,o.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,o.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,o.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,i)=>{let n=t.selected.has(i),r="option_qGol";return t.checked?e.correct?r+=" correct_LYA5":n&&(r+=" incorrect_kRFc"):n&&(r+=" chosen_H3WG"),(0,o.jsxs)("li",{children:[(0,o.jsxs)("button",{type:"button",className:r,disabled:t.checked,onClick:()=>a(i),children:[(0,o.jsx)("span",{className:"marker_vN_w",children:n?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(n||e.correct)&&e.explanation&&(0,o.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},i)})})]})}function r({questions:e}){let[t,a]=(0,i.useState)(e.map(()=>({selected:new Set,checked:!1}))),s=t.every(e=>e.checked),c=t.filter((t,a)=>{var o,i;let n;return o=e[a],i=t.selected,n=o.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),o.multiSelect?i.size===n.length&&[...i].every(e=>o.options[e].correct):1===i.size&&n.includes([...i][0])}).length;return(0,o.jsxs)("div",{className:"quiz_I014",children:[e.map((i,r)=>(0,o.jsx)(n,{q:i,state:t[r],onToggle:t=>a(a=>a.map((a,o)=>{if(o!==r)return a;let i=new Set(a.selected);return e[r].multiSelect?i.has(t)?i.delete(t):i.add(t):(i.clear(),i.add(t)),{...a,selected:i}}))},r)),(0,o.jsxs)("div",{className:"actions_XUwt",children:[(0,o.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,o.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),s&&(0,o.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>r,x:()=>s});var o=a(6540);let i={},n=o.createContext(i);function r(e){let t=o.useContext(n);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function s(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:r(e.components),o.createElement(n.Provider,{value:t},e.children)}}}]);