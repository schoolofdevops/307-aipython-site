"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["1488"],{4156(e,t,a){a.r(t),a.d(t,{metadata:()=>n,default:()=>u,frontMatter:()=>r,contentTitle:()=>l,toc:()=>d,assets:()=>c});var n=JSON.parse('{"id":"m5-types-and-data-modelling/quiz","title":"Quiz: Type Hints and Data Modelling","description":"<Quiz questions={[","source":"@site/docs/m5-types-and-data-modelling/quiz.mdx","sourceDirName":"m5-types-and-data-modelling","slug":"/m5-types-and-data-modelling/quiz","permalink":"/307-aipython-site/docs/m5-types-and-data-modelling/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Type Hints and Data Modelling"},"sidebar":"courseSidebar","previous":{"title":"Lab: Type Hints and Data Modelling","permalink":"/307-aipython-site/docs/m5-types-and-data-modelling/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m5-types-and-data-modelling/deep-dive"}}'),i=a(4848),o=a(8453),s=a(7517);let r={sidebar_position:3,title:"Quiz: Type Hints and Data Modelling"},l="Type Hints and Data Modelling \u2014 Quiz",c={},d=[];function h(e){let t={h1:"h1",header:"header",...(0,o.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(t.header,{children:(0,i.jsx)(t.h1,{id:"type-hints-and-data-modelling--quiz",children:"Type Hints and Data Modelling \u2014 Quiz"})}),"\n",(0,i.jsx)(s.A,{questions:[{prompt:'A config file has `replicas: "3"` (in quotes) instead of `replicas: 3`. This function runs with no error:\n\n```python\ndef total_cpu(replicas: int, cpu_per_replica: int) -> int:\n    return replicas * cpu_per_replica\n\ntotal_cpu("3", 2)\n```\n\nWhat does it return?',options:[{text:'`6`, because the `int` annotation forces Python to convert "3" to a number first',correct:!1,explanation:"A type annotation on its own does not convert or check anything at runtime. Python runs the multiplication with whatever it was given."},{text:'`"33"` \u2014 a string, because `"3" * 2` repeats the string rather than multiplying two numbers',correct:!0,explanation:'This is exactly the wrong-type incident from the lesson. Python treats `"3" * 2` as string repetition, silently returning "33" instead of 6, with no crash to alert anyone.'},{text:"A `TypeError`, because you cannot multiply a string by an integer",correct:!1,explanation:"Python does allow `str * int` \u2014 it is defined as string repetition. That is exactly what makes this bug dangerous: there is no crash to catch it."},{text:"A `TypeError`, because the function's annotation does not match the argument type",correct:!1,explanation:"Type annotations are not enforced at runtime by plain Python. The function runs to completion regardless of whether the argument matches the annotation."}]},{prompt:"What does a plain Python type annotation (like `def f(x: int) -> str:`) actually do at runtime, with no other tool involved?",options:[{text:"It stores metadata Python never consults while running the function \u2014 something else has to read it and act on it",correct:!0,explanation:"Annotations end up in the function's `__annotations__` dictionary. Python itself never checks a call against them; a type checker or a library like Pydantic has to read them on purpose."},{text:"It silently converts the argument to the annotated type before the function runs",correct:!1,explanation:"No conversion happens. Plain Python annotations do not coerce or validate arguments at call time."},{text:"It raises a `TypeError` immediately if the wrong type is passed",correct:!1,explanation:"This is what many learners assume, but it is false for plain Python. The function runs regardless of whether the argument matches the annotation."},{text:"It only affects what your editor shows in autocomplete, and has no other effect anywhere, ever",correct:!1,explanation:"Editor autocomplete is one consumer, but not the only one \u2014 a static type checker (mypy) and libraries like Pydantic both read annotations and act on them, just not the plain interpreter itself."}]},{prompt:"A function is annotated `def find_service(name: str, services: list[dict]) -> dict | None:`. What does `dict | None` communicate to a reader?",options:[{text:"The function always returns a dict, and `None` is just a formatting artifact",correct:!1,explanation:"`dict | None` is a union type: the function can genuinely return either a dict or None. It is not decorative."},{text:"The function can come back with no result at all, so the caller should check for `None` before using the return value",correct:!0,explanation:'This is exactly what a union with None (or `Optional[dict]` in older code) signals: a real "might be empty" case the caller needs to handle before doing anything with the result.'},{text:"The function raises an exception instead of returning `None` when nothing is found",correct:!1,explanation:"The annotation says the opposite \u2014 a `None` return is an expected, normal outcome here, not an exception path."},{text:"It means the function accepts either a dict or None as an argument",correct:!1,explanation:"The `dict | None` appears after `->`, which annotates the return value, not an argument."}]},{prompt:'A plain `dataclass` and a Pydantic `BaseModel` can look almost identical:\n\n```python\n@dataclass\nclass A:\n    memory_gb: int = 8\n\nclass B(BaseModel):\n    memory_gb: int = 8\n```\n\nWhat actually differs when you construct each with `memory_gb="lots"`?',multiSelect:!0,options:[{text:'`A(memory_gb="lots")` builds successfully \u2014 the dataclass does not check types at construction',correct:!0,explanation:"A dataclass generates an __init__ and stores whatever you pass it. It does not validate against its own annotations."},{text:'`B(memory_gb="lots")` raises a `ValidationError` \u2014 Pydantic checks every field against its annotation when the model is built',correct:!0,explanation:"This is the core difference this module teaches: Pydantic actively enforces its annotations at construction time; a dataclass does not."},{text:"Both raise the same error, because both classes have the same type annotation",correct:!1,explanation:"Having the same annotation does not mean the same enforcement. Only Pydantic's BaseModel reads and enforces it at construction."},{text:"Neither raises an error \u2014 type annotations are always advisory in Python",correct:!1,explanation:"This is true for a plain dataclass but false for Pydantic, which is specifically built to enforce annotations, not just carry them."}]},{prompt:"You write this dataclass:\n\n```python\n@dataclass\nclass ServerRecord:\n    tags: list = []\n```\n\nWhat happens?",options:[{text:"It works fine, and every instance that relies on the default silently shares the same list \u2014 the same trap as a mutable function default",correct:!1,explanation:"This is the plain-function mutable-default trap from the last module, but dataclasses handle it differently and more strictly \u2014 see the correct option."},{text:"`ValueError: mutable default <class 'list'> for field tags is not allowed: use default_factory` \u2014 raised the moment Python reads the class, before any instance is ever created",correct:!0,explanation:"Unlike a plain function default, dataclasses actively check for mutable defaults and refuse to define the class at all. The fix is `field(default_factory=list)`."},{text:"It works fine, and each instance gets a fresh empty list automatically",correct:!1,explanation:"Dataclasses do not silently fix a mutable default for you \u2014 they refuse to define the class, forcing you to use `field(default_factory=list)` instead."},{text:"It raises a `TypeError` at the moment you try to append to `tags` on an instance",correct:!1,explanation:"The error happens much earlier \u2014 at class definition time, before any instance exists \u2014 not when you later use the field."}]},{prompt:"In a Pydantic `BaseModel`, what distinguishes a required field from an optional one?",options:[{text:"A field is required if it has no `= <default>` after its type annotation; adding a default makes it optional",correct:!0,explanation:'This mirrors ordinary Python function arguments: `name: str` with no default is required; `region: str = "us-east-1"` is optional.'},{text:"Every field is optional unless it is explicitly wrapped in a `Required[...]` type",correct:!1,explanation:"Pydantic does not require a special wrapper for required fields \u2014 the absence of a default is what makes a field required, matching ordinary Python syntax."},{text:"A field is required only if it is listed in a separate `required_fields` list on the model",correct:!1,explanation:"Pydantic v2 models have no such separate list \u2014 required-ness is read directly from whether the field has a default value."},{text:"Field order determines required vs optional \u2014 the first three fields in a model are always required",correct:!1,explanation:"Field position has nothing to do with whether a field is required. It is entirely determined by the presence or absence of a default value."}]},{prompt:"The lab's `ServiceDefinition` model has an `observability: ObservabilityRefs` field, where `ObservabilityRefs` is itself a `BaseModel` with `dashboard_url` and `alert_channel`. What does Pydantic do with a nested model like this during validation?",options:[{text:"It validates `dashboard_url` and `alert_channel` inside the nested dict using the same rules as any other field \u2014 a missing or wrong-typed value there fails validation too",correct:!0,explanation:'Nested models are validated recursively. A bad value inside `observability` produces its own error entry with a `loc` like `("observability", "alert_channel")`.'},{text:"It only checks that `observability` is present as a dict \u2014 it does not look inside it",correct:!1,explanation:"Pydantic validates nested models fully, field by field, not just a shallow presence check on the outer key."},{text:"It requires `ObservabilityRefs` to be constructed and validated separately, before `ServiceDefinition` is created",correct:!1,explanation:"You can pass a plain nested dict directly \u2014 Pydantic validates it into an `ObservabilityRefs` instance as part of validating the outer model, in one call."},{text:"It silently drops the nested field if it is missing, since nested models are always optional",correct:!1,explanation:"A nested model field follows the same required/optional rule as any other field \u2014 no default means it is required, and a missing `observability` key fails validation."}]},{prompt:"A `ValidationError`'s `.errors()` method returns a list of plain dicts rather than a formatted string. Which of the following are real benefits of that structured shape, as covered in this module?",multiSelect:!0,options:[{text:"A caller can loop over the errors and build a response \u2014 like an API 422 \u2014 that names every bad field at once, instead of surfacing a raw traceback",correct:!0,explanation:"This is exactly why the shape matters operationally: each dict names its field (`loc`), the reason (`msg`) and a stable machine-matchable `type`, all usable as data."},{text:"The `type` key is a stable, machine-matchable error code (like `string_type` or `literal_error`), not free text you would have to pattern-match",correct:!0,explanation:"This lets code branch on the kind of failure programmatically, rather than parsing English sentences that could change wording between versions."},{text:"It removes the need to catch `ValidationError` at all \u2014 Pydantic returns the error list directly from `model_validate()` instead of raising",correct:!1,explanation:"Pydantic still raises `ValidationError` on failure. `validate_service` in the lab catches it and returns `.errors()` itself \u2014 Pydantic does not do that for you automatically."},{text:"Each error dict includes the bad value itself (`input`), so you can log or display exactly what was received without re-fetching it",correct:!0,explanation:"The `input` key carries the actual value that failed \u2014 useful for logging or showing a user exactly what they submitted."}]},{prompt:'This module\'s "boundary rule" says: validate once, at the point data enters your program, then trust it completely afterward. What is the practical payoff of following that rule?',options:[{text:"It makes validation run faster, because Pydantic caches results across different pieces of data",correct:!1,explanation:"The payoff is not raw speed \u2014 Pydantic validates each new payload independently. The payoff is that the rest of your code no longer needs its own defensive checks."},{text:'Code past the boundary can read fields directly without re-checking `if "environment" in data` everywhere, because a `ServiceDefinition` object is guaranteed to already have the right shape',correct:!0,explanation:"This is the actual benefit: once you hold a validated model, its guarantees hold everywhere downstream, so defensive checks do not need to be repeated in every function that touches it."},{text:"It means you never need to write tests for functions that use the validated data",correct:!1,explanation:"Validating the shape of the data does not remove the need to test what your functions do with it \u2014 those are separate concerns."},{text:"It guarantees the data is factually correct, not just correctly shaped \u2014 for example, that an AWS account ID actually exists",correct:!1,explanation:"Pydantic validates shape and type, not real-world truth. A syntactically valid but nonexistent AWS account ID would still pass \u2014 the boundary rule is about shape, not fact-checking against the outside world."}]},{prompt:"A teammate is building a one-off script that reads a JSON file they wrote themselves, transforms it, and throws the result away at the end of the run \u2014 nothing external ever touches this data. Per this module's decision table, what is the most reasonable choice for representing that data?",options:[{text:"A Pydantic `BaseModel`, since validation is always the safest default regardless of where data comes from",correct:!1,explanation:"Pydantic earns its cost at a real boundary \u2014 data you do not fully control. For fully internal, throwaway data, a plain dict is simpler and does the job."},{text:"A plain `dict` \u2014 throwaway, fully internal data that the script itself both writes and reads does not need a validation boundary",correct:!0,explanation:"This matches the decision table directly: a dict is the right call for internal, one-off data you already control end to end."},{text:"A `dataclass`, because it always outperforms a dict for any structured data",correct:!1,explanation:"A dataclass is a reasonable option once you have already-trusted, structured data you want typed attributes for, but for a quick throwaway script a dict is simpler and just as reasonable \u2014 dataclass is not about performance here."},{text:"It does not matter \u2014 dict, dataclass and Pydantic are functionally interchangeable in every case",correct:!1,explanation:"They are not interchangeable: only Pydantic actively validates at construction, and only a dataclass gives typed attributes with no validation cost. The right choice depends on where the data came from."}]},{prompt:"In the Phase B review, your coding agent's diff for the field-constraints request also modifies `pyproject.toml` to add a new dependency. What should you do, and why?",options:[{text:"Accept it \u2014 any dependency an agent adds while implementing a validation feature is presumably necessary",correct:!1,explanation:"The spec explicitly said no new third-party dependencies, since Literal types and Field(pattern=...) are already built into Pydantic and Python's own typing module. A new dependency here is unrequested scope."},{text:"Reject that part of the change \u2014 the spec explicitly ruled out new dependencies, and Pydantic's own `Literal` types and `Field(pattern=...)` are enough for all three constraints",correct:!0,explanation:"This matches the review checklist directly: check pyproject.toml a second time after the agent's change, and push back if it grew beyond what step 1's `uv add pydantic` already added."},{text:"Ignore it \u2014 `pyproject.toml` changes do not affect whether the tests pass",correct:!1,explanation:"Whether tests pass is not the only thing worth reviewing. An unrequested dependency is scope creep even if the tests are green, and it is exactly what the review checklist asks you to catch."},{text:"Delete `pyproject.toml` entirely and let `uv sync` regenerate it from scratch",correct:!1,explanation:"This is a destructive, unnecessary overreaction. The correct move is to reject just the unwanted dependency addition, not the whole file."}]},{prompt:'By default (lax mode), `Deployment(replicas="3")` succeeds and `d.replicas` is the integer `3`. With `model_config = ConfigDict(strict=True)`, the exact same call raises a `ValidationError`. What does this tell you about Pydantic\'s default behavior?',multiSelect:!0,options:[{text:"By default, Pydantic coerces some values that are not exactly the annotated type, if the conversion is unambiguous \u2014 like a numeric string to an int",correct:!0,explanation:'This is lax mode, Pydantic\'s default: "3" clearly means the number 3, so it is converted rather than rejected.'},{text:"Strict mode turns that convenience off, requiring the input to already be the exact declared type",correct:!0,explanation:"With strict=True, a string is never accepted for an int field, no matter how numeric it looks \u2014 the type itself must already match."},{text:"Lax coercion is always the safer choice, since it accepts more input without failing",correct:!1,explanation:"Accepting more input is not automatically safer \u2014 the lesson and deep dive both note that coercion can hide real mistakes (like a boolean silently becoming 1 for an int field). Safety depends on the field, not on accepting more."},{text:"Once a project sets `strict=True` anywhere, every Pydantic model in that process becomes strict",correct:!1,explanation:"strict=True is scoped to the model (or field) it is set on via ConfigDict or Field \u2014 it does not globally change every other Pydantic model in the process."}]},{prompt:"After validating a `ServiceDefinition`, calling `svc.model_dump()` returns a plain nested `dict`, and `ServiceDefinition.model_validate(svc.model_dump())` produces an object equal to the original. What is this round trip most directly useful for?",options:[{text:"Speeding up validation, since `model_dump()` skips Pydantic's normal checks",correct:!1,explanation:"model_dump() is about serialization (getting data back out), not about skipping validation. Re-validating the dump still runs the full validator."},{text:"Letting a script or agent modify one field of a validated definition and write the whole thing back out to a YAML file or an API call, without hand-reconstructing the dict",correct:!0,explanation:"This is exactly the operational use covered in the deep dive: dump the validated model, change what needs changing, and the result stays a faithful, complete representation you can write out anywhere."},{text:"Converting a dict into a Pydantic model without ever calling `model_validate()`",correct:!1,explanation:"model_dump() goes the other direction \u2014 model to dict. Getting from a dict to a model still requires model_validate() (or the model constructor)."},{text:"Removing the need for `ObservabilityRefs` as a separate nested model",correct:!1,explanation:"model_dump() still walks through nested models and includes them in the output dict \u2014 it does not eliminate the need for the nested model in the first place."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,i.jsx)(t,{...e,children:(0,i.jsx)(h,{...e})}):h(e)}},7517(e,t,a){a.d(t,{A:()=>s});var n=a(4848),i=a(6540);function o({q:e,state:t,onToggle:a}){return(0,n.jsxs)("div",{className:"question_QpiT",children:[(0,n.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,n.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,n.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,i)=>{let o=t.selected.has(i),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":o&&(s+=" incorrect_kRFc"):o&&(s+=" chosen_H3WG"),(0,n.jsxs)("li",{children:[(0,n.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>a(i),children:[(0,n.jsx)("span",{className:"marker_vN_w",children:o?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(o||e.correct)&&e.explanation&&(0,n.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},i)})})]})}function s({questions:e}){let[t,a]=(0,i.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,a)=>{var n,i;let o;return n=e[a],i=t.selected,o=n.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),n.multiSelect?i.size===o.length&&[...i].every(e=>n.options[e].correct):1===i.size&&o.includes([...i][0])}).length;return(0,n.jsxs)("div",{className:"quiz_I014",children:[e.map((i,s)=>(0,n.jsx)(o,{q:i,state:t[s],onToggle:t=>a(a=>a.map((a,n)=>{if(n!==s)return a;let i=new Set(a.selected);return e[s].multiSelect?i.has(t)?i.delete(t):i.add(t):(i.clear(),i.add(t)),{...a,selected:i}}))},s)),(0,n.jsxs)("div",{className:"actions_XUwt",children:[(0,n.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,n.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,n.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>s,x:()=>r});var n=a(6540);let i={},o=n.createContext(i);function s(e){let t=n.useContext(o);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:s(e.components),n.createElement(o.Provider,{value:t},e.children)}}}]);