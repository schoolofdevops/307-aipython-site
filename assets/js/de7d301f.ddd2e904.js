"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["9548"],{7635(e,t,n){n.r(t),n.d(t,{metadata:()=>o,default:()=>u,frontMatter:()=>r,contentTitle:()=>c,toc:()=>h,assets:()=>l});var o=JSON.parse('{"id":"m27-containerizing-python/quiz","title":"Quiz: Packaging Python Automation with Containers","description":"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read","source":"@site/docs/m27-containerizing-python/quiz.mdx","sourceDirName":"m27-containerizing-python","slug":"/m27-containerizing-python/quiz","permalink":"/307-aipython-site/docs/m27-containerizing-python/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Packaging Python Automation with Containers"},"sidebar":"courseSidebar","previous":{"title":"Lab: Packaging Python Automation with Containers","permalink":"/307-aipython-site/docs/m27-containerizing-python/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m27-containerizing-python/deep-dive"}}'),a=n(4848),i=n(8453),s=n(7517);let r={sidebar_position:3,title:"Quiz: Packaging Python Automation with Containers"},c="Packaging Python Automation with Containers \u2014 Quiz",l={},h=[];function d(e){let t={admonition:"admonition",h1:"h1",header:"header",p:"p",...(0,i.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(t.header,{children:(0,a.jsx)(t.h1,{id:"packaging-python-automation-with-containers--quiz",children:"Packaging Python Automation with Containers \u2014 Quiz"})}),"\n",(0,a.jsx)(t.admonition,{title:"This quiz covers the full module",type:"info",children:(0,a.jsx)(t.p,{children:"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read\nthe Deep Dive yet, come back to those questions after you finish it."})}),"\n",(0,a.jsx)(s.A,{questions:[{prompt:"In the lesson's shipping-container analogy, what does the standard steel box represent?",options:[{text:"The container image -- platformops and everything it needs, packed once and run unchanged everywhere",correct:!0,explanation:"The point of the analogy: build the box once, and it runs the same way on your laptop, in CI, or in Kubernetes -- no repacking per destination."},{text:"The Kubernetes cluster the image eventually runs on",correct:!1,explanation:'The cluster is one of several places the box can run (the "crane" or "ship\'s hold"), not the box itself.'},{text:"The uv.lock file that pins every dependency version",correct:!1,explanation:"uv.lock is one of the things packed inside the box, not the box itself."},{text:"The registry a built image gets pushed to",correct:!1,explanation:"A registry is where boxes are stored between the factory and their destination, not the box itself."}]},{prompt:"Why does this module's Dockerfile use a multi-stage build instead of one stage that installs uv, builds, and runs the CLI?",options:[{text:"So build-time tools (uv, a C compiler, apt package caches) and the test suite never cross into the final image",correct:!0,explanation:"Only the finished .venv folder is copied from the builder stage into the runtime stage -- everything else the builder needed stays behind when that stage is discarded."},{text:"Because uv cannot install packages inside a python:3.12-slim base image at all",correct:!1,explanation:"uv installs fine inside python:3.12-slim -- that is exactly what the builder stage does. The reason for two stages is what ships, not what uv can run inside."},{text:"Because Docker requires at least two FROM lines in every Dockerfile",correct:!1,explanation:"A single-stage Dockerfile with one FROM line is completely valid Docker -- multi-stage is a deliberate choice here, not a requirement."},{text:"Because platformops cannot be installed non-editable in a single stage",correct:!1,explanation:"--no-editable works the same whether or not the build is multi-stage -- it is unrelated to the number of stages."}]},{prompt:"What is the concrete risk this module's lesson gives for running a container as root by default?",options:[{text:"If an attacker finds a way to run code inside the container, root inside it is a much bigger prize than an unprivileged user would be",correct:!0,explanation:"The hotel master-key analogy: root is not needed for platformops to do its job, and having it only expands what a compromise could reach."},{text:"Docker refuses to start any container that has not declared a non-root USER",correct:!1,explanation:"Docker runs containers as root by default with no complaint -- nothing forces a non-root USER unless the Dockerfile sets one, which is exactly the point of adding it deliberately."},{text:"Root-owned containers cannot mount volumes",correct:!1,explanation:"Root-owned containers mount volumes fine -- the concern in this lesson is about the blast radius of a compromise, not volume mounting."},{text:"A non-root user makes the image build faster",correct:!1,explanation:"Build speed is unrelated to which user the final container runs as."}]},{prompt:"The same platformops image runs with PLATFORMOPS_ENVIRONMENT=staging in one place and no override at all in another. What changes about the image between those two runs?",options:[{text:"Nothing about the image -- only the environment variable passed at run time changes, the exact same way config.py already read PLATFORMOPS_* overrides before this module",correct:!0,explanation:"The lesson's thermostat analogy: the room (the image) is identical for every guest; only the temperature setting (the environment variable) differs, and it never touches the building itself."},{text:"A different Dockerfile has to be built for each environment",correct:!1,explanation:'That is exactly the "one image per environment" outcome this design avoids -- one image, configured differently at run time.'},{text:"The CMD instruction baked into the image changes",correct:!1,explanation:"CMD is fixed at build time in the image metadata -- environment variables are supplied separately, at container start, and never rewrite CMD."},{text:"platformops recompiles its Python source with the new setting",correct:!1,explanation:"Python source is not recompiled per run -- the environment variable is read at runtime the same way any os.environ lookup works."}]},{prompt:"Why does the lab's Kubernetes Secret hold PLATFORMOPS_ENVIRONMENT=staging instead of something genuinely confidential?",options:[{text:"To prove the envFrom.secretRef wiring actually reaches the container, using a value whose effect is visible in the Job's output -- the same wiring works identically for a real credential",correct:!0,explanation:"The lesson is explicit about this: the value is not confidential, but the mechanism (Secret -> environment variable -> os.environ) is exactly how a real GitHub token or database password would be handed to the container."},{text:"Kubernetes Secrets cannot hold real credential values, only configuration flags",correct:!1,explanation:"Kubernetes Secrets are commonly used for real credentials in production -- this lab simply chose a visible, harmless value to make the proof observable in the Job's output."},{text:"ConfigMaps are not allowed to exist in the same manifest set as a Secret",correct:!1,explanation:"The lab applies a ConfigMap and a Secret together with no conflict -- they are unrelated Kubernetes objects that can coexist freely."},{text:"platformops refuses to start if no Secret is present",correct:!1,explanation:"platformops has no such requirement -- the Job would run fine (with environment: prod, unmodified) if the Secret were removed."}]},{prompt:"Why does the image's HEALTHCHECK run `platformops local-status --path . --json` specifically, instead of a command that checks network reachability?",options:[{text:"local-status is already guaranteed by local_ops.py's contract to never raise and always exit 0, even with no git repo and no Docker socket present -- so a failure genuinely means something is wrong",correct:!0,explanation:"A healthcheck that can fail for reasons unrelated to actual container health (like a flaky network call) produces false alarms -- local-status was chosen because it degrades honestly instead of crashing."},{text:"Because Docker HEALTHCHECK instructions are only allowed to call read-only commands",correct:!1,explanation:"HEALTHCHECK can run any command that exits 0 or non-zero -- there is no such restriction. The reason for this specific command is its guaranteed-safe behavior, not a Docker rule."},{text:"Because local-status is the only platformops command that accepts --json",correct:!1,explanation:"Most platformops commands accept --json (validate, version, and others) -- local-status was chosen for its never-raises contract, not because it is uniquely JSON-capable."},{text:"Because HEALTHCHECK instructions cannot reference commands with command-line flags",correct:!1,explanation:"HEALTHCHECK commands can include any flags a normal shell command would -- this image's HEALTHCHECK does exactly that."}]},{prompt:"Select every statement below that correctly describes Kubernetes resources.requests vs resources.limits (multiple correct answers).",multiSelect:!0,options:[{text:"A request is what the scheduler reserves for a container up front, before the pod is even placed on a node",correct:!0,explanation:"This is the request's job: a promise of available capacity used at scheduling time."},{text:"A limit is enforced by the Linux kernel through cgroups, not just recorded by Kubernetes as a number",correct:!0,explanation:"The Deep Dive proves this live -- reading /sys/fs/cgroup/memory.max and cpu.max from inside a running pod shows the kernel-enforced values matching the YAML exactly."},{text:"Exceeding a memory limit slows the container down but never terminates it",correct:!1,explanation:"Exceeding a memory limit gets the container killed (OOM-killed) by the kernel -- it does not merely throttle memory the way a CPU limit throttles CPU time."},{text:"Without any limits set, one runaway container can consume an entire node's memory and affect every other pod on it",correct:!0,explanation:"This is the lesson's apartment-building point -- a limit is what stops one unit from starving the whole building's shared supply."}]},{prompt:"This module's lab uses syft and grype instead of docker scout. Why?",options:[{text:"This course's reference Docker CLI build does not have the scout subcommand installed -- syft/grype are separately installed tools that work the same way regardless of Docker CLI build",correct:!0,explanation:'The lab verified this live: `docker scout` returned "unknown command" on the reference environment, so a lab built around it would not run as documented.'},{text:"docker scout cannot scan images built with a multi-stage Dockerfile",correct:!1,explanation:"That is not the reason -- docker scout, where installed, works on any built image regardless of how many stages built it. The issue here is that it simply is not installed in this course's environment."},{text:"syft and grype are Kubernetes-native tools that only work against images already running in a cluster",correct:!1,explanation:"Both tools scan any built image directly (this lab ran them against the local platformops:2.2.0 image, before it ever touched Kubernetes) -- no cluster required."},{text:"docker scout only supports CycloneDX output, and this lab requires SPDX",correct:!1,explanation:"The lab's reason for choosing syft/grype was tool availability on this environment, not a format limitation of docker scout."}]},{prompt:"The lab's grype scan found 201 vulnerability matches, including 7 Critical and 30 High. What is the correct way to read that result, per the lesson and lab?",options:[{text:"Check which package each finding is in first -- every single one of these 201 findings is in a python:3.12-slim base-OS package, none in platformops's own code or its five pinned dependencies",correct:!0,explanation:"The lab proved this with a live check: grouping matches by package name against {boto3, httpx, pydantic, pyyaml, typer, platformops} returned zero hits -- the findings are entirely in the base image."},{text:"A Critical or High finding always means the release should be blocked immediately, no exceptions",correct:!1,explanation:'The lesson explicitly rejects "reach zero findings" as the bar -- the important question is which package is affected, not the severity label alone.'},{text:"The scan is unreliable and can be ignored since this is a small CLI tool",correct:!1,explanation:"The scan is real and its findings are real, current CVEs -- ignoring it is the wrong response. The correct response is understanding what the findings actually apply to, not dismissing the scan."},{text:"This many findings means the Dockerfile's multi-stage build failed to work as intended",correct:!1,explanation:"The multi-stage build worked exactly as intended -- it kept build tools and tests out of the image. It cannot remove CVEs that are part of the base OS image itself."}]},{prompt:"Why does the lab's k8s/job.yaml use a Kubernetes Job rather than a Deployment to run platformops?",options:[{text:"A Job runs a container to completion and stops, matching a validation task that has a clear end -- a Deployment keeps restarting a process forever, which would treat a successful completed run as a crash to recover from",correct:!0,explanation:"This is the lesson's one-off-errand vs recurring-shift analogy -- validating a service definition finishes, so a Job is the matching primitive, the same way a CI pipeline step is a Job, not a long-running service."},{text:"Deployments cannot mount ConfigMaps or read from Secrets",correct:!1,explanation:"Deployments can mount ConfigMaps and read Secrets exactly like Jobs can -- that is not the distinguishing factor here."},{text:"A Job is required any time a container runs as a non-root user",correct:!1,explanation:"Non-root execution works identically under a Job, a Deployment, or a plain Pod -- the securityContext settings are independent of which workload type you choose."},{text:"Only a Job can set resources.requests and resources.limits",correct:!1,explanation:"Every workload type (Pod, Job, Deployment, and others) supports resources.requests/limits at the container level -- this is not a Job-specific feature."}]},{prompt:"The lab's Job sets `automountServiceAccountToken: false` and never imports a Kubernetes client library. What does this reflect about this module's scope?",options:[{text:"This module packages and runs a container on Kubernetes with plain kubectl -- reading Kubernetes state programmatically from Python (the client library, kubeconfig, listing Pods/Deployments) is Module 28's scope, not this one's",correct:!0,explanation:"The lesson states this directly: the Job never calls the Kubernetes API and cannot even if it tried, because automating Kubernetes itself from Python is the next module's subject."},{text:"It means this Job is broken and cannot actually run inside the cluster",correct:!1,explanation:"The Job runs and completes successfully -- automountServiceAccountToken: false only removes a credential the Job never needed, it does not affect the Job's own ability to run."},{text:"It means Secrets and ConfigMaps are unavailable in this cluster",correct:!1,explanation:"The Job successfully reads both a ConfigMap and a Secret in this same lab -- automountServiceAccountToken only concerns the Kubernetes API service account token, unrelated to ConfigMap/Secret access via volumes and envFrom."},{text:"It disables the container's HEALTHCHECK",correct:!1,explanation:"automountServiceAccountToken has nothing to do with the image's HEALTHCHECK instruction -- they are unrelated settings from different layers (Dockerfile vs Pod spec)."}]},{prompt:"Why is `kind load docker-image` a necessary step before the Job can run, even though the image was already built with `docker build` on the same machine?",options:[{text:"A kind cluster runs its own containerd inside each node, separate from the host's Docker daemon -- an image built with docker build is not automatically visible inside the cluster until it is explicitly loaded in",correct:!0,explanation:"The Deep Dive proves this live: `docker ps` on the host only shows the kind node container itself, while the platformops container the Job ran is only visible via crictl/ctr from inside that node -- two separate container-management systems."},{text:"Because kind clusters have no local image cache at all and must always pull from a public registry",correct:!1,explanation:"kind load docker-image loads a LOCAL image directly into the cluster's containerd, with no registry involved at all -- that is the whole point of the command."},{text:"Because the image needs to be rebuilt with a different base image for Kubernetes",correct:!1,explanation:"The exact same image built for `docker run` is loaded and run inside the kind cluster -- no rebuild, no different base image."},{text:"Because Kubernetes Jobs require images to be signed before they can run",correct:!1,explanation:"Image signing is a real practice in some production clusters but was not part of this lab, and is not a requirement for kind load docker-image or for a Job to run."}]},{prompt:"In the Deep Dive's cgroup proof, `cat /sys/fs/cgroup/memory.max` inside the pod returned `134217728`. What does that number confirm?",options:[{text:"It is exactly 128 * 1024 * 1024 -- the Job's resources.limits.memory: 128Mi value, read directly from the kernel's own cgroup accounting inside the running container",correct:!0,explanation:"This is the kernel-enforced number, not a value reported back by kubectl -- proof the limit is a real ceiling, not just YAML bookkeeping."},{text:"It is the total memory available on the kind node",correct:!1,explanation:"That would be a much larger number reflecting the whole node's memory -- 134217728 bytes (128Mi) is specifically this one container's enforced ceiling."},{text:"It is the amount of memory the container was actually using at that moment",correct:!1,explanation:"memory.max is the configured ceiling, not a live usage reading -- current usage would be read from a different file, memory.current."},{text:"It is an error code indicating the cgroup was misconfigured",correct:!1,explanation:"It is a valid, correctly-set byte count matching the YAML's 128Mi limit exactly -- not an error."}]},{prompt:"The Deep Dive generated both an SPDX and a CycloneDX SBOM for the same image and got very different component counts (123 vs 2839). What is the correct explanation?",options:[{text:"SPDX and CycloneDX report at different levels of granularity by convention -- SPDX here is package-level, CycloneDX's default output goes down to individual files and embedded components -- neither count is wrong",correct:!0,explanation:'The Deep Dive is explicit: pick the format your downstream tool actually consumes (SPDX for procurement/legal-style reporting, CycloneDX for many vulnerability-management pipelines), rather than treating one count as "the real one."'},{text:"One of the two formats failed to scan the image completely",correct:!1,explanation:"Both scans completed successfully against the same image -- the difference is in how each format organizes and counts what it found, not a failure of either."},{text:"CycloneDX only works on images that have already been pushed to a registry",correct:!1,explanation:"Both formats were generated from the local image directly, exactly like the lab's SPDX generation -- no registry involved for either."},{text:"SPDX cannot report Python packages, only operating-system packages",correct:!1,explanation:"SPDX's 123 packages include both deb (OS) and python packages -- it reports both kinds fine, it simply counts at the package level rather than the file level."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,a.jsx)(t,{...e,children:(0,a.jsx)(d,{...e})}):d(e)}},7517(e,t,n){n.d(t,{A:()=>s});var o=n(4848),a=n(6540);function i({q:e,state:t,onToggle:n}){return(0,o.jsxs)("div",{className:"question_QpiT",children:[(0,o.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,o.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,o.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,a)=>{let i=t.selected.has(a),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":i&&(s+=" incorrect_kRFc"):i&&(s+=" chosen_H3WG"),(0,o.jsxs)("li",{children:[(0,o.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>n(a),children:[(0,o.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,o.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},a)})})]})}function s({questions:e}){let[t,n]=(0,a.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),c=t.filter((t,n)=>{var o,a;let i;return o=e[n],a=t.selected,i=o.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),o.multiSelect?a.size===i.length&&[...a].every(e=>o.options[e].correct):1===a.size&&i.includes([...a][0])}).length;return(0,o.jsxs)("div",{className:"quiz_I014",children:[e.map((a,s)=>(0,o.jsx)(i,{q:a,state:t[s],onToggle:t=>n(n=>n.map((n,o)=>{if(o!==s)return n;let a=new Set(n.selected);return e[s].multiSelect?a.has(t)?a.delete(t):a.add(t):(a.clear(),a.add(t)),{...n,selected:a}}))},s)),(0,o.jsxs)("div",{className:"actions_XUwt",children:[(0,o.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,o.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,o.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>s,x:()=>r});var o=n(6540);let a={},i=o.createContext(a);function s(e){let t=o.useContext(i);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:s(e.components),o.createElement(i.Provider,{value:t},e.children)}}}]);