"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["821"],{6436(e,n,t){t.r(n),t.d(n,{metadata:()=>s,default:()=>u,frontMatter:()=>o,contentTitle:()=>a,toc:()=>d,assets:()=>c});var s=JSON.parse('{"id":"m24-cloud-governance-audit/lab","title":"Lab: Cloud Governance and Compliance Automation","description":"This lab has five phases. Phase 0 verifies Floci is running. Phase 1 writes the policy file.","source":"@site/docs/m24-cloud-governance-audit/lab.md","sourceDirName":"m24-cloud-governance-audit","slug":"/m24-cloud-governance-audit/lab","permalink":"/307-aipython-site/docs/m24-cloud-governance-audit/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Cloud Governance and Compliance Automation"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Cloud Governance and Compliance Automation","permalink":"/307-aipython-site/docs/m24-cloud-governance-audit/lesson"},"next":{"title":"Quiz: Cloud Governance and Compliance Automation","permalink":"/307-aipython-site/docs/m24-cloud-governance-audit/quiz"}}'),i=t(4848),r=t(8453);let o={sidebar_position:2,title:"Lab: Cloud Governance and Compliance Automation"},a="Lab \u2014 Cloud Governance and Compliance Automation",c={},d=[{value:"Phase 0 \u2014 This module is detection only",id:"phase-0--this-module-is-detection-only",level:2},{value:"Phase 1 \u2014 Policy as data",id:"phase-1--policy-as-data",level:2},{value:"Step 1 \u2014 Write the example policy file",id:"step-1--write-the-example-policy-file",level:3},{value:"Phase 2 \u2014 Detection, test-first (pure, no Floci needed)",id:"phase-2--detection-test-first-pure-no-floci-needed",level:2},{value:"Step 1 \u2014 Write the failing test file (pure tests)",id:"step-1--write-the-failing-test-file-pure-tests",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail",level:3},{value:"Step 3 \u2014 Write <code>cloudaudit.py</code> \u2014 dataclasses, detection, report",id:"step-3--write-cloudauditpy--dataclasses-detection-report",level:3},{value:"Step 4 \u2014 Watch it pass",id:"step-4--watch-it-pass",level:3},{value:"Phase 3 \u2014 Evidence gathering, test-first against real Floci",id:"phase-3--evidence-gathering-test-first-against-real-floci",level:2},{value:"Step 1 \u2014 Append the failing live tests",id:"step-1--append-the-failing-live-tests",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail-1",level:3},{value:"Step 3 \u2014 Append evidence-gathering to <code>cloudaudit.py</code>",id:"step-3--append-evidence-gathering-to-cloudauditpy",level:3},{value:"Step 4 \u2014 Watch it pass, against real Floci",id:"step-4--watch-it-pass-against-real-floci",level:3},{value:"Phase 4 \u2014 Wire the CLI, seed real violations, and audit them live",id:"phase-4--wire-the-cli-seed-real-violations-and-audit-them-live",level:2},{value:"Step 1 \u2014 Add the CLI commands",id:"step-1--add-the-cli-commands",level:3},{value:"Step 2 \u2014 Add CLI-level tests",id:"step-2--add-cli-level-tests",level:3},{value:"Step 3 \u2014 Seed real violations in Floci",id:"step-3--seed-real-violations-in-floci",level:3},{value:"Step 4 \u2014 Run the audit, live",id:"step-4--run-the-audit-live",level:3},{value:"Step 5 \u2014 List and show findings",id:"step-5--list-and-show-findings",level:3},{value:"Teardown",id:"teardown",level:2},{value:"Phase 5 \u2014 Full quality gate and version bump",id:"phase-5--full-quality-gate-and-version-bump",level:2}];function l(e){let n={a:"a",admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,i.jsxs)(i.Fragment,{children:[(0,i.jsx)(n.header,{children:(0,i.jsx)(n.h1,{id:"lab--cloud-governance-and-compliance-automation",children:"Lab \u2014 Cloud Governance and Compliance Automation"})}),"\n",(0,i.jsxs)(n.p,{children:["This lab has five phases. Phase 0 verifies Floci is running. Phase 1 writes the policy file.\nPhases 2 and 3 each write one failing test file first, watch it fail, then write the module until\nevery test passes \u2014 Phase 2 for ",(0,i.jsx)(n.code,{children:"evaluate_policy()"})," and the rest of detection (pure, no Floci\nneeded at all), Phase 3 for evidence-gathering (real calls against Floci). Phase 4 wires the CLI,\nseeds real violations in Floci, and runs the audit against them, live. Phase 5 runs this project's\nfull quality gate and bumps the version to ",(0,i.jsx)(n.code,{children:"v1.9.0"})," \u2014 the Cloud Hygiene Auditor release."]}),"\n",(0,i.jsxs)(n.admonition,{title:"Before you start",type:"info",children:[(0,i.jsxs)(n.p,{children:["You need ",(0,i.jsx)(n.code,{children:"~/platformops"})," at ",(0,i.jsx)(n.code,{children:"v1.8.0"}),", with ",(0,i.jsx)(n.code,{children:"src/platformops/multiregion.py"})," in place from Module\n23, and the labs repo cloned at ",(0,i.jsx)(n.code,{children:"~/307-aipython-labs"}),"."]}),(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"platformops 1.8.0\n"})}),(0,i.jsxs)(n.p,{children:["If this already shows ",(0,i.jsx)(n.code,{children:"1.9.0"}),", you have already completed this lab \u2014 skip ahead to\n",(0,i.jsx)(n.a,{href:"#phase-5--full-quality-gate-and-version-bump",children:"Phase 5"}),", or work through the steps below anyway,\nsince every step is safe to repeat."]}),(0,i.jsx)(n.p,{children:"You also need Floci running:"}),(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"floci start\n"})}),(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"Floci AWS is ready (http://localhost:4566)\n"})}),(0,i.jsxs)(n.p,{children:["If you see ",(0,i.jsx)(n.code,{children:"Error: Container 'floci' is already running."})," with a non-zero exit code instead,\nthat is not a failure -- Floci does not silently reuse an already-running container, it errors\ninstead. Run ",(0,i.jsx)(n.code,{children:"floci doctor"})," to confirm the existing container is healthy and move on; do not\nrun ",(0,i.jsx)(n.code,{children:"floci stop"}),' to "fix" this -- ',(0,i.jsx)(n.code,{children:"floci stop"})," removes the container and everything it holds,\nand this lab's own Teardown depends on Floci staying up for later modules."]}),(0,i.jsx)(n.p,{children:"Export Floci's fake credentials and local endpoint for the rest of this lab session:"}),(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"export AWS_ACCESS_KEY_ID=test\nexport AWS_SECRET_ACCESS_KEY=test\nexport AWS_ENDPOINT_URL=http://localhost:4566\n"})})]}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-0--this-module-is-detection-only",children:"Phase 0 \u2014 This module is detection only"}),"\n",(0,i.jsxs)(n.p,{children:["Before writing any code: this lab never calls ",(0,i.jsx)(n.code,{children:"delete_bucket()"}),", ",(0,i.jsx)(n.code,{children:"put_bucket_tagging()"})," to fix a\nviolation, ",(0,i.jsx)(n.code,{children:"put_bucket_acl()"})," to lock one down, or any other write-mode call ",(0,i.jsx)(n.em,{children:"against a resource\nthis module is auditing"}),". Every function you write in ",(0,i.jsx)(n.code,{children:"cloudaudit.py"})," either reads (",(0,i.jsx)(n.code,{children:"list_buckets"}),",\n",(0,i.jsx)(n.code,{children:"get_bucket_encryption"}),", ",(0,i.jsx)(n.code,{children:"get_bucket_acl"}),") or works on data already in memory (",(0,i.jsx)(n.code,{children:"evaluate_policy"}),",\n",(0,i.jsx)(n.code,{children:"build_report"}),"). Fixing what this lab finds is a later module's job \u2014 Safe Cloud Remediation. Phase\n5's version bump is the only \"write\" this lab performs, and it writes to your own project's version\nnumber, not to a resource you are auditing."]}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-1--policy-as-data",children:"Phase 1 \u2014 Policy as data"}),"\n",(0,i.jsx)(n.h3,{id:"step-1--write-the-example-policy-file",children:"Step 1 \u2014 Write the example policy file"}),"\n",(0,i.jsxs)(n.p,{children:[(0,i.jsx)(n.code,{children:"policy.yaml"})," is not Python \u2014 it is data a compliance lead could read and edit without touching\nthis project's code at all."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cd ~/platformops\ncat > policy.example.yaml << \'PYEOF\'\n# Example cloud hygiene policy -- platformops cloud-audit reads a file shaped like this.\n#\n# Every rule needs: id (unique, matched against exceptions below), check (one\n# of the built-in checks cloudaudit.CHECKS knows), and severity. applies_to\n# is optional -- omit it and the rule applies to every resource type; set it\n# to limit a rule to resource types that evidence-gathering actually covers\n# (today, only s3-bucket carries encrypted/public/created_at evidence).\n\nrules:\n  - id: required-tags\n    check: required_tags\n    severity: high\n    required_tags: [team_owner, environment]\n\n  - id: approved-regions\n    check: approved_regions\n    severity: high\n    approved_regions: [us-east-1, us-west-2]\n\n  - id: max-age-days\n    check: max_age_days\n    severity: medium\n    max_age_days: 90\n\n  - id: require-encryption\n    check: require_encryption\n    severity: high\n    applies_to: [s3-bucket]\n\n  - id: no-public-exposure\n    check: no_public_exposure\n    severity: critical\n    applies_to: [s3-bucket]\n\n# Each exception suppresses exactly one (resource_id, rule_id) pair. expires\n# is a YYYY-MM-DD date; the exception stays active through the end of that\n# day. Leave expires out entirely for an exception that never expires --\n# rare, and worth a second look whenever you see one in a real report.\nexceptions:\n  - resource_id: platformops-legacy-reports\n    rule_id: required-tags\n    reason: "legacy bucket predates the tagging policy -- cleanup tracked in ticket OPS-4021"\n    expires: "2026-12-31"\nPYEOF\n'})}),"\n",(0,i.jsx)(n.p,{children:"Read it once, out loud if it helps: five rules, one exception. Nothing here requires knowing\nPython."}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-2--detection-test-first-pure-no-floci-needed",children:"Phase 2 \u2014 Detection, test-first (pure, no Floci needed)"}),"\n",(0,i.jsx)(n.h3,{id:"step-1--write-the-failing-test-file-pure-tests",children:"Step 1 \u2014 Write the failing test file (pure tests)"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_cloudaudit.py << \'PYEOF\'\nfrom datetime import UTC, datetime\n\nimport pytest\n\nfrom platformops import cloudaudit\nfrom platformops.awsclient import get_aws_client\nfrom platformops.cloudaudit import (\n    Finding,\n    ResourceEvidence,\n    build_report,\n    evaluate_policy,\n)\nfrom platformops.multiregion import list_s3_buckets\n\n# ---------------------------------------------------------------------------\n# evaluate_policy -- pure, no network calls of any kind. Every test here\n# hand-builds evidence and a policy dict directly; none of them needs Floci\n# running.\n# ---------------------------------------------------------------------------\n\nNOW = datetime(2026, 8, 5, tzinfo=UTC)\n\n\ndef _policy(**overrides):\n    policy = {"rules": [], "exceptions": []}\n    policy.update(overrides)\n    return policy\n\n\ndef test_compliant_resource_produces_no_finding():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="good-bucket",\n            region="us-east-1",\n            tags={"team_owner": "platform", "environment": "prod"},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "required-tags",\n                "check": "required_tags",\n                "severity": "high",\n                "required_tags": ["team_owner", "environment"],\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert findings == []\n\n\ndef test_required_tags_reports_only_the_missing_tags():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="bad-bucket",\n            region="us-east-1",\n            tags={"team_owner": "platform"},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "required-tags",\n                "check": "required_tags",\n                "severity": "high",\n                "required_tags": ["team_owner", "environment"],\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert findings[0].rule_id == "required-tags"\n    assert findings[0].severity == "high"\n    assert findings[0].evidence == "missing tags: environment"\n    assert findings[0].suppressed is False\n\n\ndef test_approved_regions_rejects_a_region_not_in_the_list():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="wrong-region-bucket",\n            region="ap-south-1",\n            tags={},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "approved-regions",\n                "check": "approved_regions",\n                "severity": "high",\n                "approved_regions": ["us-east-1", "us-west-2"],\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert "ap-south-1" in findings[0].evidence\n\n\ndef test_require_encryption_flags_a_disabled_bucket():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="unencrypted-bucket",\n            region="us-east-1",\n            tags={},\n            encrypted=False,\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "require-encryption",\n                "check": "require_encryption",\n                "severity": "high",\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert "not using a customer-managed KMS key" in findings[0].evidence\n\n\ndef test_require_encryption_is_skipped_when_evidence_was_never_gathered():\n    evidence = [\n        ResourceEvidence(\n            resource_type="ec2-instance",\n            resource_id="i-0123456789",\n            region="us-east-1",\n            tags={},\n            encrypted=None,\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "require-encryption",\n                "check": "require_encryption",\n                "severity": "high",\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert findings == []\n\n\ndef test_no_public_exposure_flags_a_public_bucket():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="public-bucket",\n            region="us-east-1",\n            tags={},\n            public=True,\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "no-public-exposure",\n                "check": "no_public_exposure",\n                "severity": "critical",\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert findings[0].severity == "critical"\n\n\ndef test_applies_to_skips_a_rule_for_a_resource_type_not_listed():\n    evidence = [\n        ResourceEvidence(\n            resource_type="ec2-instance",\n            resource_id="i-0123456789",\n            region="us-east-1",\n            tags={},\n            public=True,\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "no-public-exposure",\n                "check": "no_public_exposure",\n                "severity": "critical",\n                "applies_to": ["s3-bucket"],\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert findings == []\n\n\n# ---------------------------------------------------------------------------\n# max_age_days -- proven with a controlled `now` and a controlled\n# `created_at`, never against Floci\'s real clock (Floci cannot backdate a\n# resource\'s actual creation time).\n# ---------------------------------------------------------------------------\n\n\ndef test_max_age_days_flags_a_resource_older_than_the_limit():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="old-bucket",\n            region="us-east-1",\n            tags={},\n            created_at="2026-01-01T00:00:00+00:00",\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "max-age-days",\n                "check": "max_age_days",\n                "severity": "medium",\n                "max_age_days": 90,\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert "exceeds max_age_days 90" in findings[0].evidence\n\n\ndef test_max_age_days_is_silent_for_a_resource_within_the_limit():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="new-bucket",\n            region="us-east-1",\n            tags={},\n            created_at="2026-08-01T00:00:00+00:00",\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "max-age-days",\n                "check": "max_age_days",\n                "severity": "medium",\n                "max_age_days": 90,\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert findings == []\n\n\ndef test_max_age_days_is_skipped_without_created_at_evidence():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="unknown-age-bucket",\n            region="us-east-1",\n            tags={},\n            created_at=None,\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "max-age-days",\n                "check": "max_age_days",\n                "severity": "medium",\n                "max_age_days": 90,\n            }\n        ]\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert findings == []\n\n\n# ---------------------------------------------------------------------------\n# Exceptions and suppression -- a suppressed finding stays in the list with\n# suppressed=True, it is never dropped. An expired exception must NOT\n# suppress.\n# ---------------------------------------------------------------------------\n\n\ndef test_unexpired_exception_suppresses_but_does_not_drop_the_finding():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="legacy-bucket",\n            region="us-east-1",\n            tags={},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "required-tags",\n                "check": "required_tags",\n                "severity": "high",\n                "required_tags": ["team_owner"],\n            }\n        ],\n        exceptions=[\n            {\n                "resource_id": "legacy-bucket",\n                "rule_id": "required-tags",\n                "reason": "cleanup tracked in OPS-4021",\n                "expires": "2026-12-31",\n            }\n        ],\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert findings[0].suppressed is True\n    assert findings[0].suppression_reason == "cleanup tracked in OPS-4021"\n\n\ndef test_expired_exception_does_not_suppress():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="legacy-bucket",\n            region="us-east-1",\n            tags={},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "required-tags",\n                "check": "required_tags",\n                "severity": "high",\n                "required_tags": ["team_owner"],\n            }\n        ],\n        exceptions=[\n            {\n                "resource_id": "legacy-bucket",\n                "rule_id": "required-tags",\n                "reason": "cleanup tracked in OPS-4021",\n                "expires": "2026-01-01",\n            }\n        ],\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert findings[0].suppressed is False\n    assert findings[0].suppression_reason is None\n\n\ndef test_exception_only_matches_its_own_resource_and_rule():\n    evidence = [\n        ResourceEvidence(\n            resource_type="s3-bucket",\n            resource_id="other-bucket",\n            region="us-east-1",\n            tags={},\n        )\n    ]\n    policy = _policy(\n        rules=[\n            {\n                "id": "required-tags",\n                "check": "required_tags",\n                "severity": "high",\n                "required_tags": ["team_owner"],\n            }\n        ],\n        exceptions=[\n            {\n                "resource_id": "legacy-bucket",\n                "rule_id": "required-tags",\n                "reason": "not this bucket",\n                "expires": "2026-12-31",\n            }\n        ],\n    )\n\n    findings = evaluate_policy(evidence, policy, now=NOW)\n\n    assert len(findings) == 1\n    assert findings[0].suppressed is False\n\n\n# ---------------------------------------------------------------------------\n# build_report -- summary counts by severity, active vs suppressed.\n# ---------------------------------------------------------------------------\n\n\ndef test_build_report_counts_active_and_suppressed_separately():\n    findings = [\n        Finding(\n            resource_id="a",\n            resource_type="s3-bucket",\n            rule_id="required-tags",\n            severity="high",\n            evidence="missing tags: team_owner",\n        ),\n        Finding(\n            resource_id="b",\n            resource_type="s3-bucket",\n            rule_id="no-public-exposure",\n            severity="critical",\n            evidence="public exposure",\n        ),\n        Finding(\n            resource_id="c",\n            resource_type="s3-bucket",\n            rule_id="required-tags",\n            severity="high",\n            evidence="missing tags: environment",\n            suppressed=True,\n            suppression_reason="excepted",\n        ),\n    ]\n\n    report = build_report(findings)\n\n    assert report["status"] == "violations"\n    assert report["summary"]["total_findings"] == 3\n    assert report["summary"]["active"] == 2\n    assert report["summary"]["suppressed"] == 1\n    assert report["summary"]["by_severity"] == {"high": 1, "critical": 1}\n\n\ndef test_build_report_status_is_ok_when_every_finding_is_suppressed():\n    findings = [\n        Finding(\n            resource_id="a",\n            resource_type="s3-bucket",\n            rule_id="required-tags",\n            severity="high",\n            evidence="missing tags: team_owner",\n            suppressed=True,\n            suppression_reason="excepted",\n        )\n    ]\n\n    report = build_report(findings)\n\n    assert report["status"] == "ok"\n    assert report["summary"]["active"] == 0\n    assert report["summary"]["suppressed"] == 1\n\n\ndef test_build_report_on_no_findings_is_ok_and_empty():\n    report = build_report([])\n\n    assert report["status"] == "ok"\n    assert report["summary"] == {\n        "total_findings": 0,\n        "active": 0,\n        "suppressed": 0,\n        "by_severity": {},\n    }\n    assert report["findings"] == []\nPYEOF\n'})}),"\n",(0,i.jsx)(n.h3,{id:"step-2--watch-it-fail",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_cloudaudit.py -q\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_cloudaudit.py'.\nHint: make sure your test modules/packages have valid Python names.\nTraceback:\n.../importlib/__init__.py:90: in import_module\n    return _bootstrap._gcd_import(name[level:], package, level)\ntests/test_cloudaudit.py:5: in <module>\n    from platformops import cloudaudit\nE   ImportError: cannot import name 'cloudaudit' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_cloudaudit.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.19s\n"})}),"\n",(0,i.jsxs)(n.h3,{id:"step-3--write-cloudauditpy--dataclasses-detection-report",children:["Step 3 \u2014 Write ",(0,i.jsx)(n.code,{children:"cloudaudit.py"})," \u2014 dataclasses, detection, report"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/cloudaudit.py << \'PYEOF\'\n"""platformops.cloudaudit -- check real cloud resources against a written policy, and report violations.\n\nEvery prior AWS module in this project either reports what exists\n(`cloudinventory`, `multiregion`) or does something to a resource\n(`reportstore`, `findingsstore`, `workqueue`). This module does neither. It\nchecks what already exists against a policy written down in a YAML file --\nrequired tags, approved regions, a maximum age, whether encryption is\nturned on, whether a resource is exposed to the public -- and produces a\nlist of `Finding` objects, each with the exact evidence that proved the\nviolation. It never fixes anything. A later module (Safe Cloud\nRemediation) is where the fix side lives; keeping the two apart means a\ntool that only ever reports cannot ever be the thing that changes\nproduction state by accident.\n\nThat split shows up directly in this module\'s shape. `evaluate_policy()`\n-- the detection function -- takes evidence that has already been\ngathered and a loaded policy, and returns findings. It makes no network\ncall of any kind, so it can be unit-tested with hand-built evidence and no\nFloci, no AWS, nothing running at all. `gather_bucket_evidence()` is the\nonly place in this module that talks to AWS -- it is the sole function\nhere that calls `get_bucket_encryption`, `get_bucket_acl`, or\n`list_buckets`. That is not a style preference; the Deep Dive proves it\nmechanically, by grepping this file for write-mode AWS calls (`put_*`,\n`delete_*`, `create_*`, `modify_*`) and showing there are none.\n\nEvidence composes with `multiregion.ResourceRecord` instead of extending\nit. `ResourceRecord` (Module 23) is already used by `to_markdown()` and\n`to_csv()`, and by every future module that only needs "what exists, with\nwhat tags, in what region" -- adding audit-only fields like `encrypted` or\n`public` onto that shared dataclass would put fields on it that only this\none module ever fills in, and that every other caller would have to carry\naround as always-`None`. `ResourceEvidence` instead wraps one\n`ResourceRecord` and adds exactly the fields this module\'s policy checks\nneed. `multiregion.list_s3_buckets()` still does the one thing it always\ndid -- discover buckets, with their tags and region -- and this module\nnever re-implements that.\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import asdict, dataclass\nfrom datetime import UTC, datetime\nfrom pathlib import Path\nfrom typing import Any\n\nimport botocore.exceptions\n\nfrom platformops.awsclient import get_aws_client\nfrom platformops.config import load_yaml_dict\nfrom platformops.multiregion import ResourceRecord, list_s3_buckets\n\n# The well-known S3 URI for the "AllUsers" group -- a grant to this URI in a\n# bucket\'s ACL means anyone on the internet, not just AWS accounts. This is\n# the same signal the AWS console\'s own "Public Access" bucket badge is\n# built from.\nPUBLIC_ACCESS_GROUP_URI = "http://acs.amazonaws.com/groups/global/AllUsers"\n\n\n@dataclass\nclass ResourceEvidence:\n    """One resource, with everything this module\'s checks need to evaluate it.\n\n    `encrypted`, `public` and `created_at` default to `None`, meaning "not\n    gathered for this resource" -- a real, honest value, not a stand-in for\n    `False`. A rule whose check needs one of these fields skips a resource\n    that carries `None` for it, instead of reporting a false violation for\n    a resource type this module never gathered that evidence for (see\n    `applies_to` on a policy rule for the explicit version of the same\n    idea).\n    """\n\n    resource_type: str\n    resource_id: str\n    region: str\n    tags: dict[str, str]\n    encrypted: bool | None = None\n    """True if a customer-managed KMS key is set as the default encryption -- not just any encryption."""\n    public: bool | None = None\n    created_at: str | None = None\n\n    @classmethod\n    def from_record(\n        cls,\n        record: ResourceRecord,\n        *,\n        encrypted: bool | None = None,\n        public: bool | None = None,\n        created_at: str | None = None,\n    ) -> ResourceEvidence:\n        """Build evidence from a `multiregion.ResourceRecord`, without touching that dataclass itself."""\n        return cls(\n            resource_type=record.resource_type,\n            resource_id=record.resource_id,\n            region=record.region,\n            tags=record.tags,\n            encrypted=encrypted,\n            public=public,\n            created_at=created_at,\n        )\n\n\n@dataclass\nclass Finding:\n    """One rule, one resource, one violation -- with the evidence that proved it.\n\n    `evidence` is always a specific string ("missing tags: team_owner",\n    not a bare `True`) -- an auditor reading a report needs to see what was\n    actually wrong, not just that something was. `suppressed` and\n    `suppression_reason` keep a suppressed finding in the list instead of\n    dropping it: an unexpired exception explains why a real violation is\n    not being acted on right now, and that explanation belongs in the\n    report next to the finding it excuses, not hidden by a shorter list.\n    """\n\n    resource_id: str\n    resource_type: str\n    rule_id: str\n    severity: str\n    evidence: str\n    suppressed: bool = False\n    suppression_reason: str | None = None\n\n\ndef load_policy(path: Path) -> dict[str, Any]:\n    """Load a policy YAML file -- reuses `config.load_yaml_dict()`, this module\'s only file I/O."""\n    data = load_yaml_dict(path)\n    data.setdefault("rules", [])\n    data.setdefault("exceptions", [])\n    return data\n\n\n# ---------------------------------------------------------------------------\n# Detection -- pure functions. Nothing below this line makes a network call.\n# Every check takes one ResourceEvidence, one rule dict and `now`, and\n# returns either None (compliant, or not applicable to this resource) or a\n# string describing exactly what was found.\n# ---------------------------------------------------------------------------\n\n\ndef _check_required_tags(\n    evidence: ResourceEvidence, rule: dict[str, Any], *, now: datetime\n) -> str | None:\n    required = rule.get("required_tags", [])\n    missing = [tag for tag in required if tag not in evidence.tags]\n    if missing:\n        return f"missing tags: {\', \'.join(missing)}"\n    return None\n\n\ndef _check_approved_regions(\n    evidence: ResourceEvidence, rule: dict[str, Any], *, now: datetime\n) -> str | None:\n    approved = rule.get("approved_regions", [])\n    if evidence.region not in approved:\n        return f"region \'{evidence.region}\' is not in the approved list {approved}"\n    return None\n\n\ndef _check_max_age_days(\n    evidence: ResourceEvidence, rule: dict[str, Any], *, now: datetime\n) -> str | None:\n    if evidence.created_at is None:\n        return None\n    max_age_days = rule["max_age_days"]\n    created = datetime.fromisoformat(evidence.created_at)\n    if created.tzinfo is None:\n        created = created.replace(tzinfo=UTC)\n    age_days = (now - created).days\n    if age_days > max_age_days:\n        return (\n            f"age {age_days}d exceeds max_age_days {max_age_days} "\n            f"(created {evidence.created_at})"\n        )\n    return None\n\n\ndef _check_require_encryption(\n    evidence: ResourceEvidence, rule: dict[str, Any], *, now: datetime\n) -> str | None:\n    if evidence.encrypted is None:\n        return None\n    if not evidence.encrypted:\n        return "encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)"\n    return None\n\n\ndef _check_no_public_exposure(\n    evidence: ResourceEvidence, rule: dict[str, Any], *, now: datetime\n) -> str | None:\n    if evidence.public is None:\n        return None\n    if evidence.public:\n        return "public exposure: ACL grants access to the AllUsers group"\n    return None\n\n\nCHECKS = {\n    "required_tags": _check_required_tags,\n    "approved_regions": _check_approved_regions,\n    "max_age_days": _check_max_age_days,\n    "require_encryption": _check_require_encryption,\n    "no_public_exposure": _check_no_public_exposure,\n}\n\n\ndef _exception_is_active(exception: dict[str, Any], *, now: datetime) -> bool:\n    """An exception with no `expires` field never expires; one with a past `expires` date no longer suppresses."""\n    expires = exception.get("expires")\n    if not expires:\n        return True\n    expiry = datetime.strptime(expires, "%Y-%m-%d").replace(tzinfo=UTC)\n    return now.date() <= expiry.date()\n\n\ndef _find_active_exception(\n    resource_id: str,\n    rule_id: str,\n    exceptions: list[dict[str, Any]],\n    *,\n    now: datetime,\n) -> dict[str, Any] | None:\n    for exception in exceptions:\n        if (\n            exception.get("resource_id") == resource_id\n            and exception.get("rule_id") == rule_id\n            and _exception_is_active(exception, now=now)\n        ):\n            return exception\n    return None\n\n\ndef evaluate_policy(\n    evidence: list[ResourceEvidence],\n    policy: dict[str, Any],\n    *,\n    now: datetime | None = None,\n) -> list[Finding]:\n    """Check every piece of evidence against every rule, and return every finding -- suppressed ones included.\n\n    `now` defaults to the current time -- passed explicitly (as every test\n    in this module does) whenever a specific, reproducible instant matters,\n    the same convention `findingsstore.put_finding()`\'s `timestamp`\n    parameter already established for testable time.\n\n    A rule\'s optional `applies_to` field (a list of resource types) skips\n    that rule for any resource type not in the list -- `require_encryption`\n    means nothing for an EC2 instance, and this keeps that explicit in the\n    policy file instead of silently relying on `evidence.encrypted` being\n    `None`.\n    """\n    resolved_now = now if now is not None else datetime.now(UTC)\n    rules = policy.get("rules", [])\n    exceptions = policy.get("exceptions", [])\n\n    findings: list[Finding] = []\n    for item in evidence:\n        for rule in rules:\n            applies_to = rule.get("applies_to")\n            if applies_to is not None and item.resource_type not in applies_to:\n                continue\n\n            check = CHECKS[rule["check"]]\n            violation = check(item, rule, now=resolved_now)\n            if violation is None:\n                continue\n\n            active_exception = _find_active_exception(\n                item.resource_id, rule["id"], exceptions, now=resolved_now\n            )\n            findings.append(\n                Finding(\n                    resource_id=item.resource_id,\n                    resource_type=item.resource_type,\n                    rule_id=rule["id"],\n                    severity=rule["severity"],\n                    evidence=violation,\n                    suppressed=active_exception is not None,\n                    suppression_reason=(\n                        active_exception.get("reason") if active_exception else None\n                    ),\n                )\n            )\n    return findings\n\n\ndef build_report(findings: list[Finding]) -> dict[str, Any]:\n    """Summarize findings by severity and suppression state, plus the full list -- the audit\'s paper trail."""\n    active = [f for f in findings if not f.suppressed]\n    suppressed = [f for f in findings if f.suppressed]\n\n    by_severity: dict[str, int] = {}\n    for finding in active:\n        by_severity[finding.severity] = by_severity.get(finding.severity, 0) + 1\n\n    return {\n        "status": "violations" if active else "ok",\n        "summary": {\n            "total_findings": len(findings),\n            "active": len(active),\n            "suppressed": len(suppressed),\n            "by_severity": by_severity,\n        },\n        "findings": [asdict(f) for f in findings],\n    }\n\n\n__all__ = [\n    "PUBLIC_ACCESS_GROUP_URI",\n    "CHECKS",\n    "ResourceEvidence",\n    "Finding",\n    "load_policy",\n    "evaluate_policy",\n    "build_report",\n]\nPYEOF\n'})}),"\n",(0,i.jsx)(n.h3,{id:"step-4--watch-it-pass",children:"Step 4 \u2014 Watch it pass"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_cloudaudit.py -v\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"tests/test_cloudaudit.py::test_compliant_resource_produces_no_finding PASSED [  6%]\ntests/test_cloudaudit.py::test_required_tags_reports_only_the_missing_tags PASSED [ 12%]\ntests/test_cloudaudit.py::test_approved_regions_rejects_a_region_not_in_the_list PASSED [ 18%]\ntests/test_cloudaudit.py::test_require_encryption_flags_a_disabled_bucket PASSED [ 25%]\ntests/test_cloudaudit.py::test_require_encryption_is_skipped_when_evidence_was_never_gathered PASSED [ 31%]\ntests/test_cloudaudit.py::test_no_public_exposure_flags_a_public_bucket PASSED [ 37%]\ntests/test_cloudaudit.py::test_applies_to_skips_a_rule_for_a_resource_type_not_listed PASSED [ 43%]\ntests/test_cloudaudit.py::test_max_age_days_flags_a_resource_older_than_the_limit PASSED [ 50%]\ntests/test_cloudaudit.py::test_max_age_days_is_silent_for_a_resource_within_the_limit PASSED [ 56%]\ntests/test_cloudaudit.py::test_max_age_days_is_skipped_without_created_at_evidence PASSED [ 62%]\ntests/test_cloudaudit.py::test_unexpired_exception_suppresses_but_does_not_drop_the_finding PASSED [ 68%]\ntests/test_cloudaudit.py::test_expired_exception_does_not_suppress PASSED [ 75%]\ntests/test_cloudaudit.py::test_exception_only_matches_its_own_resource_and_rule PASSED [ 81%]\ntests/test_cloudaudit.py::test_build_report_counts_active_and_suppressed_separately PASSED [ 87%]\ntests/test_cloudaudit.py::test_build_report_status_is_ok_when_every_finding_is_suppressed PASSED [ 93%]\ntests/test_cloudaudit.py::test_build_report_on_no_findings_is_ok_and_empty PASSED [100%]\n\n============================== 16 passed in 0.51s ===============================\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Sixteen tests, and every one of them ran with zero network calls \u2014 no Floci, no AWS, nothing\nlistening on port 4566. ",(0,i.jsx)(n.code,{children:"evaluate_policy()"})," is fully proven before this lab has touched the network\neven once."]}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-3--evidence-gathering-test-first-against-real-floci",children:"Phase 3 \u2014 Evidence gathering, test-first against real Floci"}),"\n",(0,i.jsx)(n.h3,{id:"step-1--append-the-failing-live-tests",children:"Step 1 \u2014 Append the failing live tests"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat >> tests/test_cloudaudit.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# Live evidence-gathering against a real, running Floci -- proves\n# gather_bucket_evidence() actually detects real seeded violations, not\n# just that the pure detection logic above is correct.\n# ---------------------------------------------------------------------------\n\nFLOCI_ENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\n\n@pytest.fixture\ndef s3_client(require_floci):\n    return get_aws_client("s3", region=REGION, endpoint_url=FLOCI_ENDPOINT)\n\n\n@pytest.fixture\ndef bucket(s3_client):\n    import uuid\n\n    name = f"platformops-audit-test-{uuid.uuid4().hex[:12]}"\n    s3_client.create_bucket(Bucket=name)\n    yield name\n    s3_client.delete_bucket(Bucket=name)\n\n\ndef test_bucket_uses_kms_encryption_is_false_for_the_real_sse_s3_default(\n    s3_client, bucket\n):\n    # Floci turns on SSE-S3 (AES256) default encryption the moment a bucket\n    # is created, matching real AWS behavior since January 2023 -- so a\n    # freshly created bucket really is "encrypted", just not with a\n    # customer-managed KMS key. Confirm that default is exactly what the\n    # bucket already has before asserting the KMS-specific check reads it\n    # correctly.\n    existing = s3_client.get_bucket_encryption(Bucket=bucket)\n    algorithm = existing["ServerSideEncryptionConfiguration"]["Rules"][0][\n        "ApplyServerSideEncryptionByDefault"\n    ]["SSEAlgorithm"]\n    assert algorithm == "AES256"\n\n    assert cloudaudit.bucket_uses_kms_encryption(s3_client, bucket) is False\n\n\ndef test_bucket_uses_kms_encryption_is_true_once_a_kms_key_is_set(s3_client, bucket):\n    s3_client.put_bucket_encryption(\n        Bucket=bucket,\n        ServerSideEncryptionConfiguration={\n            "Rules": [\n                {\n                    "ApplyServerSideEncryptionByDefault": {\n                        "SSEAlgorithm": "aws:kms",\n                        "KMSMasterKeyID": "alias/platformops-audit-demo",\n                    }\n                }\n            ]\n        },\n    )\n\n    assert cloudaudit.bucket_uses_kms_encryption(s3_client, bucket) is True\n\n\ndef test_bucket_is_public_is_false_by_default(s3_client, bucket):\n    assert cloudaudit.bucket_is_public(s3_client, bucket) is False\n\n\ndef test_gather_bucket_evidence_reads_real_tags_and_region(s3_client, bucket):\n    s3_client.put_bucket_tagging(\n        Bucket=bucket, Tagging={"TagSet": [{"Key": "environment", "Value": "prod"}]}\n    )\n    records = list_s3_buckets(s3_client)\n    matching = [r for r in records if r.resource_id == bucket]\n\n    evidence = cloudaudit.gather_bucket_evidence(s3_client, matching)\n\n    assert len(evidence) == 1\n    assert evidence[0].tags == {"environment": "prod"}\n    assert evidence[0].region == REGION\n    assert evidence[0].encrypted is False\n    assert evidence[0].public is False\n\n\ndef test_run_cloud_audit_detects_a_real_seeded_violation(s3_client, bucket, tmp_path):\n    policy_path = tmp_path / "policy.yaml"\n    policy_path.write_text(\n        "rules:\\n"\n        "  - id: require-encryption\\n"\n        "    check: require_encryption\\n"\n        "    severity: high\\n"\n        "exceptions: []\\n"\n    )\n\n    report = cloudaudit.run_cloud_audit(\n        policy_path=policy_path, region=REGION, endpoint_url=FLOCI_ENDPOINT\n    )\n\n    assert report["status"] == "violations"\n    matching = [f for f in report["findings"] if f["resource_id"] == bucket]\n    assert len(matching) == 1\n    assert matching[0]["rule_id"] == "require-encryption"\n    assert "not using a customer-managed KMS key" in matching[0]["evidence"]\nPYEOF\n'})}),"\n",(0,i.jsx)(n.h3,{id:"step-2--watch-it-fail-1",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_cloudaudit.py -q\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"FAILED tests/test_cloudaudit.py::test_bucket_uses_kms_encryption_is_false_for_the_real_sse_s3_default\nFAILED tests/test_cloudaudit.py::test_bucket_uses_kms_encryption_is_true_once_a_kms_key_is_set\nFAILED tests/test_cloudaudit.py::test_bucket_is_public_is_false_by_default\nFAILED tests/test_cloudaudit.py::test_gather_bucket_evidence_reads_real_tags_and_region\nFAILED tests/test_cloudaudit.py::test_run_cloud_audit_detects_a_real_seeded_violation\n5 failed, 16 passed in 1.91s\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Every one of the sixteen Phase 2 tests still passes \u2014 appending these tests did not touch the\ndetection code at all. The five new tests fail with ",(0,i.jsx)(n.code,{children:"AttributeError: module 'platformops.cloudaudit' has no attribute 'bucket_uses_kms_encryption'"})," (and similarly for the other three new functions) \u2014\nthey call functions that do not exist yet."]}),"\n",(0,i.jsxs)(n.h3,{id:"step-3--append-evidence-gathering-to-cloudauditpy",children:["Step 3 \u2014 Append evidence-gathering to ",(0,i.jsx)(n.code,{children:"cloudaudit.py"})]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat >> src/platformops/cloudaudit.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# Evidence-gathering -- the only functions in this module that touch the\n# network. Kept separate from detection above so the detection function\n# never needs a live AWS connection (or Floci) to be tested at all.\n# ---------------------------------------------------------------------------\n\n\ndef bucket_uses_kms_encryption(client: Any, bucket_name: str) -> bool:\n    """True only if `bucket_name`\'s default encryption uses a customer-managed KMS key.\n\n    Every S3 bucket -- on real AWS since January 2023, and on Floci, which\n    matches that behavior -- already has SSE-S3 (`AES256`) default\n    encryption turned on the moment it is created. A check that only asked\n    "is default encryption configured at all" would never find a violation\n    on a real account, because that question stopped being interesting\n    once AWS made the answer always "yes". The question worth auditing is\n    narrower: does this bucket use a customer-managed KMS key\n    (`SSEAlgorithm: aws:kms`), the level of encryption many compliance\n    policies actually require, so a security team controls key rotation\n    and can see key-usage in CloudTrail -- not the AWS-managed default.\n\n    An unencrypted bucket\'s `get_bucket_encryption()` call fails with\n    `ServerSideEncryptionConfigurationNotFoundError` -- the same "expected\n    failure, not an error" pattern `multiregion._bucket_tags()` already\n    uses for `NoSuchTagSet`. On Floci and on real AWS today this should\n    never actually happen (see above), but the check still handles it\n    honestly rather than assuming it cannot occur. Anything else is a real\n    problem and is left to raise.\n    """\n    try:\n        response = client.get_bucket_encryption(Bucket=bucket_name)\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "")\n        if code == "ServerSideEncryptionConfigurationNotFoundError":\n            return False\n        raise\n    rules = response["ServerSideEncryptionConfiguration"]["Rules"]\n    return any(\n        rule["ApplyServerSideEncryptionByDefault"]["SSEAlgorithm"] == "aws:kms"\n        for rule in rules\n    )\n\n\ndef bucket_is_public(client: Any, bucket_name: str) -> bool:\n    """True if `bucket_name`\'s ACL grants any access to the AllUsers group."""\n    acl = client.get_bucket_acl(Bucket=bucket_name)\n    for grant in acl.get("Grants", []):\n        if grant.get("Grantee", {}).get("URI") == PUBLIC_ACCESS_GROUP_URI:\n            return True\n    return False\n\n\ndef gather_bucket_evidence(\n    client: Any, records: list[ResourceRecord]\n) -> list[ResourceEvidence]:\n    """Turn S3 `ResourceRecord`s into `ResourceEvidence`, adding encryption, exposure and age.\n\n    `list_buckets()` is called once, account-wide, to map every bucket name\n    to its real `CreationDate` -- the same "one account-wide call, not one\n    per bucket" discipline `multiregion.list_s3_buckets()` already applies\n    to `list_buckets()` itself. `get_bucket_encryption()` and\n    `get_bucket_acl()` are still one call per bucket -- neither has an\n    account-wide equivalent.\n\n    Records for any resource type other than `s3-bucket` pass through with\n    `encrypted`, `public` and `created_at` left `None` -- this module only\n    knows how to gather S3-specific evidence today; a rule that needs that\n    evidence skips a resource this function never gathered it for (see\n    `evaluate_policy()`\'s `applies_to` handling).\n    """\n    creation_dates = {\n        bucket["Name"]: bucket["CreationDate"]\n        for bucket in client.list_buckets()["Buckets"]\n    }\n\n    evidence: list[ResourceEvidence] = []\n    for record in records:\n        if record.resource_type != "s3-bucket":\n            evidence.append(ResourceEvidence.from_record(record))\n            continue\n\n        created_at = creation_dates.get(record.resource_id)\n        evidence.append(\n            ResourceEvidence.from_record(\n                record,\n                encrypted=bucket_uses_kms_encryption(client, record.resource_id),\n                public=bucket_is_public(client, record.resource_id),\n                created_at=created_at.isoformat() if created_at else None,\n            )\n        )\n    return evidence\n\n\ndef run_cloud_audit(\n    *,\n    policy_path: Path,\n    region: str,\n    profile: str | None = None,\n    endpoint_url: str | None = None,\n    now: datetime | None = None,\n) -> dict[str, Any]:\n    """The one call a CLI command or a script needs: gather S3 evidence, evaluate the policy, build the report.\n\n    Catches the same two exception types every AWS-backed command-level\n    function in this project catches by name (`cloudinventory.scan_inventory()`,\n    `reportstore.upload_report_to_bucket()`), and returns the same\n    `{"status": "error", ...}` shape for a failure to even run the audit --\n    kept distinct from `build_report()`\'s `"ok"`/`"violations"`, which both\n    mean the audit ran successfully and are about what it found.\n    """\n    try:\n        policy = load_policy(policy_path)\n        client = get_aws_client(\n            "s3", region=region, profile=profile, endpoint_url=endpoint_url\n        )\n        records = list_s3_buckets(client)\n        evidence = gather_bucket_evidence(client, records)\n    except botocore.exceptions.NoCredentialsError as exc:\n        return {"status": "error", "error": "no-credentials", "message": str(exc)}\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "Unknown")\n        return {"status": "error", "error": code, "message": str(exc)}\n\n    findings = evaluate_policy(evidence, policy, now=now)\n    return build_report(findings)\nPYEOF\n'})}),"\n",(0,i.jsxs)(n.p,{children:["Now add the four new names to the ",(0,i.jsx)(n.code,{children:"__all__"})," list near the top of the module (open\n",(0,i.jsx)(n.code,{children:"src/platformops/cloudaudit.py"}),", find ",(0,i.jsx)(n.code,{children:"__all__"}),", and add ",(0,i.jsx)(n.code,{children:"bucket_uses_kms_encryption"}),",\n",(0,i.jsx)(n.code,{children:"bucket_is_public"}),", ",(0,i.jsx)(n.code,{children:"gather_bucket_evidence"})," and ",(0,i.jsx)(n.code,{children:"run_cloud_audit"}),")."]}),"\n",(0,i.jsx)(n.h3,{id:"step-4--watch-it-pass-against-real-floci",children:"Step 4 \u2014 Watch it pass, against real Floci"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_cloudaudit.py -v\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"tests/test_cloudaudit.py::test_compliant_resource_produces_no_finding PASSED [  4%]\ntests/test_cloudaudit.py::test_required_tags_reports_only_the_missing_tags PASSED [  9%]\ntests/test_cloudaudit.py::test_approved_regions_rejects_a_region_not_in_the_list PASSED [ 14%]\ntests/test_cloudaudit.py::test_require_encryption_flags_a_disabled_bucket PASSED [ 19%]\ntests/test_cloudaudit.py::test_require_encryption_is_skipped_when_evidence_was_never_gathered PASSED [ 23%]\ntests/test_cloudaudit.py::test_no_public_exposure_flags_a_public_bucket PASSED [ 28%]\ntests/test_cloudaudit.py::test_applies_to_skips_a_rule_for_a_resource_type_not_listed PASSED [ 33%]\ntests/test_cloudaudit.py::test_max_age_days_flags_a_resource_older_than_the_limit PASSED [ 38%]\ntests/test_cloudaudit.py::test_max_age_days_is_silent_for_a_resource_within_the_limit PASSED [ 42%]\ntests/test_cloudaudit.py::test_max_age_days_is_skipped_without_created_at_evidence PASSED [ 47%]\ntests/test_cloudaudit.py::test_unexpired_exception_suppresses_but_does_not_drop_the_finding PASSED [ 52%]\ntests/test_cloudaudit.py::test_expired_exception_does_not_suppress PASSED [ 57%]\ntests/test_cloudaudit.py::test_exception_only_matches_its_own_resource_and_rule PASSED [ 61%]\ntests/test_cloudaudit.py::test_build_report_counts_active_and_suppressed_separately PASSED [ 66%]\ntests/test_cloudaudit.py::test_build_report_status_is_ok_when_every_finding_is_suppressed PASSED [ 71%]\ntests/test_cloudaudit.py::test_build_report_on_no_findings_is_ok_and_empty PASSED [ 76%]\ntests/test_cloudaudit.py::test_bucket_uses_kms_encryption_is_false_for_the_real_sse_s3_default PASSED [ 80%]\ntests/test_cloudaudit.py::test_bucket_uses_kms_encryption_is_true_once_a_kms_key_is_set PASSED [ 85%]\ntests/test_cloudaudit.py::test_bucket_is_public_is_false_by_default PASSED [ 90%]\ntests/test_cloudaudit.py::test_gather_bucket_evidence_reads_real_tags_and_region PASSED [ 95%]\ntests/test_cloudaudit.py::test_run_cloud_audit_detects_a_real_seeded_violation PASSED [100%]\n\n============================== 21 passed in 3.52s ===============================\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Twenty-one tests: the sixteen pure detection tests from Phase 2, unchanged, plus five that made\nreal API calls against your own running Floci container \u2014 creating a real bucket, reading its real\n(default) encryption configuration, and confirming ",(0,i.jsx)(n.code,{children:"bucket_uses_kms_encryption()"}),' correctly reads it\nas "not KMS."']}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-4--wire-the-cli-seed-real-violations-and-audit-them-live",children:"Phase 4 \u2014 Wire the CLI, seed real violations, and audit them live"}),"\n",(0,i.jsx)(n.h3,{id:"step-1--add-the-cli-commands",children:"Step 1 \u2014 Add the CLI commands"}),"\n",(0,i.jsxs)(n.p,{children:["Add three commands to ",(0,i.jsx)(n.code,{children:"src/platformops/cli.py"}),", following the same ",(0,i.jsx)(n.code,{children:"--json"}),"/plain-text pattern\nevery other command uses: ",(0,i.jsx)(n.code,{children:"cloud-audit"})," (import ",(0,i.jsx)(n.code,{children:"run_cloud_audit"})," next to the other\n",(0,i.jsx)(n.code,{children:"platformops.*"})," imports), ",(0,i.jsx)(n.code,{children:"findings-list"}),", and ",(0,i.jsx)(n.code,{children:"findings-show"})," \u2014 the last two read back a report\n",(0,i.jsx)(n.code,{children:"cloud-audit --output"})," wrote, they do not talk to AWS at all. See the module's source if you want\nto compare your CLI wiring against the finished version."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops cloud-audit --help\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:" Usage: platformops cloud-audit [OPTIONS]\n\n Check S3 buckets against a policy file and report violations (the M24\n project).\n\n This command only reads and reports -- it never modifies, deletes or\n tags a resource. Exit 0 means no active (non-suppressed) findings;\n exit 1 means at least one. `--output` writes the same report\n `findings-list` and `findings-show` read back in, so a review can\n happen in a second, separate step.\n\n\u256D\u2500 Options \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 *  --policy              <path>  path to a policy YAML file [required]       \u2502\n\u2502 *  --region              <str>   AWS region to audit, e.g. us-east-1         \u2502\n\u2502                                  [required]                                  \u2502\n\u2502    --profile             <str>   named AWS profile to use (defaults to the   \u2502\n\u2502                                  normal credential chain)                    \u2502\n\u2502    --endpoint-url        <str>   override the AWS endpoint, e.g.             \u2502\n\u2502                                  http://localhost:4566 for Floci (defaults   \u2502\n\u2502                                  to AWS_ENDPOINT_URL, then real AWS)         \u2502\n\u2502    --output              <path>  also write the full JSON report to this     \u2502\n\u2502                                  path                                        \u2502\n\u2502    --json                        print a machine-readable JSON report        \u2502\n\u2502                                  instead of plain text                       \u2502\n\u2502    --help                        Show this message and exit.                 \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n"})}),"\n",(0,i.jsx)(n.h3,{id:"step-2--add-cli-level-tests",children:"Step 2 \u2014 Add CLI-level tests"}),"\n",(0,i.jsxs)(n.p,{children:["Following the same convention ",(0,i.jsx)(n.code,{children:"test_multi_region_scan_*"})," and ",(0,i.jsx)(n.code,{children:"test_report_upload_*"})," already\nestablished in ",(0,i.jsx)(n.code,{children:"tests/test_cli.py"})," \u2014 monkeypatch ",(0,i.jsx)(n.code,{children:"run_cloud_audit"})," so these tests prove option\nparsing and exit codes, not real AWS behavior (that is already proven in ",(0,i.jsx)(n.code,{children:"test_cloudaudit.py"}),") \u2014\nadd tests for ",(0,i.jsx)(n.code,{children:"cloud-audit"}),", ",(0,i.jsx)(n.code,{children:"findings-list"})," and ",(0,i.jsx)(n.code,{children:"findings-show"}),". See the finished\n",(0,i.jsx)(n.code,{children:"tests/test_cli.py"})," if you want to compare against your own."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'uv run pytest tests/test_cli.py -k "cloud_audit or findings_list or findings_show" -v\n'})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"tests/test_cli.py::test_cloud_audit_exits_0_when_no_active_findings PASSED\ntests/test_cli.py::test_cloud_audit_exits_1_when_active_findings_exist PASSED\ntests/test_cli.py::test_cloud_audit_json_prints_the_full_report PASSED\ntests/test_cli.py::test_cloud_audit_writes_the_report_to_output PASSED\ntests/test_cli.py::test_cloud_audit_exits_1_and_reports_the_error_message PASSED\ntests/test_cli.py::test_findings_list_excludes_suppressed_by_default PASSED\ntests/test_cli.py::test_findings_list_include_suppressed_shows_both PASSED\ntests/test_cli.py::test_findings_list_filters_by_severity PASSED\ntests/test_cli.py::test_findings_list_missing_file_exits_2 PASSED\ntests/test_cli.py::test_findings_show_prints_the_matching_finding PASSED\ntests/test_cli.py::test_findings_show_json_includes_suppression_fields PASSED\ntests/test_cli.py::test_findings_show_no_match_exits_1 PASSED\n\n======================= 12 passed, 63 deselected in 0.35s =======================\n"})}),"\n",(0,i.jsx)(n.h3,{id:"step-3--seed-real-violations-in-floci",children:"Step 3 \u2014 Seed real violations in Floci"}),"\n",(0,i.jsxs)(n.p,{children:["Six buckets: five that break some part of ",(0,i.jsx)(n.code,{children:"policy.example.yaml"}),", one that is fully compliant \u2014 the\ncompliant one is the check against false positives."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m24_seed_violations.py << \'PYEOF\'\nfrom platformops.awsclient import get_aws_client\n\nENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\n\ndef main() -> None:\n    client = get_aws_client("s3", region=REGION, endpoint_url=ENDPOINT)\n\n    # missing required tags\n    client.create_bucket(Bucket="platformops-audit-missing-tags")\n\n    # wrong region + tagged (so only the region rule fires)\n    client.create_bucket(\n        Bucket="platformops-audit-wrong-region",\n        CreateBucketConfiguration={"LocationConstraint": "eu-central-1"},\n    )\n    client.put_bucket_tagging(\n        Bucket="platformops-audit-wrong-region",\n        Tagging={"TagSet": [\n            {"Key": "team_owner", "Value": "platform"},\n            {"Key": "environment", "Value": "prod"},\n        ]},\n    )\n\n    # tagged + right region, but no KMS key (default SSE-S3 only)\n    client.create_bucket(Bucket="platformops-audit-no-kms")\n    client.put_bucket_tagging(\n        Bucket="platformops-audit-no-kms",\n        Tagging={"TagSet": [\n            {"Key": "team_owner", "Value": "platform"},\n            {"Key": "environment", "Value": "prod"},\n        ]},\n    )\n\n    # tagged, right region, KMS-encrypted, but public ACL\n    client.create_bucket(Bucket="platformops-audit-public")\n    client.put_bucket_tagging(\n        Bucket="platformops-audit-public",\n        Tagging={"TagSet": [\n            {"Key": "team_owner", "Value": "platform"},\n            {"Key": "environment", "Value": "prod"},\n        ]},\n    )\n    client.put_bucket_encryption(\n        Bucket="platformops-audit-public",\n        ServerSideEncryptionConfiguration={"Rules": [{\n            "ApplyServerSideEncryptionByDefault": {\n                "SSEAlgorithm": "aws:kms",\n                "KMSMasterKeyID": "alias/platformops-audit-demo",\n            }\n        }]},\n    )\n    client.put_bucket_acl(Bucket="platformops-audit-public", ACL="public-read")\n\n    # missing tags, but matches policy.example.yaml\'s exception (unexpired)\n    client.create_bucket(Bucket="platformops-legacy-reports")\n\n    # fully compliant -- proves the audit does not false-positive\n    client.create_bucket(Bucket="platformops-audit-compliant")\n    client.put_bucket_tagging(\n        Bucket="platformops-audit-compliant",\n        Tagging={"TagSet": [\n            {"Key": "team_owner", "Value": "platform"},\n            {"Key": "environment", "Value": "prod"},\n        ]},\n    )\n    client.put_bucket_encryption(\n        Bucket="platformops-audit-compliant",\n        ServerSideEncryptionConfiguration={"Rules": [{\n            "ApplyServerSideEncryptionByDefault": {\n                "SSEAlgorithm": "aws:kms",\n                "KMSMasterKeyID": "alias/platformops-audit-demo",\n            }\n        }]},\n    )\n\n    print("seeded 6 buckets")\n\n\nif __name__ == "__main__":\n    main()\nPYEOF\nuv run python /tmp/m24_seed_violations.py\n'})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"seeded 6 buckets\n"})}),"\n",(0,i.jsx)(n.h3,{id:"step-4--run-the-audit-live",children:"Step 4 \u2014 Run the audit, live"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops cloud-audit --policy policy.example.yaml --region us-east-1 --output /tmp/m24_audit_report.json\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"8 finding(s) -- 7 active, 1 suppressed\n  [SUPPRESSED] platformops-legacy-reports (required-tags): missing tags: team_owner, environment\n  [HIGH] platformops-legacy-reports (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-missing-tags (required-tags): missing tags: team_owner, environment\n  [HIGH] platformops-audit-missing-tags (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-wrong-region (approved-regions): region 'eu-central-1' is not in the approved list ['us-east-1', 'us-west-2']\n  [HIGH] platformops-audit-wrong-region (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-no-kms (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [CRITICAL] platformops-audit-public (no-public-exposure): public exposure: ACL grants access to the AllUsers group\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Exit code ",(0,i.jsx)(n.code,{children:"1"}),". Eight findings total: seven active, one suppressed. Look closely at\n",(0,i.jsx)(n.code,{children:"platformops-legacy-reports"})," \u2014 it appears twice, once ",(0,i.jsx)(n.code,{children:"SUPPRESSED"})," (the ",(0,i.jsx)(n.code,{children:"required-tags"})," violation\nthe example policy's exception excuses) and once ",(0,i.jsx)(n.code,{children:"HIGH"})," (",(0,i.jsx)(n.code,{children:"require-encryption"}),", a rule the exception\nnever mentioned, so it is fully active). And ",(0,i.jsx)(n.code,{children:"platformops-audit-compliant"})," is not in this list at\nall \u2014 six buckets were seeded, five violate something, one does not, and the report shows exactly\nthat split."]}),"\n",(0,i.jsx)(n.admonition,{title:"This output is point-in-time",type:"warning",children:(0,i.jsxs)(n.p,{children:["Bucket names here are fixed by the seed script, so they will match exactly \u2014 but a real account's\nfindings depend entirely on what actually exists in it. Re-run this after changing\n",(0,i.jsx)(n.code,{children:"policy.example.yaml"})," or the seed script and expect a different set of findings."]})}),"\n",(0,i.jsx)(n.h3,{id:"step-5--list-and-show-findings",children:"Step 5 \u2014 List and show findings"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops findings-list /tmp/m24_audit_report.json\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"7 finding(s)\n  [HIGH] platformops-legacy-reports (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-missing-tags (required-tags): missing tags: team_owner, environment\n  [HIGH] platformops-audit-missing-tags (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-wrong-region (approved-regions): region 'eu-central-1' is not in the approved list ['us-east-1', 'us-west-2']\n  [HIGH] platformops-audit-wrong-region (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-no-kms (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [CRITICAL] platformops-audit-public (no-public-exposure): public exposure: ACL grants access to the AllUsers group\n"})}),"\n",(0,i.jsxs)(n.p,{children:["By default, suppressed findings stay out of the plain list view \u2014 seven, not eight. Add\n",(0,i.jsx)(n.code,{children:"--include-suppressed"})," to see all eight, the suppressed one included:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops findings-list /tmp/m24_audit_report.json --include-suppressed\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"8 finding(s)\n  [SUPPRESSED] platformops-legacy-reports (required-tags): missing tags: team_owner, environment\n  [HIGH] platformops-legacy-reports (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-missing-tags (required-tags): missing tags: team_owner, environment\n  [HIGH] platformops-audit-missing-tags (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-wrong-region (approved-regions): region 'eu-central-1' is not in the approved list ['us-east-1', 'us-west-2']\n  [HIGH] platformops-audit-wrong-region (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [HIGH] platformops-audit-no-kms (require-encryption): encryption: not using a customer-managed KMS key (AWS-managed SSE-S3 default only)\n  [CRITICAL] platformops-audit-public (no-public-exposure): public exposure: ACL grants access to the AllUsers group\n"})}),"\n",(0,i.jsx)(n.p,{children:"Filter to just the critical one:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops findings-list /tmp/m24_audit_report.json --severity critical\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"1 finding(s)\n  [CRITICAL] platformops-audit-public (no-public-exposure): public exposure: ACL grants access to the AllUsers group\n"})}),"\n",(0,i.jsxs)(n.p,{children:["And look at one finding's full detail \u2014 including a suppressed one, with its reason \u2014 by its\n",(0,i.jsx)(n.code,{children:"(resource_id, rule_id)"})," pair:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run platformops findings-show /tmp/m24_audit_report.json platformops-legacy-reports required-tags --json\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-json",children:'{\n  "status": "ok",\n  "finding": {\n    "resource_id": "platformops-legacy-reports",\n    "resource_type": "s3-bucket",\n    "rule_id": "required-tags",\n    "severity": "high",\n    "evidence": "missing tags: team_owner, environment",\n    "suppressed": true,\n    "suppression_reason": "legacy bucket predates the tagging policy -- cleanup tracked in ticket OPS-4021"\n  }\n}\n'})}),"\n",(0,i.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,i.jsx)(n.p,{children:"Delete every Floci resource this lab seeded:"}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m24_teardown.py << \'PYEOF\'\nfrom platformops.awsclient import get_aws_client\n\nENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\nBUCKETS = [\n    "platformops-audit-missing-tags",\n    "platformops-audit-wrong-region",\n    "platformops-audit-no-kms",\n    "platformops-audit-public",\n    "platformops-legacy-reports",\n    "platformops-audit-compliant",\n]\n\n\ndef main() -> None:\n    client = get_aws_client("s3", region=REGION, endpoint_url=ENDPOINT)\n    existing = {b["Name"] for b in client.list_buckets()["Buckets"]}\n    for name in BUCKETS:\n        if name not in existing:\n            continue\n        paginator = client.get_paginator("list_objects_v2")\n        for page in paginator.paginate(Bucket=name):\n            for obj in page.get("Contents", []):\n                client.delete_object(Bucket=name, Key=obj["Key"])\n        client.delete_bucket(Bucket=name)\n        print(f"deleted {name}")\n\n    remaining = [b["Name"] for b in client.list_buckets()["Buckets"] if b["Name"] in BUCKETS]\n    print("remaining:", remaining)\n\n\nif __name__ == "__main__":\n    main()\nPYEOF\nuv run python /tmp/m24_teardown.py\n'})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"deleted platformops-audit-missing-tags\ndeleted platformops-audit-wrong-region\ndeleted platformops-audit-no-kms\ndeleted platformops-audit-public\ndeleted platformops-legacy-reports\ndeleted platformops-audit-compliant\nremaining: []\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Do ",(0,i.jsx)(n.strong,{children:"not"})," run ",(0,i.jsx)(n.code,{children:"floci stop"})," \u2014 later modules still need it running."]}),"\n",(0,i.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,i.jsxs)(n.ul,{children:["\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:(0,i.jsx)(n.code,{children:"ImportError: cannot import name 'cloudaudit'"})})," on a step that expects it to already exist \u2014\nconfirm Phase 2 Step 3 actually wrote ",(0,i.jsx)(n.code,{children:"src/platformops/cloudaudit.py"})," before running Phase 2\nStep 4's tests."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"Live tests in Phase 3 all skip"})," with ",(0,i.jsx)(n.code,{children:'"Floci is not reachable at http://localhost:4566"'})," \u2014\nrun ",(0,i.jsx)(n.code,{children:"floci start"})," and confirm ",(0,i.jsx)(n.code,{children:"curl http://localhost:4566/_floci/health"})," returns a JSON body\nbefore retrying."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsxs)(n.strong,{children:[(0,i.jsx)(n.code,{children:"cloud-audit"})," finds zero violations against your own seeded buckets"]})," \u2014 check that\n",(0,i.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," is still exported in this shell (",(0,i.jsx)(n.code,{children:"echo $AWS_ENDPOINT_URL"}),"), and that you\npassed ",(0,i.jsx)(n.code,{children:"--region us-east-1"}),", matching the region the seed script created buckets in. A bucket\ncreated in one region and audited against another will not be found at all."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsxs)(n.strong,{children:[(0,i.jsx)(n.code,{children:"KeyError: 'check'"})," or ",(0,i.jsx)(n.code,{children:"KeyError: 'severity'"})]})," while running ",(0,i.jsx)(n.code,{children:"cloud-audit"})," \u2014 a rule in your\npolicy file is missing a required field. Every rule needs ",(0,i.jsx)(n.code,{children:"id"}),", ",(0,i.jsx)(n.code,{children:"check"})," and ",(0,i.jsx)(n.code,{children:"severity"})," at\nminimum; compare against ",(0,i.jsx)(n.code,{children:"policy.example.yaml"}),"."]}),"\n",(0,i.jsxs)(n.li,{children:[(0,i.jsx)(n.strong,{children:"A finding you expected to be suppressed shows up as active"})," \u2014 check the exception's\n",(0,i.jsx)(n.code,{children:"resource_id"})," and ",(0,i.jsx)(n.code,{children:"rule_id"})," match the finding's exactly (both are matched as plain strings, no\nwildcards), and that ",(0,i.jsx)(n.code,{children:"expires"})," is still in the future."]}),"\n"]})}),"\n",(0,i.jsx)(n.hr,{}),"\n",(0,i.jsx)(n.h2,{id:"phase-5--full-quality-gate-and-version-bump",children:"Phase 5 \u2014 Full quality gate and version bump"}),"\n",(0,i.jsxs)(n.p,{children:['Unset the credential/endpoint variables from "Before you start" first. boto3 reads\n',(0,i.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," on every client it builds, including inside ",(0,i.jsx)(n.code,{children:"test_cloudinventory.py"})," and\n",(0,i.jsx)(n.code,{children:"test_multiregion.py"}),"'s ",(0,i.jsx)(n.code,{children:"@mock_aws"})," blocks (Modules 21 and 23) -- with it still exported, those\nsupposedly-isolated moto tests get redirected to Floci's real state instead, and fail with\nconfusing assertion errors that have nothing to do with this module:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"unset AWS_ENDPOINT_URL AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY\nuv run pytest -q\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 23%]\n........................................................................ [ 47%]\n........................................................................ [ 71%]\n........................................................................ [ 95%]\n..............                                                           [100%]\n302 passed in 42.62s\n"})}),"\n",(0,i.jsxs)(n.p,{children:["302 tests total \u2014 the 269 already in place at ",(0,i.jsx)(n.code,{children:"v1.8.0"}),", plus this module's 21\n",(0,i.jsx)(n.code,{children:"test_cloudaudit.py"})," tests, plus 12 new ",(0,i.jsx)(n.code,{children:"test_cli.py"})," tests for ",(0,i.jsx)(n.code,{children:"cloud-audit"}),", ",(0,i.jsx)(n.code,{children:"findings-list"})," and\n",(0,i.jsx)(n.code,{children:"findings-show"}),"."]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format --check src/ tests/\nuv run mypy src/\n"})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"All checks passed!\n40 files already formatted\nSuccess: no issues found in 20 source files\n"})}),"\n",(0,i.jsxs)(n.p,{children:["Bump the version in ",(0,i.jsx)(n.code,{children:"pyproject.toml"})," from ",(0,i.jsx)(n.code,{children:"1.8.0"})," to ",(0,i.jsx)(n.code,{children:"1.9.0"}),", add a ",(0,i.jsx)(n.code,{children:"[1.9.0]"})," entry to\n",(0,i.jsx)(n.code,{children:"CHANGELOG.md"})," describing ",(0,i.jsx)(n.code,{children:"cloudaudit.py"})," and the ",(0,i.jsx)(n.code,{children:"cloud-audit"}),"/",(0,i.jsx)(n.code,{children:"findings-list"}),"/",(0,i.jsx)(n.code,{children:"findings-show"}),"\ncommands, then sync and tag:"]}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-bash",children:'uv sync\nuv run platformops version\ngit add -A\ngit commit -m "M24: Cloud Governance and Compliance Automation -- v1.9.0"\ngit tag v1.9.0\n'})}),"\n",(0,i.jsx)(n.p,{children:(0,i.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,i.jsx)(n.pre,{children:(0,i.jsx)(n.code,{className:"language-text",children:"platformops 1.9.0\n"})})]})}function u(e={}){let{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,i.jsx)(n,{...e,children:(0,i.jsx)(l,{...e})}):l(e)}},8453(e,n,t){t.d(n,{R:()=>o,x:()=>a});var s=t(6540);let i={},r=s.createContext(i);function o(e){let n=s.useContext(r);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(i):e.components||i:o(e.components),s.createElement(r.Provider,{value:n},e.children)}}}]);