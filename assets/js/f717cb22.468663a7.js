"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["3426"],{4924(e,t,n){n.r(t),n.d(t,{metadata:()=>a,default:()=>u,frontMatter:()=>r,contentTitle:()=>l,toc:()=>h,assets:()=>c});var a=JSON.parse('{"id":"m28-kubernetes-automation/quiz","title":"Quiz: Kubernetes Automation with Python","description":"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read","source":"@site/docs/m28-kubernetes-automation/quiz.mdx","sourceDirName":"m28-kubernetes-automation","slug":"/m28-kubernetes-automation/quiz","permalink":"/307-aipython-site/docs/m28-kubernetes-automation/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Kubernetes Automation with Python"},"sidebar":"courseSidebar","previous":{"title":"Lab: Kubernetes Automation with Python","permalink":"/307-aipython-site/docs/m28-kubernetes-automation/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m28-kubernetes-automation/deep-dive"}}'),o=n(4848),s=n(8453),i=n(7517);let r={sidebar_position:3,title:"Quiz: Kubernetes Automation with Python"},l="Kubernetes Automation with Python \u2014 Quiz",c={},h=[];function d(e){let t={admonition:"admonition",h1:"h1",header:"header",p:"p",...(0,s.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"kubernetes-automation-with-python--quiz",children:"Kubernetes Automation with Python \u2014 Quiz"})}),"\n",(0,o.jsx)(t.admonition,{title:"This quiz covers the full module",type:"info",children:(0,o.jsx)(t.p,{children:"Some questions draw on material from both the Lab and the Deep Dive page. If you have not read\nthe Deep Dive yet, come back to those questions after you finish it."})}),"\n",(0,o.jsx)(i.A,{questions:[{prompt:"In the lesson's departure-board analogy, what does the Kubernetes API server represent?",options:[{text:"The board itself -- it already knows every Deployment and Pod's current state, and a reader only asks it questions, never changes a flight's status through it",correct:!0,explanation:"The point of the analogy: the API server already holds the live state because the cluster's own controllers wrote it there -- this module's code only reads that board."},{text:"The traveler reading the board",correct:!1,explanation:"The traveler represents this module's own code -- the one asking questions and reporting what it finds, not the system holding the state."},{text:"The airline agent who can actually rebook a flight",correct:!1,explanation:"That role is explicitly Module 29's -- the governed, approval-gated code that can act on what it reads. This module never plays that role."},{text:"The gate a flight departs from",correct:!1,explanation:"A gate is one specific piece of information the board displays, not the system the analogy is built around."}]},{prompt:"What is the difference between config.load_kube_config() and config.load_incluster_config()?",options:[{text:"load_kube_config() reads a kubeconfig file (the way kubectl on your laptop already authenticates); load_incluster_config() reads the token and certificate Kubernetes mounts into a Pod's own filesystem",correct:!0,explanation:"This module's lab only uses the kubeconfig path, against a kind cluster on your laptop -- the in-cluster path is for a platformops container that itself runs inside the cluster."},{text:"load_kube_config() only works with kind clusters; load_incluster_config() only works with real cloud clusters",correct:!1,explanation:"Both functions work with any Kubernetes cluster -- the distinction is where the calling process itself is running (outside the cluster vs. inside a Pod), not which kind of cluster it talks to."},{text:"load_incluster_config() is a newer, faster replacement for load_kube_config()",correct:!1,explanation:"Neither is a replacement for the other -- they are two different authentication paths for two different situations, and this project's k8sclient.py supports both."},{text:"load_kube_config() requires no authentication at all",correct:!1,explanation:"load_kube_config() still authenticates -- it just reads the credentials from a kubeconfig file instead of a Pod's mounted ServiceAccount token."}]},{prompt:"Why does kubernetes_inspect.py take an AppsV1Api and a CoreV1Api as function parameters instead of building them itself inside the module?",options:[{text:"So the module's logic can be unit-tested against a fake client object with no real cluster running at all -- the same dependency-injection discipline cloudaudit.py already uses for its AWS client",correct:!0,explanation:"This is exactly how test_kubernetes_inspect.py works -- every test hands in a hand-built fake client, never a real Kubernetes connection."},{text:"Because the kubernetes package does not allow a client to be built inside a function",correct:!1,explanation:"Nothing stops a client from being constructed inside a function -- k8sclient.get_kubernetes_clients() does exactly that. The reason for passing it as a parameter into the inspector is testability, not a library restriction."},{text:"Because AppsV1Api and CoreV1Api must be created in the same file as the CLI command",correct:!1,explanation:"There is no such requirement -- k8sclient.py builds the clients, cli.py calls that function, and kubernetes_inspect.py receives the result as parameters. Nothing forces them into one file."},{text:"Because a Kubernetes cluster only allows one client object per process",correct:!1,explanation:"A process can create as many client objects as it wants -- this is not a constraint the library enforces at all."}]},{prompt:"A Deployment has never had a ready replica. What does the Kubernetes API return for status.ready_replicas?",options:[{text:"None",correct:!0,explanation:"The API returns None, not 0, whenever a Deployment has never had any replica reach that state -- rollout_state() treats a None exactly like 0 so callers do not have to check for it themselves."},{text:"0",correct:!1,explanation:"The field is actually omitted / None from the raw API response in this case, not a literal 0 -- code that assumes 0 without checking will crash comparing None to an int."},{text:"An empty string",correct:!1,explanation:"ready_replicas is a numeric field -- it is never represented as a string, empty or otherwise."},{text:"The API raises an exception instead of returning a value",correct:!1,explanation:"Reading the Deployment succeeds normally -- the field itself is simply None, not an error condition."}]},{prompt:"Select every statement below that correctly describes rollout_state()'s four possible results (multiple correct answers).",multiSelect:!0,options:[{text:'"healthy" means ready_replicas and available_replicas both equal desired_replicas',correct:!0,explanation:"Every replica the Deployment wants is up and has stayed up -- the only scenario in this module's own lab where the CLI exits 0."},{text:'"unavailable" means no replica is ready at all, while replicas are still desired',correct:!0,explanation:"This is where the lab's CrashLoopBackOff, ImagePullBackOff and Pending scenarios all land -- ready_replicas is None or 0 but desired_replicas is greater than 0."},{text:'"scaled-to-zero" means the Deployment failed to start any of its desired replicas',correct:!1,explanation:'scaled-to-zero means desired_replicas itself is 0 -- nothing was ever asked to run, so 0 ready is expected, not a failure. A Deployment that wanted replicas but got none ready is "unavailable", a different state entirely.'},{text:'"degraded" means at least one replica is ready but fewer than desired',correct:!0,explanation:"Somewhere between healthy and unavailable -- a partial rollout or partial failure worth investigating but not a total outage."}]},{prompt:"In the lab's ImagePullBackOff scenario, the affected Pods showed phase Pending even though their container had a specific failure reason. What does this teach about reading Pod state?",options:[{text:"pod.status.phase answers only \"has this Pod's container ever started\" -- the container's own state/reason fields are what actually name the problem, and the two must both be read",correct:!0,explanation:"The lesson's tip callout makes this explicit: do not infer one from the other. A Pending phase alone does not tell you whether a Pod was never scheduled or was scheduled but can never pull its image."},{text:"The inspector has a bug -- a Pod stuck on ImagePullBackOff should always show phase Running",correct:!1,explanation:"This is real, correct Kubernetes behavior, not a bug -- phase and container state answer genuinely different questions, and Pending here is an honest report of what the API actually returned."},{text:"Pending always means the Pod was never assigned to a node",correct:!1,explanation:"A Pod stuck on ImagePullBackOff has already been scheduled to a node -- the scheduler placed it, but the container runtime cannot pull the image. Pending does not necessarily mean unscheduled."},{text:"Only the container state matters; phase should be ignored entirely",correct:!1,explanation:"The lesson says to read both, not to ignore either one -- phase and container state each carry information the other does not."}]},{prompt:"Why does list_warning_events() match events against the Deployment's currently-listed Pods (related_names), instead of matching any event whose name starts with the Deployment's name?",options:[{text:"Kubernetes keeps events for about an hour by default -- a name-prefix match would resurface a stale event from an already-fixed rollout, describing a problem that no longer exists",correct:!0,explanation:"This is exactly the scenario the lesson walks through: matching against currently-live Pod names keeps a fixed rollout's old events out of a fresh report."},{text:"Kubernetes does not allow filtering events by name prefix at all",correct:!1,explanation:"Prefix matching is technically possible -- the reason this module avoids it is correctness (avoiding stale events), not a technical limitation of the API."},{text:"Matching by related_names is faster than matching by prefix",correct:!1,explanation:"Performance is not the reason given in the lesson -- the reason is that a name-prefix match can surface events for objects that no longer exist."},{text:"The Kubernetes API only returns events for objects that still exist",correct:!1,explanation:"This is exactly backwards -- the API happily returns events for Pods that have already been deleted, which is precisely the stale-event problem this module's filtering avoids."}]},{prompt:"What is the difference between a ConfigException and an ApiException in this module's error handling?",options:[{text:"ConfigException means the client never reached the cluster at all (no kubeconfig, or a broken one); ApiException means the API server answered, but with a rejection like 404 or 403",correct:!0,explanation:"workload-inspect exits 2 for both, but they represent genuinely different failure points -- one before any request was sent, one after the server responded."},{text:"ConfigException is raised by kubectl; ApiException is raised only by the Python client library",correct:!1,explanation:"Both exceptions come from the Python kubernetes client library itself -- kubectl is a separate program and does not raise Python exceptions at all."},{text:"ApiException always means a bug in kubernetes_inspect.py's own code",correct:!1,explanation:"An ApiException (like a 404 for a missing Deployment, or a 403 from RBAC) is an expected, handled outcome -- catching it by name is exactly what keeps a real bug in this module's own code from being silently swallowed alongside it."},{text:"ConfigException only occurs when using --in-cluster",correct:!1,explanation:"A missing or malformed kubeconfig file triggers a ConfigException on the default (non-in-cluster) path too -- it is not specific to in-cluster authentication."}]},{prompt:"workload-inspect payments -n payments exits with code 1. What does that tell you, given this command's documented exit codes?",options:[{text:"The Deployment was found and inspected successfully, but its rollout_state is degraded or unavailable -- a real problem with the workload itself",correct:!0,explanation:"Exit 1 is reserved for a workload problem the command successfully diagnosed -- not a command-level failure (that is exit 2) and not a healthy rollout (that is exit 0)."},{text:"The kubeconfig could not be loaded",correct:!1,explanation:"That failure mode returns exit code 2, a command-level failure -- distinct from exit 1, which means the command ran fine and found a real problem in the workload."},{text:"The Deployment does not exist",correct:!1,explanation:"A missing Deployment produces an ApiException (404), which this command reports as exit code 2 -- a command-level failure, not exit 1."},{text:"The command crashed with an unhandled exception",correct:!1,explanation:"An unhandled crash is not part of this command's documented exit-code contract at all -- exit 1 specifically means a successful inspection that found a degraded or unavailable rollout."}]},{prompt:"The Deep Dive greps kubernetes_inspect.py for write-mode API calls using the pattern \\.(create|patch|delete|replace)_ -- with a leading dot. Why does the leading dot matter?",options:[{text:'Without it, the grep also matches the words "create_", "patch_", "delete_" and "replace_" inside the module\'s own docstring, which discusses those verbs in prose -- producing a false positive instead of a real proof',correct:!0,explanation:"The Deep Dive demonstrates this directly: a plain (non-anchored) grep finds matches in the docstring text itself, even though the module makes zero actual write-mode API calls."},{text:"The leading dot makes the grep run faster",correct:!1,explanation:"Performance is not the reason -- the dot anchors the pattern to an actual method call syntax (client.delete_...) rather than any occurrence of the bare word anywhere in the file."},{text:"Without the dot, grep would fail to run at all",correct:!1,explanation:"The non-anchored pattern is valid and does run -- it just produces a misleading result (a false positive) rather than an error."},{text:"The dot is required by Python's import syntax",correct:!1,explanation:"This is a shell grep command checking source text, not Python import syntax -- the dot in the regex represents a method-call dot (client.delete_x), unrelated to imports."}]},{prompt:"Select every statement below that the Deep Dive's live RBAC proof actually demonstrated (multiple correct answers).",multiSelect:!0,options:[{text:"kubectl auth can-i confirmed the payments-inspector ServiceAccount can list pods, deployments and events in the payments namespace",correct:!0,explanation:"This matches the Role's granted verbs (get/list/watch) on exactly those three object kinds."},{text:"A live delete_namespaced_deployment() call made directly through the Python client, using that same restricted identity, returned a real 403 Forbidden from the API server",correct:!0,explanation:"This is the second, independent proof -- not a can-i dry-run, but an actual attempted mutation that the API server itself refused."},{text:"The same restricted identity was also denied when listing pods in the default namespace",correct:!0,explanation:'The Role is scoped to the payments namespace only -- kubectl auth can-i list pods -n default returned "no" for this identity.'},{text:"The 403 Forbidden proves that kubernetes_inspect.py itself contains code that tries to delete Deployments",correct:!1,explanation:"The opposite is true -- the grep proof shows the module never calls a write method at all. The 403 was produced by a deliberate, separate test script calling delete directly, to prove the identity itself is blocked even if the code ever tried."}]},{prompt:"What actually distinguishes a Kubernetes controller from the workload-inspect script, according to the Deep Dive?",options:[{text:"A controller holds a watch connection open and reacts continuously and unattended to every change; workload-inspect makes one-shot GET/list calls, prints a report, and exits",correct:!0,explanation:"The Deep Dive captured this live: a watch stream delivering an ADDED event followed by MODIFIED events over one held-open connection, contrasted with this module's single request-response calls."},{text:"A controller uses a completely different, more privileged API than a script can access",correct:!1,explanation:"Both hit the identical Kubernetes API server -- a watch is a different kind of HTTP request (a long-held connection) against the same objects, not a separate or more powerful API."},{text:"A controller always runs inside the cluster; a script can only run on a laptop",correct:!1,explanation:"Where a process runs (in-cluster vs. on a laptop) is a separate question from whether it is a script or a controller -- this module's script could run in-cluster too, using --in-cluster auth, and would still be a one-shot script."},{text:"A controller is written in Go; a script must be written in Python",correct:!1,explanation:"The language a controller or script is written in is unrelated to this distinction -- it is about the request pattern (one-shot vs. continuous watch) and whether it runs unattended."}]},{prompt:"Why does the lesson say a controller with write access needs the guardrails Module 29 builds, while this module's read-only script does not?",options:[{text:"A controller runs unattended and reacts continuously with no human approving each action -- write access under those conditions needs approval, dry-run and audit; a script only ever runs because a person decided to run it, right now",correct:!0,explanation:"This is the Deep Dive's closing point: the operational difference (continuous and unattended vs. one-shot and human-triggered) is exactly what makes unattended write access risky enough to need guardrails."},{text:"Controllers are inherently less reliable than scripts",correct:!1,explanation:"Reliability is not the stated reason -- the concern is about unattended, continuous write access acting without a human in the loop each time, regardless of how reliable the code is."},{text:"Module 29's guardrails apply to every Kubernetes automation regardless of whether it reads or writes",correct:!1,explanation:"This module's workload-inspect, a read-only script, needs none of Module 29's approval/dry-run/audit guardrails -- those exist specifically because Module 29 introduces the first workload in this project that writes to a cluster."},{text:"A controller cannot make the same API calls a script makes",correct:!1,explanation:"The Deep Dive shows the opposite -- both hit the identical API server. The difference is the pattern of calls (continuous watch vs. one-shot) and who is in the loop, not which calls are technically possible."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,n){n.d(t,{A:()=>i});var a=n(4848),o=n(6540);function s({q:e,state:t,onToggle:n}){return(0,a.jsxs)("div",{className:"question_QpiT",children:[(0,a.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,a.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,a.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let s=t.selected.has(o),i="option_qGol";return t.checked?e.correct?i+=" correct_LYA5":s&&(i+=" incorrect_kRFc"):s&&(i+=" chosen_H3WG"),(0,a.jsxs)("li",{children:[(0,a.jsxs)("button",{type:"button",className:i,disabled:t.checked,onClick:()=>n(o),children:[(0,a.jsx)("span",{className:"marker_vN_w",children:s?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(s||e.correct)&&e.explanation&&(0,a.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function i({questions:e}){let[t,n]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,n)=>{var a,o;let s;return a=e[n],o=t.selected,s=a.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),a.multiSelect?o.size===s.length&&[...o].every(e=>a.options[e].correct):1===o.size&&s.includes([...o][0])}).length;return(0,a.jsxs)("div",{className:"quiz_I014",children:[e.map((o,i)=>(0,a.jsx)(s,{q:o,state:t[i],onToggle:t=>n(n=>n.map((n,a)=>{if(a!==i)return n;let o=new Set(n.selected);return e[i].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...n,selected:o}}))},i)),(0,a.jsxs)("div",{className:"actions_XUwt",children:[(0,a.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,a.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,a.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>i,x:()=>r});var a=n(6540);let o={},s=a.createContext(o);function i(e){let t=a.useContext(s);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:i(e.components),a.createElement(s.Provider,{value:t},e.children)}}}]);