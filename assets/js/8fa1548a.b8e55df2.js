"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["9446"],{103(e,t,a){a.r(t),a.d(t,{metadata:()=>i,default:()=>p,frontMatter:()=>r,contentTitle:()=>l,toc:()=>h,assets:()=>c});var i=JSON.parse('{"id":"m6-files-yaml-config/quiz","title":"Quiz: Files, YAML, JSON and Configuration","description":"<Quiz questions={[","source":"@site/docs/m6-files-yaml-config/quiz.mdx","sourceDirName":"m6-files-yaml-config","slug":"/m6-files-yaml-config/quiz","permalink":"/307-aipython-site/docs/m6-files-yaml-config/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Files, YAML, JSON and Configuration"},"sidebar":"courseSidebar","previous":{"title":"Lab: Files, YAML, JSON and Configuration","permalink":"/307-aipython-site/docs/m6-files-yaml-config/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m6-files-yaml-config/deep-dive"}}'),n=a(4848),o=a(8453),s=a(7517);let r={sidebar_position:3,title:"Quiz: Files, YAML, JSON and Configuration"},l="Files, YAML, JSON and Configuration -- Quiz",c={},h=[];function d(e){let t={h1:"h1",header:"header",...(0,o.R)(),...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.header,{children:(0,n.jsx)(t.h1,{id:"files-yaml-json-and-configuration----quiz",children:"Files, YAML, JSON and Configuration -- Quiz"})}),"\n",(0,n.jsx)(s.A,{questions:[{prompt:'Two scripts build the same path. One writes `base_dir + "/" + name + "/service.yaml"`. The other writes `Path(base_dir) / name / "service.yaml"`. What is the real advantage of the second form?',options:[{text:"It runs faster, because Path objects are compiled to machine code",correct:!1,explanation:"Speed is not the reason to prefer pathlib. A Path object is still a Python object with normal overhead -- the benefit is correctness, not performance."},{text:"It joins path parts using the separator the current OS actually expects, and exposes `.name`, `.suffix`, `.parent` and `.exists()` without you slicing strings",correct:!0,explanation:"This is the core reason the lesson opens with pathlib: a `Path` understands what a path *is*, so `/` joins correctly on any OS and common questions (does this exist, what is the extension) are one call instead of string surgery."},{text:"It automatically creates the file if it does not already exist",correct:!1,explanation:"Building a Path never creates anything on disk -- it is just a representation of a location. Nothing is created until you explicitly write to it."},{text:"It converts the file to whatever format the reader eventually parses it as",correct:!1,explanation:"A Path knows nothing about file contents or format -- it is purely about the location, not what is inside."}]},{prompt:"Why does this module insist on `yaml.safe_load()` and never plain `yaml.load()` for a config file?",options:[{text:"`yaml.load()` is deprecated and simply does not exist in recent PyYAML versions",correct:!1,explanation:"`yaml.load()` still exists (it takes a required `Loader` argument now). The concern is not that it is missing -- it is what it is capable of doing if you are not careful."},{text:"YAML supports tags that can tell a loader to construct arbitrary Python objects, not just plain data -- `safe_load` refuses anything beyond plain dicts, lists, strings and numbers",correct:!0,explanation:"This is exactly the reasoning from the lesson: a config file is data you did not write, and the YAML format itself allows more than data if the loader is permissive. `safe_load` is the loader that only ever builds plain values."},{text:"`safe_load()` is faster because it skips indentation checks",correct:!1,explanation:"Both loaders parse the same YAML syntax and indentation rules. The difference is what a loader is willing to construct from a tag, not parsing speed."},{text:"`safe_load()` automatically validates the data against a schema",correct:!1,explanation:"Neither loader validates anything -- they only turn YAML text into plain Python data. Validation is `ServiceDefinition.model_validate()`'s job, a separate step after loading."}]},{prompt:"In `platformops.config`, a service has `region: us-west-2` in `service.yaml`. The command runs with `PLATFORMOPS_REGION=eu-west-1` set. What ends up in the resolved config?",options:[{text:"`us-west-2` -- the file always wins over the environment",correct:!1,explanation:"This module's precedence rule is the opposite: an environment variable overrides the file, not the other way round."},{text:"`eu-west-1` -- the environment variable overrides the file value for this run only",correct:!0,explanation:"This is the three-layer rule from the lesson: environment variable beats file value. `service.yaml` on disk is unchanged; only the resolved output for this one command reflects the override."},{text:"Both values are kept, and the caller has to pick one",correct:!1,explanation:'`apply_env_overrides` resolves to a single dict with one value per field -- there is no "both" outcome for a single field.'},{text:"An error, because the file and the environment disagree",correct:!1,explanation:"A file/environment mismatch is the normal case this layering exists to handle, not an error condition -- the environment variable simply takes precedence."}]},{prompt:"Which statements about `apply_env_overrides` in this lab's Phase B design are true? (Select all that apply.)",multiSelect:!0,options:[{text:"It only ever overrides a fixed, named list of fields (`environment`, `region`, `team_owner`, `kubernetes_namespace`) -- never an arbitrary field an env var happens to name",correct:!0,explanation:"This is the deliberate scope limit from the spec: `ENV_OVERRIDE_FIELDS` is a fixed list, so an env var for a field nobody asked it to touch (say `PLATFORMOPS_AWS_ACCOUNT`) has no effect."},{text:"A field the file never set, and that has no matching environment variable either, is left out of the resolved data entirely",correct:!1,explanation:"The function only ever adds or overrides -- it never removes a key. A field present in the file stays present regardless of whether an env var exists for it."},{text:"It does not validate the overridden values -- `validate_service` still runs afterward and can reject a bad override the same way it rejects a bad file value",correct:!0,explanation:"This is why `PLATFORMOPS_REGION=eu-central-1` still fails: `apply_env_overrides` just substitutes a string, and the existing `Region` `Literal` constraint catches an invalid one downstream, the same as it would from the file."},{text:"It mutates the dict it was given in place, so the original file data is lost after the call",correct:!1,explanation:"It builds and returns a new dict (`resolved = dict(data)`) rather than mutating the caller's dict -- the original parsed file data is untouched."}]},{prompt:"Why does `write_resolved_config` write to a temporary file first and call `os.replace()`, instead of calling `out_path.write_text(...)` directly?",options:[{text:"`write_text()` is slower than writing through a temp file",correct:!1,explanation:"Performance is not the concern here -- both approaches write the same bytes. The difference is what happens if the process is interrupted partway through."},{text:"`write_text()` truncates the target file the instant it opens it; if the write is interrupted, the target is left corrupt or empty. `os.replace()` swaps a complete new file into place in one atomic step, so a reader always sees either the old file or the new one",correct:!0,explanation:"This is the power-cut argument from the lesson. Direct writes have a window where the file is neither the old nor the new content. The temp-file-then-rename pattern removes that window entirely."},{text:"The temp file lets multiple processes write to the same config file at the same time without conflict",correct:!1,explanation:"This pattern protects against a crash mid-write for one writer -- it does not by itself coordinate multiple concurrent writers, which is a different problem (file locking)."},{text:"It is required because `yaml.safe_dump()` cannot write directly to an open file handle",correct:!1,explanation:"`yaml.safe_dump()` writes to any open file handle fine, including the target file directly. The temp file is a deliberate safety choice, not a technical limitation of the YAML writer."}]},{prompt:"You review a coding agent's Phase B diff for this lab. Which of these should make you reject or ask for changes? (Select all that apply.)",multiSelect:!0,options:[{text:"`pyproject.toml` shows a new dependency added beyond the `pyyaml` line from step 1",correct:!0,explanation:"The spec explicitly says no new third-party dependencies -- `argparse`, `os`, `tempfile` and `json` are all standard library. A new dependency is scope creep, exactly what the review checklist exists to catch."},{text:"`write_resolved_config` calls `out_path.write_text(...)` directly instead of writing a temp file first",correct:!0,explanation:"This defeats the entire point of Phase B's atomic-write requirement -- it would reintroduce the exact corruption risk the lesson opens with."},{text:"The diff adds a new test for each of the six new behaviors the spec asked for",correct:!1,explanation:"This is exactly what you want to see -- one test per new behavior is the expected shape of a compliant diff, not a reason to reject it."},{text:"`--check` prints what it would write and also creates the resolved file",correct:!0,explanation:"The spec is explicit: `--check` reports what would happen and writes nothing. A `--check` run that writes a file has a real bug -- the dry run is not actually dry."}]},{prompt:"A `service.yaml` file is missing entirely -- the path just does not exist. What does `load_service_yaml` do in this module's design?",options:[{text:"It returns `None`, which the caller must remember to check for",correct:!1,explanation:'The function does not distinguish "file missing" from "success" by returning None -- it raises a specific exception instead, so the failure cannot be silently ignored.'},{text:"It raises `ConfigError` with a plain-English message naming the missing path, distinct from a data-validation failure",correct:!0,explanation:"This is the whole point of `ConfigError`: a missing or unparsable file is a different kind of problem from bad data inside a file that does exist, and the two are never confused with each other."},{text:"It returns the list of Pydantic validation errors, the same shape as a bad `environment` value would produce",correct:!1,explanation:"That shape (`list[dict]`) is reserved for data problems `validate_service` catches. A missing file never reaches validation at all -- `ConfigError` is raised before that."},{text:"It silently falls back to `service-bad.yaml` if that file exists in the same folder",correct:!1,explanation:"There is no fallback behavior in this design -- a missing file is reported as an error, not quietly substituted with a different file."}]},{prompt:"A report needs every service's name and owning team, one row per service, opened later in a spreadsheet. Which format from this lesson fits best, and why?",options:[{text:"YAML, because it supports comments explaining each row",correct:!1,explanation:"YAML is built for nested, hand-edited structure -- a flat list of rows for a spreadsheet is not what it is optimized for, and most spreadsheet tools do not open YAML directly."},{text:"CSV, because a spreadsheet opens it directly and `csv.DictWriter` handles a comma inside a value correctly, unlike hand-splitting on `,`",correct:!0,explanation:"This is exactly the CSV use case from the lesson: flat, tabular, one row per record, and a format spreadsheet tools understand natively -- with quoting handled for you."},{text:"JSON, because it is the fastest format for Python to parse",correct:!1,explanation:"Parse speed is not the deciding factor here. JSON is a fine general-purpose format, but it is not what a non-technical reviewer opens directly in a spreadsheet the way a CSV is."},{text:"Any format works identically well for this use case",correct:!1,explanation:"The formats are not interchangeable in practice -- the actual constraint here (a spreadsheet-openable, tabular report) points specifically at CSV."}]},{prompt:"What does `platformops.config service.yaml --check` do differently from a plain `platformops.config service.yaml`?",options:[{text:"It skips validating the file and only checks that the YAML syntax is well-formed",correct:!1,explanation:"Validation still runs in full with `--check` -- a bad `environment` value is still reported. What changes is only what happens after validation succeeds."},{text:"It runs the same load, override and validate steps, but reports what would be written to the resolved-config file instead of actually writing it",correct:!0,explanation:"This is the dry-run concept from the lab: everything up to and including validation happens for real; only the final write is skipped and replaced with a description of what would have happened."},{text:"It ignores any environment-variable overrides, showing only the raw file contents",correct:!1,explanation:"Environment overrides still apply under `--check` -- the resolved values shown (or that would be written) already reflect them, same as a normal run."},{text:"It requires `--json` to also be set, since `--check` has no plain-text output of its own",correct:!1,explanation:"`--check` and `--json` are independent flags. `--check` alone prints a plain-text summary; adding `--json` changes the output format, not whether the run is a dry run."}]},{prompt:"Why does this course tell you to never put a real API key or database password directly into `service.yaml`?",options:[{text:"YAML files cannot store long strings reliably",correct:!1,explanation:"YAML has no length limit that would matter for a typical secret -- this is not a format limitation."},{text:"The config file gets committed to git, copied into backups, and attached to support tickets -- once a secret is in git history it stays there even after a later commit removes the line",correct:!0,explanation:"This is the lesson's exact reasoning: a config file circulates far more widely than the person who wrote it expects, and git history in particular does not forget a deleted line -- the credential has to be rotated, not just removed."},{text:"Pydantic refuses to validate a model containing a field that looks like a secret",correct:!1,explanation:"Pydantic has no built-in secret detection in this model -- it validates `str` fields as `str` regardless of what the value looks like."},{text:"`yaml.safe_load()` strips out any value matching a known secret pattern automatically",correct:!1,explanation:"`safe_load` only controls what *kinds of objects* a loader can construct from YAML tags -- it does not inspect or filter the plain string values it loads."}]},{prompt:"What is the purpose of a committed `.env.example` file alongside a `.gitignore`d `.env`?",options:[{text:"It documents every environment variable a secret should come from, with no real value, so a new teammate knows exactly what to set without a real secret ever touching git",correct:!0,explanation:"This is the exact convention from the lesson: `.env.example` carries the keys (`DATABASE_PASSWORD=`) as documentation; the real values live only in the gitignored `.env`, never in the repository."},{text:"It is a backup copy of `.env` in case the real file is lost",correct:!1,explanation:"`.env.example` deliberately has no real values in it -- it cannot serve as a backup of actual secrets, only as a template of which keys exist."},{text:"It is read automatically by Python instead of `.env` when running tests",correct:!1,explanation:"Nothing in this module or the standard library treats `.env.example` specially at runtime -- it is a convention for humans, not a file Python auto-loads in place of `.env`."},{text:"It disables the need for environment-variable overrides entirely",correct:!1,explanation:"`.env.example` and environment-variable overrides are unrelated concepts here -- one documents which secrets should exist as env vars, the other is this lab's file-vs-env precedence layering."}]},{prompt:"A script calls `path.write_text(new_content)` directly on a config file that fifty other processes read continuously. The process gets killed by the OS exactly while `write_text` is running. What is the worst-case outcome for `service.yaml`?",options:[{text:"Nothing -- Python guarantees `write_text` either fully succeeds or leaves the original file untouched",correct:!1,explanation:"This guarantee is exactly what plain `write_text()` does *not* provide. That guarantee only holds for the temp-file-then-`os.replace()` pattern from section 6 of the lesson."},{text:"The file can be left truncated or containing only part of the new content -- because `write_text()` opens the file (truncating it) before it finishes writing the new bytes",correct:!0,explanation:"This is the opening scenario of the lesson: a direct write truncates immediately on open, so a kill signal partway through can leave a half-written or empty file for every one of those fifty readers to hit next."},{text:"The old file is preserved exactly, and the new content is discarded entirely",correct:!1,explanation:"This clean, all-or-nothing outcome is only guaranteed by an atomic swap (temp file + `os.replace()`) -- a direct `write_text()` call offers no such guarantee."},{text:"Python automatically retries the write until it succeeds",correct:!1,explanation:"There is no automatic retry behavior in `write_text()` or file writes in general -- an interrupted write simply stops wherever it was interrupted."}]}]})]})}function p(e={}){let{wrapper:t}={...(0,o.R)(),...e.components};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>s});var i=a(4848),n=a(6540);function o({q:e,state:t,onToggle:a}){return(0,i.jsxs)("div",{className:"question_QpiT",children:[(0,i.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,i.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,i.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,n)=>{let o=t.selected.has(n),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":o&&(s+=" incorrect_kRFc"):o&&(s+=" chosen_H3WG"),(0,i.jsxs)("li",{children:[(0,i.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>a(n),children:[(0,i.jsx)("span",{className:"marker_vN_w",children:o?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(o||e.correct)&&e.explanation&&(0,i.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},n)})})]})}function s({questions:e}){let[t,a]=(0,n.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,a)=>{var i,n;let o;return i=e[a],n=t.selected,o=i.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),i.multiSelect?n.size===o.length&&[...n].every(e=>i.options[e].correct):1===n.size&&o.includes([...n][0])}).length;return(0,i.jsxs)("div",{className:"quiz_I014",children:[e.map((n,s)=>(0,i.jsx)(o,{q:n,state:t[s],onToggle:t=>a(a=>a.map((a,i)=>{if(i!==s)return a;let n=new Set(a.selected);return e[s].multiSelect?n.has(t)?n.delete(t):n.add(t):(n.clear(),n.add(t)),{...a,selected:n}}))},s)),(0,i.jsxs)("div",{className:"actions_XUwt",children:[(0,i.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,i.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,i.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>s,x:()=>r});var i=a(6540);let n={},o=i.createContext(n);function s(e){let t=i.useContext(o);return i.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(n):e.components||n:s(e.components),i.createElement(o.Provider,{value:t},e.children)}}}]);