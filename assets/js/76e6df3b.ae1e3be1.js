"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["8757"],{2324(e,t,n){n.r(t),n.d(t,{metadata:()=>a,default:()=>u,frontMatter:()=>s,contentTitle:()=>c,toc:()=>l,assets:()=>h});var a=JSON.parse('{"id":"m10-concurrency/quiz","title":"Quiz: Concurrency for Operational Automation","description":"<Quiz questions={[","source":"@site/docs/m10-concurrency/quiz.mdx","sourceDirName":"m10-concurrency","slug":"/m10-concurrency/quiz","permalink":"/307-aipython-site/docs/m10-concurrency/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Concurrency for Operational Automation"},"sidebar":"courseSidebar","previous":{"title":"Lab: Concurrency for Operational Automation","permalink":"/307-aipython-site/docs/m10-concurrency/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m10-concurrency/deep-dive"}}'),o=n(4848),r=n(8453),i=n(7517);let s={sidebar_position:3,title:"Quiz: Concurrency for Operational Automation"},c="Concurrency for Operational Automation \u2014 Quiz",h={},l=[];function d(e){let t={h1:"h1",header:"header",...(0,r.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"concurrency-for-operational-automation--quiz",children:"Concurrency for Operational Automation \u2014 Quiz"})}),"\n",(0,o.jsx)(i.A,{questions:[{prompt:"Checking 50 endpoints sequentially takes roughly the sum of every individual check's latency. Why does overlapping the checks (threaded or async) cut that total time down, even though Python has a Global Interpreter Lock (GIL)?",options:[{text:"Almost all of `check_health()`'s time is spent waiting on a network reply, not holding the GIL, so other work can proceed while one check waits",correct:!0,explanation:"This is the module's central point: the GIL blocks two threads from running Python bytecode at the same instant, but a thread blocked on network I/O has already released it -- so other threads (or coroutines) get to run during that wait."},{text:"Python disables the GIL automatically whenever a script makes an HTTP request",correct:!1,explanation:"The GIL is not disabled for HTTP calls -- it is still in effect. The reason overlap works is that a blocked-on-I/O thread has released the GIL, not that the GIL turned off."},{text:"The GIL only applies to `asyncio` code, never to threads",correct:!1,explanation:"The GIL applies to all Python threads, `asyncio` or not. `check_many()`'s thread pool overlaps waits despite the GIL, for the same I/O-release reason `check_many_async()` does."},{text:"Each new thread gets its own separate GIL, so threads never contend with each other",correct:!1,explanation:"CPython has exactly one GIL per process, shared by every thread -- there is no per-thread GIL. Overlap comes from I/O release, not from multiple locks."}]},{prompt:"`check_many()` uses `ThreadPoolExecutor(max_workers=N)` and `check_many_async()` uses `asyncio.Semaphore(N)`. What do these two mechanisms have in common?",options:[{text:"Both cap how many requests are ever in flight at the same time, refusing to start one more until a slot frees up",correct:!0,explanation:"This is bounded concurrency, the module's core safety rule -- a thread pool will not start a new thread past `max_workers`, and a semaphore will not admit a new coroutine past its count, for exactly the same reason: unbounded concurrency looks like a self-inflicted denial of service against whatever you are checking."},{text:"Both open one operating-system thread per URL being checked",correct:!1,explanation:"Only the thread-pool path opens OS threads (bounded by `max_workers`). `asyncio.Semaphore` runs every coroutine inside one thread -- that difference is the whole reason the module presents both approaches."},{text:"Both retry a failed request automatically up to a fixed number of times",correct:!1,explanation:"Neither mechanism retries anything -- that is `_request_with_retry()`'s job from M9, unrelated to how many checks run concurrently."},{text:"Both require a coding agent to configure correctly",correct:!1,explanation:"Both are set with a plain integer argument you choose yourself -- no agent involvement is required to configure a concurrency cap."}]},{prompt:"A batch of 200 health checks runs with no concurrency cap at all -- every request fires at once. What is the realistic risk this module warns about?",options:[{text:"It can overwhelm a shared load balancer or trip a rate limiter, behaving like an unintentional denial-of-service against the very services being checked",correct:!0,explanation:'This is the module\'s "good citizen" rule -- 200 simultaneous connections landing on one switchboard looks like an attack, even though the intent was only "are you healthy?"'},{text:"Python raises a `RecursionError` once more than 100 requests are in flight",correct:!1,explanation:"There is no such built-in limit -- Python happily lets you fire many concurrent requests. The real-world risk is the load this creates on the endpoints, not an interpreter error."},{text:"Nothing -- unbounded concurrency is always safe as long as `timeout` is set on each request",correct:!1,explanation:"A per-request timeout does not fix unbounded concurrency -- 200 requests can still all fire at once and overload a shared resource, even if each one individually times out sanely."},{text:"The checks silently run one at a time regardless of how many are requested",correct:!1,explanation:"With no cap, checks run as concurrently as the underlying mechanism (threads or coroutines) allows -- the opposite of silently serializing them."}]},{prompt:"Why does `check_many()` use `executor.map(...)` instead of `[executor.submit(...) for url in urls]` collected with `as_completed()`?",options:[{text:"`executor.map()` returns results in the same order as the input list, regardless of which request finishes first; `as_completed()` returns them in completion order",correct:!0,explanation:"Preserving input order means a caller can zip results against the original URL list without re-sorting -- exactly the contract `check_many()` and `check_many_async()` both promise."},{text:"`executor.submit()` cannot be used with `check_health()` because it takes keyword arguments",correct:!1,explanation:"`executor.submit()` works fine with keyword arguments -- the reason `check_many()` avoids it here is about result ordering, not argument compatibility."},{text:"`as_completed()` does not exist in the standard library",correct:!1,explanation:"`concurrent.futures.as_completed()` is a real standard-library function -- it is simply not the one that preserves input order, which is why `check_many()` does not use it."},{text:"`executor.map()` is the only method that supports a `max_workers` limit",correct:!1,explanation:"`max_workers` is set on the `ThreadPoolExecutor` itself, at construction time -- it applies no matter which submission method (`.map()` or `.submit()`) you call afterward."}]},{prompt:"A per-request `timeout` and `check_many_async()`'s `batch_timeout` both involve giving up after some time passes. What is the actual difference between them?",multiSelect:!0,options:[{text:"`timeout` bounds how long ONE request is allowed to wait for its own response",correct:!0,explanation:"This is the same per-request budget M9 introduced -- it applies independently to every single check in a batch, so one slow endpoint fails on its own schedule."},{text:"`batch_timeout` bounds how long the WHOLE round of checks is allowed to run, cancelling anything still in flight once it passes",correct:!0,explanation:'This answers a different question than a per-request timeout: "how long am I willing to wait for this entire batch," even if every individual request would eventually have finished within its own timeout.'},{text:"A check cancelled by `batch_timeout` is dropped from the results entirely, as if it were never attempted",correct:!1,explanation:'The opposite is true and is a deliberate design choice: a cancelled check still returns a `HealthResult` with `error="cancelled: batch timeout exceeded"` -- never silently dropped.'},{text:"`timeout` and `batch_timeout` always have to be set to the exact same value",correct:!1,explanation:"They are independent parameters answering different questions -- there is no requirement that they match, and in practice `batch_timeout` is often set well below the sum of every individual `timeout`."}]},{prompt:"When `check_many_async()`'s `batch_timeout` deadline passes with some checks still running, what happens to those in-flight tasks?",options:[{text:"Each one is cancelled with `task.cancel()`, then awaited (via `asyncio.gather(..., return_exceptions=True)`) so the cancellation actually completes before the function returns",correct:!0,explanation:"Cancellation in `asyncio` is a request, not an instant switch -- the task needs a moment to actually unwind (for example, closing its share of the connection pool cleanly), which is why the code awaits the cancelled tasks instead of just calling `.cancel()` and moving on."},{text:"They keep running in the background indefinitely, even after `check_many_async()` has returned",correct:!1,explanation:"This is exactly the leak the explicit cancel-then-await step prevents -- without it, an in-flight task really could keep running unmonitored after the function returns, which is why the code awaits every cancelled task."},{text:"The whole batch raises an unhandled exception the moment one check does not finish in time",correct:!1,explanation:"Nothing raises here -- a batch timeout is treated the same way every other failure in this module is: a `HealthResult` recording what happened, not an exception that would take down the caller."},{text:"Only the first task past the deadline is cancelled; the rest are left to finish on their own",correct:!1,explanation:"Every task still in `pending` after the deadline is cancelled, not just the first -- `batch_timeout` is a deadline for the whole round of checks, not for one task at a time."}]},{prompt:"Fifty endpoints are checked in one batch: forty-seven answer healthy, three do not, each for a different reason. What does `check_many()` / `check_many_async()` do?",options:[{text:"It returns one `HealthResult` per URL -- forty-seven `ok=True` and three `ok=False`, each with its own reason -- in one pass, with no exception raised",correct:!0,explanation:"This follows the same rule `check_health()` set in M9: a failure is data, not an exception. Partial failure is the normal case for a batch, not something that aborts the rest of the checks."},{text:"It raises the first exception it encounters and stops checking the remaining endpoints",correct:!1,explanation:"This is exactly the failure mode the module avoids -- a batch check that dies on the first bad endpoint would hide the status of every endpoint checked after it."},{text:"It retries every failed check automatically until all fifty report healthy",correct:!1,explanation:"Neither `check_many()` nor `check_many_async()` retries anything on its own -- a failed check is reported as-is, once, in the results."},{text:"It drops the three unhealthy results and returns only the forty-seven healthy ones",correct:!1,explanation:"Dropping failures would hide exactly the information a batch health check exists to surface -- every URL gets a result, healthy or not."}]},{prompt:"Which of the following are genuine reasons to pick `--mode threaded` over `--mode async` for a typical operational script checking a few dozen to a few hundred endpoints? (Select all that apply.)",multiSelect:!0,options:[{text:"It works directly with the existing synchronous `check_health()`, with no `async`/`await` needed anywhere in the call chain",correct:!0,explanation:"The thread-pool path reuses `check_health()` as-is -- no function in the chain needs to become a coroutine, which keeps the code closer to what M9 already built."},{text:"At this scale, the difference in overhead between a thread and a coroutine is small enough not to matter in practice",correct:!0,explanation:"The lesson is explicit that the real gap between threads and `asyncio` opens up at hundreds-to-thousands of concurrent checks -- for a few dozen or a few hundred, the choice is mostly about which style is easier to read and maintain."},{text:"Threads are immune to the concurrency cap problem that `asyncio` code has to guard against",correct:!1,explanation:"Both approaches need an explicit cap -- `max_workers` for threads, `max_concurrency` for the semaphore. Neither one is safe by default with no cap set."},{text:"`ThreadPoolExecutor` requires no `max_workers` argument, so it always uses as many threads as URLs",correct:!1,explanation:'`max_workers` is a required design decision either way -- `check_many()` defaults it to `DEFAULT_MAX_CONCURRENCY`, not "one thread per URL."'}]},{prompt:"At what point does `asyncio` typically become worth its extra `async`/`await` complexity over a thread pool, according to this module?",options:[{text:"Once you are checking hundreds to thousands of endpoints, or already building on an async-first codebase, where a thread's per-request memory overhead starts to matter and a coroutine's does not",correct:!0,explanation:"This is the module's stated guidance -- for a handful of checks the difference is noise; the real advantage shows up at a scale where thread memory overhead becomes the bottleneck."},{text:"Immediately -- `asyncio` should always be used instead of threads, even for two or three checks",correct:!1,explanation:"The module explicitly recommends against reaching for `asyncio` by default -- for a small number of checks, sequential or threaded is simpler and the performance difference is negligible."},{text:"Only once every endpoint being checked is itself built with FastAPI",correct:!1,explanation:"The choice of `asyncio` on the client side has nothing to do with what framework the endpoints being checked happen to run -- `check_many_async()` can check any HTTP endpoint, sync or async server-side."},{text:"Never -- threads should always be preferred for HTTP health checks",correct:!1,explanation:'The module does not take an absolute "always threads" position -- it gives a scale-based guideline where `asyncio` becomes the better trade-off once concurrency needs grow large.'}]},{prompt:"On the `health-check-many` CLI command, what does passing `--batch-timeout` together with `--mode threaded` do?",options:[{text:'It fails immediately with a usage error (exit code 2) and the message "--batch-timeout only applies to --mode async", before any request is attempted',correct:!0,explanation:"`batch_timeout` only has meaning for the `asyncio` code path -- the command validates this combination up front and refuses it as a usage error, the same class of check `http-check` uses for a URL missing a scheme."},{text:"It silently ignores `--batch-timeout` and runs the threaded check normally",correct:!1,explanation:"Silently ignoring an option a user explicitly set would hide a mistake rather than surface it -- the command rejects the combination instead."},{text:"It converts the threaded run into an async run automatically",correct:!1,explanation:"The command does not auto-switch modes based on which options are present -- `--mode` controls execution strategy explicitly, and an incompatible combination is rejected, not silently reinterpreted."},{text:"It applies `--batch-timeout` as if it were the per-request `--timeout` instead",correct:!1,explanation:"`--batch-timeout` and `--timeout` are never merged or substituted for one another -- the command treats the unsupported combination as an error rather than reinterpreting one flag as another."}]},{prompt:"`health-check-many` exits `0` only if every endpoint answered healthy, and exits `1` if one or more failed. What still happens on that exit-`1` case?",options:[{text:"The command still prints every result -- healthy and unhealthy endpoints alike -- so a partial failure is visible in the output, not just implied by the exit code",correct:!0,explanation:'This mirrors `check_many()`\'s own partial-failure-aggregation rule at the CLI layer -- the exit code alone would only tell you "something failed," not what or how many, so every result is still printed.'},{text:"The command exits immediately without printing anything, the same way a usage error does",correct:!1,explanation:"A usage error (exit `2`) does stop before printing per-endpoint results, but exit `1` (a genuine health failure) still prints the full per-URL report -- the two exit codes mean different things and behave differently."},{text:"Only the unhealthy endpoints are printed; healthy ones are suppressed to keep the output short",correct:!1,explanation:'Every endpoint\'s result is printed, healthy or not -- suppressing the healthy ones would make it harder to tell "did this endpoint get checked at all" from "this endpoint failed."'},{text:"The command retries the whole batch once automatically before exiting",correct:!1,explanation:"Neither the CLI command nor the underlying `check_many()`/`check_many_async()` functions retry anything on their own -- a failure is reported once, immediately."}]},{prompt:"A teammate proposes opening a fresh `httpx.AsyncClient()` inside the loop body, once per URL, instead of the one shared client `check_many_async()` currently opens outside the loop. What is the main cost of that change?",options:[{text:"It throws away connection reuse across requests -- the main efficiency gain a shared client and its connection pool are meant to provide",correct:!0,explanation:"`check_health_async()` deliberately takes an already-open `client` parameter instead of creating its own, specifically so `check_many_async()` can share one `httpx.AsyncClient` (and its underlying connection pool) across every URL in a batch."},{text:"It would make the code fail to type-check, since `httpx.AsyncClient` cannot be constructed inside a loop",correct:!1,explanation:"There is nothing stopping `httpx.AsyncClient()` from being constructed inside a loop syntactically or at the type level -- the problem is purely the lost efficiency of connection reuse, not a type error."},{text:"It would violate the `max_concurrency` cap by allowing more requests than the semaphore permits",correct:!1,explanation:"The semaphore still gates how many coroutines run at once, regardless of how many client instances exist -- the concurrency cap is unaffected either way."},{text:"It has no real cost -- opening a client per request performs identically to sharing one",correct:!1,explanation:"Opening a fresh client per request means a fresh connection (and TLS handshake, for HTTPS) per request too, instead of reusing an already-open one -- that is a real, avoidable cost."}]},{prompt:"Why does `check_health_async()` catch `httpx.TimeoutException`, `httpx.ConnectError` and `httpx.HTTPError` and return a `HealthResult` with `error` set, instead of letting any of them propagate up through `check_many_async()`?",options:[{text:"So one endpoint's failure never crashes the coroutine checking it, which would otherwise be indistinguishable from a genuinely unhandled bug and could disrupt the rest of the batch",correct:!0,explanation:"This is the same never-raises contract `check_health()` established in M9, carried into the async version -- a network failure is an expected, reportable outcome, not an exceptional condition that should propagate."},{text:"`asyncio.gather()` and `asyncio.wait()` are physically incapable of running a coroutine that might raise an exception",correct:!1,explanation:"Both `asyncio.gather()` and `asyncio.wait()` can run coroutines that raise -- the reason to catch these exceptions here is the module's own design contract, not a limitation of those functions."},{text:"Because `httpx.AsyncClient` does not support try/except blocks inside async functions",correct:!1,explanation:"Ordinary try/except works fine inside `async def` functions -- there is no such restriction in Python, and `check_health_async()`'s own body demonstrates this directly."},{text:"Because only synchronous code is allowed to raise exceptions in Python",correct:!1,explanation:"Async code raises and handles exceptions exactly like synchronous code does -- the decision to catch these specific ones here is a deliberate design choice, not a language restriction."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,r.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,n){n.d(t,{A:()=>i});var a=n(4848),o=n(6540);function r({q:e,state:t,onToggle:n}){return(0,a.jsxs)("div",{className:"question_QpiT",children:[(0,a.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,a.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,a.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let r=t.selected.has(o),i="option_qGol";return t.checked?e.correct?i+=" correct_LYA5":r&&(i+=" incorrect_kRFc"):r&&(i+=" chosen_H3WG"),(0,a.jsxs)("li",{children:[(0,a.jsxs)("button",{type:"button",className:i,disabled:t.checked,onClick:()=>n(o),children:[(0,a.jsx)("span",{className:"marker_vN_w",children:r?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(r||e.correct)&&e.explanation&&(0,a.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function i({questions:e}){let[t,n]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),s=t.every(e=>e.checked),c=t.filter((t,n)=>{var a,o;let r;return a=e[n],o=t.selected,r=a.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),a.multiSelect?o.size===r.length&&[...o].every(e=>a.options[e].correct):1===o.size&&r.includes([...o][0])}).length;return(0,a.jsxs)("div",{className:"quiz_I014",children:[e.map((o,i)=>(0,a.jsx)(r,{q:o,state:t[i],onToggle:t=>n(n=>n.map((n,a)=>{if(a!==i)return n;let o=new Set(n.selected);return e[i].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...n,selected:o}}))},i)),(0,a.jsxs)("div",{className:"actions_XUwt",children:[(0,a.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>n(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,a.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>n(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),s&&(0,a.jsxs)("span",{className:"score_AN7p",children:["Score: ",c," / ",e.length]})]})]})}},8453(e,t,n){n.d(t,{R:()=>i,x:()=>s});var a=n(6540);let o={},r=a.createContext(o);function i(e){let t=a.useContext(r);return a.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function s(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:i(e.components),a.createElement(r.Provider,{value:t},e.children)}}}]);