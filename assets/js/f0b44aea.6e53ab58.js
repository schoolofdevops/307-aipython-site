"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["9"],{5770(e,n,t){t.r(n),t.d(n,{metadata:()=>s,default:()=>d,frontMatter:()=>i,contentTitle:()=>o,toc:()=>h,assets:()=>c});var s=JSON.parse('{"id":"m10-concurrency/deep-dive","title":"Deep Dive: What Actually Overlaps When You Run Checks Concurrently","description":"The lab built checkmany() and checkmany_async() and proved, by timing them, that concurrency","source":"@site/docs/m10-concurrency/deep-dive.md","sourceDirName":"m10-concurrency","slug":"/m10-concurrency/deep-dive","permalink":"/307-aipython-site/docs/m10-concurrency/deep-dive","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":4,"frontMatter":{"sidebar_position":4,"title":"Deep Dive: What Actually Overlaps When You Run Checks Concurrently","sidebar_label":"Deep Dive (Part 2)"},"sidebar":"courseSidebar","previous":{"title":"Quiz: Concurrency for Operational Automation","permalink":"/307-aipython-site/docs/m10-concurrency/quiz"},"next":{"title":"Lesson: System and Process Automation","permalink":"/307-aipython-site/docs/m11-system-process-automation/lesson"}}'),a=t(4848),r=t(8453);let i={sidebar_position:4,title:"Deep Dive: What Actually Overlaps When You Run Checks Concurrently",sidebar_label:"Deep Dive (Part 2)"},o="Deep Dive \u2014 What Actually Overlaps When You Run Checks Concurrently",c={},h=[{value:"1 -- The GIL blocks Python bytecode, not a network wait",id:"1----the-gil-blocks-python-bytecode-not-a-network-wait",level:2},{value:"2 -- What actually happens on <code>await</code>",id:"2----what-actually-happens-on-await",level:2},{value:"3 -- One shared connection pool behaves differently than eight separate clients",id:"3----one-shared-connection-pool-behaves-differently-than-eight-separate-clients",level:2},{value:"4 -- Progressive health checks and circuit breakers: what &quot;check everything&quot; grows into",id:"4----progressive-health-checks-and-circuit-breakers-what-check-everything-grows-into",level:2},{value:"Teardown",id:"teardown",level:2}];function l(e){let n={admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",header:"header",li:"li",mermaid:"mermaid",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,r.R)(),...e.components};return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(n.header,{children:(0,a.jsx)(n.h1,{id:"deep-dive--what-actually-overlaps-when-you-run-checks-concurrently",children:"Deep Dive \u2014 What Actually Overlaps When You Run Checks Concurrently"})}),"\n",(0,a.jsxs)(n.p,{children:["The lab built ",(0,a.jsx)(n.code,{children:"check_many()"})," and ",(0,a.jsx)(n.code,{children:"check_many_async()"})," and proved, by timing them, that concurrency\noverlaps the wait instead of queuing it. This page opens up the machinery underneath that result:\nwhat the GIL actually blocks and why a thread pool still speeds up I/O-bound work in CPython, what\nhappens inside the ",(0,a.jsx)(n.code,{children:"asyncio"})," event loop on every ",(0,a.jsx)(n.code,{children:"await"}),', what changes in a connection pool once\neight requests share it instead of one, and the fleet-checking patterns you reach for once "check\neverything at once" stops being good enough on its own. You will use this the day you are asked\nwhy a concurrent script did not get faster, why a shared client behaved unexpectedly under load, or\nwhy checking two hundred endpoints every thirty seconds during an incident is not actually the\nright design.']}),"\n",(0,a.jsxs)(n.admonition,{title:"Where this picks up",type:"info",children:[(0,a.jsxs)(n.p,{children:["You need ",(0,a.jsx)(n.code,{children:"~/platformops"})," at ",(0,a.jsx)(n.code,{children:"v0.8"})," from the lab, with ",(0,a.jsx)(n.code,{children:"check_many()"}),", ",(0,a.jsx)(n.code,{children:"check_health_async()"})," and\n",(0,a.jsx)(n.code,{children:"check_many_async()"})," in place. Every command below runs a short, disposable script in its own\nscratch folder -- none of them touch ",(0,a.jsx)(n.code,{children:"httpclient.py"}),", ",(0,a.jsx)(n.code,{children:"cli.py"})," or your project's git history."]}),(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"mkdir -p ~/platformops/dd-scratch && cd ~/platformops/dd-scratch\n"})}),(0,a.jsx)(n.p,{children:"Re-running any command on this page is safe."})]}),"\n",(0,a.jsx)(n.h2,{id:"1----the-gil-blocks-python-bytecode-not-a-network-wait",children:"1 -- The GIL blocks Python bytecode, not a network wait"}),"\n",(0,a.jsxs)(n.p,{children:["Picture a single-lane bridge with one toll booth: only one car crosses at a time, no matter how\nmany cars are queued on either side. That is the Global Interpreter Lock (GIL) -- only one thread\nin a CPython process may execute Python bytecode at any given instant, even on a machine with\nsixteen cores. The lesson already stated this; what it did not show is ",(0,a.jsx)(n.em,{children:"when"})," a thread gives up its\nplace at the booth. A thread blocked inside a C-level system call -- waiting on a socket for\n",(0,a.jsx)(n.code,{children:"httpx"})," to receive bytes back from a server -- has already stepped off the bridge. It released the\nGIL before it started waiting, specifically so another thread can cross while it does nothing but\nwait."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-python",metastring:'title="dd-scratch/gil_release_demo.py"',children:'import threading\nimport time\n\nN_THREADS = 4\nITERATIONS = 5_000_000\n\n\ndef cpu_bound_spin():\n    total = 0\n    for i in range(ITERATIONS):\n        total += i\n\n\ndef io_bound_wait():\n    time.sleep(0.5)\n\n\ndef time_it(target, label):\n    threads = [threading.Thread(target=target) for _ in range(N_THREADS)]\n    start = time.perf_counter()\n    for t in threads:\n        t.start()\n    for t in threads:\n        t.join()\n    elapsed = time.perf_counter() - start\n    return elapsed\n\n\nsingle_start = time.perf_counter()\ncpu_bound_spin()\nsingle_elapsed = time.perf_counter() - single_start\n\ncpu_elapsed = time_it(cpu_bound_spin, "cpu")\nio_elapsed = time_it(io_bound_wait, "io")\n\nprint(f"1 CPU-bound thread:  {single_elapsed:.2f}s")\nprint(f"{N_THREADS} CPU-bound threads: {cpu_elapsed:.2f}s  (GIL forces them to take turns)")\nprint(f"{N_THREADS} I/O-bound threads: {io_elapsed:.2f}s  (GIL released during the wait)")\n'})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"uv run python3 dd-scratch/gil_release_demo.py\n"})}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"1 CPU-bound thread:  0.13s\n4 CPU-bound threads: 0.53s  (GIL forces them to take turns)\n4 I/O-bound threads: 0.50s  (GIL released during the wait)\n"})}),"\n",(0,a.jsxs)(n.p,{children:["One thread counts to five million in about 0.13 seconds. Four threads doing the same work take\nabout four times as long (0.53s) -- the GIL forces them to take turns through the same\nsingle-lane bridge, so ",(0,a.jsx)(n.code,{children:"threading"})," buys nothing for CPU-bound work. Four threads each sleeping for\nhalf a second finish in about half a second total, because ",(0,a.jsx)(n.code,{children:"time.sleep()"})," (like a real socket read\ninside ",(0,a.jsx)(n.code,{children:"httpx"}),") releases the GIL for the duration of the wait, letting the other three threads run\nduring that same window. ",(0,a.jsx)(n.code,{children:"check_health()"}),"'s call to ",(0,a.jsx)(n.code,{children:"client.get(url)"})," spends almost all of its\ntime in exactly this released state -- which is the entire reason\n",(0,a.jsx)(n.code,{children:"ThreadPoolExecutor(max_workers=8)"})," in ",(0,a.jsx)(n.code,{children:"check_many()"})," produces a real speedup in CPython, despite\nthe GIL, for this specific kind of work."]}),"\n",(0,a.jsx)(n.mermaid,{value:'flowchart TD\n    subgraph CPU["CPU-bound: 4 threads, GIL held almost the whole time"]\n        direction LR\n        T1["thread 1\\nspin"] -.->|GIL handoff| T2["thread 2\\nspin"] -.->|GIL handoff| T3["thread 3\\nspin"] -.->|GIL handoff| T4["thread 4\\nspin"]\n    end\n    subgraph IO["I/O-bound: 4 threads, GIL released during the wait"]\n        direction LR\n        S1["thread 1\\nsocket read\\n(GIL released)"]\n        S2["thread 2\\nsocket read\\n(GIL released)"]\n        S3["thread 3\\nsocket read\\n(GIL released)"]\n        S4["thread 4\\nsocket read\\n(GIL released)"]\n        S1 & S2 & S3 & S4 --\x3e DONE(["all 4 done ~500ms later"])\n    end'}),"\n",(0,a.jsxs)(n.p,{children:["This is also why the module's guidance draws a hard line between I/O-bound and CPU-bound work:\n",(0,a.jsx)(n.code,{children:"ThreadPoolExecutor"})," speeds up ",(0,a.jsx)(n.code,{children:"check_health()"})," because the wait happens outside the GIL, but the\nexact same pool would give you no speedup at all wrapped around a CPU-heavy function -- parsing a\nhuge JSON payload, computing a checksum over a large file -- because that work never releases the\nGIL in the first place. ",(0,a.jsx)(n.code,{children:"multiprocessing"})," (separate processes, separate GILs each) is the tool for\nthat case, not ",(0,a.jsx)(n.code,{children:"ThreadPoolExecutor"}),"."]}),"\n",(0,a.jsxs)(n.h2,{id:"2----what-actually-happens-on-await",children:["2 -- What actually happens on ",(0,a.jsx)(n.code,{children:"await"})]}),"\n",(0,a.jsxs)(n.p,{children:['A single receptionist juggling three calls only works because the receptionist has a clear signal\nfor "this call is on hold, check the next line" -- otherwise they would just stare at a silent\nphone. ',(0,a.jsx)(n.code,{children:"await"})," is that signal for ",(0,a.jsx)(n.code,{children:"asyncio"}),". When ",(0,a.jsx)(n.code,{children:"check_health_async()"})," hits ",(0,a.jsx)(n.code,{children:"await client.get(url, timeout=timeout)"}),", the coroutine does not block the thread -- it suspends itself\nand hands control back to the ",(0,a.jsx)(n.strong,{children:"event loop"}),", a single loop running in the one thread that owns\nevery task in ",(0,a.jsx)(n.code,{children:"check_many_async()"}),". The event loop looks at every other task that is not currently\nsuspended waiting on I/O and runs whichever one is ready to make progress, until that task also\nhits an ",(0,a.jsx)(n.code,{children:"await"})," and suspends. When the operating system tells the event loop that URL A's socket\nhas data ready to read, the loop resumes exactly the coroutine that was waiting on it, picking up\nright after that ",(0,a.jsx)(n.code,{children:"await"})," line."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-python",metastring:'title="dd-scratch/event_loop_interleave.py"',children:'import asyncio\nimport time\n\n\nasync def labeled_wait(name: str, seconds: float) -> None:\n    print(f"{time.perf_counter():.3f} {name}: starting, about to await")\n    await asyncio.sleep(seconds)\n    print(f"{time.perf_counter():.3f} {name}: resumed after await")\n\n\nasync def main() -> None:\n    await asyncio.gather(\n        labeled_wait("A", 0.3),\n        labeled_wait("B", 0.1),\n        labeled_wait("C", 0.2),\n    )\n\n\nasyncio.run(main())\n'})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"uv run python3 dd-scratch/event_loop_interleave.py\n"})}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"1167104.894 A: starting, about to await\n1167104.894 B: starting, about to await\n1167104.894 C: starting, about to await\n1167104.994 B: resumed after await\n1167105.095 C: resumed after await\n1167105.195 A: resumed after await\n"})}),"\n",(0,a.jsx)(n.admonition,{title:"Your timestamps will differ",type:"info",children:(0,a.jsxs)(n.p,{children:["The exact numbers come from ",(0,a.jsx)(n.code,{children:"time.perf_counter()"}),' on the machine that ran this -- yours will be\ncompletely different. The important shape: all three "starting" lines print first, then the\n"resumed" lines appear in a different order (B, C, A), matching the shortest sleep first.']})}),"\n",(0,a.jsxs)(n.p,{children:['All three "starting" lines print first, back to back, before any "resumed" line -- each coroutine\nruns up to its own ',(0,a.jsx)(n.code,{children:"await"}),', suspends, and hands control back immediately, rather than running to\ncompletion one at a time. The "resumed" lines then print out of order, B first, then C, then A --\nin the order each one\'s wait actually finished, not the order they were started in. This is exactly\nthe mechanism ',(0,a.jsx)(n.code,{children:"check_many_async()"})," relies on: every ",(0,a.jsx)(n.code,{children:"check_health_async()"})," call reaches its ",(0,a.jsx)(n.code,{children:"await client.get(...)"})," almost immediately, all of them suspend together, and the event loop wakes each\none up independently as its own response arrives."]}),"\n",(0,a.jsx)(n.mermaid,{value:"sequenceDiagram\n    participant Loop as Event loop (1 thread)\n    participant A as Task A\n    participant B as Task B\n    participant C as Task C\n\n    Loop->>A: run until await\n    A--\x3e>Loop: suspended (waiting on socket)\n    Loop->>B: run until await\n    B--\x3e>Loop: suspended (waiting on socket)\n    Loop->>C: run until await\n    C--\x3e>Loop: suspended (waiting on socket)\n    Note over Loop: no task is ready -- loop polls the OS\n    Loop->>B: B's socket ready, resume\n    Loop->>C: C's socket ready, resume\n    Loop->>A: A's socket ready, resume"}),"\n",(0,a.jsxs)(n.p,{children:[(0,a.jsx)(n.code,{children:"asyncio.Semaphore(max_concurrency)"})," gates entry to this same suspend/resume cycle, not raw\nparallelism -- there is only ever one thread here. ",(0,a.jsx)(n.code,{children:"async with semaphore:"})," in ",(0,a.jsx)(n.code,{children:"check_many_async()"}),"'s\n",(0,a.jsx)(n.code,{children:"_bounded_check()"}),' suspends a coroutine at the semaphore itself if the cap is already full, exactly\nthe same "await, then get resumed later" mechanism, just resumed by another task calling\n',(0,a.jsx)(n.code,{children:"semaphore.release()"})," (implicitly, on exiting the ",(0,a.jsx)(n.code,{children:"async with"})," block) instead of by a socket\nbecoming readable. A ninth coroutine waiting on ",(0,a.jsx)(n.code,{children:"Semaphore(8)"})," is parked in the same event loop\nqueue as one waiting on a slow server -- neither one is spending a thread doing nothing, because\nthere is only the one thread to begin with."]}),"\n",(0,a.jsx)(n.h2,{id:"3----one-shared-connection-pool-behaves-differently-than-eight-separate-clients",children:"3 -- One shared connection pool behaves differently than eight separate clients"}),"\n",(0,a.jsxs)(n.p,{children:["A single shared taxi rank with eight cabs on call behaves very differently from eight commuters\neach owning a private car: the rank can reuse a cab the moment it drops someone off, while eight\nprivate cars each sit idle between trips, and each one had to be bought (a handshake) before it\ncould be used at all. ",(0,a.jsx)(n.code,{children:"check_many_async()"})," opens exactly one ",(0,a.jsx)(n.code,{children:"httpx.AsyncClient()"})," for the whole\nbatch and passes it into every ",(0,a.jsx)(n.code,{children:"check_health_async()"})," call -- every request in that batch shares\none connection pool, and a repeated request to the same host can reuse an already-open, already\nTLS-handshaked connection instead of paying for a new one."]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-python",metastring:'title="dd-scratch/shared_pool_demo.py"',children:'import time\nimport httpx\n\nN = 6\nURL = "https://example.com"\n\n\ndef shared_client() -> float:\n    start = time.perf_counter()\n    with httpx.Client() as client:\n        for _ in range(N):\n            client.get(URL, timeout=10.0)\n    return time.perf_counter() - start\n\n\ndef fresh_client_per_request() -> float:\n    start = time.perf_counter()\n    for _ in range(N):\n        with httpx.Client() as client:\n            client.get(URL, timeout=10.0)\n    return time.perf_counter() - start\n\n\nshared = shared_client()\nfresh = fresh_client_per_request()\nprint(f"one shared Client, {N} sequential requests: {shared:.2f}s")\nprint(f"fresh Client per request, {N} sequential requests: {fresh:.2f}s")\n'})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"uv run python3 dd-scratch/shared_pool_demo.py\n"})}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"one shared Client, 6 sequential requests: 0.46s\nfresh Client per request, 6 sequential requests: 0.89s\n"})}),"\n",(0,a.jsx)(n.admonition,{title:"Your exact times will differ",type:"info",children:(0,a.jsx)(n.p,{children:"Network conditions change the numbers each run. The important shape: the shared client is\nmeasurably faster than the fresh-client-per-request run, typically close to twice as fast,\nbecause it pays the TCP-plus-TLS handshake only once and reuses the open connection for every\nlater request to the same host."})}),"\n",(0,a.jsxs)(n.p,{children:["The shared client finishes roughly twice as fast -- the first request against a host pays the\nTCP-plus-TLS handshake, and every later request to that same host inside the shared pool reuses the\nconnection that handshake already opened. The fresh-client run pays that handshake cost six times\nover, once per throwaway client, even though every request targets the identical host. This is the\nconcrete reason ",(0,a.jsx)(n.code,{children:"check_many_async()"}),"'s spec insisted on \"one shared ",(0,a.jsx)(n.code,{children:"httpx.AsyncClient()"}),' across\nevery URL in the batch" instead of letting each call open its own -- opening a fresh client per URL\ninside a concurrency-bounded loop throws away exactly the efficiency the concurrency was meant to\nbuy.']}),"\n",(0,a.jsxs)(n.p,{children:[(0,a.jsx)(n.code,{children:"httpx.Client"})," is documented as thread-safe, so ",(0,a.jsx)(n.code,{children:"check_many()"}),"'s thread pool could share one client\nacross its eight threads the same way -- the lab's version does not do this (each ",(0,a.jsx)(n.code,{children:"check_health()"}),"\ncall opens its own short-lived ",(0,a.jsx)(n.code,{children:"httpx.Client()"}),"), which is a real, deliberate cost: every threaded\ncheck pays its own handshake, where a shared-client version would not. The trade-off is code\nsimplicity -- ",(0,a.jsx)(n.code,{children:"check_health()"})," stays a single-purpose, dependency-free function usable outside a\nbatch context, at the price of losing pool reuse when it is called inside one. DNS resolution\nfollows the same shared-vs-fresh pattern: a resolved hostname is cached by the OS resolver (and, for\nthe lifetime of a connection, by the connection pool itself) for a short window, so a shared client\nissuing many requests to the same host resolves DNS once and reuses that answer, where opening a\nfresh client per request re-resolves it every single time."]}),"\n",(0,a.jsx)(n.h2,{id:"4----progressive-health-checks-and-circuit-breakers-what-check-everything-grows-into",children:'4 -- Progressive health checks and circuit breakers: what "check everything" grows into'}),"\n",(0,a.jsxs)(n.p,{children:["Checking three hundred endpoints with ",(0,a.jsx)(n.code,{children:"max_concurrency=8"}),' every time is honest and bounded, but it\ntreats every endpoint as equally worth a full check on every pass -- which stops being true the\nmoment part of the fleet is degraded. Three patterns show up in real fleet-checking tools once the\nnaive "check everything, every time" version starts costing too much:']}),"\n",(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Progressive checks."})," Run a fast, cheap pass first (a lightweight ",(0,a.jsx)(n.code,{children:"HEAD"})," or a shallow\n",(0,a.jsx)(n.code,{children:"/healthz"}),") across the whole fleet, then only run a slower, deeper check (a full dependency\ncheck, a synthetic transaction) against the endpoints the fast pass flagged. This is the same\nidea as triage in an incident -- you do not run a full diagnostic on every service, only on the\nones the quick pass already said are suspect."]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Circuit breakers."}),' Once an endpoint has failed enough consecutive checks, stop sending it new\nrequests for a cooldown window and report it as "known bad" from cache instead. This protects two\nthings at once: it stops a batch of checks from repeatedly hammering a service that is already\nknown to be down, and it stops that one dead endpoint\'s timeout from eating a slot in your\nconcurrency cap on every single pass.']}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Exponential fan-out."})," Rather than always checking the full fleet at a fixed concurrency, widen\nthe cap when everything is healthy (cheap to check fast) and narrow it automatically when\nfailures start appearing (avoid adding load to a system that is already struggling) -- the\nconcurrency cap becomes a dial the checker itself adjusts, not a constant."]}),"\n"]}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-python",metastring:'title="dd-scratch/circuit_breaker_sketch.py"',children:'from dataclasses import dataclass, field\n\n\n@dataclass\nclass CircuitState:\n    consecutive_failures: int = 0\n    open_until: float = 0.0  # a perf_counter() timestamp; 0 means "closed"\n\n\nFAILURE_THRESHOLD = 3\nCOOLDOWN_SECONDS = 30.0\n\n\ndef record_result(state: CircuitState, ok: bool, now: float) -> None:\n    if ok:\n        state.consecutive_failures = 0\n        state.open_until = 0.0\n        return\n    state.consecutive_failures += 1\n    if state.consecutive_failures >= FAILURE_THRESHOLD:\n        state.open_until = now + COOLDOWN_SECONDS\n\n\ndef should_skip(state: CircuitState, now: float) -> bool:\n    # "Open" means the breaker has tripped -- skip real checks until the cooldown passes.\n    return now < state.open_until\n\n\nbreakers: dict[str, CircuitState] = {}\n\n\ndef circuit_for(url: str) -> CircuitState:\n    return breakers.setdefault(url, CircuitState())\n'})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"uv run python3 -c \"\nfrom importlib import util\nspec = util.spec_from_file_location('cb', 'dd-scratch/circuit_breaker_sketch.py')\ncb = util.module_from_spec(spec)\nspec.loader.exec_module(cb)\n\nstate = cb.circuit_for('https://flaky.example')\nnow = 1000.0\nfor i in range(3):\n    cb.record_result(state, ok=False, now=now)\n    print(f'after failure {i + 1}: consecutive={state.consecutive_failures}, skip={cb.should_skip(state, now)}')\nprint(f'skip 10s later: {cb.should_skip(state, now + 10)}')\nprint(f'skip 31s later: {cb.should_skip(state, now + 31)}')\n\"\n"})}),"\n",(0,a.jsx)(n.p,{children:(0,a.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-text",children:"after failure 1: consecutive=1, skip=False\nafter failure 2: consecutive=2, skip=False\nafter failure 3: consecutive=3, skip=True\nskip 10s later: True\nskip 31s later: False\n"})}),"\n",(0,a.jsxs)(n.p,{children:["Three consecutive failures trip the breaker (",(0,a.jsx)(n.code,{children:"should_skip"})," flips to ",(0,a.jsx)(n.code,{children:"True"}),"); ten seconds into the\nthirty-second cooldown it is still tripped; past thirty-one seconds it has reset. This sketch is\nintentionally not wired into ",(0,a.jsx)(n.code,{children:"check_many_async()"})," -- it is the shape you would layer on top of it\nonce a fleet checker runs continuously (a monitoring loop, not a one-shot CLI command) and starts\nspending most of its concurrency budget re-checking endpoints everyone already knows are down."]}),"\n",(0,a.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,a.jsx)(n.pre,{children:(0,a.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -rf dd-scratch\n"})}),"\n",(0,a.jsxs)(n.p,{children:["This removes only the scratch folder this page created. Your real ",(0,a.jsx)(n.code,{children:"httpclient.py"}),", ",(0,a.jsx)(n.code,{children:"cli.py"})," and\nthe rest of ",(0,a.jsx)(n.code,{children:"~/platformops"})," are untouched."]}),"\n",(0,a.jsx)(n.admonition,{title:"Where you will use this",type:"tip",children:(0,a.jsxs)(n.ul,{children:["\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"The GIL blocks Python bytecode, not a thread that is parked in a socket read."})," ",(0,a.jsx)(n.strong,{children:"Use it\nwhen:"})," deciding whether to reach for ",(0,a.jsx)(n.code,{children:"ThreadPoolExecutor"})," or ",(0,a.jsx)(n.code,{children:"multiprocessing"})," for a new batch\njob -- a thread pool speeds up work that spends its time waiting on I/O; it does nothing for a\nCPU-heavy loop that never releases the GIL."]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsxs)(n.strong,{children:[(0,a.jsx)(n.code,{children:"await"})," suspends a coroutine and hands the single thread to whichever other task is ready to\nrun; nothing runs in true parallel inside one event loop."]})," ",(0,a.jsx)(n.strong,{children:"Use it when:"})," debugging why an\n",(0,a.jsx)(n.code,{children:"asyncio"})," script feels stuck -- a coroutine that never hits an ",(0,a.jsx)(n.code,{children:"await"})," (a long synchronous call\nslipped into async code) blocks the entire event loop, not just itself."]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsxs)(n.strong,{children:["A shared ",(0,a.jsx)(n.code,{children:"httpx.Client"}),"/",(0,a.jsx)(n.code,{children:"httpx.AsyncClient"})," reuses TCP and TLS handshakes across requests to\nthe same host; a fresh client per request pays that cost every time."]})," ",(0,a.jsx)(n.strong,{children:"Use it when:"})," writing\nor reviewing any batch or fan-out HTTP code -- confirm one client object is built once and passed\ndown, not constructed inside the loop that calls it."]}),"\n",(0,a.jsxs)(n.li,{children:[(0,a.jsx)(n.strong,{children:"Progressive checks and circuit breakers exist because checking every endpoint the same way,\nevery time, wastes capacity on endpoints already known to be down."})," ",(0,a.jsx)(n.strong,{children:"Use it when:"})," a health\nchecker is asked to run continuously rather than once -- before widening ",(0,a.jsx)(n.code,{children:"max_concurrency"}),"\nfurther, check whether the real fix is skipping known-bad endpoints instead of checking them\nfaster."]}),"\n"]})})]})}function d(e={}){let{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,a.jsx)(n,{...e,children:(0,a.jsx)(l,{...e})}):l(e)}},8453(e,n,t){t.d(n,{R:()=>i,x:()=>o});var s=t(6540);let a={},r=s.createContext(a);function i(e){let n=s.useContext(r);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function o(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(a):e.components||a:i(e.components),s.createElement(r.Provider,{value:n},e.children)}}}]);