"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["6452"],{7646(e,n,t){t.r(n),t.d(n,{metadata:()=>r,default:()=>p,frontMatter:()=>o,contentTitle:()=>i,toc:()=>c,assets:()=>l});var r=JSON.parse('{"id":"m30-observability-automation/lab","title":"Lab: Observability Automation","description":"This lab has five phases. Phase 1 adds three new read-only observability endpoints to","source":"@site/docs/m30-observability-automation/lab.md","sourceDirName":"m30-observability-automation","slug":"/m30-observability-automation/lab","permalink":"/307-aipython-site/docs/m30-observability-automation/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Observability Automation"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Observability Automation","permalink":"/307-aipython-site/docs/m30-observability-automation/lesson"},"next":{"title":"Quiz: Observability Automation","permalink":"/307-aipython-site/docs/m30-observability-automation/quiz"}}'),s=t(4848),a=t(8453);let o={sidebar_position:2,title:"Lab: Observability Automation"},i="Lab \u2014 Build PlatformOps v2.5.0: Observability Automation",l={},c=[{value:"Phase 0 -- Read-only, and fully offline on purpose",id:"phase-0----read-only-and-fully-offline-on-purpose",level:2},{value:"Why fixtures, not a real observability backend",id:"why-fixtures-not-a-real-observability-backend",level:3},{value:"Phase 1 -- Three new observability endpoints",id:"phase-1----three-new-observability-endpoints",level:2},{value:"Step 1 -- Let <code>_request_with_retry()</code> accept query parameters",id:"step-1----let-_request_with_retry-accept-query-parameters",level:3},{value:"Step 2 -- Add the three endpoints to <code>httpclient.py</code>",id:"step-2----add-the-three-endpoints-to-httpclientpy",level:3},{value:"Step 3 -- Add the fixture bodies",id:"step-3----add-the-fixture-bodies",level:3},{value:"Step 4 -- Add tests for the new endpoints, and run them",id:"step-4----add-tests-for-the-new-endpoints-and-run-them",level:3},{value:"Phase 2 -- <code>telemetry.py</code>: structured logs, traces, and one correlation ID",id:"phase-2----telemetrypy-structured-logs-traces-and-one-correlation-id",level:2},{value:"Step 1 -- Write the module",id:"step-1----write-the-module",level:3},{value:"Step 2 -- Add <code>opentelemetry-api</code> and <code>opentelemetry-sdk</code> as dependencies",id:"step-2----add-opentelemetry-api-and-opentelemetry-sdk-as-dependencies",level:3},{value:"Step 3 -- Write <code>tests/test_telemetry.py</code>, and run it",id:"step-3----write-teststest_telemetrypy-and-run-it",level:3},{value:"Step 4 -- See it work: one correlation ID, across a log line and a trace span",id:"step-4----see-it-work-one-correlation-id-across-a-log-line-and-a-trace-span",level:3},{value:"Phase 3 -- <code>observability.py</code>: fetch evidence, then judge it separately",id:"phase-3----observabilitypy-fetch-evidence-then-judge-it-separately",level:2},{value:"Step 1 -- Write the module",id:"step-1----write-the-module-1",level:3},{value:"Step 2 -- Write <code>tests/test_observability.py</code>, and run it",id:"step-2----write-teststest_observabilitypy-and-run-it",level:3},{value:"Step 3 -- See it work at full scale: one correlation ID, across three fetches and a trace span",id:"step-3----see-it-work-at-full-scale-one-correlation-id-across-three-fetches-and-a-trace-span",level:3},{value:"Phase 4 -- Wire the CLI, then run it against three real scenarios",id:"phase-4----wire-the-cli-then-run-it-against-three-real-scenarios",level:2},{value:"Step 1 -- Add the <code>observability-inspect</code> command",id:"step-1----add-the-observability-inspect-command",level:3},{value:"Step 2 -- Run it live: a full pass",id:"step-2----run-it-live-a-full-pass",level:3},{value:"Step 3 -- Run it live: missing telemetry, honestly reported",id:"step-3----run-it-live-missing-telemetry-honestly-reported",level:3},{value:"Step 4 -- Run it live: an unreachable source",id:"step-4----run-it-live-an-unreachable-source",level:3},{value:"Phase 5 -- Full quality gate and version bump",id:"phase-5----full-quality-gate-and-version-bump",level:2},{value:"Teardown",id:"teardown",level:2}];function d(e){let n={a:"a",admonition:"admonition",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,s.jsxs)(s.Fragment,{children:[(0,s.jsx)(n.header,{children:(0,s.jsx)(n.h1,{id:"lab--build-platformops-v250-observability-automation",children:"Lab \u2014 Build PlatformOps v2.5.0: Observability Automation"})}),"\n",(0,s.jsxs)(n.p,{children:["This lab has five phases. Phase 1 adds three new read-only observability endpoints to\n",(0,s.jsx)(n.code,{children:"src/platformops/httpclient.py"}),". Phase 2 builds ",(0,s.jsx)(n.code,{children:"src/platformops/telemetry.py"})," -- structured JSON\nlogging, OpenTelemetry tracing, and the correlation ID that ties them together. Phase 3 builds\n",(0,s.jsx)(n.code,{children:"src/platformops/observability.py"})," -- the fetch/aggregate split that turns three evidence sources\ninto one honest snapshot. Phase 4 wires the ",(0,s.jsx)(n.code,{children:"observability-inspect"})," CLI command and runs it live\nagainst deterministic fixtures, across three scenarios: a full pass, missing telemetry, and an\nunreachable source. Phase 5 runs this project's full quality gate and bumps the version to\n",(0,s.jsx)(n.code,{children:"v2.5.0"})," -- the Observable Automation release."]}),"\n",(0,s.jsxs)(n.admonition,{title:"Before you start",type:"info",children:[(0,s.jsxs)(n.p,{children:["You need ",(0,s.jsx)(n.code,{children:"~/platformops"})," at ",(0,s.jsx)(n.code,{children:"v2.4.0"}),", with ",(0,s.jsx)(n.code,{children:"src/platformops/kubernetes_restart.py"})," in place from\nModule 29, and the labs repo cloned at ",(0,s.jsx)(n.code,{children:"~/307-aipython-labs"}),"."]}),(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"platformops 2.4.0\n"})}),(0,s.jsxs)(n.p,{children:["If this already shows ",(0,s.jsx)(n.code,{children:"2.5.0"}),", you have already completed this lab -- skip ahead to\n",(0,s.jsx)(n.a,{href:"#phase-5----full-quality-gate-and-version-bump",children:"Phase 5"}),". Every ",(0,s.jsx)(n.code,{children:"cat >>"})," step in this lab is guarded\nso it is safe to repeat even from an already-completed state."]}),(0,s.jsxs)(n.p,{children:["This module does not touch AWS, Floci or a Kubernetes cluster at all. You do not need Floci or\n",(0,s.jsx)(n.code,{children:"kind"})," running, and you do not need ",(0,s.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," set for anything in this lab. It also does\nnot need a real Prometheus, Loki or Alertmanager -- every observability call in this lab runs\nagainst fixtures."]})]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"phase-0----read-only-and-fully-offline-on-purpose",children:"Phase 0 -- Read-only, and fully offline on purpose"}),"\n",(0,s.jsx)(n.p,{children:"Before writing any code: this lab never writes a metric, never deletes a log line, and never\nacknowledges or silences an alert. Every function you write here either reads from an observability\nbackend or works on data already sitting in memory -- the same read-only-composition discipline\nModule 24's cloud audit and Module 26's release checker already used, applied here to metrics, logs\nand alerts instead of AWS resources or GitHub state."}),"\n",(0,s.jsx)(n.h3,{id:"why-fixtures-not-a-real-observability-backend",children:"Why fixtures, not a real observability backend"}),"\n",(0,s.jsxs)(n.p,{children:["Check ",(0,s.jsx)(n.code,{children:"course.config.json"}),"'s lab-tools list: this course's laptop lab has no metrics backend, no\nlog-search backend and no alerting system running. Standing one up is a real, valuable skill on its\nown -- and it is out of scope for an 8 GB laptop lab meant to run several other tools already."]}),"\n",(0,s.jsxs)(n.p,{children:["So this lab builds against ",(0,s.jsx)(n.strong,{children:"deterministic JSON fixtures"})," instead -- recorded response bodies that\nfollow the exact shape a real backend returns (Prometheus's own query API for metrics, Alertmanager's\nown v2 API for alerts, a small shape this project defines itself for log search), replayed through\n",(0,s.jsx)(n.code,{children:"respx"}),", the same library ",(0,s.jsx)(n.code,{children:"tests/test_httpclient.py"})," has used since Module 9. The functions you write\nin this lab are the same functions that would call a real backend in production -- only the transport\nunderneath them is swapped out for the lab."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"phase-1----three-new-observability-endpoints",children:"Phase 1 -- Three new observability endpoints"}),"\n",(0,s.jsxs)(n.h3,{id:"step-1----let-_request_with_retry-accept-query-parameters",children:["Step 1 -- Let ",(0,s.jsx)(n.code,{children:"_request_with_retry()"})," accept query parameters"]}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.code,{children:"query_metrics()"}),", ",(0,s.jsx)(n.code,{children:"search_logs()"})," and ",(0,s.jsx)(n.code,{children:"list_alerts()"})," all need to send query parameters (a PromQL\nstring, a log-search query, an alert filter) -- something none of this project's GitHub endpoints\never needed, since every GitHub list endpoint embedded its parameters directly in the URL string.\nThis step extends ",(0,s.jsx)(n.code,{children:"_request_with_retry()"}),"'s signature with an optional ",(0,s.jsx)(n.code,{children:"params"})," dict, passed straight\nthrough to ",(0,s.jsx)(n.code,{children:"httpx"})," so it handles the URL-encoding."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\ngrep -q 'params: dict\\[str, Any\\] | None = None' src/platformops/httpclient.py || uv run python -c \"\nimport pathlib\n\npath = pathlib.Path('src/platformops/httpclient.py')\ntext = path.read_text()\n\nold_signature = '''def _request_with_retry(\n    client: httpx.Client, method: str, url: str, *, max_retries: int | None = None\n) -> httpx.Response:'''\nnew_signature = '''def _request_with_retry(\n    client: httpx.Client,\n    method: str,\n    url: str,\n    *,\n    params: dict[str, Any] | None = None,\n    max_retries: int | None = None,\n) -> httpx.Response:'''\nassert text.count(old_signature) == 1, 'signature not found -- already patched?'\ntext = text.replace(old_signature, new_signature, 1)\n\nold_call = 'response = client.request(method, url)'\nnew_call = 'response = client.request(method, url, params=params)'\nassert text.count(old_call) == 1, 'call site not found -- already patched?'\ntext = text.replace(old_call, new_call, 1)\n\npath.write_text(text)\nprint('patched _request_with_retry to accept params')\n\"\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"patched _request_with_retry to accept params\n"})}),"\n",(0,s.jsxs)(n.p,{children:["If you re-run this step after the patch already landed, ",(0,s.jsx)(n.code,{children:"grep -q"})," finds it and the whole ",(0,s.jsx)(n.code,{children:"python -c"}),"\nblock is skipped -- nothing changes."]}),"\n",(0,s.jsxs)(n.h3,{id:"step-2----add-the-three-endpoints-to-httpclientpy",children:["Step 2 -- Add the three endpoints to ",(0,s.jsx)(n.code,{children:"httpclient.py"})]}),"\n",(0,s.jsxs)(n.p,{children:["Append this to the end of ",(0,s.jsx)(n.code,{children:"src/platformops/httpclient.py"}),". It follows the exact\nretry/validate-with-Pydantic shape ",(0,s.jsx)(n.code,{children:"get_pull_request()"})," and ",(0,s.jsx)(n.code,{children:"list_deployments()"})," already use from\nModule 26 -- the only real difference is that ",(0,s.jsx)(n.code,{children:"base_url"})," is a parameter here instead of the\nmodule-level ",(0,s.jsx)(n.code,{children:"GITHUB_API"})," constant, because an observability backend's address is always local to\nwhoever runs it."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'grep -q "^def query_metrics" src/platformops/httpclient.py || cat >> src/platformops/httpclient.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# Observability endpoints (Module 30) -- three more read-only calls,\n# following the exact same shape every earlier endpoint in this module\n# established: build the request, go through `_request_with_retry()`, raise\n# `_raise_for_status()`, validate the body against a Pydantic model before\n# it leaves this module. Unlike GITHUB_API, there is no one fixed public\n# host here -- `base_url` is explicit on every function, because an\n# observability backend\'s address is always local to whoever is running it.\n# `query_metrics()` follows Prometheus\'s own HTTP query API response\n# envelope -- the format Prometheus itself, and anything that speaks its\n# query API (Thanos, Cortex, Mimir), actually returns. `search_logs()` uses\n# a small envelope this project defines itself (`query`/`total`/`logs`)\n# rather than pinning to one vendor\'s proprietary schema -- Loki,\n# Elasticsearch and Datadog each shape a log search differently; a real\n# integration sits a small adapter in front of whichever one it talks to.\n# `list_alerts()` follows Prometheus Alertmanager\'s v2 API shape, a bare\n# JSON array of alert objects. `platformops.observability` is the module\n# that turns what these three functions return into one snapshot; nothing\n# here decides whether a result is "healthy" -- it only reports what the\n# backend said, including an honestly empty result.\n# ---------------------------------------------------------------------------\n\n\nclass MetricSample(BaseModel):\n    """One instant-vector sample: a metric\'s label set plus its `[unix_timestamp, string_value]` pair.\n\n    `value` stays a two-element tuple exactly as Prometheus\'s wire format\n    sends it -- the value itself is a JSON string, not a number, because\n    Prometheus\'s own API represents it that way to avoid floating-point\n    precision loss in transit. This model does not convert it; the caller\n    decides whether and how to parse it.\n    """\n\n    metric: dict[str, str]\n    value: tuple[float, str]\n\n\nclass MetricQueryData(BaseModel):\n    """The `data` object inside a Prometheus-shaped query response."""\n\n    result_type: str = Field(alias="resultType")\n    result: list[MetricSample]\n\n\nclass MetricQueryResponse(BaseModel):\n    """The full envelope Prometheus\'s `/api/v1/query` (and anything that speaks its query API) returns."""\n\n    status: str\n    data: MetricQueryData\n\n\ndef query_metrics(\n    base_url: str,\n    query: str,\n    *,\n    time: float | None = None,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """Run one instant query against `GET {base_url}/api/v1/query`, Prometheus\'s own query API shape.\n\n    An empty `result` list is a real, known answer -- "no series matched\n    this query right now" -- not a failure. `platformops.observability`\n    is the layer that turns that into an honest "no data found" report\n    instead of treating it the same as an unreachable backend.\n    """\n    url = f"{base_url}/api/v1/query"\n    params: dict[str, Any] = {"query": query}\n    if time is not None:\n        params["time"] = time\n    logger.debug("querying metrics at %s: %s", base_url, query)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        parsed = MetricQueryResponse.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info(\n        "fetched %d metric sample(s) for query %r", len(parsed.data.result), query\n    )\n    return [sample.model_dump() for sample in parsed.data.result]\n\n\nclass LogEntry(BaseModel):\n    """One log line: a timestamp, a level, the service that emitted it, and its message.\n\n    `correlation_id` and `trace_id` are optional -- a log-search backend\n    happily returns log lines that predate this project ever attaching\n    either one to anything it emits. When present, `correlation_id` is\n    exactly the identifier `platformops.telemetry.traced_operation()`\n    tags a log line and a trace span with, which is what lets a search\n    like `correlation_id:req-...` return every line one run of this\n    project\'s own automation wrote.\n    """\n\n    timestamp: str\n    level: str\n    service: str\n    message: str\n    correlation_id: str | None = None\n    trace_id: str | None = None\n\n\nclass LogSearchResponse(BaseModel):\n    """The envelope `search_logs()` expects back: the query that ran, a total, and the matched lines."""\n\n    query: str\n    total: int\n    logs: list[LogEntry]\n\n\ndef search_logs(\n    base_url: str,\n    query: str,\n    *,\n    limit: int = 100,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """Search log lines from `GET {base_url}/api/v1/logs/search`.\n\n    Zero matching lines is a real, known answer this function returns as\n    an empty list, not an error -- a query for a correlation ID that never\n    logged anything (because the run failed before logging a single line,\n    or the ID was mistyped) is exactly the "missing telemetry" case\n    `platformops.observability` has to report honestly rather than\n    silently.\n    """\n    url = f"{base_url}/api/v1/logs/search"\n    params = {"query": query, "limit": limit}\n    logger.debug("searching logs at %s: %s", base_url, query)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        parsed = LogSearchResponse.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched %d log line(s) for query %r", len(parsed.logs), query)\n    return [entry.model_dump() for entry in parsed.logs]\n\n\nclass AlertStatus(BaseModel):\n    """An alert\'s current lifecycle state -- `"active"`, `"suppressed"` or `"unprocessed"`."""\n\n    state: str\n\n\nclass Alert(BaseModel):\n    """One alert record, following Alertmanager\'s own v2 API shape.\n\n    `labels` always carries `alertname`; this project\'s fixtures also set\n    `service` and `severity` on it, the two labels\n    `platformops.observability` filters and reports on. `annotations` is\n    free-form, human-readable context (a `summary`, typically) the alerting\n    rule author attached -- this project reads it, it never writes one.\n    """\n\n    fingerprint: str\n    status: AlertStatus\n    labels: dict[str, str]\n    annotations: dict[str, str] = {}\n    starts_at: str = Field(alias="startsAt")\n\n\ndef list_alerts(\n    base_url: str,\n    *,\n    service: str | None = None,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """List alerts from `GET {base_url}/api/v2/alerts`, the same bare-array shape Alertmanager\'s v2 API returns.\n\n    An empty list is a real, known answer -- nothing is currently firing\n    for this service -- and is never treated as a failure. This function\n    never acknowledges, silences or resolves an alert; it only reads what\n    the alerting backend currently reports.\n    """\n    url = f"{base_url}/api/v2/alerts"\n    params: dict[str, Any] = {}\n    if service is not None:\n        params["filter"] = f\'service="{service}"\'\n    logger.debug("fetching alerts at %s (service=%s)", base_url, service)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    if not isinstance(payload, list):\n        raise ResponseFormatError(f"{url} response is not a JSON array")\n\n    try:\n        alerts = [Alert.model_validate(raw) for raw in payload]\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched %d alert(s) from %s", len(alerts), base_url)\n    return [alert.model_dump() for alert in alerts]\nPYEOF\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.code,{children:"httpclient.py"})," also needs ",(0,s.jsx)(n.code,{children:"Field"})," added to its ",(0,s.jsx)(n.code,{children:"pydantic"})," import (",(0,s.jsx)(n.code,{children:"from pydantic import BaseModel, Field, ValidationError"}),") -- ",(0,s.jsx)(n.code,{children:"MetricQueryData.result_type"})," and ",(0,s.jsx)(n.code,{children:"Alert.starts_at"})," both read a camelCase\nfield (",(0,s.jsx)(n.code,{children:"resultType"}),", ",(0,s.jsx)(n.code,{children:"startsAt"}),") from the real API shape through a snake_case Python name, which is\nexactly what ",(0,s.jsx)(n.code,{children:"Field(alias=...)"})," is for."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"grep -q \"from pydantic import BaseModel, Field, ValidationError\" src/platformops/httpclient.py || \\\n  sed -i.bak 's/from pydantic import BaseModel, ValidationError/from pydantic import BaseModel, Field, ValidationError/' src/platformops/httpclient.py && rm -f src/platformops/httpclient.py.bak\n"})}),"\n",(0,s.jsx)(n.p,{children:"No output from either command when they succeed -- both write silently. The next step confirms the\nfile is valid."}),"\n",(0,s.jsx)(n.h3,{id:"step-3----add-the-fixture-bodies",children:"Step 3 -- Add the fixture bodies"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'mkdir -p tests/fixtures/observability\n\ncat > tests/fixtures/observability/metrics_query_success.json << \'PYEOF\'\n{\n  "status": "success",\n  "data": {\n    "resultType": "vector",\n    "result": [\n      {\n        "metric": { "__name__": "http_requests_total", "service": "payments", "status": "5xx" },\n        "value": [1722900000, "42"]\n      },\n      {\n        "metric": { "__name__": "http_requests_total", "service": "payments", "status": "2xx" },\n        "value": [1722900000, "1985"]\n      }\n    ]\n  }\n}\nPYEOF\n\ncat > tests/fixtures/observability/metrics_query_empty.json << \'PYEOF\'\n{\n  "status": "success",\n  "data": {\n    "resultType": "vector",\n    "result": []\n  }\n}\nPYEOF\n\ncat > tests/fixtures/observability/logs_search_success.json << \'PYEOF\'\n{\n  "query": "service:payments AND correlation_id:req-a1b2c3d4e5f6",\n  "total": 2,\n  "logs": [\n    {\n      "timestamp": "2026-08-01T09:00:01Z",\n      "level": "INFO",\n      "service": "payments",\n      "correlation_id": "req-a1b2c3d4e5f6",\n      "message": "received checkout request",\n      "trace_id": "f2b4f8e212a5367e9652705c8a22731c"\n    },\n    {\n      "timestamp": "2026-08-01T09:00:02Z",\n      "level": "ERROR",\n      "service": "payments",\n      "correlation_id": "req-a1b2c3d4e5f6",\n      "message": "payment gateway timeout after 3 retries",\n      "trace_id": "f2b4f8e212a5367e9652705c8a22731c"\n    }\n  ]\n}\nPYEOF\n\ncat > tests/fixtures/observability/logs_search_empty.json << \'PYEOF\'\n{\n  "query": "service:payments AND correlation_id:req-000000000000",\n  "total": 0,\n  "logs": []\n}\nPYEOF\n\ncat > tests/fixtures/observability/alerts_active.json << \'PYEOF\'\n[\n  {\n    "fingerprint": "a1b2c3d4e5f6a7b8",\n    "status": { "state": "active" },\n    "labels": { "alertname": "HighErrorRate", "service": "payments", "severity": "critical" },\n    "annotations": { "summary": "5xx rate above 2% for the last 5 minutes" },\n    "startsAt": "2026-08-01T08:55:00Z"\n  }\n]\nPYEOF\n\ncat > tests/fixtures/observability/alerts_none.json << \'PYEOF\'\n[]\nPYEOF\n\nls tests/fixtures/observability/\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"alerts_active.json\nalerts_none.json\nlogs_search_empty.json\nlogs_search_success.json\nmetrics_query_empty.json\nmetrics_query_success.json\n"})}),"\n",(0,s.jsx)(n.h3,{id:"step-4----add-tests-for-the-new-endpoints-and-run-them",children:"Step 4 -- Add tests for the new endpoints, and run them"}),"\n",(0,s.jsxs)(n.p,{children:["First, update the import block: ",(0,s.jsx)(n.code,{children:"test_httpclient.py"})," needs ",(0,s.jsx)(n.code,{children:"list_alerts"}),", ",(0,s.jsx)(n.code,{children:"query_metrics"})," and\n",(0,s.jsx)(n.code,{children:"search_logs"})," added to the existing ",(0,s.jsx)(n.code,{children:"from platformops.httpclient import (...)"})," block, an\n",(0,s.jsx)(n.code,{children:'OBS_FIXTURES = Path(__file__).parent / "fixtures" / "observability"'})," constant, and an\n",(0,s.jsx)(n.code,{children:"_obs_fixture(name)"})," helper next to the existing ",(0,s.jsx)(n.code,{children:"_fixture()"})," helper -- the same shape Module 26's\n",(0,s.jsx)(n.code,{children:"FIXTURES"}),"/",(0,s.jsx)(n.code,{children:"_fixture()"})," pair already established. The tests in the next step need all of this to\nexist first, so this patch runs before them:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"grep -q '^def _obs_fixture' tests/test_httpclient.py || uv run python -c \"\nimport pathlib\n\npath = pathlib.Path('tests/test_httpclient.py')\ntext = path.read_text()\n\nold_block = '''    get_latest_release,\n    get_pull_request,\n    get_repo_info,\n    list_artifacts,\n    list_check_runs,\n    list_deployments,\n    list_workflow_runs,\n    summarize_health_results,\n)\n\nFIXTURES = Path(__file__).parent / \\\"fixtures\\\" / \\\"github\\\"\n\n\ndef _fixture(name: str) -> dict:\n    return json.loads((FIXTURES / name).read_text())'''\nnew_block = '''    get_latest_release,\n    get_pull_request,\n    get_repo_info,\n    list_alerts,\n    list_artifacts,\n    list_check_runs,\n    list_deployments,\n    list_workflow_runs,\n    query_metrics,\n    search_logs,\n    summarize_health_results,\n)\n\nFIXTURES = Path(__file__).parent / \\\"fixtures\\\" / \\\"github\\\"\nOBS_FIXTURES = Path(__file__).parent / \\\"fixtures\\\" / \\\"observability\\\"\n\n\ndef _fixture(name: str) -> dict:\n    return json.loads((FIXTURES / name).read_text())\n\n\ndef _obs_fixture(name: str) -> dict:\n    return json.loads((OBS_FIXTURES / name).read_text())'''\nassert text.count(old_block) == 1, 'import block not found -- already patched?'\ntext = text.replace(old_block, new_block, 1)\npath.write_text(text)\nprint('patched test_httpclient.py imports and added _obs_fixture()')\n\"\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"patched test_httpclient.py imports and added _obs_fixture()\n"})}),"\n",(0,s.jsxs)(n.p,{children:["If you re-run this after the patch already landed, ",(0,s.jsx)(n.code,{children:"grep -q"})," finds ",(0,s.jsx)(n.code,{children:"_obs_fixture"})," already defined\nand the whole ",(0,s.jsx)(n.code,{children:"python -c"})," block is skipped."]}),"\n",(0,s.jsx)(n.p,{children:"Now append the five tests that use it:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat >> tests/test_httpclient.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# query_metrics, search_logs, list_alerts -- Module 30\'s three new\n# observability endpoints.\n# ---------------------------------------------------------------------------\n\nOBS_BASE_URL = "http://localhost:9090"\nLOGS_BASE_URL = "http://localhost:3100"\nALERTS_BASE_URL = "http://localhost:9093"\n\n\n@respx.mock\ndef test_query_metrics_returns_the_matched_samples():\n    respx.get(f"{OBS_BASE_URL}/api/v1/query").mock(\n        return_value=httpx.Response(\n            200, json=_obs_fixture("metrics_query_success.json")\n        )\n    )\n\n    samples = query_metrics(OBS_BASE_URL, \'http_requests_total{service="payments"}\')\n\n    assert len(samples) == 2\n    assert samples[0]["metric"]["service"] == "payments"\n    assert samples[0]["value"] == (1722900000, "42")\n\n\n@respx.mock\ndef test_query_metrics_returns_an_empty_list_when_no_series_match():\n    respx.get(f"{OBS_BASE_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_obs_fixture("metrics_query_empty.json"))\n    )\n\n    assert query_metrics(OBS_BASE_URL, \'http_requests_total{service="unknown"}\') == []\n\n\n@respx.mock\ndef test_search_logs_returns_matching_entries():\n    respx.get(f"{LOGS_BASE_URL}/api/v1/logs/search").mock(\n        return_value=httpx.Response(200, json=_obs_fixture("logs_search_success.json"))\n    )\n\n    logs = search_logs(\n        LOGS_BASE_URL, "service:payments AND correlation_id:req-a1b2c3d4e5f6"\n    )\n\n    assert len(logs) == 2\n    assert logs[1]["level"] == "ERROR"\n\n\n@respx.mock\ndef test_list_alerts_returns_active_alerts():\n    respx.get(f"{ALERTS_BASE_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_obs_fixture("alerts_active.json"))\n    )\n\n    alerts = list_alerts(ALERTS_BASE_URL, service="payments")\n\n    assert len(alerts) == 1\n    assert alerts[0]["status"]["state"] == "active"\n\n\n@respx.mock\ndef test_query_metrics_is_unreachable_after_retries_are_exhausted(monkeypatch):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    respx.get(f"{OBS_BASE_URL}/api/v1/query").mock(\n        side_effect=httpx.ConnectError("connection refused")\n    )\n\n    with pytest.raises(EndpointUnreachableError):\n        query_metrics(OBS_BASE_URL, "up")\nPYEOF\n'})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_httpclient.py -q\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:".................................................                        [100%]\n49 passed in 1.16s\n"})}),"\n",(0,s.jsx)(n.admonition,{title:"This is a shortened version",type:"info",children:(0,s.jsxs)(n.p,{children:["This step showed five representative tests, added to the 44 this file already had -- the full\n",(0,s.jsx)(n.code,{children:"tests/test_httpclient.py"})," you get in the checkpoint also covers ",(0,s.jsx)(n.code,{children:"search_logs()"}),"'s empty-result case,\n",(0,s.jsx)(n.code,{children:"list_alerts()"}),'\'s empty-result case, and a malformed-response case for each endpoint (53 tests in\ntotal) -- the same "found" / "empty" / "malformed" trio every earlier endpoint in this file already\ntests. Extend the tests above the same way. Once this module ships, the finished file also becomes\npart of this project\'s own checkpoint history, at\n',(0,s.jsx)(n.code,{children:"~/307-aipython-labs/labs/checkpoints/v2.5.0/tests/test_httpclient.py"}),", the same place every earlier\nmodule's finished files live -- useful to compare against later, not required to finish this step now."]})}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsxs)(n.h2,{id:"phase-2----telemetrypy-structured-logs-traces-and-one-correlation-id",children:["Phase 2 -- ",(0,s.jsx)(n.code,{children:"telemetry.py"}),": structured logs, traces, and one correlation ID"]}),"\n",(0,s.jsx)(n.h3,{id:"step-1----write-the-module",children:"Step 1 -- Write the module"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/telemetry.py << \'PYEOF\'\n"""platformops.telemetry -- structured logs and OpenTelemetry traces for this project\'s own automation.\n\nEvery module up to this one produces evidence *about* something else -- a\nservice definition, a pull request, a Kubernetes workload. None of them\nanswer a different question: when this toolkit itself misbehaves in\nproduction -- a `release-check` run that took nine minutes instead of nine\nseconds, a `restart-execute` run that a teammate swears they never\ntriggered -- how do you reconstruct what actually happened, across every log\nline and every network call that one run made? "Add a print statement and\nrun it again" does not work once the run already happened once, unfinished,\nsomewhere you cannot reproduce on demand.\n\nTwo signals, one identifier, ties them together. `configure_json_logging()`\nmakes every log line in this process one JSON object a log-search backend\ncan index. `configure_tracing()` makes every instrumented operation one\nOpenTelemetry span a trace backend can visualize. `traced_operation()` is\nthe one function that touches both at once: it opens a span, and for the\nspan\'s whole duration it makes the same correlation ID appear on every log\nline written underneath it, via a `contextvars.ContextVar` a plain function\nargument could not reach across nested calls without being threaded through\nevery signature. Grep one correlation ID in this project\'s own log output\nand every line in the story shows up together -- an agent or a script doing\nthat grep, not a human paging through a dashboard, is the actual point of\nthis module: correlated telemetry is data automation can act on, not just\ndata a person can look at.\n\nThis module never sends a span or a log line anywhere but the exporter it\nis explicitly given. The CLI defaults to `ConsoleSpanExporter` (prints\nspans to stdout) precisely because this course\'s lab environment has no\nOpenTelemetry Collector, no Jaeger, no Tempo -- see `course.config.json`\'s\n`lab.tools` list. Tests hand `traced_operation()` a tracer built from an\n`InMemorySpanExporter` instead, the same swap-the-transport-not-the-logic\npattern `platformops.httpclient.check_health()`\'s `transport` parameter\nalready uses for HTTP.\n"""\n\nfrom __future__ import annotations\n\nimport contextvars\nimport json\nimport logging\nimport uuid\nfrom collections.abc import Iterator, Mapping\nfrom contextlib import contextmanager\nfrom typing import Any\n\nfrom opentelemetry import trace\nfrom opentelemetry.sdk.resources import Resource\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import (\n    ConsoleSpanExporter,\n    SimpleSpanProcessor,\n    SpanExporter,\n)\nfrom opentelemetry.trace import Span, Tracer\n\nlogger = logging.getLogger("platformops.telemetry")\n\nSERVICE_NAME = "platformops"\n\n# The one place this run\'s correlation ID actually lives. A ContextVar,\n# not a plain module-level variable, because it has to survive nested\n# function calls (gather_metrics_evidence() called from\n# inspect_observability(), several stack frames deep) without every one of\n# those functions taking a correlation_id parameter just to pass it along --\n# and because a ContextVar, unlike a plain global, keeps concurrent asyncio\n# tasks or threads from bleeding their correlation IDs into each other.\n_correlation_id_var: contextvars.ContextVar[str | None] = contextvars.ContextVar(\n    "correlation_id", default=None\n)\n\n\ndef new_correlation_id() -> str:\n    """A short, greppable run identifier -- `req-` plus 12 hex characters.\n\n    Twelve hex characters (48 bits of randomness from `uuid4`) is\n    deliberately shorter than a full UUID -- long enough that two runs\n    colliding by chance never happens in this project\'s lifetime, short\n    enough that a person can actually read one aloud or paste it into a\n    search box without it wrapping across two lines of a terminal.\n    """\n    return f"req-{uuid.uuid4().hex[:12]}"\n\n\nclass _CorrelationIdFilter(logging.Filter):\n    """Attaches the current correlation ID (or None) to every log record it sees.\n\n    A `logging.Filter` runs on every record a handler processes, whether or\n    not that record\'s code even knows this module exists -- which is the\n    point: a log line written deep inside `platformops.httpclient` picks up\n    the correlation ID of whichever `traced_operation()` block is currently\n    open, with no import and no `extra=` argument required at that call\n    site.\n    """\n\n    def filter(self, record: logging.LogRecord) -> bool:\n        record.correlation_id = _correlation_id_var.get()\n        return True\n\n\nclass _JsonFormatter(logging.Formatter):\n    """Renders one log record as one line of JSON -- the shape a log-search backend can index.\n\n    `platformops.observability.search_logs()`\'s fixtures use exactly this\n    shape (`timestamp`/`level`/`message`/`correlation_id`) for the same\n    reason: a real log-search backend built on structured JSON lines reads\n    fields, not a human-formatted sentence it would have to re-parse.\n    """\n\n    def format(self, record: logging.LogRecord) -> str:\n        payload: dict[str, Any] = {\n            "timestamp": self.formatTime(record, "%Y-%m-%dT%H:%M:%S%z"),\n            "level": record.levelname,\n            "logger": record.name,\n            "message": record.getMessage(),\n            "correlation_id": getattr(record, "correlation_id", None),\n        }\n        if record.exc_info:\n            payload["exc_info"] = self.formatException(record.exc_info)\n        return json.dumps(payload)\n\n\ndef configure_json_logging(level: int = logging.INFO) -> logging.StreamHandler[Any]:\n    """Route every log record in this process through one JSON-line formatter.\n\n    Replaces the root logger\'s handlers outright rather than adding to\n    them -- safe to call more than once in the same process (the test\n    suite, and a CLI command that runs after `platformops`\'s own\n    `logging.basicConfig(force=True)` in `cli.py`\'s callback, both call\n    this), the same call-more-than-once safety that `force=True` already\n    gives the plain-text logging path.\n    """\n    handler: logging.StreamHandler[Any] = logging.StreamHandler()\n    handler.setFormatter(_JsonFormatter())\n    handler.addFilter(_CorrelationIdFilter())\n\n    root = logging.getLogger()\n    root.handlers = [handler]\n    root.setLevel(level)\n    return handler\n\n\ndef configure_tracing(\n    service_name: str = SERVICE_NAME, *, exporter: SpanExporter | None = None\n) -> TracerProvider:\n    """Install a process-wide `TracerProvider`, exporting to `exporter` (a `ConsoleSpanExporter` by default).\n\n    OpenTelemetry\'s global tracer provider can only be installed once per\n    process -- a second `trace.set_tracer_provider()` call is silently\n    ignored (it only logs a warning), which is easy to trip over the first\n    time a test suite invokes this project\'s CLI more than once in the same\n    pytest session. This function checks whether a real `TracerProvider` is\n    already installed and, if so, returns the existing one instead of\n    trying (and quietly failing) to replace it -- see the Deep Dive for\n    this working, and failing, both proven live. Tests that need an\n    isolated exporter per test should build their own `TracerProvider` and\n    pass a tracer from it straight to `traced_operation()` instead of\n    calling this function at all -- exactly what `tests/test_telemetry.py`\n    and `tests/test_observability.py` do.\n    """\n    current = trace.get_tracer_provider()\n    if isinstance(current, TracerProvider):\n        logger.debug(\n            "tracing already configured for this process -- reusing the existing TracerProvider"\n        )\n        return current\n\n    provider = TracerProvider(resource=Resource.create({"service.name": service_name}))\n    provider.add_span_processor(SimpleSpanProcessor(exporter or ConsoleSpanExporter()))\n    trace.set_tracer_provider(provider)\n    logger.debug("tracing configured for service %s", service_name)\n    return provider\n\n\n@contextmanager\ndef traced_operation(\n    name: str,\n    *,\n    correlation_id: str,\n    tracer: Tracer | None = None,\n    attributes: Mapping[str, Any] | None = None,\n) -> Iterator[Span]:\n    """Open one span named `name`, and make `correlation_id` appear on both that span and every log line inside it.\n\n    This is the module\'s central mechanism, and the reason it exists at\n    all: for the duration of this `with` block, `correlation_id` becomes a\n    span attribute a trace backend can filter on, AND\n    `_correlation_id_var` makes that exact same value appear on every\n    structured log line `_JsonFormatter` renders anywhere in the call\n    stack underneath it -- one identifier, greppable across both signals,\n    without threading it through every function\'s argument list by hand.\n\n    `tracer` defaults to the process-wide tracer `configure_tracing()`\n    installs; pass an explicit one (from a `TracerProvider` built just for\n    a test) to keep that test\'s spans out of the global registry entirely.\n    """\n    resolved_tracer = tracer or trace.get_tracer(SERVICE_NAME)\n    token = _correlation_id_var.set(correlation_id)\n    try:\n        with resolved_tracer.start_as_current_span(name) as span:\n            span.set_attribute("correlation_id", correlation_id)\n            for key, value in (attributes or {}).items():\n                span.set_attribute(key, value)\n            yield span\n    finally:\n        _correlation_id_var.reset(token)\nPYEOF\n'})}),"\n",(0,s.jsxs)(n.h3,{id:"step-2----add-opentelemetry-api-and-opentelemetry-sdk-as-dependencies",children:["Step 2 -- Add ",(0,s.jsx)(n.code,{children:"opentelemetry-api"})," and ",(0,s.jsx)(n.code,{children:"opentelemetry-sdk"})," as dependencies"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv add opentelemetry-api opentelemetry-sdk\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"Resolved 67 packages in 1.03s\n   Building platformops @ file:///Users/you/platformops\n      Built platformops @ file:///Users/you/platformops\nPrepared 1 package in 355ms\nUninstalled 1 package in 2ms\nInstalled 4 packages in 9ms\n + opentelemetry-api==1.44.0\n + opentelemetry-sdk==1.44.0\n + opentelemetry-semantic-conventions==0.65b0\n ~ platformops==2.4.0 (from file:///Users/you/platformops)\n"})}),"\n",(0,s.jsxs)(n.p,{children:['The exact "Prepared N packages" line and timings depend on your local ',(0,s.jsx)(n.code,{children:"uv"})," cache -- the four\ninstalled package lines are what to check."]}),"\n",(0,s.jsxs)(n.p,{children:["Both packages ship a ",(0,s.jsx)(n.code,{children:"py.typed"})," marker, so no ",(0,s.jsx)(n.code,{children:"mypy"})," override is needed -- unlike ",(0,s.jsx)(n.code,{children:"boto3"})," in Module\n21, ",(0,s.jsx)(n.code,{children:"mypy"})," can already check every call into ",(0,s.jsx)(n.code,{children:"opentelemetry"})," in this project."]}),"\n",(0,s.jsxs)(n.h3,{id:"step-3----write-teststest_telemetrypy-and-run-it",children:["Step 3 -- Write ",(0,s.jsx)(n.code,{children:"tests/test_telemetry.py"}),", and run it"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_telemetry.py << \'PYEOF\'\nimport json\nimport logging\n\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import SimpleSpanProcessor\nfrom opentelemetry.sdk.trace.export.in_memory_span_exporter import InMemorySpanExporter\n\nfrom platformops.telemetry import (\n    configure_json_logging,\n    configure_tracing,\n    new_correlation_id,\n    traced_operation,\n)\n\n\ndef _test_tracer():\n    provider = TracerProvider()\n    exporter = InMemorySpanExporter()\n    provider.add_span_processor(SimpleSpanProcessor(exporter))\n    return provider.get_tracer("test"), exporter\n\n\ndef test_new_correlation_id_has_a_stable_greppable_shape():\n    cid = new_correlation_id()\n    assert cid.startswith("req-")\n    assert len(cid) == len("req-") + 12\n\n\ndef test_traced_operation_tags_the_span_with_the_correlation_id():\n    tracer, exporter = _test_tracer()\n    cid = "req-abc123def456"\n\n    with traced_operation("demo-op", correlation_id=cid, tracer=tracer):\n        pass\n\n    spans = exporter.get_finished_spans()\n    assert len(spans) == 1\n    assert spans[0].name == "demo-op"\n    assert spans[0].attributes["correlation_id"] == cid\n\n\ndef test_traced_operation_makes_the_correlation_id_available_to_log_records():\n    tracer, _ = _test_tracer()\n    cid = "req-abc123def456"\n    handler = configure_json_logging()\n    logger = logging.getLogger("platformops.telemetry.test")\n\n    captured: list[str] = []\n    handler.stream.write = captured.append\n\n    with traced_operation("demo-op", correlation_id=cid, tracer=tracer):\n        logger.info("hello from inside the span")\n\n    line = "".join(captured).strip().splitlines()[0]\n    record = json.loads(line)\n    assert record["correlation_id"] == cid\n\n\ndef test_correlation_id_is_absent_outside_any_traced_operation():\n    handler = configure_json_logging()\n    logger = logging.getLogger("platformops.telemetry.test2")\n\n    captured: list[str] = []\n    handler.stream.write = captured.append\n\n    logger.info("no span here")\n\n    line = "".join(captured).strip().splitlines()[0]\n    record = json.loads(line)\n    assert record["correlation_id"] is None\n\n\ndef test_configure_tracing_is_idempotent_within_one_process():\n    provider_a = configure_tracing("platformops-test-idempotent")\n    provider_b = configure_tracing("platformops-test-idempotent")\n    assert provider_a is provider_b\nPYEOF\nuv run pytest tests/test_telemetry.py -q\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:".....                                                                     [100%]\n5 passed in 0.03s\n"})}),"\n",(0,s.jsx)(n.admonition,{title:"This is a shortened version",type:"info",children:(0,s.jsxs)(n.p,{children:["The full ",(0,s.jsx)(n.code,{children:"tests/test_telemetry.py"})," in the checkpoint also covers ",(0,s.jsx)(n.code,{children:"_JsonFormatter"})," rendering directly\n(no logger involved) and ",(0,s.jsx)(n.code,{children:"traced_operation()"})," passing through extra ",(0,s.jsx)(n.code,{children:"attributes"}),". Extend the file\nabove the same way. Once this module ships, the finished version also lives at\n",(0,s.jsx)(n.code,{children:"~/307-aipython-labs/labs/checkpoints/v2.5.0/tests/test_telemetry.py"}),", useful to compare against\nlater."]})}),"\n",(0,s.jsx)(n.h3,{id:"step-4----see-it-work-one-correlation-id-across-a-log-line-and-a-trace-span",children:"Step 4 -- See it work: one correlation ID, across a log line and a trace span"}),"\n",(0,s.jsxs)(n.p,{children:["Prove the module's central mechanism directly, using only what exists so far -- ",(0,s.jsx)(n.code,{children:"telemetry.py"}),"\nitself, with no dependency on the observability fetch functions Phase 3 builds next. The script\nbelow configures real JSON logging and real console tracing (not test doubles), opens one\n",(0,s.jsx)(n.code,{children:"traced_operation()"})," block, and writes two log lines inside it."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m30_correlation_demo.py << \'PYEOF\'\nimport json\nimport logging\n\nfrom platformops.telemetry import (\n    configure_json_logging,\n    configure_tracing,\n    new_correlation_id,\n    traced_operation,\n)\n\nconfigure_json_logging(level=logging.INFO)\nconfigure_tracing("platformops-demo")\n\nlogger = logging.getLogger("platformops.telemetry.demo")\ncorrelation_id = new_correlation_id()\n\nwith traced_operation(\n    "demo-operation", correlation_id=correlation_id, attributes={"example": "true"}\n):\n    logger.info("step one: gathering evidence")\n    logger.info("step two: evaluating evidence")\n\nprint("---correlation id for this run---")\nprint(correlation_id)\nPYEOF\nuv run python /tmp/m30_correlation_demo.py > /tmp/m30_correlation_out.txt 2>&1\ncat /tmp/m30_correlation_out.txt\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:'{"timestamp": "2026-08-06T06:02:01+0530", "level": "INFO", "logger": "platformops.telemetry.demo", "message": "step one: gathering evidence", "correlation_id": "req-aa046338db76"}\n{"timestamp": "2026-08-06T06:02:01+0530", "level": "INFO", "logger": "platformops.telemetry.demo", "message": "step two: evaluating evidence", "correlation_id": "req-aa046338db76"}\n{\n    "name": "demo-operation",\n    "context": {\n        "trace_id": "0xc1e0d3614476b554fe7d27ae144fe510",\n        "span_id": "0x1684e3c47b9be87b",\n        "trace_state": "[]"\n    },\n    "kind": "SpanKind.INTERNAL",\n    "parent_id": null,\n    "start_time": "2026-08-06T00:32:01.128264Z",\n    "end_time": "2026-08-06T00:32:01.128430Z",\n    "status": {\n        "status_code": "UNSET"\n    },\n    "attributes": {\n        "correlation_id": "req-aa046338db76",\n        "example": "true"\n    },\n    "events": [],\n    "links": [],\n    "resource": {\n        "attributes": {\n            "telemetry.sdk.language": "python",\n            "telemetry.sdk.name": "opentelemetry",\n            "telemetry.sdk.version": "1.44.0",\n            "service.instance.id": "35beb0f4-742f-4f98-88b9-9af0a68df179",\n            "service.name": "platformops-demo"\n        },\n        "schema_url": ""\n    }\n}\n---correlation id for this run---\nreq-aa046338db76\n'})}),"\n",(0,s.jsxs)(n.p,{children:["Your own correlation ID, trace ID, span ID and ",(0,s.jsx)(n.code,{children:"service.instance.id"})," will all be different random\nvalues -- that is expected, each run generates fresh ones. Now grep for the one ID this run actually\nprinted, across every line of output, log lines and span alike:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'grep -c "$(tail -n1 /tmp/m30_correlation_out.txt)" /tmp/m30_correlation_out.txt\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"4\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Two structured log lines, the span's ",(0,s.jsx)(n.code,{children:"attributes"})," block, and the final printed line all carry the\nsame ID -- one grep, the whole story of this one operation, exactly the claim-number promise from\nthe lesson. Phase 3's Step 3 proves the same thing again, at full scale, once ",(0,s.jsx)(n.code,{children:"observability.py"}),"\nexists and there are three fetches -- not two log lines -- to correlate."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsxs)(n.h2,{id:"phase-3----observabilitypy-fetch-evidence-then-judge-it-separately",children:["Phase 3 -- ",(0,s.jsx)(n.code,{children:"observability.py"}),": fetch evidence, then judge it separately"]}),"\n",(0,s.jsx)(n.h3,{id:"step-1----write-the-module-1",children:"Step 1 -- Write the module"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/observability.py << \'PYEOF\'\n"""platformops.observability -- read metrics, logs and alerts for one service, honestly.\n\nEvery operational question this course\'s tools have answered so far is\nabout state something else already computed: GitHub\'s CI state (M26), AWS\'s\nresource state (M24), a Kubernetes workload\'s spec (M28). Observability\ndata is different in one specific way that matters here: a query can\nsucceed and still come back with nothing in it. A metrics backend that\nanswers instantly with zero data points is telling you something real --\nthis service emits no metric under that name, or nothing happened in the\nqueried window -- and that is a completely different fact from "the metrics\nbackend could not be reached at all." Confusing the two is the single\neasiest mistake to make in an automated observability check, and this\nmodule exists mainly to keep them apart everywhere it reports anything.\n\nThe fetch/aggregate split every prior read-only inspector in this project\nuses (`cloudaudit.py`, M24; `releasecheck.py`, M26) carries over exactly.\nThree `gather_*_evidence()` functions are the only functions here that call\ninto `platformops.httpclient` -- each one talks to exactly one backend,\ncatches every way that call can fail, and hands back the raw facts it\nfound (or an honest "could not fetch this" result). `evaluate_observability_snapshot()`\nis the other half: a pure function that takes three already-gathered\nevidence dicts and turns them into one `ObservabilitySnapshot`. It never\nimports `httpx` and never calls a `gather_*` function.\n\n`inspect_observability()` is the one function that ties this module to\n`platformops.telemetry`: it opens one `traced_operation()` span for the\nwhole inspection, tagged with a correlation ID, so every log line this\nrun\'s own fetch calls write (and the span itself) share one identifier a\nperson or an agent can grep. This module never writes to a metrics, log or\nalert backend -- every function here is a GET, and this module reports what\nit found, it never acknowledges an alert or deletes a log.\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any\n\nfrom opentelemetry.trace import Tracer\n\nfrom platformops.httpclient import (\n    EndpointStatusError,\n    EndpointUnreachableError,\n    HttpCheckError,\n    ResponseFormatError,\n    list_alerts,\n    query_metrics,\n    search_logs,\n)\nfrom platformops.telemetry import new_correlation_id, traced_operation\n\n_FETCHABLE_HTTP_ERRORS = (\n    EndpointStatusError,\n    EndpointUnreachableError,\n    ResponseFormatError,\n    HttpCheckError,\n)\n\n\n# ---------------------------------------------------------------------------\n# Fetch -- one function per evidence source. These, and only these, call\n# into httpclient.py. Every one of them catches its own network failures and\n# hands back a plain dict of raw facts (or "fetched": False on failure) --\n# never a judgment about whether that data is good or bad news.\n# ---------------------------------------------------------------------------\n\n\ndef gather_metrics_evidence(\n    base_url: str, query: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch metric samples for `query`. An empty result is reported honestly, not as a failure."""\n    try:\n        samples = query_metrics(base_url, query, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    return {\n        "fetched": True,\n        "query": query,\n        "sample_count": len(samples),\n        "samples": samples,\n    }\n\n\ndef gather_logs_evidence(\n    base_url: str,\n    service: str,\n    correlation_id: str | None,\n    *,\n    limit: int = 100,\n    token: str | None = None,\n) -> dict[str, Any]:\n    """Fetch log lines for `service`, narrowed to `correlation_id` when one is given.\n\n    Searching by `correlation_id` alone, once a run\'s ID is known, is the\n    whole point of tagging log lines with it in the first place -- see\n    `platformops.telemetry.traced_operation()`.\n    """\n    query = (\n        f"service:{service} AND correlation_id:{correlation_id}"\n        if correlation_id\n        else f"service:{service}"\n    )\n    try:\n        logs = search_logs(base_url, query, limit=limit, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    return {"fetched": True, "query": query, "total": len(logs), "logs": logs}\n\n\ndef gather_alerts_evidence(\n    base_url: str, service: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch alerts for `service`, split into every alert found and the ones currently active."""\n    try:\n        alerts = list_alerts(base_url, service=service, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    active = [alert for alert in alerts if alert["status"]["state"] == "active"]\n    return {"fetched": True, "total": len(alerts), "active": active}\n\n\n# ---------------------------------------------------------------------------\n# Aggregate -- pure functions. Nothing below this line imports httpx, calls\n# a gather_* function, or makes a network call of any kind.\n# ---------------------------------------------------------------------------\n\n\n@dataclass\nclass ObservabilitySnapshot:\n    """One service\'s combined metrics/logs/alerts picture, tagged with the run\'s correlation ID.\n\n    Each section\'s `status` is one of `"OK"` (data found), `"EMPTY"` (the\n    source answered but found nothing -- a real, known answer), `"FIRING"`\n    (alerts only: at least one active alert), or `"UNKNOWN"` (the source\n    could not be reached at all). `sources_failed` lists which sources this\n    run genuinely could not fetch -- `"EMPTY"` sections are never counted as\n    failed, because a source that answered "nothing here" did its job.\n    """\n\n    service: str\n    correlation_id: str\n    metrics: dict[str, Any]\n    logs: dict[str, Any]\n    alerts: dict[str, Any]\n    sources_ok: list[str]\n    sources_failed: list[str]\n\n\ndef _evaluate_metrics(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "metrics evidence unavailable"),\n        }\n    if evidence["sample_count"] == 0:\n        return {\n            "status": "EMPTY",\n            "detail": f"no data points found for query \'{evidence[\'query\']}\'",\n        }\n    return {\n        "status": "OK",\n        "detail": f"{evidence[\'sample_count\']} sample(s) found for query \'{evidence[\'query\']}\'",\n    }\n\n\ndef _evaluate_logs(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "log evidence unavailable"),\n        }\n    if evidence["total"] == 0:\n        return {\n            "status": "EMPTY",\n            "detail": f"no log line(s) matched \'{evidence[\'query\']}\'",\n        }\n    return {\n        "status": "OK",\n        "detail": f"{evidence[\'total\']} log line(s) matched \'{evidence[\'query\']}\'",\n    }\n\n\ndef _evaluate_alerts(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "alert evidence unavailable"),\n        }\n    active = evidence["active"]\n    if active:\n        names = ", ".join(\n            alert["labels"].get("alertname", "unknown") for alert in active\n        )\n        return {"status": "FIRING", "detail": f"{len(active)} active alert(s): {names}"}\n    return {"status": "OK", "detail": "no active alerts"}\n\n\ndef evaluate_observability_snapshot(\n    *,\n    service: str,\n    correlation_id: str,\n    metrics_evidence: dict[str, Any],\n    logs_evidence: dict[str, Any],\n    alerts_evidence: dict[str, Any],\n) -> ObservabilitySnapshot:\n    """Combine three already-gathered evidence dicts into one snapshot. Pure -- no network call, ever.\n\n    A source that failed to fetch (`"fetched": False`) never becomes a\n    silent `"EMPTY"` here -- it becomes an honest `"UNKNOWN"` section and a\n    name in `sources_failed`. This is the rule this whole module exists to\n    enforce: "no data" and "could not check" are never the same report.\n    """\n    sources = {\n        "metrics": metrics_evidence,\n        "logs": logs_evidence,\n        "alerts": alerts_evidence,\n    }\n    sources_ok = [name for name, ev in sources.items() if ev.get("fetched")]\n    sources_failed = [name for name, ev in sources.items() if not ev.get("fetched")]\n\n    return ObservabilitySnapshot(\n        service=service,\n        correlation_id=correlation_id,\n        metrics=_evaluate_metrics(metrics_evidence),\n        logs=_evaluate_logs(logs_evidence),\n        alerts=_evaluate_alerts(alerts_evidence),\n        sources_ok=sources_ok,\n        sources_failed=sources_failed,\n    )\n\n\ndef inspect_observability(\n    *,\n    service: str,\n    metrics_base_url: str,\n    metrics_query: str,\n    logs_base_url: str,\n    alerts_base_url: str,\n    correlation_id: str | None = None,\n    token: str | None = None,\n    tracer: Tracer | None = None,\n) -> ObservabilitySnapshot:\n    """Gather every evidence source under one traced, correlated run, and evaluate it.\n\n    Opens exactly one `traced_operation()` span for the whole inspection --\n    every fetch below happens inside it, so every log line those fetch\n    calls write carries this run\'s correlation ID, and the span itself\n    records `service` as an attribute a trace backend can filter on. This\n    is the only function in this module that both fetches evidence and\n    calls the aggregation function; it is intentionally a thin\n    orchestrator, the same shape `platformops.releasecheck.run_release_check()`\n    already established.\n    """\n    resolved_correlation_id = correlation_id or new_correlation_id()\n\n    with traced_operation(\n        "observability-inspect",\n        correlation_id=resolved_correlation_id,\n        tracer=tracer,\n        attributes={"service": service},\n    ):\n        metrics_evidence = gather_metrics_evidence(\n            metrics_base_url, metrics_query, token=token\n        )\n        logs_evidence = gather_logs_evidence(\n            logs_base_url, service, resolved_correlation_id, token=token\n        )\n        alerts_evidence = gather_alerts_evidence(alerts_base_url, service, token=token)\n\n    return evaluate_observability_snapshot(\n        service=service,\n        correlation_id=resolved_correlation_id,\n        metrics_evidence=metrics_evidence,\n        logs_evidence=logs_evidence,\n        alerts_evidence=alerts_evidence,\n    )\nPYEOF\n'})}),"\n",(0,s.jsxs)(n.h3,{id:"step-2----write-teststest_observabilitypy-and-run-it",children:["Step 2 -- Write ",(0,s.jsx)(n.code,{children:"tests/test_observability.py"}),", and run it"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_observability.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom opentelemetry.sdk.trace import TracerProvider\nfrom opentelemetry.sdk.trace.export import SimpleSpanProcessor\nfrom opentelemetry.sdk.trace.export.in_memory_span_exporter import InMemorySpanExporter\n\nfrom platformops.observability import (\n    evaluate_observability_snapshot,\n    gather_metrics_evidence,\n    inspect_observability,\n)\n\nFIXTURES = Path(__file__).parent / "fixtures" / "observability"\n\n\ndef _fixture(name: str) -> dict:\n    return json.loads((FIXTURES / name).read_text())\n\n\nMETRICS_URL = "http://localhost:9090"\nLOGS_URL = "http://localhost:3100"\nALERTS_URL = "http://localhost:9093"\n\n\ndef _test_tracer():\n    provider = TracerProvider()\n    exporter = InMemorySpanExporter()\n    provider.add_span_processor(SimpleSpanProcessor(exporter))\n    return provider.get_tracer("test"), exporter\n\n\n@respx.mock\ndef test_gather_metrics_evidence_reports_samples_on_success():\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_success.json"))\n    )\n\n    evidence = gather_metrics_evidence(\n        METRICS_URL, \'http_requests_total{service="payments"}\'\n    )\n\n    assert evidence["fetched"] is True\n    assert evidence["sample_count"] == 2\n\n\n_METRICS_EMPTY = {"fetched": True, "query": "up", "sample_count": 0, "samples": []}\n_METRICS_UNREACHABLE = {"fetched": False, "error": "boom"}\n_LOGS_FOUND = {"fetched": True, "query": "q", "total": 2, "logs": []}\n_ALERTS_NONE_ACTIVE = {"fetched": True, "total": 0, "active": []}\n\n\ndef test_evaluate_observability_snapshot_reports_empty_as_a_real_answer_not_unknown():\n    snapshot = evaluate_observability_snapshot(\n        service="payments",\n        correlation_id="req-abc123def456",\n        metrics_evidence=_METRICS_EMPTY,\n        logs_evidence=_LOGS_FOUND,\n        alerts_evidence=_ALERTS_NONE_ACTIVE,\n    )\n\n    assert snapshot.metrics["status"] == "EMPTY"\n    assert (\n        snapshot.sources_failed == []\n    )  # the source DID answer -- it just found nothing\n\n\ndef test_evaluate_observability_snapshot_reports_unknown_when_a_source_is_unreachable():\n    snapshot = evaluate_observability_snapshot(\n        service="payments",\n        correlation_id="req-abc123def456",\n        metrics_evidence=_METRICS_UNREACHABLE,\n        logs_evidence=_LOGS_FOUND,\n        alerts_evidence=_ALERTS_NONE_ACTIVE,\n    )\n\n    assert snapshot.metrics["status"] == "UNKNOWN"\n    assert snapshot.sources_failed == ["metrics"]\n\n\n@respx.mock\ndef test_inspect_observability_runs_end_to_end_and_tags_one_span_with_the_correlation_id():\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_success.json"))\n    )\n    respx.get(f"{LOGS_URL}/api/v1/logs/search").mock(\n        return_value=httpx.Response(200, json=_fixture("logs_search_success.json"))\n    )\n    respx.get(f"{ALERTS_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_fixture("alerts_active.json"))\n    )\n    tracer, exporter = _test_tracer()\n\n    snapshot = inspect_observability(\n        service="payments",\n        metrics_base_url=METRICS_URL,\n        metrics_query=\'http_requests_total{service="payments"}\',\n        logs_base_url=LOGS_URL,\n        alerts_base_url=ALERTS_URL,\n        correlation_id="req-a1b2c3d4e5f6",\n        tracer=tracer,\n    )\n\n    assert snapshot.correlation_id == "req-a1b2c3d4e5f6"\n    assert snapshot.alerts["status"] == "FIRING"\n\n    spans = exporter.get_finished_spans()\n    assert spans[0].attributes["correlation_id"] == "req-a1b2c3d4e5f6"\nPYEOF\nuv run pytest tests/test_observability.py -q\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"....                                                                      [100%]\n4 passed in 0.16s\n"})}),"\n",(0,s.jsx)(n.admonition,{title:"This is a shortened version",type:"info",children:(0,s.jsxs)(n.p,{children:["The full ",(0,s.jsx)(n.code,{children:"tests/test_observability.py"})," in the checkpoint also covers ",(0,s.jsx)(n.code,{children:"gather_logs_evidence()"})," and\n",(0,s.jsx)(n.code,{children:"gather_alerts_evidence()"})," directly, every ",(0,s.jsx)(n.code,{children:"_evaluate_*"})," branch (including ",(0,s.jsx)(n.code,{children:"FIRING"}),"), and\n",(0,s.jsx)(n.code,{children:"inspect_observability()"})," degrading honestly when the logs backend is unreachable. Extend the file\nabove the same way. Once this module ships, the finished version also lives at\n",(0,s.jsx)(n.code,{children:"~/307-aipython-labs/labs/checkpoints/v2.5.0/tests/test_observability.py"}),", useful to compare against\nlater."]})}),"\n",(0,s.jsx)(n.h3,{id:"step-3----see-it-work-at-full-scale-one-correlation-id-across-three-fetches-and-a-trace-span",children:"Step 3 -- See it work at full scale: one correlation ID, across three fetches and a trace span"}),"\n",(0,s.jsxs)(n.p,{children:["Phase 2's Step 4 proved the mechanism with two log lines from one function. Now that\n",(0,s.jsx)(n.code,{children:"observability.py"})," exists, prove the same thing again at the scale this module actually runs at --\nthree real fetches (metrics, logs, alerts), each writing its own log lines, all under the one span\n",(0,s.jsx)(n.code,{children:"inspect_observability()"})," opens."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m30_full_correlation_demo.py << \'PYEOF\'\nimport json\nimport logging\nfrom pathlib import Path\n\nimport httpx\nimport respx\n\nfrom platformops.observability import inspect_observability\nfrom platformops.telemetry import configure_json_logging, configure_tracing\n\nconfigure_json_logging(level=logging.INFO)\nconfigure_tracing("platformops-demo-full")\n\nFIXTURES = Path("tests/fixtures/observability")\nMETRICS_URL = "http://localhost:9090"\nLOGS_URL = "http://localhost:3100"\nALERTS_URL = "http://localhost:9093"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_success.json"))\n    )\n    respx.get(f"{LOGS_URL}/api/v1/logs/search").mock(\n        return_value=httpx.Response(200, json=_fixture("logs_search_success.json"))\n    )\n    respx.get(f"{ALERTS_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_fixture("alerts_active.json"))\n    )\n\n    snapshot = inspect_observability(\n        service="payments",\n        metrics_base_url=METRICS_URL,\n        metrics_query=\'http_requests_total{service="payments"}\',\n        logs_base_url=LOGS_URL,\n        alerts_base_url=ALERTS_URL,\n    )\n\nprint("---correlation id for this run---")\nprint(snapshot.correlation_id)\nPYEOF\nuv run python /tmp/m30_full_correlation_demo.py > /tmp/m30_full_correlation_out.txt 2>&1\ncat /tmp/m30_full_correlation_out.txt\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:'{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "httpx", "message": "HTTP Request: GET http://localhost:9090/api/v1/query?query=http_requests_total%7Bservice%3D%22payments%22%7D \\"HTTP/1.1 200 OK\\"", "correlation_id": "req-17491741aee5"}\n{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "platformops.httpclient", "message": "fetched 2 metric sample(s) for query \'http_requests_total{service=\\"payments\\"}\'", "correlation_id": "req-17491741aee5"}\n{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "httpx", "message": "HTTP Request: GET http://localhost:3100/api/v1/logs/search?query=service%3Apayments+AND+correlation_id%3Areq-17491741aee5&limit=100 \\"HTTP/1.1 200 OK\\"", "correlation_id": "req-17491741aee5"}\n{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "platformops.httpclient", "message": "fetched 2 log line(s) for query \'service:payments AND correlation_id:req-17491741aee5\'", "correlation_id": "req-17491741aee5"}\n{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "httpx", "message": "HTTP Request: GET http://localhost:9093/api/v2/alerts?filter=service%3D%22payments%22 \\"HTTP/1.1 200 OK\\"", "correlation_id": "req-17491741aee5"}\n{"timestamp": "2026-08-06T06:03:27+0530", "level": "INFO", "logger": "platformops.httpclient", "message": "fetched 1 alert(s) from http://localhost:9093", "correlation_id": "req-17491741aee5"}\n{\n    "name": "observability-inspect",\n    "context": {\n        "trace_id": "0x54b5d6a0c7c8f09718b879ed69837ce1",\n        "span_id": "0xc716409a83b05346",\n        "trace_state": "[]"\n    },\n    "kind": "SpanKind.INTERNAL",\n    "parent_id": null,\n    "start_time": "2026-08-06T00:33:27.685840Z",\n    "end_time": "2026-08-06T00:33:27.738129Z",\n    "status": {\n        "status_code": "UNSET"\n    },\n    "attributes": {\n        "correlation_id": "req-17491741aee5",\n        "service": "payments"\n    },\n    "events": [],\n    "links": [],\n    "resource": {\n        "attributes": {\n            "telemetry.sdk.language": "python",\n            "telemetry.sdk.name": "opentelemetry",\n            "telemetry.sdk.version": "1.44.0",\n            "service.instance.id": "d41985d9-1b39-4b06-a1b1-af1a2bb24a15",\n            "service.name": "platformops-demo-full"\n        },\n        "schema_url": ""\n    }\n}\n---correlation id for this run---\nreq-17491741aee5\n'})}),"\n",(0,s.jsx)(n.p,{children:"Now grep for the one ID this run actually printed, across every line of output:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'grep -c "$(tail -n1 /tmp/m30_full_correlation_out.txt)" /tmp/m30_full_correlation_out.txt\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"8\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Six structured log lines (two per fetch: the underlying ",(0,s.jsx)(n.code,{children:"httpx"})," request log and this project's own\n",(0,s.jsx)(n.code,{children:"platformops.httpclient"})," log), the span's ",(0,s.jsx)(n.code,{children:"attributes"})," block, and the final printed line -- eight\nplaces, one correlation ID, the whole story of one ",(0,s.jsx)(n.code,{children:"observability-inspect"})," run."]}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"phase-4----wire-the-cli-then-run-it-against-three-real-scenarios",children:"Phase 4 -- Wire the CLI, then run it against three real scenarios"}),"\n",(0,s.jsxs)(n.h3,{id:"step-1----add-the-observability-inspect-command",children:["Step 1 -- Add the ",(0,s.jsx)(n.code,{children:"observability-inspect"})," command"]}),"\n",(0,s.jsxs)(n.p,{children:["Add the import in alphabetical order among the other ",(0,s.jsx)(n.code,{children:"platformops.*"})," imports in\n",(0,s.jsx)(n.code,{children:"src/platformops/cli.py"})," -- it sorts between ",(0,s.jsx)(n.code,{children:"multiregion"})," and ",(0,s.jsx)(n.code,{children:"releasecheck"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/cli.py"',children:"from platformops.multiregion import ResourceRecord, scan_regions, to_csv, to_markdown\nfrom platformops.observability import inspect_observability\nfrom platformops.releasecheck import run_release_check\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Add the command itself, right before ",(0,s.jsx)(n.code,{children:"report-upload"}),":"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/cli.py"',children:'@app.command("observability-inspect")\ndef observability_inspect(\n    ctx: typer.Context,\n    service: Annotated[\n        str, typer.Argument(help="service name to inspect, e.g. payments")\n    ],\n    metrics_url: Annotated[\n        str,\n        typer.Option(\n            "--metrics-url",\n            help="metrics backend base URL (Prometheus query API shape)",\n        ),\n    ] = "http://localhost:9090",\n    logs_url: Annotated[\n        str, typer.Option("--logs-url", help="log-search backend base URL")\n    ] = "http://localhost:3100",\n    alerts_url: Annotated[\n        str,\n        typer.Option(\n            "--alerts-url", help="alert backend base URL (Alertmanager v2 API shape)"\n        ),\n    ] = "http://localhost:9093",\n    metrics_query: Annotated[\n        str | None,\n        typer.Option(\n            "--metrics-query",\n            help="query to run against the metrics backend (defaults to a per-service request-count query)",\n        ),\n    ] = None,\n    correlation_id: Annotated[\n        str | None,\n        typer.Option(\n            "--correlation-id",\n            help="reuse an existing correlation ID instead of generating a new one",\n        ),\n    ] = None,\n    as_json: Annotated[\n        bool,\n        typer.Option(\n            "--json",\n            help="print a machine-readable JSON snapshot instead of plain text",\n        ),\n    ] = False,\n) -> None:\n    """Fetch metrics, logs and alerts for one service under one correlation ID, and report what each source found.\n\n    Read-only: this command never writes a metric, never deletes a log, and\n    never acknowledges or silences an alert -- it only reads what each\n    backend already has. A source this command cannot reach (a timeout, a\n    connection refused, an auth failure) is reported as `UNKNOWN` for that\n    one section, never silently treated as `EMPTY` -- see\n    `platformops.observability` for the fetch/evaluate split that keeps\n    "no data found" and "could not check" apart. Every fetch in this run\n    happens inside one traced, correlated span -- the correlation ID\n    printed below is the same one that appears on every structured log\n    line this run wrote; see `platformops.telemetry`. Exit 0 when every\n    source answered (whether or not it found data, and whether or not an\n    alert is firing); exit 1 when at least one source could not be\n    reached.\n    """\n    quiet = ctx.obj["quiet"]\n    resolved_query = metrics_query or f\'http_requests_total{{service="{service}"}}\'\n\n    snapshot = inspect_observability(\n        service=service,\n        metrics_base_url=metrics_url,\n        metrics_query=resolved_query,\n        logs_base_url=logs_url,\n        alerts_base_url=alerts_url,\n        correlation_id=correlation_id,\n        token=os.environ.get("OBSERVABILITY_TOKEN"),\n    )\n\n    if as_json:\n        typer.echo(\n            json.dumps(\n                {\n                    "status": "ok",\n                    "service": snapshot.service,\n                    "correlation_id": snapshot.correlation_id,\n                    "metrics": snapshot.metrics,\n                    "logs": snapshot.logs,\n                    "alerts": snapshot.alerts,\n                    "sources_ok": snapshot.sources_ok,\n                    "sources_failed": snapshot.sources_failed,\n                },\n                indent=2,\n            )\n        )\n        raise typer.Exit(code=0 if not snapshot.sources_failed else 1)\n\n    if not quiet or snapshot.sources_failed:\n        typer.echo(f"{snapshot.service} (correlation_id={snapshot.correlation_id})")\n    for label, section in (\n        ("metrics", snapshot.metrics),\n        ("logs", snapshot.logs),\n        ("alerts", snapshot.alerts),\n    ):\n        typer.echo(f"  [{section[\'status\']}] {label}: {section[\'detail\']}")\n    if snapshot.sources_failed:\n        typer.echo(f"  unreachable source(s): {\', \'.join(snapshot.sources_failed)}")\n    raise typer.Exit(code=0 if not snapshot.sources_failed else 1)\n'})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run platformops observability-inspect --help\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:' Usage: platformops observability-inspect [OPTIONS] {service}\n\n Fetch metrics, logs and alerts for one service under one correlation ID, and\n report what each source found.\n\n Read-only: this command never writes a metric, never deletes a log, and\n never acknowledges or silences an alert -- it only reads what each\n backend already has. A source this command cannot reach (a timeout, a\n connection refused, an auth failure) is reported as `UNKNOWN` for that\n one section, never silently treated as `EMPTY` -- see\n `platformops.observability` for the fetch/evaluate split that keeps\n "no data found" and "could not check" apart. Every fetch in this run\n happens inside one traced, correlated span -- the correlation ID\n printed below is the same one that appears on every structured log\n line this run wrote; see `platformops.telemetry`. Exit 0 when every\n source answered (whether or not it found data, and whether or not an\n alert is firing); exit 1 when at least one source could not be\n reached.\n\n\u256D\u2500 Arguments \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 *    service      <str>  service name to inspect, e.g. payments [required]   \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n\u256D\u2500 Options \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 --metrics-url           <str>  metrics backend base URL (Prometheus query    \u2502\n\u2502                                API shape)                                    \u2502\n\u2502                                [default: http://localhost:9090]              \u2502\n\u2502 --logs-url              <str>  log-search backend base URL                   \u2502\n\u2502                                [default: http://localhost:3100]              \u2502\n\u2502 --alerts-url            <str>  alert backend base URL (Alertmanager v2 API   \u2502\n\u2502                                shape)                                        \u2502\n\u2502                                [default: http://localhost:9093]              \u2502\n\u2502 --metrics-query         <str>  query to run against the metrics backend      \u2502\n\u2502                                (defaults to a per-service request-count      \u2502\n\u2502                                query)                                        \u2502\n\u2502 --correlation-id        <str>  reuse an existing correlation ID instead of   \u2502\n\u2502                                generating a new one                          \u2502\n\u2502 --json                         print a machine-readable JSON snapshot        \u2502\n\u2502                                instead of plain text                         \u2502\n\u2502 --help                         Show this message and exit.                   \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n'})}),"\n",(0,s.jsx)(n.h3,{id:"step-2----run-it-live-a-full-pass",children:"Step 2 -- Run it live: a full pass"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m30_demo_full.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/observability")\nMETRICS_URL = "http://localhost:9090"\nLOGS_URL = "http://localhost:3100"\nALERTS_URL = "http://localhost:9093"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_success.json"))\n    )\n    respx.get(f"{LOGS_URL}/api/v1/logs/search").mock(\n        return_value=httpx.Response(200, json=_fixture("logs_search_success.json"))\n    )\n    respx.get(f"{ALERTS_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_fixture("alerts_active.json"))\n    )\n\n    result = runner.invoke(app, ["observability-inspect", "payments"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m30_demo_full.py\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"payments (correlation_id=req-032be301c244)\n  [OK] metrics: 2 sample(s) found for query 'http_requests_total{service=\"payments\"}'\n  [OK] logs: 2 log line(s) matched 'service:payments AND correlation_id:req-032be301c244'\n  [FIRING] alerts: 1 active alert(s): HighErrorRate\n\nexit code: 0\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Your correlation ID will differ (a fresh one is generated every run) -- notice the logs query\nembedded it automatically, and the alert's own name (",(0,s.jsx)(n.code,{children:"HighErrorRate"}),") is named right in the report."]}),"\n",(0,s.jsx)(n.h3,{id:"step-3----run-it-live-missing-telemetry-honestly-reported",children:"Step 3 -- Run it live: missing telemetry, honestly reported"}),"\n",(0,s.jsxs)(n.p,{children:["Point the same command at a service with genuinely nothing to report -- every backend answers, every\none of them found zero results. ",(0,s.jsx)(n.code,{children:"--correlation-id"})," is passed explicitly here so the output below is\nreproducible."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m30_demo_empty.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/observability")\nMETRICS_URL = "http://localhost:9090"\nLOGS_URL = "http://localhost:3100"\nALERTS_URL = "http://localhost:9093"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_empty.json"))\n    )\n    respx.get(f"{LOGS_URL}/api/v1/logs/search").mock(\n        return_value=httpx.Response(200, json=_fixture("logs_search_empty.json"))\n    )\n    respx.get(f"{ALERTS_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_fixture("alerts_none.json"))\n    )\n\n    result = runner.invoke(\n        app,\n        ["observability-inspect", "new-checkout-service", "--correlation-id", "req-000000000000"],\n    )\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m30_demo_empty.py\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"new-checkout-service (correlation_id=req-000000000000)\n  [EMPTY] metrics: no data points found for query 'http_requests_total{service=\"new-checkout-service\"}'\n  [EMPTY] logs: no log line(s) matched 'service:new-checkout-service AND correlation_id:req-000000000000'\n  [OK] alerts: no active alerts\n\nexit code: 0\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Exit code ",(0,s.jsx)(n.code,{children:"0"})," -- every source answered. ",(0,s.jsx)(n.code,{children:"EMPTY"})," is not a problem this command reports as one; it is\nthe honest, correct answer for a brand-new service with no traffic yet."]}),"\n",(0,s.jsx)(n.h3,{id:"step-4----run-it-live-an-unreachable-source",children:"Step 4 -- Run it live: an unreachable source"}),"\n",(0,s.jsx)(n.p,{children:"Now simulate a real outage on the log-search backend -- it refuses every connection attempt, while\nmetrics and alerts answer normally."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m30_demo_unreachable.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nimport platformops.httpclient as httpclient\nfrom platformops.cli import app\n\nhttpclient.time.sleep = lambda _seconds: None\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/observability")\nMETRICS_URL = "http://localhost:9090"\nLOGS_URL = "http://localhost:3100"\nALERTS_URL = "http://localhost:9093"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{METRICS_URL}/api/v1/query").mock(\n        return_value=httpx.Response(200, json=_fixture("metrics_query_success.json"))\n    )\n    # Simulate an outage: the log-search backend refuses every connection attempt.\n    respx.get(f"{LOGS_URL}/api/v1/logs/search").mock(\n        side_effect=httpx.ConnectError("connection refused")\n    )\n    respx.get(f"{ALERTS_URL}/api/v2/alerts").mock(\n        return_value=httpx.Response(200, json=_fixture("alerts_none.json"))\n    )\n\n    result = runner.invoke(app, ["observability-inspect", "payments", "--json"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m30_demo_unreachable.py\n'})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:'{\n  "status": "ok",\n  "service": "payments",\n  "correlation_id": "req-a3263455631d",\n  "metrics": {\n    "status": "OK",\n    "detail": "2 sample(s) found for query \'http_requests_total{service=\\"payments\\"}\'"\n  },\n  "logs": {\n    "status": "UNKNOWN",\n    "detail": "http://localhost:3100/api/v1/logs/search unreachable after 5 attempt(s): connection refused"\n  },\n  "alerts": {\n    "status": "OK",\n    "detail": "no active alerts"\n  },\n  "sources_ok": [\n    "metrics",\n    "alerts"\n  ],\n  "sources_failed": [\n    "logs"\n  ]\n}\n\nexit code: 1\n'})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.code,{children:"metrics"})," and ",(0,s.jsx)(n.code,{children:"alerts"})," still answered normally and are marked ",(0,s.jsx)(n.code,{children:"OK"}),". ",(0,s.jsx)(n.code,{children:"logs"})," alone is ",(0,s.jsx)(n.code,{children:"UNKNOWN"}),", listed\nin ",(0,s.jsx)(n.code,{children:"sources_failed"}),", and the exit code is ",(0,s.jsx)(n.code,{children:"1"}),' -- a script or a CI job gating on this command\'s exit\ncode will correctly treat this run as "could not fully check", even though two of the three sources\nwere perfectly healthy.']}),"\n",(0,s.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,s.jsxs)(n.ul,{children:["\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:(0,s.jsx)(n.code,{children:"ModuleNotFoundError: No module named 'opentelemetry'"})})," -- run ",(0,s.jsx)(n.code,{children:"uv sync"})," after Phase 2 Step 2 to\nmake sure your environment picked up the new dependency."]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"The demo scripts print a real network error instead of the fixture data"})," -- confirm the ",(0,s.jsx)(n.code,{children:"with respx.mock:"})," block wraps the ",(0,s.jsx)(n.code,{children:"runner.invoke(...)"})," call itself, not just the ",(0,s.jsx)(n.code,{children:"respx.get(...)"})," lines\nabove it."]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsxs)(n.strong,{children:[(0,s.jsx)(n.code,{children:"observability-inspect"})," shows ",(0,s.jsx)(n.code,{children:"UNKNOWN"})," when you expected ",(0,s.jsx)(n.code,{children:"OK"}),", or the other way round"]})," -- check\nwhich fixture file each mocked route returns. ",(0,s.jsx)(n.code,{children:"metrics_query_success.json"})," and\n",(0,s.jsx)(n.code,{children:"logs_search_success.json"})," have real data; the ",(0,s.jsx)(n.code,{children:"_empty.json"})," variants intentionally have none."]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsxs)(n.strong,{children:["A section shows ",(0,s.jsx)(n.code,{children:"EMPTY"})," when you expected ",(0,s.jsx)(n.code,{children:"UNKNOWN"}),", or the reverse"]})," -- ",(0,s.jsx)(n.code,{children:"EMPTY"})," means the fetch\nsucceeded and found nothing (a 200 response with an empty result); ",(0,s.jsx)(n.code,{children:"UNKNOWN"})," means the fetch itself\nfailed (a connection error, a timeout, a non-2xx status). Check which kind of ",(0,s.jsx)(n.code,{children:"respx"})," mock the test\nor demo actually set up -- ",(0,s.jsx)(n.code,{children:"return_value=httpx.Response(200, ...)"})," versus ",(0,s.jsx)(n.code,{children:"side_effect=httpx.ConnectError(...)"}),"."]}),"\n",(0,s.jsxs)(n.li,{children:[(0,s.jsx)(n.strong,{children:"The correlation-ID grep in Phase 2 Step 4 finds fewer than 8 matches"})," -- confirm\n",(0,s.jsx)(n.code,{children:"configure_json_logging()"})," ran before ",(0,s.jsx)(n.code,{children:"inspect_observability()"}),", and that the whole\n",(0,s.jsx)(n.code,{children:"with respx.mock:"})," block (all three fetches) sits inside it -- a fetch that happens before logging\nis configured writes to whatever handler was active before, not the JSON one."]}),"\n"]})}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"phase-5----full-quality-gate-and-version-bump",children:"Phase 5 -- Full quality gate and version bump"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run pytest -q\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 16%]\n........................................................................ [ 32%]\n........................................................................ [ 49%]\n........................................................................ [ 65%]\n........................................................................ [ 82%]\n........................................................................ [ 98%]\n.....                                                                    [100%]\n437 passed in 34.72s\n"})}),"\n",(0,s.jsx)(n.p,{children:"Your count will differ from the checkpoint's full count if you followed the shortened test steps\nabove rather than the extended versions -- that is expected, not a bug."}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format src/ tests/\nuv run mypy src/\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"All checks passed!\n3 files reformatted, 50 files left unchanged\nSuccess: no issues found in 27 source files\n"})}),"\n",(0,s.jsxs)(n.p,{children:["If you have followed this project through every module in order, three files predate this\nmodule's own changes and were never actually ",(0,s.jsx)(n.code,{children:"ruff format"}),"-clean -- ",(0,s.jsx)(n.code,{children:"kubernetes_restart.py"}),",\n",(0,s.jsx)(n.code,{children:"test_cli.py"})," and ",(0,s.jsx)(n.code,{children:"test_kubernetes_restart.py"})," from Module 29. ",(0,s.jsx)(n.code,{children:"ruff format"})," (not just ",(0,s.jsx)(n.code,{children:"--check"}),")\nfixes them here, as a harmless, purely cosmetic line-wrap -- no behavior changes, and this project's\nown test suite already confirmed that above. Run the check again to confirm the whole tree is now\nclean:"]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run ruff format --check src/ tests/\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"53 files already formatted\n"})}),"\n",(0,s.jsx)(n.p,{children:"Bump the version and update the changelog:"}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"uv run python -c \"\nimport re\npath = 'pyproject.toml'\ntext = open(path).read()\ntext = text.replace('version = \\\"2.4.0\\\"', 'version = \\\"2.5.0\\\"', 1)\nopen(path, 'w').write(text)\n\"\nuv sync\nuv run platformops version\n"})}),"\n",(0,s.jsx)(n.p,{children:(0,s.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-text",children:"platformops 2.5.0\n"})}),"\n",(0,s.jsxs)(n.p,{children:["Add a ",(0,s.jsx)(n.code,{children:"## [2.5.0] - Observable Automation"})," section to ",(0,s.jsx)(n.code,{children:"CHANGELOG.md"}),", above the previous release,\nfollowing the same ",(0,s.jsx)(n.code,{children:"### Added"})," structure every prior release entry already uses."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:'git add -A\ngit commit -m "platformops v2.5.0 -- Observable Automation"\ngit tag v2.5.0\n'})}),"\n",(0,s.jsx)(n.admonition,{title:"Lost your progress? Compare against the checkpoint",type:"info",children:(0,s.jsxs)(n.p,{children:["The instructor's copy of this exact ",(0,s.jsx)(n.code,{children:"v2.5.0"})," end state ships in the labs repository at\n",(0,s.jsx)(n.code,{children:"307-aipython-labs/labs/checkpoints/v2.5.0"}),". If your project stops passing the gates, clone or pull\nthat repository and compare ",(0,s.jsx)(n.code,{children:"src/platformops/telemetry.py"}),", ",(0,s.jsx)(n.code,{children:"src/platformops/observability.py"}),",\n",(0,s.jsx)(n.code,{children:"src/platformops/httpclient.py"})," and ",(0,s.jsx)(n.code,{children:"src/platformops/cli.py"})," against yours file by file."]})}),"\n",(0,s.jsx)(n.hr,{}),"\n",(0,s.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,s.jsxs)(n.p,{children:["Every evidence source in this lab is a ",(0,s.jsx)(n.code,{children:"respx"}),"-mocked fixture, replayed in-process, and every span\nthis lab produced went to a ",(0,s.jsx)(n.code,{children:"ConsoleSpanExporter"})," (printed to your terminal) or an in-memory test\nexporter -- there is no real observability backend, no container, and no cluster this lab started.\nThere is nothing external to tear down."]}),"\n",(0,s.jsx)(n.pre,{children:(0,s.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -f /tmp/m30_correlation_demo.py /tmp/m30_correlation_out.txt \\\n      /tmp/m30_full_correlation_demo.py /tmp/m30_full_correlation_out.txt \\\n      /tmp/m30_demo_full.py /tmp/m30_demo_empty.py /tmp/m30_demo_unreachable.py\n"})}),"\n",(0,s.jsxs)(n.p,{children:[(0,s.jsx)(n.code,{children:"src/platformops/telemetry.py"}),", ",(0,s.jsx)(n.code,{children:"src/platformops/observability.py"}),", ",(0,s.jsx)(n.code,{children:"httpclient.py"}),"'s three new\nendpoints, the ",(0,s.jsx)(n.code,{children:"observability-inspect"})," CLI command, ",(0,s.jsx)(n.code,{children:"tests/fixtures/observability/"}),", the ",(0,s.jsx)(n.code,{children:"v2.5.0"}),"\nversion bump and the ",(0,s.jsx)(n.code,{children:"CHANGELOG.md"})," entry all stay -- they are permanent additions to the project, and\ncarry forward into every later module."]})]})}function p(e={}){let{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,s.jsx)(n,{...e,children:(0,s.jsx)(d,{...e})}):d(e)}},8453(e,n,t){t.d(n,{R:()=>o,x:()=>i});var r=t(6540);let s={},a=r.createContext(s);function o(e){let n=r.useContext(a);return r.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(s):e.components||s:o(e.components),r.createElement(a.Provider,{value:n},e.children)}}}]);