"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["1750"],{1696(e,n,t){t.r(n),t.d(n,{metadata:()=>s,default:()=>p,frontMatter:()=>o,contentTitle:()=>i,toc:()=>l,assets:()=>c});var s=JSON.parse('{"id":"m26-cicd-automation/lab","title":"Lab: Source Control and CI/CD Automation","description":"This lab has four phases. Phase 1 adds five new read-only GitHub endpoints to","source":"@site/docs/m26-cicd-automation/lab.md","sourceDirName":"m26-cicd-automation","slug":"/m26-cicd-automation/lab","permalink":"/307-aipython-site/docs/m26-cicd-automation/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Source Control and CI/CD Automation"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Source Control and CI/CD Automation","permalink":"/307-aipython-site/docs/m26-cicd-automation/lesson"},"next":{"title":"Quiz: Source Control and CI/CD Automation","permalink":"/307-aipython-site/docs/m26-cicd-automation/quiz"}}'),r=t(4848),a=t(8453);let o={sidebar_position:2,title:"Lab: Source Control and CI/CD Automation"},i="Lab \u2014 Build PlatformOps v2.1.0: the Release Readiness Checker",c={},l=[{value:"Phase 0 \u2014 Read-only, and fully offline on purpose",id:"phase-0--read-only-and-fully-offline-on-purpose",level:2},{value:"Why fixtures, not live GitHub calls",id:"why-fixtures-not-live-github-calls",level:3},{value:"Phase 1 \u2014 Five new GitHub endpoints",id:"phase-1--five-new-github-endpoints",level:2},{value:"Step 1 \u2014 Add the endpoints to <code>httpclient.py</code>",id:"step-1--add-the-endpoints-to-httpclientpy",level:3},{value:"Step 2 \u2014 Add the fixture bodies",id:"step-2--add-the-fixture-bodies",level:3},{value:"Step 3 \u2014 Add tests for the new endpoints, and run them",id:"step-3--add-tests-for-the-new-endpoints-and-run-them",level:3},{value:"Phase 2 \u2014 <code>releasecheck.py</code>: fetch evidence, then judge it separately",id:"phase-2--releasecheckpy-fetch-evidence-then-judge-it-separately",level:2},{value:"Step 1 \u2014 Write the module",id:"step-1--write-the-module",level:3},{value:"Step 2 \u2014 Write <code>tests/test_releasecheck.py</code>",id:"step-2--write-teststest_releasecheckpy",level:3},{value:"Phase 3 \u2014 Wire the CLI, then run it live against both scenarios",id:"phase-3--wire-the-cli-then-run-it-live-against-both-scenarios",level:2},{value:"Step 1 \u2014 Add the <code>release-check</code> command",id:"step-1--add-the-release-check-command",level:3},{value:"Step 2 \u2014 Run it live against the fixtures: a full pass",id:"step-2--run-it-live-against-the-fixtures-a-full-pass",level:3},{value:"Step 3 \u2014 Run it live against the fixtures: a partial failure",id:"step-3--run-it-live-against-the-fixtures-a-partial-failure",level:3},{value:"Phase 4 \u2014 Full quality gate and version bump",id:"phase-4--full-quality-gate-and-version-bump",level:2},{value:"Teardown",id:"teardown",level:2}];function d(e){let n={a:"a",admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"lab--build-platformops-v210-the-release-readiness-checker",children:"Lab \u2014 Build PlatformOps v2.1.0: the Release Readiness Checker"})}),"\n",(0,r.jsxs)(n.p,{children:["This lab has four phases. Phase 1 adds five new read-only GitHub endpoints to\n",(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"}),", the same module Module 9 started. Phase 2 builds\n",(0,r.jsx)(n.code,{children:"src/platformops/releasecheck.py"})," \u2014 the fetch/aggregate split that turns six evidence sources into\none verdict. Phase 3 wires the ",(0,r.jsx)(n.code,{children:"release-check"})," CLI command and runs it live against deterministic\nfixtures, both a full pass and a partial failure. Phase 4 runs this project's full quality gate and\nbumps the version to ",(0,r.jsx)(n.code,{children:"v2.1.0"})," \u2014 the Release Readiness Checker release."]}),"\n",(0,r.jsxs)(n.admonition,{title:"Before you start",type:"info",children:[(0,r.jsxs)(n.p,{children:["You need ",(0,r.jsx)(n.code,{children:"~/platformops"})," at ",(0,r.jsx)(n.code,{children:"v2.0.0"}),", with ",(0,r.jsx)(n.code,{children:"src/platformops/cloudremediate.py"})," in place from Module\n25, and the labs repo cloned at ",(0,r.jsx)(n.code,{children:"~/307-aipython-labs"}),"."]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 2.0.0\n"})}),(0,r.jsxs)(n.p,{children:["If this already shows ",(0,r.jsx)(n.code,{children:"2.1.0"}),", you have already completed this lab \u2014 skip ahead to\n",(0,r.jsx)(n.a,{href:"#phase-4--full-quality-gate-and-version-bump",children:"Phase 4"}),". Phase 1 Step 1 is guarded so it is safe\nto repeat even from an already-completed state; Phase 2 Step 2's test file is a ",(0,r.jsx)(n.code,{children:"cat >"}),"\noverwrite by design (you extend it by hand afterward per that step's own instructions), so\nre-running it will reset it back to the short starting shape shown there -- expected, not a bug,\nbut worth knowing if you have already extended it yourself."]}),(0,r.jsxs)(n.p,{children:["This module does not touch AWS or Floci at all. You do not need Floci running, and you do not need\n",(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," set for anything in this lab."]})]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-0--read-only-and-fully-offline-on-purpose",children:"Phase 0 \u2014 Read-only, and fully offline on purpose"}),"\n",(0,r.jsx)(n.p,{children:"Before writing any code: this lab never merges a pull request, never re-runs a check, never\ntriggers a build, and never creates a deployment. Every function you write in this lab either reads\nfrom GitHub's API or works on data already sitting in memory \u2014 the same read-only-composition\ndiscipline Module 19's service-readiness report and Module 24's cloud audit already used, applied\nhere to a repository's own CI/CD state instead of AWS resources."}),"\n",(0,r.jsx)(n.h3,{id:"why-fixtures-not-live-github-calls",children:"Why fixtures, not live GitHub calls"}),"\n",(0,r.jsxs)(n.p,{children:["Module 9's lab called the real, public GitHub API \u2014 ",(0,r.jsx)(n.code,{children:"repo-info"})," and ",(0,r.jsx)(n.code,{children:"workflow-runs"})," are safe to\ndemonstrate against a real repository because a GET against a repository's summary or its run\nhistory is cheap, has no side effects, and answers close enough to instantly. This lab is\ndifferent. A release-readiness check depends on the state of a ",(0,r.jsx)(n.em,{children:"specific"})," pull request, a\n",(0,r.jsx)(n.em,{children:"specific"})," commit's check runs, and a ",(0,r.jsx)(n.em,{children:"specific"})," deployment record all agreeing with each other at\none moment in time. A real repository's pull requests get merged, its check runs get re-run, and\nits artifacts expire on their own schedule \u2014 the exact state this lab's PASS and FAIL scenarios\nneed would not still be there the next time you, or anyone grading this lab, ran it again."]}),"\n",(0,r.jsxs)(n.p,{children:["So this lab builds against ",(0,r.jsx)(n.strong,{children:"deterministic JSON fixtures"})," instead \u2014 recorded GitHub API response\nbodies, replayed through ",(0,r.jsx)(n.code,{children:"respx"})," (already a dev dependency in this project since Module 9), the\nsame library ",(0,r.jsx)(n.code,{children:"tests/test_httpclient.py"})," already uses to mock ",(0,r.jsx)(n.code,{children:"get_repo_info()"}),". No ",(0,r.jsx)(n.code,{children:"GITHUB_TOKEN"}),",\nno rate limit, no network flakiness, and the exact same PASS and FAIL scenario every single time you\nrun it. The Deep Dive contrasts this directly against Module 9's live-call approach."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-1--five-new-github-endpoints",children:"Phase 1 \u2014 Five new GitHub endpoints"}),"\n",(0,r.jsxs)(n.h3,{id:"step-1--add-the-endpoints-to-httpclientpy",children:["Step 1 \u2014 Add the endpoints to ",(0,r.jsx)(n.code,{children:"httpclient.py"})]}),"\n",(0,r.jsxs)(n.p,{children:["Append this to the end of ",(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"})," \u2014 it follows the exact\nretry/pagination/auth/validation shape ",(0,r.jsx)(n.code,{children:"get_repo_info()"})," and ",(0,r.jsx)(n.code,{children:"list_workflow_runs()"})," already use.\nThe ",(0,r.jsx)(n.code,{children:"grep -q ... ||"})," guard makes this safe to run more than once -- without it, re-running this\nstep would append a second copy of all five functions and break Phase 4's quality gate with\n",(0,r.jsx)(n.code,{children:"F811 redefinition"})," and ",(0,r.jsx)(n.code,{children:"mypy [no-redef]"})," errors:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cd ~/platformops\ngrep -q "^def get_pull_request" src/platformops/httpclient.py || cat >> src/platformops/httpclient.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# Release-readiness endpoints (Module 26) -- five more read-only GitHub REST\n# calls, following the exact same shape `get_repo_info()` and\n# `list_workflow_runs()` already established: build the URL, go through\n# `_request_with_retry()`, raise `_raise_for_status()`, validate the body\n# against a Pydantic model before it leaves this module. Every one of these\n# is still a GET against GitHub\'s own API -- this module never triggers a\n# build, never merges a pull request, never creates a deployment. It only\n# reads what CI/CD already decided. `platformops.releasecheck` is the module\n# that combines what these functions return into one verdict; nothing here\n# knows what a "ready" release looks like -- that judgment lives one layer up.\n# ---------------------------------------------------------------------------\n\n\nclass PullRequestRef(BaseModel):\n    """One side (head or base) of a pull request -- just enough to say which branch and commit."""\n\n    ref: str\n    sha: str\n\n\nclass PullRequestInfo(BaseModel):\n    """The subset of a GitHub pull request payload `platformops.releasecheck` needs.\n\n    `mergeable_state` is GitHub\'s own summary of whether a PR can be merged\n    right now -- `"clean"` (no blockers), `"blocked"` (a required review or\n    status check has not passed), `"dirty"` (merge conflicts), `"draft"`,\n    `"behind"`, `"unstable"`, or `"unknown"` (GitHub has not finished\n    computing it yet -- a real, if unhelpful, value some payloads return).\n    This module reads it, it never recomputes it -- GitHub already owns that\n    calculation, including branch protection rules this project can\'t see.\n    """\n\n    number: int\n    state: str\n    draft: bool\n    merged: bool = False\n    mergeable_state: str | None = None\n    title: str\n    html_url: str\n    head: PullRequestRef\n    base: PullRequestRef\n\n\ndef get_pull_request(\n    owner: str,\n    repo: str,\n    number: int,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> dict[str, Any]:\n    """Fetch one pull request\'s merge/review state from `GET /repos/{owner}/{repo}/pulls/{number}`.\n\n    Raises `EndpointStatusError` for a PR number that does not exist (404),\n    and `ResponseFormatError` if the response is missing a field this\n    toolkit relies on. This function only reads the PR -- it never approves,\n    merges or comments on it.\n    """\n    url = f"{GITHUB_API}/repos/{owner}/{repo}/pulls/{number}"\n    logger.debug("fetching pull request %s/%s#%d", owner, repo, number)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        info = PullRequestInfo.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched pull request %s/%s#%d", owner, repo, number)\n    return info.model_dump()\n\n\nclass CheckRun(BaseModel):\n    """One check run\'s name, run status and conclusion.\n\n    `status` is where the run is in its lifecycle (`"queued"`,\n    `"in_progress"`, `"completed"`). `conclusion` is only meaningful once\n    `status` is `"completed"` -- GitHub sets it to `None` for a run still in\n    progress, which is exactly the "genuinely don\'t know yet" case this\n    toolkit is careful to report as unknown, not as a failure.\n    """\n\n    name: str\n    status: str\n    conclusion: str | None = None\n\n\ndef list_check_runs(\n    owner: str,\n    repo: str,\n    ref: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """List check runs for one commit or branch from `GET /repos/{owner}/{repo}/commits/{ref}/check-runs`.\n\n    `ref` is a branch name, tag or full commit SHA -- GitHub resolves any of\n    the three to the same commit. One page (`per_page=100`) covers every\n    real-world case this toolkit\'s lab and tests use; a repository running\n    more than 100 check runs against one commit is far outside this\n    course\'s scope.\n    """\n    url = f"{GITHUB_API}/repos/{owner}/{repo}/commits/{ref}/check-runs?per_page=100"\n    logger.debug("fetching check runs for %s/%s@%s", owner, repo, ref)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    if not isinstance(payload, dict) or "check_runs" not in payload:\n        raise ResponseFormatError(f"{url} response is missing \'check_runs\'")\n\n    try:\n        runs = [CheckRun.model_validate(raw) for raw in payload["check_runs"]]\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched %d check run(s) for %s/%s@%s", len(runs), owner, repo, ref)\n    return [run.model_dump() for run in runs]\n\n\nclass Artifact(BaseModel):\n    """One CI-produced build artifact\'s name, size and whether it has expired."""\n\n    name: str\n    size_in_bytes: int\n    expired: bool\n\n\ndef list_artifacts(\n    owner: str,\n    repo: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """List build artifacts for a repository from `GET /repos/{owner}/{repo}/actions/artifacts`.\n\n    An artifact GitHub still lists but marks `expired: true` is not\n    downloadable any more -- it aged out under the repository\'s retention\n    policy. `platformops.releasecheck` treats an expired artifact the same\n    as a missing one; this function just reports what GitHub said, honestly,\n    and leaves that judgment to the caller.\n    """\n    url = f"{GITHUB_API}/repos/{owner}/{repo}/actions/artifacts?per_page=100"\n    logger.debug("fetching artifacts for %s/%s", owner, repo)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    if not isinstance(payload, dict) or "artifacts" not in payload:\n        raise ResponseFormatError(f"{url} response is missing \'artifacts\'")\n\n    try:\n        artifacts = [Artifact.model_validate(raw) for raw in payload["artifacts"]]\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched %d artifact(s) for %s/%s", len(artifacts), owner, repo)\n    return [artifact.model_dump() for artifact in artifacts]\n\n\nclass ReleaseInfo(BaseModel):\n    """The subset of a GitHub release payload this toolkit reads."""\n\n    tag_name: str\n    name: str | None = None\n    draft: bool\n    prerelease: bool\n    published_at: str | None = None\n    html_url: str\n\n\ndef get_latest_release(\n    owner: str,\n    repo: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> dict[str, Any]:\n    """Fetch the newest published release from `GET /repos/{owner}/{repo}/releases/latest`.\n\n    A repository with no releases at all answers this endpoint with a real\n    404 -- `EndpointStatusError(status_code=404)` -- which is a normal,\n    expected outcome here (not every repository has cut a release yet), not\n    a sign anything is broken. The caller in `platformops.releasecheck`\n    is the layer that turns that 404 into "no release found" instead of\n    treating it as an unreachable source.\n    """\n    url = f"{GITHUB_API}/repos/{owner}/{repo}/releases/latest"\n    logger.debug("fetching latest release for %s/%s", owner, repo)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        info = ReleaseInfo.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched latest release for %s/%s: %s", owner, repo, info.tag_name)\n    return info.model_dump()\n\n\nclass Deployment(BaseModel):\n    """One deployment record -- including `payload`, the free-form JSON the deploying system attached.\n\n    GitHub\'s Deployments API does not define what `payload` contains; it is\n    whatever the system that created the deployment chose to record there.\n    This project\'s lab fixtures use it to carry the image reference (a tag\n    or a digest) a real deployment pipeline would set -- the same metadata\n    shape Module 27 builds and pushes for real.\n    """\n\n    id: int\n    sha: str\n    ref: str\n    environment: str\n    created_at: str\n    payload: dict[str, Any] = {}\n\n\ndef list_deployments(\n    owner: str,\n    repo: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> list[dict[str, Any]]:\n    """List deployment records from `GET /repos/{owner}/{repo}/deployments`, most recent first.\n\n    Unlike every other list endpoint in this module, GitHub returns this one\n    as a bare JSON array, not an object with a named key -- `_raise_for_status()`\n    and the retry path are identical, only the body shape differs.\n    """\n    url = f"{GITHUB_API}/repos/{owner}/{repo}/deployments?per_page=30"\n    logger.debug("fetching deployments for %s/%s", owner, repo)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    if not isinstance(payload, list):\n        raise ResponseFormatError(f"{url} response is not a JSON array")\n\n    try:\n        deployments = [Deployment.model_validate(raw) for raw in payload]\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched %d deployment(s) for %s/%s", len(deployments), owner, repo)\n    return [deployment.model_dump() for deployment in deployments]\nPYEOF\n'})}),"\n",(0,r.jsxs)(n.p,{children:["No output from this command when the functions are not yet present -- the guard writes\nsilently. If you re-run this step after the functions already exist, ",(0,r.jsx)(n.code,{children:"grep -q"})," finds them and\nthe whole ",(0,r.jsx)(n.code,{children:"cat >>"})," is skipped, so nothing changes and nothing is duplicated."]}),"\n",(0,r.jsx)(n.h3,{id:"step-2--add-the-fixture-bodies",children:"Step 2 \u2014 Add the fixture bodies"}),"\n",(0,r.jsx)(n.p,{children:"These are recorded GitHub API response shapes, not real API output \u2014 you will reuse them across\nthis whole lab and in the tests you write next."}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'mkdir -p tests/fixtures/github\n\ncat > tests/fixtures/github/pr_approved.json << \'PYEOF\'\n{\n  "number": 482,\n  "state": "closed",\n  "draft": false,\n  "merged": true,\n  "mergeable_state": "unknown",\n  "title": "Add retry budget to the payments webhook consumer",\n  "html_url": "https://github.com/acme-platform/payments/pull/482",\n  "head": { "ref": "retry-budget", "sha": "a1b2c3d4e5f60718293a4b5c6d7e8f9012345678" },\n  "base": { "ref": "main", "sha": "0f1e2d3c4b5a69788796a5b4c3d2e1f001234567" }\n}\nPYEOF\n\ncat > tests/fixtures/github/pr_pending.json << \'PYEOF\'\n{\n  "number": 491,\n  "state": "open",\n  "draft": false,\n  "merged": false,\n  "mergeable_state": "blocked",\n  "title": "Bump payment-processor client to 3.2.0",\n  "html_url": "https://github.com/acme-platform/payments/pull/491",\n  "head": { "ref": "bump-processor-client", "sha": "b2c3d4e5f60718293a4b5c6d7e8f90123456789a" },\n  "base": { "ref": "main", "sha": "0f1e2d3c4b5a69788796a5b4c3d2e1f001234567" }\n}\nPYEOF\n\ncat > tests/fixtures/github/workflow_success.json << \'PYEOF\'\n{\n  "total_count": 1,\n  "workflow_runs": [\n    {\n      "id": 4821093771,\n      "name": "CI",\n      "status": "completed",\n      "conclusion": "success",\n      "head_branch": "main",\n      "html_url": "https://github.com/acme-platform/payments/actions/runs/4821093771"\n    }\n  ]\n}\nPYEOF\n\ncat > tests/fixtures/github/check_runs_pass.json << \'PYEOF\'\n{\n  "total_count": 3,\n  "check_runs": [\n    { "name": "lint", "status": "completed", "conclusion": "success" },\n    { "name": "unit-tests", "status": "completed", "conclusion": "success" },\n    { "name": "integration-tests", "status": "completed", "conclusion": "success" }\n  ]\n}\nPYEOF\n\ncat > tests/fixtures/github/check_runs_pending.json << \'PYEOF\'\n{\n  "total_count": 3,\n  "check_runs": [\n    { "name": "lint", "status": "completed", "conclusion": "success" },\n    { "name": "unit-tests", "status": "completed", "conclusion": "success" },\n    { "name": "integration-tests", "status": "in_progress", "conclusion": null }\n  ]\n}\nPYEOF\n\ncat > tests/fixtures/github/artifacts_present.json << \'PYEOF\'\n{\n  "total_count": 1,\n  "artifacts": [\n    { "name": "payments-build", "size_in_bytes": 15423104, "expired": false }\n  ]\n}\nPYEOF\n\ncat > tests/fixtures/github/artifacts_missing.json << \'PYEOF\'\n{\n  "total_count": 0,\n  "artifacts": []\n}\nPYEOF\n\ncat > tests/fixtures/github/release_latest.json << \'PYEOF\'\n{\n  "tag_name": "v1.4.2",\n  "name": "v1.4.2",\n  "draft": false,\n  "prerelease": false,\n  "published_at": "2026-07-30T10:15:00Z",\n  "html_url": "https://github.com/acme-platform/payments/releases/tag/v1.4.2"\n}\nPYEOF\n\ncat > tests/fixtures/github/deployment.json << \'PYEOF\'\n[\n  {\n    "id": 900123,\n    "sha": "a1b2c3d4e5f60718293a4b5c6d7e8f9012345678",\n    "ref": "main",\n    "environment": "production",\n    "created_at": "2026-08-01T09:00:00Z",\n    "payload": {\n      "image": "ghcr.io/acme-platform/payments:1.4.2",\n      "digest": "sha256:9f2a5c3d1e0b7a6c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b7c6d5e4f3a2b1c"\n    }\n  }\n]\nPYEOF\n\nls tests/fixtures/github/\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"artifacts_missing.json\nartifacts_present.json\ncheck_runs_pass.json\ncheck_runs_pending.json\ndeployment.json\npr_approved.json\npr_pending.json\nrelease_latest.json\nworkflow_success.json\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Two more fixtures \u2014 ",(0,r.jsx)(n.code,{children:"check_runs_failed.json"})," and ",(0,r.jsx)(n.code,{children:"deployment_no_image.json"})," \u2014 cover extra failure\nshapes the test suite below exercises; the full set ships in the checkpoint if you want to compare."]}),"\n",(0,r.jsx)(n.h3,{id:"step-3--add-tests-for-the-new-endpoints-and-run-them",children:"Step 3 \u2014 Add tests for the new endpoints, and run them"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat >> tests/test_httpclient.py << \'PYEOF\'\n\n\n# ---------------------------------------------------------------------------\n# get_pull_request, list_check_runs, list_artifacts, get_latest_release,\n# list_deployments -- Module 26\'s five new GitHub endpoints.\n# ---------------------------------------------------------------------------\n\n\n@respx.mock\ndef test_get_pull_request_returns_expected_fields_for_a_merged_pr():\n    respx.get("https://api.github.com/repos/acme-platform/payments/pulls/482").mock(\n        return_value=httpx.Response(200, json=_fixture("pr_approved.json"))\n    )\n\n    pr = get_pull_request("acme-platform", "payments", 482)\n\n    assert pr["number"] == 482\n    assert pr["merged"] is True\n    assert pr["head"]["ref"] == "retry-budget"\n\n\n@respx.mock\ndef test_list_check_runs_returns_name_status_conclusion():\n    respx.get(\n        "https://api.github.com/repos/acme-platform/payments/commits/main/check-runs"\n    ).mock(return_value=httpx.Response(200, json=_fixture("check_runs_pending.json")))\n\n    runs = list_check_runs("acme-platform", "payments", "main")\n\n    assert len(runs) == 3\n    pending = [r for r in runs if r["status"] != "completed"]\n    assert pending == [\n        {"name": "integration-tests", "status": "in_progress", "conclusion": None}\n    ]\n\n\n@respx.mock\ndef test_list_artifacts_returns_empty_list_when_none_exist():\n    respx.get(\n        "https://api.github.com/repos/acme-platform/payments/actions/artifacts"\n    ).mock(return_value=httpx.Response(200, json=_fixture("artifacts_missing.json")))\n\n    assert list_artifacts("acme-platform", "payments") == []\n\n\n@respx.mock\ndef test_get_latest_release_raises_endpoint_status_error_on_404_no_releases(\n    monkeypatch,\n):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    respx.get(\n        "https://api.github.com/repos/acme-platform/brand-new-service/releases/latest"\n    ).mock(return_value=httpx.Response(404, json={"message": "Not Found"}))\n\n    with pytest.raises(EndpointStatusError) as exc_info:\n        get_latest_release("acme-platform", "brand-new-service")\n\n    assert exc_info.value.status_code == 404\n\n\n@respx.mock\ndef test_list_deployments_returns_the_payload_image_metadata():\n    respx.get("https://api.github.com/repos/acme-platform/payments/deployments").mock(\n        return_value=httpx.Response(200, json=_fixture("deployment.json"))\n    )\n\n    deployments = list_deployments("acme-platform", "payments")\n\n    assert len(deployments) == 1\n    assert deployments[0]["payload"]["image"] == "ghcr.io/acme-platform/payments:1.4.2"\nPYEOF\n'})}),"\n",(0,r.jsxs)(n.p,{children:["You also need a small ",(0,r.jsx)(n.code,{children:"_fixture()"})," helper and a few new imports at the top of the file \u2014 add\n",(0,r.jsx)(n.code,{children:"json"}),", ",(0,r.jsx)(n.code,{children:"from pathlib import Path"}),", ",(0,r.jsx)(n.code,{children:'FIXTURES = Path(__file__).parent / "fixtures" / "github"'}),", a\n",(0,r.jsx)(n.code,{children:"_fixture(name)"})," helper that loads and parses one fixture file, and ",(0,r.jsx)(n.code,{children:"get_latest_release"}),",\n",(0,r.jsx)(n.code,{children:"get_pull_request"}),", ",(0,r.jsx)(n.code,{children:"list_artifacts"}),", ",(0,r.jsx)(n.code,{children:"list_check_runs"}),", ",(0,r.jsx)(n.code,{children:"list_deployments"})," to the existing\n",(0,r.jsx)(n.code,{children:"from platformops.httpclient import (...)"})," block."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_httpclient.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".......................................                                  [100%]\n39 passed in 1.05s\n"})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsxs)(n.h2,{id:"phase-2--releasecheckpy-fetch-evidence-then-judge-it-separately",children:["Phase 2 \u2014 ",(0,r.jsx)(n.code,{children:"releasecheck.py"}),": fetch evidence, then judge it separately"]}),"\n",(0,r.jsx)(n.h3,{id:"step-1--write-the-module",children:"Step 1 \u2014 Write the module"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/releasecheck.py << \'PYEOF\'\n"""platformops.releasecheck -- combine GitHub\'s own CI/CD state into one release-readiness verdict.\n\nThis module never re-implements what a CI system already decided. It reads\nfive independent pieces of evidence GitHub already computed -- pull request\nmerge/review state, the latest workflow run\'s conclusion, individual\ncheck-run results, whether a build artifact exists, and the deployment\nrecord\'s image metadata -- and combines them into a single verdict. It never\nmerges a pull request, never re-runs a check, never triggers a build and\nnever creates a deployment. Same discipline `cloudaudit.py` (Module 24)\napplies to AWS resources, applied here to a repository\'s own CI/CD state:\nthis is an inspector, not a mechanic.\n\nThe fetch/aggregate split `cloudaudit.py` established carries over exactly.\nSix `gather_*_evidence()` functions are the only functions in this module\nthat call into `platformops.httpclient` -- each one talks to exactly one\nGitHub endpoint, catches every way that call can fail, and hands back the\nraw facts it found (or an honest "could not fetch this" result). None of\nthem decides pass or fail; none of them contains the words "ready" or\n"PASS" or "FAIL" anywhere in its body. `evaluate_release_readiness()` is the\nother half: a pure function that takes six already-gathered evidence dicts\nand a formal service/branch name, and returns a `ReleaseReadinessReport`. It\nnever imports `httpx`, never calls a `gather_*` function, and can be fully\nunit-tested by handing it evidence built by hand -- no network, no mocking,\nno fixtures required for that half of the test suite at all.\n\nThe honest-degradation rule this module exists to enforce: a source that\ncould not be reached is never treated as a pass, and never crashes the\nwhole report. `evaluate_release_readiness()` reports a fetch failure as\n`UNKNOWN` for that one section, and folds it into `sources_failed` --\nexactly the discipline `service_readiness.py` (Module 19) already applies\nto a cloud or Kubernetes adapter that does not exist yet, applied here to a\nGitHub call that failed for a different reason (a timeout, a 500, an\nauth error) instead of never having existed at all. A report that quietly\nturned an unreachable check-runs endpoint into a silent pass would be worse\nthan useless -- it would tell a release engineer "go ahead" on evidence\nthat was never actually collected.\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any\n\nfrom platformops.httpclient import (\n    EndpointStatusError,\n    EndpointUnreachableError,\n    HttpCheckError,\n    ResponseFormatError,\n    get_latest_release,\n    get_pull_request,\n    list_artifacts,\n    list_check_runs,\n    list_deployments,\n    list_workflow_runs,\n)\n\n# The same failure-conclusion set `service_readiness.py` (Module 19) already\n# uses for a workflow run\'s `conclusion` field. `None` (still running) is\n# deliberately not in this set -- an in-progress run is UNKNOWN, not FAIL.\n_FAILURE_CONCLUSIONS = {\n    "failure",\n    "cancelled",\n    "timed_out",\n    "action_required",\n    "startup_failure",\n}\n\n# conclusion values a completed check run can carry without it counting as a\n# failure -- "neutral" and "skipped" are real, valid outcomes GitHub reports\n# for a check that chose not to block a merge.\n_NON_FAILING_CHECK_CONCLUSIONS = {"success", "neutral", "skipped"}\n\n_FETCHABLE_HTTP_ERRORS = (\n    EndpointStatusError,\n    EndpointUnreachableError,\n    ResponseFormatError,\n    HttpCheckError,\n)\n\n\n# ---------------------------------------------------------------------------\n# Fetch -- one function per evidence source. These, and only these, call\n# into httpclient.py. Every one of them catches its own network failures and\n# hands back a plain dict of raw facts (or "fetched": False on failure) --\n# never a verdict, never the word "ready", "PASS" or "FAIL".\n# ---------------------------------------------------------------------------\n\n\ndef gather_pr_evidence(\n    owner: str, repo: str, number: int, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch one pull request\'s raw merge/review state. Reports what GitHub said, decides nothing."""\n    try:\n        pr = get_pull_request(owner, repo, number, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    return {\n        "fetched": True,\n        "number": pr["number"],\n        "state": pr["state"],\n        "draft": pr["draft"],\n        "merged": pr["merged"],\n        "mergeable_state": pr["mergeable_state"],\n    }\n\n\ndef gather_ci_evidence(\n    owner: str, repo: str, branch: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch the most recent workflow run for `branch`. `conclusion` is None if none is found."""\n    try:\n        runs = list_workflow_runs(owner, repo, token=token, max_pages=1)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n\n    for run in runs:\n        if run.get("head_branch") == branch:\n            return {\n                "fetched": True,\n                "status": run.get("status"),\n                "conclusion": run.get("conclusion"),\n            }\n    return {"fetched": True, "status": None, "conclusion": None}\n\n\ndef gather_check_runs_evidence(\n    owner: str, repo: str, ref: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch every check run for `ref`, reduced to pass/fail/pending names -- no verdict here."""\n    try:\n        runs = list_check_runs(owner, repo, ref, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n\n    failed = [\n        run["name"]\n        for run in runs\n        if run["status"] == "completed"\n        and run["conclusion"] not in _NON_FAILING_CHECK_CONCLUSIONS\n    ]\n    pending = [run["name"] for run in runs if run["status"] != "completed"]\n    passed = sum(\n        1\n        for run in runs\n        if run["status"] == "completed"\n        and run["conclusion"] in _NON_FAILING_CHECK_CONCLUSIONS\n    )\n    return {\n        "fetched": True,\n        "total": len(runs),\n        "passed": passed,\n        "failed": failed,\n        "pending": pending,\n    }\n\n\ndef gather_artifact_evidence(\n    owner: str, repo: str, artifact_name: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch the repository\'s artifacts and report whether `artifact_name` is present and unexpired."""\n    try:\n        artifacts = list_artifacts(owner, repo, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n\n    matching = [a for a in artifacts if a["name"] == artifact_name and not a["expired"]]\n    return {\n        "fetched": True,\n        "present": bool(matching),\n        "matched": matching[0] if matching else None,\n    }\n\n\ndef gather_release_evidence(\n    owner: str, repo: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch the latest published release. A 404 (no release yet) is a real, known answer, not a failure."""\n    try:\n        release = get_latest_release(owner, repo, token=token)\n    except EndpointStatusError as exc:\n        if exc.status_code == 404:\n            return {"fetched": True, "found": False}\n        return {"fetched": False, "error": str(exc)}\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n\n    return {\n        "fetched": True,\n        "found": True,\n        "tag_name": release["tag_name"],\n        "published_at": release["published_at"],\n    }\n\n\ndef gather_deployment_evidence(\n    owner: str, repo: str, environment: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch the most recent deployment record for `environment` and its image metadata.\n\n    This reads a deployment\'s `payload` for an `image`/`digest` reference --\n    it never creates a deployment, and it never builds, pulls or inspects\n    the image itself. Validating the image actually exists in a registry is\n    a container-tooling concern, out of scope here (see Module 27).\n    """\n    try:\n        deployments = list_deployments(owner, repo, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n\n    matching = [d for d in deployments if d["environment"] == environment]\n    if not matching:\n        return {"fetched": True, "found": False, "image": None, "digest": None}\n\n    latest = matching[0]\n    image_payload = latest.get("payload") or {}\n    return {\n        "fetched": True,\n        "found": True,\n        "image": image_payload.get("image"),\n        "digest": image_payload.get("digest"),\n    }\n\n\n# ---------------------------------------------------------------------------\n# Aggregate -- pure functions. Nothing below this line imports httpx, calls\n# a gather_* function, or makes a network call of any kind. Each _evaluate_*\n# helper takes one already-gathered evidence dict and returns a\n# {"status": ..., "detail": ...} section; evaluate_release_readiness()\n# combines all six into one ReleaseReadinessReport.\n# ---------------------------------------------------------------------------\n\n\n@dataclass\nclass ReleaseReadinessReport:\n    """One release\'s combined readiness, with every section\'s status and which sources actually answered.\n\n    `verdict` is `"ready"` only when every gating section (`pr`, `ci`,\n    `checks`, `artifacts`, `deployment`) came back `PASS` -- `release` is\n    informational and never gates the verdict on its own, the same way\n    `service_readiness.py`\'s cloud/kubernetes sections inform a report\n    without being required to reach `"high"` confidence. `sources_failed`\n    lists which evidence sources this run could not even reach -- a report\n    with a non-empty `sources_failed` never reports `"ready"`, no matter\n    what the sources that did answer said.\n    """\n\n    service: str\n    branch: str\n    pr: dict[str, Any]\n    ci: dict[str, Any]\n    checks: dict[str, Any]\n    artifacts: dict[str, Any]\n    release: dict[str, Any]\n    deployment: dict[str, Any]\n    verdict: str\n    sources_ok: list[str]\n    sources_failed: list[str]\n\n\ndef _evaluate_pr(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "PR evidence unavailable"),\n        }\n    if evidence["merged"]:\n        return {"status": "PASS", "detail": f"PR #{evidence[\'number\']} merged"}\n    if evidence["draft"]:\n        return {\n            "status": "FAIL",\n            "detail": f"PR #{evidence[\'number\']} is still a draft",\n        }\n    if evidence["mergeable_state"] == "clean":\n        return {\n            "status": "PASS",\n            "detail": f"PR #{evidence[\'number\']} open, approved and mergeable",\n        }\n    return {\n        "status": "FAIL",\n        "detail": (\n            f"PR #{evidence[\'number\']} not ready to merge "\n            f"(mergeable_state={evidence[\'mergeable_state\']})"\n        ),\n    }\n\n\ndef _evaluate_ci(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "CI evidence unavailable"),\n        }\n    conclusion = evidence.get("conclusion")\n    if conclusion == "success":\n        return {"status": "PASS", "detail": "latest workflow run succeeded"}\n    if conclusion in _FAILURE_CONCLUSIONS:\n        return {\n            "status": "FAIL",\n            "detail": f"latest workflow run concluded \'{conclusion}\'",\n        }\n    return {\n        "status": "UNKNOWN",\n        "detail": "no completed workflow run found for this branch",\n    }\n\n\ndef _evaluate_checks(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "check-run evidence unavailable"),\n        }\n    if evidence["failed"]:\n        return {\n            "status": "FAIL",\n            "detail": f"failing check(s): {\', \'.join(evidence[\'failed\'])}",\n        }\n    if evidence["pending"]:\n        return {\n            "status": "UNKNOWN",\n            "detail": f"pending check(s): {\', \'.join(evidence[\'pending\'])}",\n        }\n    if evidence["total"] == 0:\n        return {"status": "UNKNOWN", "detail": "no check runs reported for this commit"}\n    return {\n        "status": "PASS",\n        "detail": f"all {evidence[\'total\']} check run(s) passed",\n    }\n\n\ndef _evaluate_artifact(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "artifact evidence unavailable"),\n        }\n    if evidence["present"]:\n        size = evidence["matched"]["size_in_bytes"]\n        return {"status": "PASS", "detail": f"build artifact present ({size} bytes)"}\n    return {"status": "FAIL", "detail": "expected build artifact not found"}\n\n\ndef _evaluate_release(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "release evidence unavailable"),\n        }\n    if evidence["found"]:\n        return {\n            "status": "INFO",\n            "detail": f"latest release {evidence[\'tag_name\']} (published {evidence[\'published_at\']})",\n        }\n    return {"status": "INFO", "detail": "no release published yet"}\n\n\ndef _evaluate_deployment(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "deployment evidence unavailable"),\n        }\n    if not evidence["found"]:\n        return {\n            "status": "FAIL",\n            "detail": "no deployment record found for this environment",\n        }\n    if not evidence.get("image"):\n        return {"status": "FAIL", "detail": "deployment record has no image reference"}\n    return {\n        "status": "PASS",\n        "detail": f"deployment targets image {evidence[\'image\']}",\n    }\n\n\ndef evaluate_release_readiness(\n    *,\n    service: str,\n    branch: str,\n    pr_evidence: dict[str, Any],\n    ci_evidence: dict[str, Any],\n    checks_evidence: dict[str, Any],\n    artifact_evidence: dict[str, Any],\n    release_evidence: dict[str, Any],\n    deployment_evidence: dict[str, Any],\n) -> ReleaseReadinessReport:\n    """Combine six already-gathered evidence dicts into one report. Pure -- no network call, ever.\n\n    A source that failed to fetch (`"fetched": False`) never becomes a\n    silent `PASS` here -- it becomes an `UNKNOWN` section and a name in\n    `sources_failed`, and an `UNKNOWN` in any gating section keeps the\n    overall verdict at `"not_ready"`, the same as an outright `FAIL`. This\n    is the one rule this whole module exists to enforce: incomplete\n    evidence is never treated as good evidence.\n    """\n    sources = {\n        "pr": pr_evidence,\n        "ci": ci_evidence,\n        "checks": checks_evidence,\n        "artifacts": artifact_evidence,\n        "release": release_evidence,\n        "deployment": deployment_evidence,\n    }\n    sources_ok = [name for name, ev in sources.items() if ev.get("fetched")]\n    sources_failed = [name for name, ev in sources.items() if not ev.get("fetched")]\n\n    pr_section = _evaluate_pr(pr_evidence)\n    ci_section = _evaluate_ci(ci_evidence)\n    checks_section = _evaluate_checks(checks_evidence)\n    artifact_section = _evaluate_artifact(artifact_evidence)\n    release_section = _evaluate_release(release_evidence)\n    deployment_section = _evaluate_deployment(deployment_evidence)\n\n    # release_section is informational only -- it never gates the verdict.\n    gating_sections = [\n        pr_section,\n        ci_section,\n        checks_section,\n        artifact_section,\n        deployment_section,\n    ]\n    has_fail = any(section["status"] == "FAIL" for section in gating_sections)\n    has_unknown = any(section["status"] == "UNKNOWN" for section in gating_sections)\n    verdict = "ready" if not has_fail and not has_unknown else "not_ready"\n\n    return ReleaseReadinessReport(\n        service=service,\n        branch=branch,\n        pr=pr_section,\n        ci=ci_section,\n        checks=checks_section,\n        artifacts=artifact_section,\n        release=release_section,\n        deployment=deployment_section,\n        verdict=verdict,\n        sources_ok=sources_ok,\n        sources_failed=sources_failed,\n    )\n\n\ndef run_release_check(\n    *,\n    owner: str,\n    repo: str,\n    branch: str,\n    pr: int,\n    service: str,\n    environment: str = "production",\n    artifact_name: str | None = None,\n    token: str | None = None,\n) -> ReleaseReadinessReport:\n    """Gather every evidence source and evaluate it -- the one call a CLI command or a script needs.\n\n    This is the only function in this module that both fetches evidence and\n    calls the aggregation function -- it is intentionally a thin\n    orchestrator, not a place where fetching and judging blend together.\n    """\n    resolved_artifact_name = artifact_name or f"{service}-build"\n\n    pr_evidence = gather_pr_evidence(owner, repo, pr, token=token)\n    ci_evidence = gather_ci_evidence(owner, repo, branch, token=token)\n    checks_evidence = gather_check_runs_evidence(owner, repo, branch, token=token)\n    artifact_evidence = gather_artifact_evidence(\n        owner, repo, resolved_artifact_name, token=token\n    )\n    release_evidence = gather_release_evidence(owner, repo, token=token)\n    deployment_evidence = gather_deployment_evidence(\n        owner, repo, environment, token=token\n    )\n\n    return evaluate_release_readiness(\n        service=service,\n        branch=branch,\n        pr_evidence=pr_evidence,\n        ci_evidence=ci_evidence,\n        checks_evidence=checks_evidence,\n        artifact_evidence=artifact_evidence,\n        release_evidence=release_evidence,\n        deployment_evidence=deployment_evidence,\n    )\nPYEOF\n'})}),"\n",(0,r.jsx)(n.p,{children:"No output from this command \u2014 it writes the file silently. The next steps confirm it landed\ncorrectly."}),"\n",(0,r.jsxs)(n.p,{children:["Notice the two clearly separated halves: everything above the second ",(0,r.jsx)(n.code,{children:"# ---"})," banner calls into\n",(0,r.jsx)(n.code,{children:"httpclient.py"}),"; everything below it never does. That is not a comment you have to trust \u2014 the\nDeep Dive proves it with a real grep."]}),"\n",(0,r.jsxs)(n.h3,{id:"step-2--write-teststest_releasecheckpy",children:["Step 2 \u2014 Write ",(0,r.jsx)(n.code,{children:"tests/test_releasecheck.py"})]}),"\n",(0,r.jsxs)(n.p,{children:["This file has two parts that need no shared setup: ",(0,r.jsx)(n.code,{children:"respx"}),"-mocked tests for each ",(0,r.jsx)(n.code,{children:"gather_*"}),"\nfunction against the fixtures from Phase 1, and plain, no-mocking tests for\n",(0,r.jsx)(n.code,{children:"evaluate_release_readiness()"})," that build evidence dicts by hand."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_releasecheck.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\n\nfrom platformops.releasecheck import (\n    evaluate_release_readiness,\n    gather_pr_evidence,\n)\n\nFIXTURES = Path(__file__).parent / "fixtures" / "github"\n\n\ndef _fixture(name: str) -> dict:\n    return json.loads((FIXTURES / name).read_text())\n\n\nOWNER = "acme-platform"\nREPO = "payments"\n\n\n@respx.mock\ndef test_gather_pr_evidence_reports_a_merged_pr():\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/pulls/482").mock(\n        return_value=httpx.Response(200, json=_fixture("pr_approved.json"))\n    )\n\n    evidence = gather_pr_evidence(OWNER, REPO, 482)\n\n    assert evidence["fetched"] is True\n    assert evidence["merged"] is True\n\n\n@respx.mock\ndef test_gather_pr_evidence_degrades_honestly_on_a_404(monkeypatch):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/pulls/9999").mock(\n        return_value=httpx.Response(404, json={"message": "Not Found"})\n    )\n\n    evidence = gather_pr_evidence(OWNER, REPO, 9999)\n\n    assert evidence["fetched"] is False\n    assert "404" in evidence["error"]\n\n\n_PASS_PR = {\n    "fetched": True,\n    "number": 482,\n    "state": "closed",\n    "draft": False,\n    "merged": True,\n    "mergeable_state": "unknown",\n}\n_PASS_CI = {"fetched": True, "status": "completed", "conclusion": "success"}\n_PASS_CHECKS = {"fetched": True, "total": 3, "passed": 3, "failed": [], "pending": []}\n_PASS_ARTIFACT = {\n    "fetched": True,\n    "present": True,\n    "matched": {"name": "payments-build", "size_in_bytes": 15423104, "expired": False},\n}\n_PASS_RELEASE = {\n    "fetched": True,\n    "found": True,\n    "tag_name": "v1.4.2",\n    "published_at": "2026-07-30T10:15:00Z",\n}\n_PASS_DEPLOYMENT = {\n    "fetched": True,\n    "found": True,\n    "image": "ghcr.io/acme-platform/payments:1.4.2",\n    "digest": "sha256:9f2a5c3d1e0b7a6c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b7c6d5e4f3a2b1c",\n}\n\n\ndef test_evaluate_release_readiness_is_ready_when_every_gating_section_passes():\n    report = evaluate_release_readiness(\n        service="payments",\n        branch="main",\n        pr_evidence=_PASS_PR,\n        ci_evidence=_PASS_CI,\n        checks_evidence=_PASS_CHECKS,\n        artifact_evidence=_PASS_ARTIFACT,\n        release_evidence=_PASS_RELEASE,\n        deployment_evidence=_PASS_DEPLOYMENT,\n    )\n\n    assert report.verdict == "ready"\n    assert report.sources_failed == []\n\n\ndef test_evaluate_release_readiness_degrades_honestly_when_checks_are_unreachable():\n    report = evaluate_release_readiness(\n        service="payments",\n        branch="main",\n        pr_evidence=_PASS_PR,\n        ci_evidence=_PASS_CI,\n        checks_evidence={\n            "fetched": False,\n            "error": "payments unreachable after 5 attempt(s): connection refused",\n        },\n        artifact_evidence=_PASS_ARTIFACT,\n        release_evidence=_PASS_RELEASE,\n        deployment_evidence=_PASS_DEPLOYMENT,\n    )\n\n    assert report.verdict == "not_ready"\n    assert report.checks["status"] == "UNKNOWN"\n    assert report.sources_failed == ["checks"]\nPYEOF\n'})}),"\n",(0,r.jsx)(n.admonition,{title:"This is a shortened version",type:"info",children:(0,r.jsxs)(n.p,{children:["The full ",(0,r.jsx)(n.code,{children:"tests/test_releasecheck.py"})," you get in the checkpoint covers every ",(0,r.jsx)(n.code,{children:"gather_*"})," function\n(both its pass and its degraded-source path), every ",(0,r.jsx)(n.code,{children:"_evaluate_*"})," branch, and ",(0,r.jsx)(n.code,{children:"run_release_check()"}),"\nend to end with all six endpoints mocked at once. What you just wrote above is the shape \u2014 extend it\nwith the remaining scenarios (a blocked PR, a failed workflow run, a missing artifact, a source that\nraises for every gating section, not just ",(0,r.jsx)(n.code,{children:"checks"}),") the same way. If you would rather compare\nagainst the finished file than write every case by hand, copy it in from\n",(0,r.jsx)(n.code,{children:"~/307-aipython-labs/labs/checkpoints/v2.1.0/tests/test_releasecheck.py"}),"."]})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_releasecheck.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"....                                                                      [100%]\n4 passed in 0.22s\n"})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-3--wire-the-cli-then-run-it-live-against-both-scenarios",children:"Phase 3 \u2014 Wire the CLI, then run it live against both scenarios"}),"\n",(0,r.jsxs)(n.h3,{id:"step-1--add-the-release-check-command",children:["Step 1 \u2014 Add the ",(0,r.jsx)(n.code,{children:"release-check"})," command"]}),"\n",(0,r.jsxs)(n.p,{children:["Add the import in alphabetical order among the other ",(0,r.jsx)(n.code,{children:"platformops.*"})," imports in\n",(0,r.jsx)(n.code,{children:"src/platformops/cli.py"})," -- it sorts between ",(0,r.jsx)(n.code,{children:"multiregion"})," and ",(0,r.jsx)(n.code,{children:"reportstore"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/cli.py"',children:"from platformops.multiregion import ResourceRecord, scan_regions, to_csv, to_markdown\nfrom platformops.releasecheck import run_release_check\nfrom platformops.reportstore import upload_report_to_bucket\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Add the command itself, right after ",(0,r.jsx)(n.code,{children:"remediate-execute-batch"})," and before ",(0,r.jsx)(n.code,{children:"report-upload"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/cli.py"',children:'@app.command("release-check")\ndef release_check(\n    ctx: typer.Context,\n    service: Annotated[\n        str,\n        typer.Argument(\n            help="service name, e.g. payments -- also the default repo name"\n        ),\n    ],\n    owner: Annotated[\n        str,\n        typer.Option(\n            "--owner", help="GitHub organization or user that owns the repository"\n        ),\n    ],\n    pr: Annotated[\n        int,\n        typer.Option(\n            "--pr", help="pull request number this release is tracked against"\n        ),\n    ],\n    repo: Annotated[\n        str | None,\n        typer.Option(\n            "--repo", help="repository name, if different from the service name"\n        ),\n    ] = None,\n    branch: Annotated[\n        str, typer.Option("--branch", help="branch this release is cut from")\n    ] = "main",\n    environment: Annotated[\n        str,\n        typer.Option(\n            "--environment", help="deployment environment to check, e.g. production"\n        ),\n    ] = "production",\n    artifact_name: Annotated[\n        str | None,\n        typer.Option(\n            "--artifact-name",\n            help="expected build artifact name (defaults to \'<service>-build\')",\n        ),\n    ] = None,\n    as_json: Annotated[\n        bool,\n        typer.Option(\n            "--json", help="print a machine-readable JSON report instead of plain text"\n        ),\n    ] = False,\n) -> None:\n    """Combine PR, CI, check-run, artifact, release and deployment evidence into one release verdict.\n\n    Read-only: this command never merges the pull request, never re-runs a\n    check, never triggers a build and never creates a deployment -- it only\n    reads what GitHub\'s own CI/CD state already says. A source this command\n    cannot reach (a timeout, a rate limit, an auth failure) is reported as\n    `UNKNOWN` for that one section, never silently treated as a pass -- see\n    `platformops.releasecheck` for the full fetch/evaluate split. Exit 0\n    only when every gating section (`pr`, `ci`, `checks`, `artifacts`,\n    `deployment`) is `PASS`; exit 1 otherwise.\n    """\n    quiet = ctx.obj["quiet"]\n\n    report = run_release_check(\n        owner=owner,\n        repo=repo or service,\n        branch=branch,\n        pr=pr,\n        service=service,\n        environment=environment,\n        artifact_name=artifact_name,\n        token=os.environ.get("GITHUB_TOKEN"),\n    )\n\n    if as_json:\n        typer.echo(\n            json.dumps(\n                {\n                    "status": "ok",\n                    "service": report.service,\n                    "branch": report.branch,\n                    "verdict": report.verdict,\n                    "pr": report.pr,\n                    "ci": report.ci,\n                    "checks": report.checks,\n                    "artifacts": report.artifacts,\n                    "release": report.release,\n                    "deployment": report.deployment,\n                    "sources_ok": report.sources_ok,\n                    "sources_failed": report.sources_failed,\n                },\n                indent=2,\n            )\n        )\n        raise typer.Exit(code=0 if report.verdict == "ready" else 1)\n\n    if not quiet or report.verdict != "ready":\n        typer.echo(f"{report.service} ({report.branch}): {report.verdict.upper()}")\n    for label, section in (\n        ("pr", report.pr),\n        ("ci", report.ci),\n        ("checks", report.checks),\n        ("artifacts", report.artifacts),\n        ("release", report.release),\n        ("deployment", report.deployment),\n    ):\n        typer.echo(f"  [{section[\'status\']}] {label}: {section[\'detail\']}")\n    if report.sources_failed:\n        typer.echo(f"  unreachable source(s): {\', \'.join(report.sources_failed)}")\n    raise typer.Exit(code=0 if report.verdict == "ready" else 1)\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run platformops release-check --help\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:" Usage: platformops release-check [OPTIONS] {service}\n\n Combine PR, CI, check-run, artifact, release and deployment evidence into one\n release verdict.\n\n Read-only: this command never merges the pull request, never re-runs a\n check, never triggers a build and never creates a deployment -- it only\n reads what GitHub's own CI/CD state already says. A source this command\n cannot reach (a timeout, a rate limit, an auth failure) is reported as\n `UNKNOWN` for that one section, never silently treated as a pass -- see\n `platformops.releasecheck` for the full fetch/evaluate split. Exit 0\n only when every gating section (`pr`, `ci`, `checks`, `artifacts`,\n `deployment`) is `PASS`; exit 1 otherwise.\n\n\u256D\u2500 Arguments \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 *    service      <str>  service name, e.g. payments -- also the default     \u2502\n\u2502                          repo name                                           \u2502\n\u2502                          [required]                                          \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n\u256D\u2500 Options \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E\n\u2502 *  --owner                <str>  GitHub organization or user that owns the   \u2502\n\u2502                                  repository                                  \u2502\n\u2502                                  [required]                                  \u2502\n\u2502 *  --pr                   <int>  pull request number this release is tracked \u2502\n\u2502                                  against                                     \u2502\n\u2502                                  [required]                                  \u2502\n\u2502    --repo                 <str>  repository name, if different from the      \u2502\n\u2502                                  service name                                \u2502\n\u2502    --branch               <str>  branch this release is cut from             \u2502\n\u2502                                  [default: main]                             \u2502\n\u2502    --environment          <str>  deployment environment to check, e.g.       \u2502\n\u2502                                  production                                  \u2502\n\u2502                                  [default: production]                       \u2502\n\u2502    --artifact-name        <str>  expected build artifact name (defaults to   \u2502\n\u2502                                  '<service>-build')                          \u2502\n\u2502    --json                        print a machine-readable JSON report        \u2502\n\u2502                                  instead of plain text                       \u2502\n\u2502    --help                        Show this message and exit.                 \u2502\n\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F\n"})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--run-it-live-against-the-fixtures-a-full-pass",children:"Step 2 \u2014 Run it live against the fixtures: a full pass"}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"release-check"})," calls the real GitHub API by default \u2014 for this lab, you run it inside a small\nscript that patches those calls with ",(0,r.jsx)(n.code,{children:"respx"}),", pointed at the fixtures you already wrote. This is\nthe same technique Module 21's Phase 3 used with ",(0,r.jsx)(n.code,{children:"moto"})," to demonstrate ",(0,r.jsx)(n.code,{children:"scan_inventory()"})," without a\nreal AWS account: import the real, unmodified code, and swap only the network layer underneath it."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m26_release_check_demo.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/github")\nOWNER = "acme-platform"\nREPO = "payments"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/pulls/482").mock(\n        return_value=httpx.Response(200, json=_fixture("pr_approved.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/actions/runs").mock(\n        return_value=httpx.Response(200, json=_fixture("workflow_success.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/commits/main/check-runs").mock(\n        return_value=httpx.Response(200, json=_fixture("check_runs_pass.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/actions/artifacts").mock(\n        return_value=httpx.Response(200, json=_fixture("artifacts_present.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/releases/latest").mock(\n        return_value=httpx.Response(200, json=_fixture("release_latest.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/deployments").mock(\n        return_value=httpx.Response(200, json=_fixture("deployment.json"))\n    )\n\n    result = runner.invoke(app, ["release-check", "payments", "--owner", OWNER, "--pr", "482"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m26_release_check_demo.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"payments (main): READY\n  [PASS] pr: PR #482 merged\n  [PASS] ci: latest workflow run succeeded\n  [PASS] checks: all 3 check run(s) passed\n  [PASS] artifacts: build artifact present (15423104 bytes)\n  [INFO] release: latest release v1.4.2 (published 2026-07-30T10:15:00Z)\n  [PASS] deployment: deployment targets image ghcr.io/acme-platform/payments:1.4.2\n\nexit code: 0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Every gating section is ",(0,r.jsx)(n.code,{children:"PASS"}),", ",(0,r.jsx)(n.code,{children:"release"})," is ",(0,r.jsx)(n.code,{children:"INFO"})," (it never gates), and the exit code is ",(0,r.jsx)(n.code,{children:"0"})," \u2014 a\nrelease pipeline can gate a deploy step on this exit code directly, no JSON parsing required."]}),"\n",(0,r.jsx)(n.h3,{id:"step-3--run-it-live-against-the-fixtures-a-partial-failure",children:"Step 3 \u2014 Run it live against the fixtures: a partial failure"}),"\n",(0,r.jsxs)(n.p,{children:["Change three of the six mocked endpoints \u2014 a pull request that is not yet mergeable, one check run\nstill in progress, and a missing build artifact \u2014 and run the same command against pull request\n",(0,r.jsx)(n.code,{children:"#491"})," instead of ",(0,r.jsx)(n.code,{children:"#482"}),"."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m26_release_check_demo_fail.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/github")\nOWNER = "acme-platform"\nREPO = "payments"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/pulls/491").mock(\n        return_value=httpx.Response(200, json=_fixture("pr_pending.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/actions/runs").mock(\n        return_value=httpx.Response(200, json=_fixture("workflow_success.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/commits/main/check-runs").mock(\n        return_value=httpx.Response(200, json=_fixture("check_runs_pending.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/actions/artifacts").mock(\n        return_value=httpx.Response(200, json=_fixture("artifacts_missing.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/releases/latest").mock(\n        return_value=httpx.Response(200, json=_fixture("release_latest.json"))\n    )\n    respx.get(f"https://api.github.com/repos/{OWNER}/{REPO}/deployments").mock(\n        return_value=httpx.Response(200, json=_fixture("deployment.json"))\n    )\n\n    result = runner.invoke(app, ["release-check", "payments", "--owner", OWNER, "--pr", "491"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m26_release_check_demo_fail.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"payments (main): NOT_READY\n  [FAIL] pr: PR #491 not ready to merge (mergeable_state=blocked)\n  [PASS] ci: latest workflow run succeeded\n  [UNKNOWN] checks: pending check(s): integration-tests\n  [FAIL] artifacts: expected build artifact not found\n  [INFO] release: latest release v1.4.2 (published 2026-07-30T10:15:00Z)\n  [PASS] deployment: deployment targets image ghcr.io/acme-platform/payments:1.4.2\n\nexit code: 1\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Three different reasons this release is not ready, side by side: a real ",(0,r.jsx)(n.code,{children:"FAIL"})," on the pull request\n(blocked, not approved), a real ",(0,r.jsx)(n.code,{children:"FAIL"})," on the artifact (never built), and an ",(0,r.jsx)(n.code,{children:"UNKNOWN"})," on checks (a\ntest still running, not a failure \u2014 the report is careful not to call that a ",(0,r.jsx)(n.code,{children:"FAIL"}),"). All three keep\nthe verdict at ",(0,r.jsx)(n.code,{children:"not_ready"})," and the exit code at ",(0,r.jsx)(n.code,{children:"1"}),", but a reader can tell at a glance which of the\nthree actually needs attention right now versus which one just needs a few more minutes."]}),"\n",(0,r.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:(0,r.jsx)(n.code,{children:"ModuleNotFoundError: No module named 'respx'"})})," \u2014 ",(0,r.jsx)(n.code,{children:"respx"})," has been a dev dependency since\nModule 9; run ",(0,r.jsx)(n.code,{children:"uv sync"})," to make sure your environment is current."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"The demo script prints a real network error instead of the fixture data"})," \u2014 confirm the\n",(0,r.jsx)(n.code,{children:"with respx.mock:"})," block wraps the ",(0,r.jsx)(n.code,{children:"runner.invoke(...)"})," call itself, not just the ",(0,r.jsx)(n.code,{children:"respx.get(...)"}),"\nlines above it. ",(0,r.jsx)(n.code,{children:"respx.mock"})," only intercepts ",(0,r.jsx)(n.code,{children:"httpx"})," calls made while its context is active."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"release-check"})," exits 0 when you expected 1, or the other way round"]})," \u2014 check which pull\nrequest number you passed. ",(0,r.jsx)(n.code,{children:"#482"})," is wired to the PASS fixtures in this lab; ",(0,r.jsx)(n.code,{children:"#491"})," is wired to\nthe FAIL ones. Passing ",(0,r.jsx)(n.code,{children:"--pr 482"})," against the FAIL scenario's mocks (or vice versa) gives you a\n",(0,r.jsx)(n.code,{children:"respx.mock"})," error instead, because no route matches \u2014 that error itself is a useful signal you\nmixed up the two scripts."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:["A test in ",(0,r.jsx)(n.code,{children:"tests/test_releasecheck.py"})," hits the real network"]})," \u2014 the pure ",(0,r.jsx)(n.code,{children:"evaluate_release_readiness()"}),"\ntests never should (they take plain dicts, no ",(0,r.jsx)(n.code,{children:"respx"})," needed at all); a ",(0,r.jsx)(n.code,{children:"gather_*"})," test that does\nis missing its ",(0,r.jsx)(n.code,{children:"@respx.mock"})," decorator."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"--pr"}),' shows as required and your command fails with "Missing option"']})," \u2014 this command has no\ndefault pull request number on purpose; a real release-readiness check always ties to a specific\nPR, the same way a real CI job knows its own PR number from the event that triggered it."]}),"\n"]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-4--full-quality-gate-and-version-bump",children:"Phase 4 \u2014 Full quality gate and version bump"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 41%]\n........................................................................ [ 62%]\n........................................................................ [ 83%]\n.......................................................                  [100%]\n343 passed in 39.92s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Your count will differ from the checkpoint's ",(0,r.jsx)(n.code,{children:"365"})," -- this lab's Phase 2 Step 2 has you write the\nshortened test shape, not the full extended file, so this count reflects that shape plus the rest\nof the project's existing suite."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format --check src/ tests/\nuv run mypy src/\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"All checks passed!\n44 files already formatted\nSuccess: no issues found in 22 source files\n"})}),"\n",(0,r.jsx)(n.p,{children:"Bump the version and update the changelog:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run python -c \"\nimport re\npath = 'pyproject.toml'\ntext = open(path).read()\ntext = text.replace('version = \\\"2.0.0\\\"', 'version = \\\"2.1.0\\\"', 1)\nopen(path, 'w').write(text)\n\"\nuv sync\nuv run platformops version\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 2.1.0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"## [2.1.0] - Release Readiness Checker"})," section to ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"}),", above the previous\nrelease, following the same ",(0,r.jsx)(n.code,{children:"### Added"})," structure every prior release entry already uses."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'git add -A\ngit commit -m "platformops v2.1.0 -- Release Readiness Checker"\ngit tag v2.1.0\n'})}),"\n",(0,r.jsx)(n.admonition,{title:"Lost your progress? Compare against the checkpoint",type:"info",children:(0,r.jsxs)(n.p,{children:["The instructor's copy of this exact ",(0,r.jsx)(n.code,{children:"v2.1.0"})," end state ships in the labs repository at\n",(0,r.jsx)(n.code,{children:"307-aipython-labs/labs/checkpoints/v2.1.0"}),". If your project stops passing the gates, clone or pull\nthat repository and compare ",(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"}),", ",(0,r.jsx)(n.code,{children:"src/platformops/releasecheck.py"})," and\n",(0,r.jsx)(n.code,{children:"src/platformops/cli.py"})," against yours file by file."]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,r.jsxs)(n.p,{children:["Every evidence source in this lab is a ",(0,r.jsx)(n.code,{children:"respx"}),"-mocked fixture, replayed in-process \u2014 there is no\nreal GitHub state, no container, and no cluster this lab started. There is nothing external to tear\ndown."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -f /tmp/m26_release_check_demo.py /tmp/m26_release_check_demo_fail.py\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"}),"'s five new endpoints, ",(0,r.jsx)(n.code,{children:"src/platformops/releasecheck.py"}),", the\n",(0,r.jsx)(n.code,{children:"release-check"})," CLI command, ",(0,r.jsx)(n.code,{children:"tests/fixtures/github/"}),", the ",(0,r.jsx)(n.code,{children:"v2.1.0"})," version bump and the\n",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," entry all stay \u2014 they are permanent additions to the project, and carry forward into\nevery later module."]})]})}function p(e={}){let{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(d,{...e})}):d(e)}},8453(e,n,t){t.d(n,{R:()=>o,x:()=>i});var s=t(6540);let r={},a=s.createContext(r);function o(e){let n=s.useContext(a);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),s.createElement(a.Provider,{value:n},e.children)}}}]);