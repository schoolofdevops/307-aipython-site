"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["4002"],{3048(e,t,a){a.r(t),a.d(t,{metadata:()=>o,default:()=>d,frontMatter:()=>r,contentTitle:()=>l,toc:()=>p,assets:()=>c});var o=JSON.parse('{"id":"m8-building-clis/quiz","title":"Quiz: Building Command-Line Tools","description":"<Quiz questions={[","source":"@site/docs/m8-building-clis/quiz.mdx","sourceDirName":"m8-building-clis","slug":"/m8-building-clis/quiz","permalink":"/307-aipython-site/docs/m8-building-clis/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: Building Command-Line Tools"},"sidebar":"courseSidebar","previous":{"title":"Lab: Building Command-Line Tools","permalink":"/307-aipython-site/docs/m8-building-clis/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m8-building-clis/deep-dive"}}'),n=a(4848),i=a(8453),s=a(7517);let r={sidebar_position:3,title:"Quiz: Building Command-Line Tools"},l="Building Command-Line Tools \u2014 Quiz",c={},p=[];function h(e){let t={h1:"h1",header:"header",...(0,i.R)(),...e.components};return(0,n.jsxs)(n.Fragment,{children:[(0,n.jsx)(t.header,{children:(0,n.jsx)(t.h1,{id:"building-command-line-tools--quiz",children:"Building Command-Line Tools \u2014 Quiz"})}),"\n",(0,n.jsx)(s.A,{questions:[{prompt:"You are adding a new capability to `platformops`: the file to check, and a `--strict` flag that turns warnings into failures. Which should `--strict` be, and which should the file be?",options:[{text:"The file is an argument; `--strict` is an option (a flag)",correct:!0,explanation:"The command is meaningless without the file (an argument, given by position), while `--strict` changes how the command behaves without being required -- that is exactly what an option, specifically a flag, is for."},{text:"Both should be arguments, given by position",correct:!1,explanation:"`--strict` is not required for the command to run at all, and giving it by position instead of by name would make `platformops check service.yaml true` much harder to read than `--strict`."},{text:"Both should be options",correct:!1,explanation:"The file cannot be left out -- the command has nothing to check without it. That makes it an argument, not an optional flag."},{text:"The file should be an option (`--file`); `--strict` should be an argument",correct:!1,explanation:"This is backwards. The file is required, so it belongs as a positional argument; `--strict` is optional, so it belongs as a named option."}]},{prompt:"Which of the following are legitimate reasons to reach for Typer instead of `argparse` for a new tool? (Select all that apply.)",multiSelect:!0,options:[{text:"The tool will have more than one command, and you want sub-command dispatch built for you",correct:!0,explanation:"This is exactly where Typer earns its place -- `@app.command()` per function replaces the subparser wiring you would otherwise hand-write for each command."},{text:"You want consistently formatted `--help` across every command without writing that formatting once per command",correct:!0,explanation:"Typer generates the boxed, grouped `--help` output from your functions' type hints and docstrings -- one formatting engine for every command, not one per command."},{text:"You want to avoid adding any third-party dependency at all",correct:!1,explanation:"This is a reason to prefer `argparse`, which is in the standard library -- Typer is a real dependency, worth it once you have more than one command, not free."},{text:"You are writing a single, flat, throwaway script with one or two flags",correct:!1,explanation:"The lesson is explicit here: for one flat command, plain `argparse` (already used by `platformops.config` and `platformops.diagnostics`) is still the right call."}]},{prompt:"`platformops validate` can exit with 0, 1 or 2. What does exit code `2` mean, and how is it different from `1`?",options:[{text:"Exit `2` means the command itself was called wrong (a usage error); exit `1` means the file it was given has a real problem",correct:!0,explanation:"This is the M7 exit-code contract extended by one code: `0` clean, `1` a genuine problem with the input, `2` the caller made a mistake calling the command at all -- `--verbose` together with `--quiet` in this module's CLI is exactly that case."},{text:"Exit `2` means the file had two separate problems instead of one",correct:!1,explanation:"Exit codes are not a count of problems -- `validate` always exits `1` for a data or file problem, regardless of how many individual field errors it found."},{text:'There is no real difference; both mean "something went wrong"',correct:!1,explanation:'A script branching on the exit code needs the two to mean different things -- `2` says "fix how you are calling this," `1` says "fix the input file."'},{text:"Exit `2` is reserved for Python itself and a CLI should never use it",correct:!1,explanation:"Exit `2` is a normal, common convention for a usage error -- `argparse` already uses it for a missing required argument, and this module's Typer app raises it deliberately for a flag conflict."}]},{prompt:"A CI job runs `platformops validate service.yaml --json` and pipes the output into `jq`. What must stay true for that pipeline to work reliably?",options:[{text:"The `--json` output must be identical whether or not `--verbose` or `--quiet` was also passed",correct:!0,explanation:'A script asking for `--json` is telling the command "a machine is reading this" -- if `--verbose` changed the JSON shape, the same command would behave unpredictably depending on flags a human happened to also pass.'},{text:"The plain-text report and the JSON report must always be word-for-word identical",correct:!1,explanation:"They serve different readers on purpose -- a human-readable line and a machine-parseable object are allowed, expected, to look different. What must stay stable is the JSON shape itself, not that the two formats match each other."},{text:"The exit code should be ignored, since `--json` already tells the script what happened",correct:!1,explanation:"The exit code is still the fastest signal a script reads -- `--json` gives detail, the exit code gives the pass/fail verdict without parsing anything."},{text:"`--json` output should be printed to stderr so it does not interfere with normal terminal output",correct:!1,explanation:"JSON meant for a machine belongs on stdout, the stream a pipe (`| jq`) reads by default -- stderr is for the log noise a human debugging interactively wants to see, not the payload a script depends on."}]},{prompt:'You changed `[project.scripts]` in `pyproject.toml` from `platformops = "platformops:main"` to `platformops = "platformops.cli:app"`, but running `platformops --help` still shows the old, empty command. What is the most likely cause?',options:[{text:"You have not run `uv sync` since editing `pyproject.toml`, so the installed console-script shim was never regenerated",correct:!0,explanation:"The entry point in `pyproject.toml` is only a declaration -- `uv sync` is the step that reads it and rewrites the actual executable shim on `PATH`. Skip that step and the old shim keeps running."},{text:"Typer requires a full reinstall of Python itself before a new entry point takes effect",correct:!1,explanation:"Nothing about Python's own installation is involved -- only the project's own console-script shim, which `uv sync` regenerates from `pyproject.toml`."},{text:"The `app` object in `cli.py` needs to be renamed to `main` to match the old entry point",correct:!1,explanation:"The entry point name on the right of `=` in `[project.scripts]` can be any valid importable name -- `app` is fine as long as `pyproject.toml` names it correctly and `uv sync` has actually run."},{text:"Typer apps cannot be used as `[project.scripts]` entry points -- only plain functions can",correct:!1,explanation:"A `typer.Typer()` instance is callable, which is all `[project.scripts]` requires -- that is exactly why `platformops.cli:app` works as an entry point target."}]},{prompt:"`platformops.config` already has a `--check` flag that validates and resolves a config, then reports what it *would* write, without writing anything. What is this pattern called, and why does it matter?",options:[{text:"A dry run -- it lets someone (or a CI job) preview a real change before it actually happens",correct:!0,explanation:"Dry-run flags run the real logic all the way up to the side effect, then stop and report the plan instead of executing it -- exactly the safety pattern the cloud modules later in this course lean on before anything destructive runs for real."},{text:'A confirmation prompt -- it asks the user to type "y" before proceeding',correct:!1,explanation:"A confirmation prompt is a different, complementary pattern -- it interrupts an interactive run to ask permission. `--check` does not ask anything; it simply reports the plan and stops."},{text:"A retry mechanism -- it checks whether a previous run failed before trying again",correct:!1,explanation:"Nothing about `--check` involves retrying a previous attempt -- it is about previewing a single run's effect before committing to it."},{text:"A caching mechanism -- it reuses a previous validation result instead of running it again",correct:!1,explanation:"`--check` still runs the real validation and resolution logic every time; it just stops one step short of the write, it does not skip work by reusing an old result."}]},{prompt:'A command deletes a resource and normally asks "are you sure? [y/N]" before doing it. How should it behave when a CI pipeline runs it unattended?',options:[{text:"The pipeline should pass a `--yes` (or `--force`) flag that skips the prompt on purpose",correct:!0,explanation:"This is the standard shape: interactive by default (safe for a human typing at a terminal), non-interactive when a flag says so explicitly (safe for CI, because the intent to skip confirmation was stated, not assumed)."},{text:"The command should never ask for confirmation at all, so it always works the same way everywhere",correct:!1,explanation:"Removing the prompt entirely removes the safety net for the common case -- a human running the command by hand. The fix is an explicit opt-out, not removing the check for everyone."},{text:'The CI job should pipe the letter "y" into the command\'s standard input',correct:!1,explanation:"This works by accident, but it is fragile -- it depends on the exact prompt wording never changing, and it does not communicate the CI job's intent the way a named flag does."},{text:"The CI job should catch the resulting timeout and retry until it succeeds",correct:!1,explanation:"A hanging prompt is not a transient failure to retry past -- it will hang identically every time. The fix is a flag that skips the prompt, not a retry loop around a design problem."}]},{prompt:"While reviewing the coding agent's diff for `--quiet`, you want to confirm it followed the module's rule correctly. Which behavior is correct?",options:[{text:'`--quiet` suppresses the "OK -- ..." line on a successful `validate`, but a FAIL report still prints in full',correct:!0,explanation:'Quiet means "no chatter when everything is fine," never "hide a real problem." A FAIL is the command\'s report about a genuine issue -- it must stay visible under `--quiet`, exactly as the lab\'s review checklist calls out.'},{text:"`--quiet` suppresses all output, including a FAIL report, so CI logs stay as short as possible",correct:!1,explanation:"This is the bug the lab's review checklist specifically asks you to catch -- a `--quiet` that hides a real failure is worse than no `--quiet` at all, because a CI job reading a silent log could miss a genuine problem."},{text:"`--quiet` has no effect on `validate`, only on `inspect`",correct:!1,explanation:"`--quiet` is a global option that this module wires into `validate`'s success-path output specifically -- it is not inert there."},{text:"`--quiet` changes the `--json` output to a shorter object",correct:!1,explanation:"The `--json` payload stays the same regardless of `--quiet` or `--verbose` -- a script reading `--json` should never have to guess which shape it is getting based on other flags a human passed."}]},{prompt:"Which of the following statements about `--help` are true, according to this module? (Select all that apply.)",multiSelect:!0,options:[{text:"For most users, `--help` is the only documentation of a command they will ever open",correct:!0,explanation:"This is the module's core claim about `--help`: people run the command with `--help` before they go looking for a wiki page, so it has to be treated as the real documentation, not an afterthought."},{text:"Typer builds `--help` text from each command function's docstring and type hints",correct:!0,explanation:"A function's docstring becomes its command's help text, and its type-hinted parameters become the listed arguments and options -- you do not write a separate help string by hand."},{text:"`--help` text should be written once and never revisited, since Typer generates it automatically",correct:!1,explanation:"Typer generates the *formatting* automatically, but the *content* comes from your docstrings -- if a command's behavior changes and the docstring does not, `--help` quietly becomes wrong."},{text:"`--help` output is identical for every sub-command, since it all comes from the same `app` object",correct:!1,explanation:"Each command has its own `--help` output, built from that specific function's own docstring, arguments and options -- `platformops validate --help` and `platformops inspect --help` show different things."}]},{prompt:"A reviewer asks why `platformops.diagnostics` (from M7) is still using `argparse` instead of being rewritten with Typer now that the CLI exists. What is the best answer?",options:[{text:"It does not need to change -- `argparse` is still a reasonable choice for a single, flat command, and `platformops.diagnostics` is not going away just because `platformops.cli` now exists",correct:!0,explanation:"The lesson is explicit that Typer earns its place once you have multiple commands to dispatch between -- a single-command module with a handful of flags has no dispatch problem to solve, so rewriting it buys little."},{text:"It must be rewritten immediately, because a project cannot mix `argparse` and Typer",correct:!1,explanation:"There is no such restriction -- `platformops.config` and `platformops.diagnostics` keep using `argparse` for their own `python -m` entry points, entirely independent of the new `platformops.cli` Typer app."},{text:"Typer cannot import functions from a module that also defines an `argparse`-based `main()`",correct:!1,explanation:"`platformops.cli` already imports directly from `platformops.config` and reuses its loader -- the two coexist in the same codebase without conflict."},{text:"`argparse` no longer works once `typer` is installed as a dependency",correct:!1,explanation:"`argparse` is part of the Python standard library and is unaffected by any third-party package being installed alongside it."}]},{prompt:'You are testing `platformops validate` and choose Typer\'s `CliRunner` instead of calling `subprocess.run(["uv", "run", "platformops", "validate", ...])`. What is the main practical benefit?',options:[{text:"`CliRunner` invokes the app in-process, so it runs far faster than spawning a real subprocess for every test case",correct:!0,explanation:"No new Python interpreter starts, no `uv run` resolution happens -- the app object is called directly in memory, which is why a test suite with dozens of CLI tests still runs in a fraction of a second."},{text:"`CliRunner` is required because Typer apps cannot be run as real subprocesses at all",correct:!1,explanation:"A Typer app works perfectly well as a real subprocess -- that is exactly how a learner runs `uv run platformops validate ...` by hand. `CliRunner` is a testing convenience, not the only way to invoke the app."},{text:"`CliRunner` automatically generates test cases for every command without you writing any assertions",correct:!1,explanation:"`CliRunner` still requires you to call `.invoke(app, [...])` and write your own assertions on `.exit_code` and `.stdout` -- it does not generate test cases on its own."},{text:"`CliRunner` is the only way to test code that uses `typer.Exit`",correct:!1,explanation:"`typer.Exit` is a normal exception Typer catches at the top level -- `CliRunner` makes capturing its resulting exit code convenient, but it is not the only mechanism that could observe it."}]},{prompt:"Which of these are part of this module's exit-code and output-design contract for `platformops`? (Select all that apply.)",multiSelect:!0,options:[{text:"Exit `0` for a clean result, `1` for a genuine problem with the input, `2` for a usage error calling the command itself",correct:!0,explanation:'This three-way split is the extended contract this module builds on top of M7\'s two-way (`0`/`1`) split -- `2` is new, reserved specifically for "you called this wrong."'},{text:"`--json` output must stay stable regardless of `--verbose` or `--quiet`",correct:!0,explanation:"`--verbose` and `--quiet` are about how much a *human* sees; `--json` is for a machine, and the two axes are kept independent on purpose."},{text:"A validation FAIL report should be silenced whenever `--quiet` is passed",correct:!1,explanation:"This is the exact mistake the module warns against -- `--quiet` silences success chatter only, never a real failure."},{text:"Every command should print its report to stderr so pipelines never see accidental output",correct:!1,explanation:"The module's convention is the opposite for a normal report: the human/machine-facing result (OK, FAIL, or JSON) belongs on stdout, where a pipe reads by default; stderr is for log noise and command-level errors."}]},{prompt:"What does the Common Pitfalls list in the lesson warn against regarding a confirmation prompt?",options:[{text:"Adding a prompt with no way to skip it in CI, which means the command simply cannot run unattended",correct:!0,explanation:"A prompt without a `--yes`/`--force` escape hatch blocks any automated caller forever -- the lesson's fix is always to pair a prompt with a documented way to skip it on purpose."},{text:"Adding too many confirmation prompts to the same command",correct:!1,explanation:"The lesson's concern is about a missing escape hatch, not about the number of prompts a single command has."},{text:"Using `--yes` as the flag name instead of `--force`",correct:!1,explanation:"The lesson treats `--yes` and `--force` as equivalent naming choices for the same pattern -- neither name is called out as wrong."},{text:"Confirmation prompts being incompatible with `--json` output",correct:!1,explanation:"This is not a pitfall the lesson raises -- the actual warning is specifically about a prompt with no unattended escape hatch."}]}]})]})}function d(e={}){let{wrapper:t}={...(0,i.R)(),...e.components};return t?(0,n.jsx)(t,{...e,children:(0,n.jsx)(h,{...e})}):h(e)}},7517(e,t,a){a.d(t,{A:()=>s});var o=a(4848),n=a(6540);function i({q:e,state:t,onToggle:a}){return(0,o.jsxs)("div",{className:"question_QpiT",children:[(0,o.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,o.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,o.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,n)=>{let i=t.selected.has(n),s="option_qGol";return t.checked?e.correct?s+=" correct_LYA5":i&&(s+=" incorrect_kRFc"):i&&(s+=" chosen_H3WG"),(0,o.jsxs)("li",{children:[(0,o.jsxs)("button",{type:"button",className:s,disabled:t.checked,onClick:()=>a(n),children:[(0,o.jsx)("span",{className:"marker_vN_w",children:i?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(i||e.correct)&&e.explanation&&(0,o.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},n)})})]})}function s({questions:e}){let[t,a]=(0,n.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,a)=>{var o,n;let i;return o=e[a],n=t.selected,i=o.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),o.multiSelect?n.size===i.length&&[...n].every(e=>o.options[e].correct):1===n.size&&i.includes([...n][0])}).length;return(0,o.jsxs)("div",{className:"quiz_I014",children:[e.map((n,s)=>(0,o.jsx)(i,{q:n,state:t[s],onToggle:t=>a(a=>a.map((a,o)=>{if(o!==s)return a;let n=new Set(a.selected);return e[s].multiSelect?n.has(t)?n.delete(t):n.add(t):(n.clear(),n.add(t)),{...a,selected:n}}))},s)),(0,o.jsxs)("div",{className:"actions_XUwt",children:[(0,o.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,o.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,o.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>s,x:()=>r});var o=a(6540);let n={},i=o.createContext(n);function s(e){let t=o.useContext(i);return o.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(n):e.components||n:s(e.components),o.createElement(i.Provider,{value:t},e.children)}}}]);