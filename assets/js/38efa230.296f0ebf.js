"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["4272"],{6401(e,n,t){t.r(n),t.d(n,{metadata:()=>s,default:()=>u,frontMatter:()=>o,contentTitle:()=>a,toc:()=>c,assets:()=>l});var s=JSON.parse('{"id":"m22-local-aws-floci/lab","title":"Lab: Local AWS Automation with Floci","description":"This lab has five phases. Phase 0 verifies Floci is running and reachable. Phases 1 through 4 each","source":"@site/docs/m22-local-aws-floci/lab.md","sourceDirName":"m22-local-aws-floci","slug":"/m22-local-aws-floci/lab","permalink":"/307-aipython-site/docs/m22-local-aws-floci/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Local AWS Automation with Floci"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Local AWS Automation with Floci","permalink":"/307-aipython-site/docs/m22-local-aws-floci/lesson"},"next":{"title":"Quiz: Local AWS Automation with Floci","permalink":"/307-aipython-site/docs/m22-local-aws-floci/quiz"}}'),r=t(4848),i=t(8453);let o={sidebar_position:2,title:"Lab: Local AWS Automation with Floci"},a="Lab \u2014 Local AWS Automation with Floci",l={},c=[{value:"Phase 0 \u2014 Credentials and endpoint, exported once",id:"phase-0--credentials-and-endpoint-exported-once",level:2},{value:"Phase 1 \u2014 The client factory, test-first",id:"phase-1--the-client-factory-test-first",level:2},{value:"Step 1 \u2014 Write the failing test file",id:"step-1--write-the-failing-test-file",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail",level:3},{value:"Step 3 \u2014 Write <code>awsclient.py</code>",id:"step-3--write-awsclientpy",level:3},{value:"Step 4 \u2014 Watch it pass",id:"step-4--watch-it-pass",level:3},{value:"Phase 2 \u2014 S3 report storage, test-first",id:"phase-2--s3-report-storage-test-first",level:2},{value:"Step 1 \u2014 Shared fixture: skip cleanly if Floci is not running",id:"step-1--shared-fixture-skip-cleanly-if-floci-is-not-running",level:3},{value:"Step 2 \u2014 Write the failing test file",id:"step-2--write-the-failing-test-file",level:3},{value:"Step 3 \u2014 Watch it fail",id:"step-3--watch-it-fail",level:3},{value:"Step 4 \u2014 Write <code>reportstore.py</code>",id:"step-4--write-reportstorepy",level:3},{value:"Step 5 \u2014 Watch it pass, against real Floci",id:"step-5--watch-it-pass-against-real-floci",level:3},{value:"Step 6 \u2014 Wire the CLI, and see it work live",id:"step-6--wire-the-cli-and-see-it-work-live",level:3},{value:"Phase 3 \u2014 DynamoDB findings store, test-first",id:"phase-3--dynamodb-findings-store-test-first",level:2},{value:"Step 1 \u2014 Write the failing test file",id:"step-1--write-the-failing-test-file-1",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail-1",level:3},{value:"Step 3 \u2014 Write <code>findingsstore.py</code>",id:"step-3--write-findingsstorepy",level:3},{value:"Step 4 \u2014 Watch it pass, against real Floci",id:"step-4--watch-it-pass-against-real-floci",level:3},{value:"Step 5 \u2014 See it work live",id:"step-5--see-it-work-live",level:3},{value:"Phase 4 \u2014 SQS work queue with a dead-letter queue, test-first",id:"phase-4--sqs-work-queue-with-a-dead-letter-queue-test-first",level:2},{value:"Step 1 \u2014 Write the failing test file",id:"step-1--write-the-failing-test-file-2",level:3},{value:"Step 2 \u2014 Watch it fail",id:"step-2--watch-it-fail-2",level:3},{value:"Step 3 \u2014 Write <code>workqueue.py</code>",id:"step-3--write-workqueuepy",level:3},{value:"Step 4 \u2014 Watch it pass, against real Floci",id:"step-4--watch-it-pass-against-real-floci-1",level:3},{value:"Step 5 \u2014 Wire the CLI, and see it work live",id:"step-5--wire-the-cli-and-see-it-work-live",level:3},{value:"Step 6 \u2014 See the dead-letter queue work, live, end to end",id:"step-6--see-the-dead-letter-queue-work-live-end-to-end",level:3},{value:"Phase 5 \u2014 Full quality gate and version bump",id:"phase-5--full-quality-gate-and-version-bump",level:2},{value:"Teardown",id:"teardown",level:2}];function d(e){let n={a:"a",admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"lab--local-aws-automation-with-floci",children:"Lab \u2014 Local AWS Automation with Floci"})}),"\n",(0,r.jsxs)(n.p,{children:["This lab has five phases. Phase 0 verifies Floci is running and reachable. Phases 1 through 4 each\nwrite one failing test file first, watch it fail, then write the module until every test passes \u2014\n",(0,r.jsx)(n.code,{children:"awsclient.py"})," (the client factory), ",(0,r.jsx)(n.code,{children:"reportstore.py"})," (S3), ",(0,r.jsx)(n.code,{children:"findingsstore.py"})," (DynamoDB) and\n",(0,r.jsx)(n.code,{children:"workqueue.py"})," (SQS with a dead-letter queue). Phase 5 runs this project's full quality gate and\nbumps the version to ",(0,r.jsx)(n.code,{children:"v1.7.0"})," \u2014 the Local AWS Automation Environment release."]}),"\n",(0,r.jsxs)(n.admonition,{title:"Before you start",type:"info",children:[(0,r.jsxs)(n.p,{children:["You need ",(0,r.jsx)(n.code,{children:"~/platformops"})," at ",(0,r.jsx)(n.code,{children:"v1.6.0"}),", with ",(0,r.jsx)(n.code,{children:"src/platformops/cloudinventory.py"})," in place from\nModule 21, and the labs repo cloned at ",(0,r.jsx)(n.code,{children:"~/307-aipython-labs"}),"."]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 1.6.0\n"})}),(0,r.jsxs)(n.p,{children:["If this already shows ",(0,r.jsx)(n.code,{children:"1.7.0"}),", you have already completed this lab -- skip ahead to\n",(0,r.jsx)(n.a,{href:"#phase-5--full-quality-gate-and-version-bump",children:"Phase 5"}),', or work through the steps below anyway,\nsince every step in this lab is safe to repeat. One consequence of repeating it: each phase\'s\n"watch it fail" step (Phase 1 Step 2, Phase 2 Step 3, Phase 3 Step 2, Phase 4 Step 2) expects an\n',(0,r.jsx)(n.code,{children:"ImportError"})," because the module doesn't exist yet -- if you're re-running this lab, the module\nalready exists from last time, so that step's tests will simply pass instead. That's expected,\nnot a problem; the \"write it, watch it pass\" step right after each one still teaches the point."]}),(0,r.jsx)(n.p,{children:"You also need Floci installed and running:"}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"floci start\nfloci doctor\n"})}),(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"Floci AWS Doctor \u2014 checking your environment\n\n  \u2713 docker.installed           Docker 29.5.2 detected\n  \u2713 docker.daemon              Daemon reachable\n  \u2713 docker.version             Docker 29.5.2 (>= 20.10)\n  \u2713 port.available             Port 4566 in use by container 'floci' (expected)\n  \u2713 image.present              floci/floci image present locally\n  \u2713 image.version              Server image version 1.5.34 (>= 1.5.0)\n  \u2713 container.running          Container 'floci' is running\n  \u2713 endpoint.reachable         http://localhost:4566 is reachable (server v1.5.34)\n  \u26A0 aws.cli.endpoint           AWS_ENDPOINT_URL is not set\n  \u26A0 aws.cli.s3.pathstyle       ~/.aws/config missing 's3.addressing_style = path'\n"})}),(0,r.jsxs)(n.p,{children:["The two ",(0,r.jsx)(n.code,{children:"\u26A0"})," warnings only matter for the separate ",(0,r.jsx)(n.code,{children:"aws"})," CLI tool, not for this lab's Python code \u2014\nsafe to ignore here. If ",(0,r.jsx)(n.code,{children:"docker.daemon"})," shows ",(0,r.jsx)(n.code,{children:"\u2717"}),", open Docker Desktop (or your Docker daemon of\nchoice) and try again; ",(0,r.jsx)(n.code,{children:"floci start"})," needs a running Docker daemon. If only ",(0,r.jsx)(n.code,{children:"docker.socket"})," shows\n",(0,r.jsx)(n.code,{children:"\u2717"})," while ",(0,r.jsx)(n.code,{children:"docker.daemon"})," shows ",(0,r.jsx)(n.code,{children:"\u2713"}),", that is a known false negative on setups like Rancher\nDesktop that use a different socket path than Docker Desktop's default -- ",(0,r.jsx)(n.code,{children:"floci"})," itself still\nworks correctly, safe to ignore."]}),(0,r.jsxs)(n.p,{children:["If ",(0,r.jsx)(n.code,{children:"floci start"})," reports ",(0,r.jsx)(n.code,{children:"Error: Container 'floci' is already running"}),", that is not a failure --\nFloci is already up from an earlier step or session. Skip straight to ",(0,r.jsx)(n.code,{children:"floci doctor"})," to confirm."]})]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-0--credentials-and-endpoint-exported-once",children:"Phase 0 \u2014 Credentials and endpoint, exported once"}),"\n",(0,r.jsxs)(n.p,{children:["Floci does not check whether your AWS credentials are real \u2014 only that boto3 sends ",(0,r.jsx)(n.em,{children:"something"}),".\nExport obviously-fake placeholder values and Floci's own local endpoint for the rest of this lab\nsession:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"export AWS_ACCESS_KEY_ID=test\nexport AWS_SECRET_ACCESS_KEY=test\nexport AWS_ENDPOINT_URL=http://localhost:4566\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," is the environment-variable fallback ",(0,r.jsx)(n.code,{children:"awsclient.get_aws_client()"})," reads in\nPhase 1 -- exporting it here means the CLI commands later in this lab do not need ",(0,r.jsx)(n.code,{children:"--endpoint-url"}),"\ntyped on every single call, while the tests in Phases 2-4 still pass ",(0,r.jsx)(n.code,{children:"endpoint_url"})," explicitly, to\nprove the client factory's precedence rule for themselves."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-1--the-client-factory-test-first",children:"Phase 1 \u2014 The client factory, test-first"}),"\n",(0,r.jsx)(n.h3,{id:"step-1--write-the-failing-test-file",children:"Step 1 \u2014 Write the failing test file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cd ~/platformops\ncat > tests/test_awsclient.py << \'PYEOF\'\nfrom platformops import awsclient\n\n# ---------------------------------------------------------------------------\n# get_aws_client -- session, profile, region and retry config, the same\n# wiring cloudinventory.get_client() proved for EC2 alone, generalized to\n# any service name. boto3.Session is faked so these tests prove the\n# arguments this function passes, not real network behavior.\n# ---------------------------------------------------------------------------\n\n\nclass FakeSession:\n    def __init__(self, profile_name=None):\n        self.profile_name = profile_name\n        self.calls = []\n\n    def client(self, service_name, *, region_name=None, endpoint_url=None, config=None):\n        self.calls.append(\n            {\n                "service_name": service_name,\n                "region_name": region_name,\n                "endpoint_url": endpoint_url,\n                "config": config,\n            }\n        )\n        return f"fake-{service_name}-client"\n\n\ndef _install_fake_session(monkeypatch):\n    fake = FakeSession.__new__(FakeSession)\n\n    def factory(profile_name=None):\n        fake.__init__(profile_name=profile_name)\n        return fake\n\n    monkeypatch.setattr(awsclient.boto3, "Session", factory)\n    return fake\n\n\ndef test_passes_service_name_region_and_profile(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.delenv("AWS_ENDPOINT_URL", raising=False)\n\n    client = awsclient.get_aws_client("s3", region="us-east-1", profile="course-lab")\n\n    assert client == "fake-s3-client"\n    assert fake.profile_name == "course-lab"\n    call = fake.calls[0]\n    assert call["service_name"] == "s3"\n    assert call["region_name"] == "us-east-1"\n\n\ndef test_uses_retry_config(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.delenv("AWS_ENDPOINT_URL", raising=False)\n\n    awsclient.get_aws_client("sqs", region="us-east-1")\n\n    assert fake.calls[0]["config"].retries == {"max_attempts": 5, "mode": "standard"}\n\n\ndef test_endpoint_url_is_none_by_default_so_it_talks_to_real_aws(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.delenv("AWS_ENDPOINT_URL", raising=False)\n\n    awsclient.get_aws_client("dynamodb", region="us-east-1")\n\n    assert fake.calls[0]["endpoint_url"] is None\n\n\ndef test_explicit_endpoint_url_parameter_is_used(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.delenv("AWS_ENDPOINT_URL", raising=False)\n\n    awsclient.get_aws_client(\n        "s3", region="us-east-1", endpoint_url="http://localhost:4566"\n    )\n\n    assert fake.calls[0]["endpoint_url"] == "http://localhost:4566"\n\n\ndef test_endpoint_url_falls_back_to_env_var_when_not_passed(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.setenv("AWS_ENDPOINT_URL", "http://localhost:4566")\n\n    awsclient.get_aws_client("dynamodb", region="us-east-1")\n\n    assert fake.calls[0]["endpoint_url"] == "http://localhost:4566"\n\n\ndef test_explicit_endpoint_url_parameter_overrides_env_var(monkeypatch):\n    fake = _install_fake_session(monkeypatch)\n    monkeypatch.setenv("AWS_ENDPOINT_URL", "http://localhost:4566")\n\n    awsclient.get_aws_client(\n        "s3", region="us-east-1", endpoint_url="http://localhost:9999"\n    )\n\n    assert fake.calls[0]["endpoint_url"] == "http://localhost:9999"\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--watch-it-fail",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_awsclient.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_awsclient.py'.\n...\nE   ImportError: cannot import name 'awsclient' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_awsclient.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.09s\n"})}),"\n",(0,r.jsxs)(n.h3,{id:"step-3--write-awsclientpy",children:["Step 3 \u2014 Write ",(0,r.jsx)(n.code,{children:"awsclient.py"})]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/awsclient.py << \'PYEOF\'\n"""platformops.awsclient -- one function that builds a boto3 client for any AWS service.\n\n`cloudinventory.get_client()` (Module 21) builds an EC2 client and nothing\nelse -- it has no way to point boto3 at anything but real AWS. This module\ngeneralizes that same session/profile/region wiring to any service name, and\nadds one more axis: an `endpoint_url`. Left at its default (`None`), a\nclient this function returns talks to real AWS, exactly like\n`cloudinventory.get_client()` already does. Set it to Floci\'s local endpoint\n(`http://localhost:4566`) and every call the returned client makes goes\nthere instead -- no other code has to know the difference.\n\nboto3 itself already reads an `AWS_ENDPOINT_URL` environment variable and\napplies it automatically, for every client any code in the process creates.\nThis module deliberately does not lean on that. It reads the same variable,\nbut only as a fallback inside this one function, and only for clients built\nthrough it -- an explicit `endpoint_url=` argument always wins. That keeps\nthe decision local and testable (a unit test can assert exactly which\nargument reached `session.client()`) instead of ambient: a `platformops`\nprocess that also happens to import some other library which creates its\nown boto3 client would silently redirect that client too, if the env var\nwere left to boto3\'s own global handling. Routing every AWS client in this\nproject through `get_aws_client()` means "is this call still hitting real\nAWS?" is answered by reading one function, not by tracing environment state\nthrough the whole process.\n"""\n\nfrom __future__ import annotations\n\nimport os\nfrom typing import Any\n\nimport boto3\nfrom botocore.config import Config\n\nDEFAULT_RETRY_CONFIG = Config(retries={"max_attempts": 5, "mode": "standard"})\n\n\ndef get_aws_client(\n    service_name: str,\n    *,\n    region: str,\n    profile: str | None = None,\n    endpoint_url: str | None = None,\n) -> Any:\n    """Build a boto3 client for `service_name`, the one place this project resolves an AWS endpoint.\n\n    `endpoint_url` left as `None` (the default) falls back to the\n    `AWS_ENDPOINT_URL` environment variable, and `None` from that lookup\n    means "no endpoint override at all" -- the client talks to real AWS. An\n    explicit `endpoint_url` argument always wins over the environment\n    variable, the same precedence `profile` already has over boto3\'s wider\n    credential chain.\n    """\n    resolved_endpoint = (\n        endpoint_url if endpoint_url is not None else os.environ.get("AWS_ENDPOINT_URL")\n    )\n    session = boto3.Session(profile_name=profile)\n    return session.client(\n        service_name,\n        region_name=region,\n        endpoint_url=resolved_endpoint,\n        config=DEFAULT_RETRY_CONFIG,\n    )\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4--watch-it-pass",children:"Step 4 \u2014 Watch it pass"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_awsclient.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"tests/test_awsclient.py::test_passes_service_name_region_and_profile PASSED [ 16%]\ntests/test_awsclient.py::test_uses_retry_config PASSED                   [ 33%]\ntests/test_awsclient.py::test_endpoint_url_is_none_by_default_so_it_talks_to_real_aws PASSED [ 50%]\ntests/test_awsclient.py::test_explicit_endpoint_url_parameter_is_used PASSED [ 66%]\ntests/test_awsclient.py::test_endpoint_url_falls_back_to_env_var_when_not_passed PASSED [ 83%]\ntests/test_awsclient.py::test_explicit_endpoint_url_parameter_overrides_env_var PASSED [100%]\n\n============================== 6 passed in 0.12s ===============================\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Six tests, unit-level, no Floci involved -- ",(0,r.jsx)(n.code,{children:"boto3.Session"})," is faked out entirely, so these prove\nthe arguments ",(0,r.jsx)(n.code,{children:"get_aws_client()"})," passes, not real network behavior."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-2--s3-report-storage-test-first",children:"Phase 2 \u2014 S3 report storage, test-first"}),"\n",(0,r.jsx)(n.h3,{id:"step-1--shared-fixture-skip-cleanly-if-floci-is-not-running",children:"Step 1 \u2014 Shared fixture: skip cleanly if Floci is not running"}),"\n",(0,r.jsxs)(n.p,{children:["Every test file from here on needs a real, running Floci -- not a mock. ",(0,r.jsx)(n.code,{children:"tests/conftest.py"})," adds\none shared fixture every one of them uses:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > tests/conftest.py << \'PYEOF\'\n"""Shared fixtures for tests that need a real, running Floci container.\n\n`reportstore`, `findingsstore` and `workqueue` are tested against a real\nlocal Floci instance, not a mock -- the point of Module 22 is proving Floci\nitself works as a local stand-in, not re-proving what Module 21 already\nproved about mocking boto3 with moto. A learner (or CI) without Floci\nrunning should see these test files skip cleanly, not fail with a\nconnection error buried in a traceback.\n"""\n\nfrom __future__ import annotations\n\nimport httpx\nimport pytest\n\nFLOCI_HEALTH_URL = "http://localhost:4566/_floci/health"\n\n\n@pytest.fixture(scope="session")\ndef require_floci() -> None:\n    """Skip the whole test file if Floci is not reachable at localhost:4566."""\n    try:\n        response = httpx.get(FLOCI_HEALTH_URL, timeout=2.0)\n        response.raise_for_status()\n    except httpx.HTTPError:\n        pytest.skip(\n            "Floci is not reachable at http://localhost:4566 -- run `floci start` first"\n        )\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--write-the-failing-test-file",children:"Step 2 \u2014 Write the failing test file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_reportstore.py << \'PYEOF\'\nimport uuid\n\nimport botocore.exceptions\nimport pytest\n\nfrom platformops import reportstore\nfrom platformops.awsclient import get_aws_client\n\npytestmark = pytest.mark.usefixtures("require_floci")\n\nFLOCI_ENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\n\n@pytest.fixture\ndef s3_client():\n    return get_aws_client("s3", region=REGION, endpoint_url=FLOCI_ENDPOINT)\n\n\n@pytest.fixture\ndef bucket(s3_client):\n    name = f"platformops-test-{uuid.uuid4().hex[:12]}"\n    reportstore.ensure_bucket(s3_client, name, region=REGION)\n    yield name\n    reportstore.empty_bucket(s3_client, name)\n    s3_client.delete_bucket(Bucket=name)\n\n\n# ---------------------------------------------------------------------------\n# ensure_bucket -- idempotent create. Run twice against the same name and\n# confirm no error and no duplication, the same idempotency-guard discipline\n# every prior module\'s "ensure" style function has to prove.\n# ---------------------------------------------------------------------------\n\n\ndef test_ensure_bucket_is_idempotent(s3_client):\n    name = f"platformops-test-{uuid.uuid4().hex[:12]}"\n\n    reportstore.ensure_bucket(s3_client, name, region=REGION)\n    reportstore.ensure_bucket(s3_client, name, region=REGION)  # must not raise\n\n    response = s3_client.list_buckets()\n    matching = [b for b in response["Buckets"] if b["Name"] == name]\n    assert len(matching) == 1\n\n    reportstore.empty_bucket(s3_client, name)\n    s3_client.delete_bucket(Bucket=name)\n\n\n# ---------------------------------------------------------------------------\n# upload_report / download_report -- a JSON report round-trips exactly, and\n# the key it lands under carries a timestamp so repeated uploads never\n# collide.\n# ---------------------------------------------------------------------------\n\n\ndef test_upload_report_returns_a_timestamped_key_under_the_prefix(s3_client, bucket):\n    report = {"status": "ok", "service": "checkout-api", "concerns": []}\n\n    key = reportstore.upload_report(s3_client, bucket, report)\n\n    assert key.startswith("reports/")\n    assert key.endswith(".json")\n\n\ndef test_upload_then_download_round_trips_the_same_report(s3_client, bucket):\n    report = {"status": "error", "service": "billing-api", "concerns": ["dirty tree"]}\n\n    key = reportstore.upload_report(s3_client, bucket, report)\n    downloaded = reportstore.download_report(s3_client, bucket, key)\n\n    assert downloaded == report\n\n\ndef test_two_uploads_in_a_row_get_different_keys(s3_client, bucket):\n    key_one = reportstore.upload_report(s3_client, bucket, {"n": 1})\n    key_two = reportstore.upload_report(s3_client, bucket, {"n": 2})\n\n    assert key_one != key_two\n\n\n# ---------------------------------------------------------------------------\n# list_reports -- most recent first, capped at max_results.\n# ---------------------------------------------------------------------------\n\n\ndef test_list_reports_returns_most_recent_first(s3_client, bucket):\n    keys = [reportstore.upload_report(s3_client, bucket, {"n": i}) for i in range(3)]\n\n    listed = reportstore.list_reports(s3_client, bucket)\n\n    assert [entry["key"] for entry in listed] == list(reversed(keys))\n\n\ndef test_list_reports_respects_max_results(s3_client, bucket):\n    for i in range(5):\n        reportstore.upload_report(s3_client, bucket, {"n": i})\n\n    listed = reportstore.list_reports(s3_client, bucket, max_results=2)\n\n    assert len(listed) == 2\n\n\ndef test_list_reports_on_empty_bucket_returns_empty_list(s3_client, bucket):\n    listed = reportstore.list_reports(s3_client, bucket)\n\n    assert listed == []\n\n\n# ---------------------------------------------------------------------------\n# upload_report_to_bucket -- the CLI-facing wrapper. Builds its own client\n# through get_aws_client(), ensures the bucket, uploads, and turns AWS\n# errors into the same structured {"status": "error", ...} report every\n# other command-level function in this project already returns (see\n# cloudinventory.scan_inventory()).\n# ---------------------------------------------------------------------------\n\n\ndef test_upload_report_to_bucket_returns_an_ok_report(bucket):\n    report = {"status": "ok", "service": "checkout-api"}\n\n    result = reportstore.upload_report_to_bucket(\n        report, bucket=bucket, region=REGION, endpoint_url=FLOCI_ENDPOINT\n    )\n\n    assert result["status"] == "ok"\n    assert result["bucket"] == bucket\n    assert result["key"].startswith("reports/")\n\n\ndef test_upload_report_to_bucket_catches_client_error(monkeypatch):\n    class BoomClient:\n        def create_bucket(self, **kwargs):\n            raise botocore.exceptions.ClientError(\n                {"Error": {"Code": "AccessDenied", "Message": "nope"}}, "CreateBucket"\n            )\n\n    monkeypatch.setattr(reportstore, "get_aws_client", lambda *a, **k: BoomClient())\n\n    result = reportstore.upload_report_to_bucket(\n        {"status": "ok"}, bucket="some-bucket", region=REGION\n    )\n\n    assert result["status"] == "error"\n    assert result["error"] == "AccessDenied"\n\n\ndef test_upload_report_to_bucket_catches_no_credentials_error(monkeypatch):\n    class BoomClient:\n        def create_bucket(self, **kwargs):\n            raise botocore.exceptions.NoCredentialsError()\n\n    monkeypatch.setattr(reportstore, "get_aws_client", lambda *a, **k: BoomClient())\n\n    result = reportstore.upload_report_to_bucket(\n        {"status": "ok"}, bucket="some-bucket", region=REGION\n    )\n\n    assert result["status"] == "error"\n    assert result["error"] == "no-credentials"\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-3--watch-it-fail",children:"Step 3 \u2014 Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_reportstore.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_reportstore.py'.\n...\nE   ImportError: cannot import name 'reportstore' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_reportstore.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.08s\n"})}),"\n",(0,r.jsxs)(n.h3,{id:"step-4--write-reportstorepy",children:["Step 4 \u2014 Write ",(0,r.jsx)(n.code,{children:"reportstore.py"})]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/reportstore.py << \'PYEOF\'\n"""platformops.reportstore -- upload, list and download JSON reports in S3.\n\nEvery prior module that produces a report (`review-service.py`,\n`service_readiness.py`, `incident_context.py`) prints it to stdout and\nstops there. Once a report is worth keeping past the terminal session that\nproduced it -- comparing today\'s readiness check against last week\'s, or\nhanding an incident report to someone who was not on the call -- it needs\nsomewhere durable to live. S3 is that place: `upload_report()` stores a\nreport under a timestamped key so repeated uploads never collide,\n`list_reports()` finds recent ones, and `download_report()` reads one back.\n\nEvery client this module uses comes from `platformops.awsclient.get_aws_client()`\n-- no function here ever calls `boto3.client()` directly, and no function\nhere ever hardcodes `endpoint_url="http://localhost:4566"`. Point\n`get_aws_client()` at Floci for a lab run, or leave `endpoint_url` unset for\na real S3 bucket -- this module does not know or care which one it is\ntalking to.\n"""\n\nfrom __future__ import annotations\n\nimport json\nfrom datetime import UTC, datetime\nfrom typing import Any\n\nimport botocore.exceptions\n\nfrom platformops.awsclient import get_aws_client\n\nDEFAULT_PREFIX = "reports"\n\n\ndef ensure_bucket(client: Any, bucket: str, *, region: str) -> None:\n    """Create `bucket` if it does not already exist -- safe to call every run.\n\n    S3\'s `us-east-1` is the one region that rejects a `CreateBucketConfiguration`\n    with a `LocationConstraint` matching its own name -- every other region\n    requires one. `BucketAlreadyOwnedByYou` is the expected outcome the\n    second time this runs against the same bucket; it is caught by name and\n    treated as success, the same way `findingsstore.ensure_table()` treats\n    `ResourceInUseException`.\n    """\n    kwargs: dict[str, Any] = {"Bucket": bucket}\n    if region != "us-east-1":\n        kwargs["CreateBucketConfiguration"] = {"LocationConstraint": region}\n    try:\n        client.create_bucket(**kwargs)\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "")\n        if code not in {"BucketAlreadyOwnedByYou", "BucketAlreadyExists"}:\n            raise\n\n\ndef empty_bucket(client: Any, bucket: str) -> None:\n    """Delete every object in `bucket` -- S3 refuses to delete a non-empty bucket."""\n    paginator = client.get_paginator("list_objects_v2")\n    for page in paginator.paginate(Bucket=bucket):\n        for obj in page.get("Contents", []):\n            client.delete_object(Bucket=bucket, Key=obj["Key"])\n\n\ndef upload_report(\n    client: Any, bucket: str, report: dict[str, Any], *, prefix: str = DEFAULT_PREFIX\n) -> str:\n    """Upload `report` as JSON under a timestamped key, and return that key.\n\n    The timestamp carries microseconds precision (`%Y-%m-%dT%H-%M-%S-%fZ`,\n    colons replaced with dashes -- S3 keys allow colons, but several shells\n    and most local filesystems do not, and a report key that is also a safe\n    local filename is one less thing to think about) so two uploads in the\n    same second still land under two different keys instead of one\n    overwriting the other silently.\n    """\n    timestamp = datetime.now(UTC).strftime("%Y-%m-%dT%H-%M-%S-%fZ")\n    key = f"{prefix}/{timestamp}.json"\n    client.put_object(\n        Bucket=bucket,\n        Key=key,\n        Body=json.dumps(report).encode("utf-8"),\n        ContentType="application/json",\n    )\n    return key\n\n\ndef download_report(client: Any, bucket: str, key: str) -> dict[str, Any]:\n    """Download the object at `key` and parse it back into the report dict."""\n    response = client.get_object(Bucket=bucket, Key=key)\n    body: bytes = response["Body"].read()\n    result: dict[str, Any] = json.loads(body)\n    return result\n\n\ndef list_reports(\n    client: Any,\n    bucket: str,\n    *,\n    prefix: str = DEFAULT_PREFIX,\n    max_results: int = 20,\n) -> list[dict[str, Any]]:\n    """List reports under `prefix`, most recently uploaded first.\n\n    Walks every page through `list_objects_v2`\'s paginator -- the same\n    discipline `cloudinventory.list_instances()` uses for EC2 -- so a bucket\n    with more reports than one page holds still sorts correctly before\n    `max_results` trims it, instead of only ever seeing the first page.\n    """\n    paginator = client.get_paginator("list_objects_v2")\n    entries: list[dict[str, Any]] = []\n    for page in paginator.paginate(Bucket=bucket, Prefix=prefix):\n        for obj in page.get("Contents", []):\n            entries.append(\n                {\n                    "key": obj["Key"],\n                    "last_modified": obj["LastModified"].isoformat(),\n                    "size": obj["Size"],\n                }\n            )\n    entries.sort(key=lambda entry: entry["key"], reverse=True)\n    return entries[:max_results]\n\n\ndef upload_report_to_bucket(\n    report: dict[str, Any],\n    *,\n    bucket: str,\n    region: str,\n    profile: str | None = None,\n    endpoint_url: str | None = None,\n    prefix: str = DEFAULT_PREFIX,\n) -> dict[str, Any]:\n    """The one call a CLI command or a script needs: build a client, ensure the bucket, upload.\n\n    Catches the same two exception types `cloudinventory.scan_inventory()`\n    catches, by name, and returns the same `{"status": "error", ...}` shape\n    -- one error-reporting convention across every AWS-backed command in\n    this project.\n    """\n    try:\n        client = get_aws_client(\n            "s3", region=region, profile=profile, endpoint_url=endpoint_url\n        )\n        ensure_bucket(client, bucket, region=region)\n        key = upload_report(client, bucket, report, prefix=prefix)\n    except botocore.exceptions.NoCredentialsError as exc:\n        return {"status": "error", "error": "no-credentials", "message": str(exc)}\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "Unknown")\n        return {"status": "error", "error": code, "message": str(exc)}\n\n    return {"status": "ok", "bucket": bucket, "key": key}\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-5--watch-it-pass-against-real-floci",children:"Step 5 \u2014 Watch it pass, against real Floci"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_reportstore.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"tests/test_reportstore.py::test_ensure_bucket_is_idempotent PASSED       [ 10%]\ntests/test_reportstore.py::test_upload_report_returns_a_timestamped_key_under_the_prefix PASSED [ 20%]\ntests/test_reportstore.py::test_upload_then_download_round_trips_the_same_report PASSED [ 30%]\ntests/test_reportstore.py::test_two_uploads_in_a_row_get_different_keys PASSED [ 40%]\ntests/test_reportstore.py::test_list_reports_returns_most_recent_first PASSED [ 50%]\ntests/test_reportstore.py::test_list_reports_respects_max_results PASSED [ 60%]\ntests/test_reportstore.py::test_list_reports_on_empty_bucket_returns_empty_list PASSED [ 70%]\ntests/test_reportstore.py::test_upload_report_to_bucket_returns_an_ok_report PASSED [ 80%]\ntests/test_reportstore.py::test_upload_report_to_bucket_catches_client_error PASSED [ 90%]\ntests/test_reportstore.py::test_upload_report_to_bucket_catches_no_credentials_error PASSED [100%]\n\n============================== 10 passed in 1.18s ===============================\n"})}),"\n",(0,r.jsx)(n.p,{children:"Ten tests, and eight of them ran real S3 API calls against your own running Floci container --\nnot a mock."}),"\n",(0,r.jsx)(n.h3,{id:"step-6--wire-the-cli-and-see-it-work-live",children:"Step 6 \u2014 Wire the CLI, and see it work live"}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"report-upload"})," command to ",(0,r.jsx)(n.code,{children:"src/platformops/cli.py"})," (import ",(0,r.jsx)(n.code,{children:"upload_report_to_bucket"})," next\nto the other ",(0,r.jsx)(n.code,{children:"platformops.*"})," imports, then the command itself -- see the module's source if you\nwant to compare against your own). Then try it against Floci for real:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'echo \'{"status": "ok", "service": "checkout-api", "concerns": []}\' > /tmp/m22_readiness_report.json\nuv run platformops report-upload /tmp/m22_readiness_report.json --bucket platformops-reports-demo --region us-east-1\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"uploaded to s3://platformops-reports-demo/reports/2026-08-04T19-52-42-034224Z.json\n"})}),"\n",(0,r.jsx)(n.admonition,{title:"This output is point-in-time",type:"warning",children:(0,r.jsxs)(n.p,{children:["The timestamp in the key is generated fresh on every run -- yours will differ. The shape\n(",(0,r.jsx)(n.code,{children:"reports/<timestamp>.json"}),") stays the same every time."]})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run platformops report-upload /tmp/m22_readiness_report.json --bucket platformops-reports-demo --region us-east-1 --json\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",children:'{\n  "status": "ok",\n  "bucket": "platformops-reports-demo",\n  "key": "reports/2026-08-04T19-52-51-468095Z.json"\n}\n'})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-3--dynamodb-findings-store-test-first",children:"Phase 3 \u2014 DynamoDB findings store, test-first"}),"\n",(0,r.jsx)(n.h3,{id:"step-1--write-the-failing-test-file-1",children:"Step 1 \u2014 Write the failing test file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_findingsstore.py << \'PYEOF\'\nimport uuid\n\nimport pytest\n\nfrom platformops import findingsstore\nfrom platformops.awsclient import get_aws_client\n\npytestmark = pytest.mark.usefixtures("require_floci")\n\nFLOCI_ENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\n\n@pytest.fixture\ndef ddb_client():\n    return get_aws_client("dynamodb", region=REGION, endpoint_url=FLOCI_ENDPOINT)\n\n\n@pytest.fixture\ndef table(ddb_client):\n    name = f"platformops-test-{uuid.uuid4().hex[:12]}"\n    findingsstore.ensure_table(ddb_client, name)\n    yield name\n    ddb_client.delete_table(TableName=name)\n\n\n# ---------------------------------------------------------------------------\n# ensure_table -- idempotent create, composite key (service + timestamp).\n# ---------------------------------------------------------------------------\n\n\ndef test_ensure_table_is_idempotent(ddb_client):\n    name = f"platformops-test-{uuid.uuid4().hex[:12]}"\n\n    findingsstore.ensure_table(ddb_client, name)\n    findingsstore.ensure_table(ddb_client, name)  # must not raise\n\n    description = ddb_client.describe_table(TableName=name)\n    assert description["Table"]["TableStatus"] == "ACTIVE"\n\n    ddb_client.delete_table(TableName=name)\n\n\n# ---------------------------------------------------------------------------\n# put_finding / get_finding -- a finding round-trips exactly, keyed on\n# service (partition) + timestamp (sort).\n# ---------------------------------------------------------------------------\n\n\ndef test_put_then_get_finding_round_trips(ddb_client, table):\n    written = findingsstore.put_finding(\n        ddb_client,\n        table,\n        service="checkout-api",\n        severity="high",\n        message="dashboard_url missing",\n        timestamp="2026-08-05T00:00:00+00:00",\n    )\n\n    fetched = findingsstore.get_finding(\n        ddb_client, table, service="checkout-api", timestamp="2026-08-05T00:00:00+00:00"\n    )\n\n    assert fetched == written\n    assert fetched["severity"] == "high"\n    assert fetched["message"] == "dashboard_url missing"\n\n\ndef test_get_finding_returns_none_when_missing(ddb_client, table):\n    fetched = findingsstore.get_finding(\n        ddb_client, table, service="nonexistent", timestamp="2026-08-05T00:00:00+00:00"\n    )\n\n    assert fetched is None\n\n\ndef test_put_finding_generates_a_timestamp_when_not_given(ddb_client, table):\n    written = findingsstore.put_finding(\n        ddb_client, table, service="billing-api", severity="low", message="ok"\n    )\n\n    assert "timestamp" in written\n    fetched = findingsstore.get_finding(\n        ddb_client, table, service="billing-api", timestamp=written["timestamp"]\n    )\n    assert fetched == written\n\n\n# ---------------------------------------------------------------------------\n# query_findings -- every finding for one service (the partition key), most\n# recent first. This is why the table needs a composite key up front: a\n# service can have many findings over time, and DynamoDB\'s key schema is\n# fixed at table-create time, unlike a SQL table where you can add an index\n# later.\n# ---------------------------------------------------------------------------\n\n\ndef test_query_findings_returns_only_the_matching_service_most_recent_first(\n    ddb_client, table\n):\n    findingsstore.put_finding(\n        ddb_client,\n        table,\n        service="checkout-api",\n        severity="low",\n        message="first",\n        timestamp="2026-08-01T00:00:00+00:00",\n    )\n    findingsstore.put_finding(\n        ddb_client,\n        table,\n        service="checkout-api",\n        severity="high",\n        message="second",\n        timestamp="2026-08-02T00:00:00+00:00",\n    )\n    findingsstore.put_finding(\n        ddb_client,\n        table,\n        service="billing-api",\n        severity="high",\n        message="unrelated",\n        timestamp="2026-08-01T00:00:00+00:00",\n    )\n\n    results = findingsstore.query_findings(ddb_client, table, service="checkout-api")\n\n    assert [r["message"] for r in results] == ["second", "first"]\n\n\ndef test_query_findings_respects_limit(ddb_client, table):\n    for i in range(3):\n        findingsstore.put_finding(\n            ddb_client,\n            table,\n            service="checkout-api",\n            severity="low",\n            message=f"finding-{i}",\n            timestamp=f"2026-08-0{i + 1}T00:00:00+00:00",\n        )\n\n    results = findingsstore.query_findings(\n        ddb_client, table, service="checkout-api", limit=2\n    )\n\n    assert len(results) == 2\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--watch-it-fail-1",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_findingsstore.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_findingsstore.py'.\n...\nE   ImportError: cannot import name 'findingsstore' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_findingsstore.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.07s\n"})}),"\n",(0,r.jsxs)(n.h3,{id:"step-3--write-findingsstorepy",children:["Step 3 \u2014 Write ",(0,r.jsx)(n.code,{children:"findingsstore.py"})]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/findingsstore.py << \'PYEOF\'\n"""platformops.findingsstore -- store and query findings in DynamoDB.\n\nA "finding" here is the same concept `incident_context.py` (Module 20)\nalready calls a "concern": one problem an automated check noticed, with a\nseverity and a message. That script only ever prints a finding once, to\nstdout, for the person watching the terminal right now. This module gives a\nfinding somewhere durable to live, keyed so "every finding for this\nservice, most recent first" is a single, fast query.\n\nDynamoDB is not a SQL table. A SQL table can grow a new index on any column\nwhenever you decide you need one to query by it. DynamoDB\'s key schema --\nthe partition key and, optionally, a sort key -- is fixed at table-create\ntime; the only queries that stay fast without a full scan are ones that\nmatch that key schema. This module picks `service` as the partition key\n(every finding for one service lives together, physically, so "give me\ncheckout-api\'s findings" is one targeted lookup) and `timestamp` as the\nsort key (so results come back ordered, newest first, for free). Deciding\nthat up front, before the first `put_finding()` call, is the whole point of\nthe Deep Dive\'s "hot partition" discussion.\n\nEvery client comes from `platformops.awsclient.get_aws_client()` -- no\nfunction here calls `boto3.client()` directly.\n"""\n\nfrom __future__ import annotations\n\nfrom datetime import UTC, datetime\nfrom typing import Any\n\nimport botocore.exceptions\n\nPARTITION_KEY = "service"\nSORT_KEY = "timestamp"\n\n\ndef ensure_table(client: Any, table_name: str) -> None:\n    """Create `table_name` with the service/timestamp composite key if it does not exist.\n\n    `ResourceInUseException` is DynamoDB\'s answer when the table already\n    exists -- caught by name and treated as success, the same idempotency\n    guard `reportstore.ensure_bucket()` applies to S3\'s\n    `BucketAlreadyOwnedByYou`. `PAY_PER_REQUEST` billing means no read/write\n    capacity to provision or size ahead of time -- the right default for a\n    lab table with unpredictable, low traffic.\n    """\n    try:\n        client.create_table(\n            TableName=table_name,\n            KeySchema=[\n                {"AttributeName": PARTITION_KEY, "KeyType": "HASH"},\n                {"AttributeName": SORT_KEY, "KeyType": "RANGE"},\n            ],\n            AttributeDefinitions=[\n                {"AttributeName": PARTITION_KEY, "AttributeType": "S"},\n                {"AttributeName": SORT_KEY, "AttributeType": "S"},\n            ],\n            BillingMode="PAY_PER_REQUEST",\n        )\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "")\n        if code != "ResourceInUseException":\n            raise\n    client.get_waiter("table_exists").wait(TableName=table_name)\n\n\ndef _deserialize(item: dict[str, Any]) -> dict[str, Any]:\n    return {key: value["S"] for key, value in item.items()}\n\n\ndef put_finding(\n    client: Any,\n    table_name: str,\n    *,\n    service: str,\n    severity: str,\n    message: str,\n    timestamp: str | None = None,\n) -> dict[str, str]:\n    """Write one finding, and return it as the plain dict that was stored.\n\n    `timestamp` defaults to the current time in ISO-8601 -- pass it\n    explicitly (as the lab and tests do) when a specific, reproducible\n    sort-key value matters.\n    """\n    resolved_timestamp = timestamp or datetime.now(UTC).isoformat()\n    finding = {\n        PARTITION_KEY: service,\n        SORT_KEY: resolved_timestamp,\n        "severity": severity,\n        "message": message,\n    }\n    client.put_item(\n        TableName=table_name,\n        Item={key: {"S": value} for key, value in finding.items()},\n    )\n    return finding\n\n\ndef get_finding(\n    client: Any, table_name: str, *, service: str, timestamp: str\n) -> dict[str, str] | None:\n    """Fetch one finding by its full key, or `None` if it does not exist."""\n    response = client.get_item(\n        TableName=table_name,\n        Key={PARTITION_KEY: {"S": service}, SORT_KEY: {"S": timestamp}},\n    )\n    item = response.get("Item")\n    if item is None:\n        return None\n    return _deserialize(item)\n\n\ndef query_findings(\n    client: Any, table_name: str, *, service: str, limit: int | None = None\n) -> list[dict[str, str]]:\n    """Every finding for `service`, most recent first.\n\n    `ScanIndexForward=False` reverses the sort key\'s natural ascending\n    order -- newest timestamp first -- entirely server-side; this function\n    never fetches everything and reverses it in Python.\n    """\n    kwargs: dict[str, Any] = {\n        "TableName": table_name,\n        "KeyConditionExpression": "#svc = :service",\n        "ExpressionAttributeNames": {"#svc": PARTITION_KEY},\n        "ExpressionAttributeValues": {":service": {"S": service}},\n        "ScanIndexForward": False,\n    }\n    if limit is not None:\n        kwargs["Limit"] = limit\n    response = client.query(**kwargs)\n    return [_deserialize(item) for item in response["Items"]]\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4--watch-it-pass-against-real-floci",children:"Step 4 \u2014 Watch it pass, against real Floci"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_findingsstore.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"tests/test_findingsstore.py::test_ensure_table_is_idempotent PASSED      [ 16%]\ntests/test_findingsstore.py::test_put_then_get_finding_round_trips PASSED [ 33%]\ntests/test_findingsstore.py::test_get_finding_returns_none_when_missing PASSED [ 50%]\ntests/test_findingsstore.py::test_put_finding_generates_a_timestamp_when_not_given PASSED [ 66%]\ntests/test_findingsstore.py::test_query_findings_returns_only_the_matching_service_most_recent_first PASSED [ 83%]\ntests/test_findingsstore.py::test_query_findings_respects_limit PASSED   [100%]\n\n============================== 6 passed in 0.78s ===============================\n"})}),"\n",(0,r.jsx)(n.h3,{id:"step-5--see-it-work-live",children:"Step 5 \u2014 See it work live"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m22_findings_demo.py << \'PYEOF\'\nimport json\n\nfrom platformops.awsclient import get_aws_client\nfrom platformops import findingsstore\n\nclient = get_aws_client("dynamodb", region="us-east-1", endpoint_url="http://localhost:4566")\n\ntable = "demo-findings"\nfindingsstore.ensure_table(client, table)\n\nfindingsstore.put_finding(\n    client, table, service="checkout-api", severity="high",\n    message="dashboard_url missing", timestamp="2026-08-01T09-00-00+00:00",\n)\nfindingsstore.put_finding(\n    client, table, service="checkout-api", severity="low",\n    message="alert_channel set but unused", timestamp="2026-08-03T09-00-00+00:00",\n)\n\nprint("--- findings for checkout-api, most recent first ---")\nprint(json.dumps(findingsstore.query_findings(client, table, service="checkout-api"), indent=2))\n\nclient.delete_table(TableName=table)\nPYEOF\nuv run python /tmp/m22_findings_demo.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:'--- findings for checkout-api, most recent first ---\n[\n  {\n    "service": "checkout-api",\n    "timestamp": "2026-08-03T09-00-00+00:00",\n    "severity": "low",\n    "message": "alert_channel set but unused"\n  },\n  {\n    "service": "checkout-api",\n    "timestamp": "2026-08-01T09-00-00+00:00",\n    "severity": "high",\n    "message": "dashboard_url missing"\n  }\n]\n'})}),"\n",(0,r.jsxs)(n.p,{children:["The most recently written finding (",(0,r.jsx)(n.code,{children:"2026-08-03"}),") comes back first, entirely server-side, from\n",(0,r.jsx)(n.code,{children:"ScanIndexForward=False"})," -- not from sorting in Python."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-4--sqs-work-queue-with-a-dead-letter-queue-test-first",children:"Phase 4 \u2014 SQS work queue with a dead-letter queue, test-first"}),"\n",(0,r.jsx)(n.h3,{id:"step-1--write-the-failing-test-file-2",children:"Step 1 \u2014 Write the failing test file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > tests/test_workqueue.py << \'PYEOF\'\nimport uuid\n\nimport botocore.exceptions\nimport pytest\n\nfrom platformops import workqueue\nfrom platformops.awsclient import get_aws_client\n\npytestmark = pytest.mark.usefixtures("require_floci")\n\nFLOCI_ENDPOINT = "http://localhost:4566"\nREGION = "us-east-1"\n\n\n@pytest.fixture\ndef sqs_client():\n    return get_aws_client("sqs", region=REGION, endpoint_url=FLOCI_ENDPOINT)\n\n\n@pytest.fixture\ndef queue_name():\n    return f"platformops-test-{uuid.uuid4().hex[:12]}"\n\n\ndef _teardown_queue(client, queue_url):\n    client.delete_queue(QueueUrl=queue_url)\n\n\n# ---------------------------------------------------------------------------\n# ensure_queue -- idempotent create, and it round-trips VisibilityTimeout /\n# ReceiveMessageWaitTimeSeconds as real queue attributes, not just\n# arguments this module remembers.\n# ---------------------------------------------------------------------------\n\n\ndef test_ensure_queue_is_idempotent_and_sets_attributes(sqs_client, queue_name):\n    url_one = workqueue.ensure_queue(\n        sqs_client, queue_name, visibility_timeout=15, wait_time_seconds=5\n    )\n    url_two = workqueue.ensure_queue(\n        sqs_client, queue_name, visibility_timeout=15, wait_time_seconds=5\n    )\n\n    assert url_one == url_two\n\n    attrs = sqs_client.get_queue_attributes(\n        QueueUrl=url_one,\n        AttributeNames=["VisibilityTimeout", "ReceiveMessageWaitTimeSeconds"],\n    )["Attributes"]\n    assert attrs["VisibilityTimeout"] == "15"\n    assert attrs["ReceiveMessageWaitTimeSeconds"] == "5"\n\n    _teardown_queue(sqs_client, url_one)\n\n\n# ---------------------------------------------------------------------------\n# send_message / receive_messages / delete_message -- the basic round trip.\n# ---------------------------------------------------------------------------\n\n\ndef test_send_then_receive_then_delete_round_trip(sqs_client, queue_name):\n    queue_url = workqueue.ensure_queue(sqs_client, queue_name)\n\n    message_id = workqueue.send_message(sqs_client, queue_url, "do the thing")\n    assert message_id\n\n    messages = workqueue.receive_messages(sqs_client, queue_url, max_messages=1)\n    assert len(messages) == 1\n    assert messages[0]["body"] == "do the thing"\n\n    workqueue.delete_message(sqs_client, queue_url, messages[0]["receipt_handle"])\n    remaining = workqueue.receive_messages(\n        sqs_client, queue_url, max_messages=1, wait_time_seconds=1\n    )\n    assert remaining == []\n\n    _teardown_queue(sqs_client, queue_url)\n\n\n# ---------------------------------------------------------------------------\n# Dead-letter-queue mechanics -- a message that is received (and never\n# deleted) past maxReceiveCount moves to the DLQ instead of being retried\n# forever or silently dropped. This is the real, working code the lesson\n# and deep dive point back to.\n# ---------------------------------------------------------------------------\n\n\ndef test_dead_letter_queue_receives_a_message_after_max_receive_count(\n    sqs_client, queue_name\n):\n    dlq_name = f"{queue_name}-dlq"\n    dlq_url, dlq_arn = workqueue.ensure_dead_letter_queue(sqs_client, dlq_name)\n    main_url = workqueue.ensure_queue(\n        sqs_client,\n        queue_name,\n        dead_letter_arn=dlq_arn,\n        max_receive_count=2,\n        visibility_timeout=1,\n        wait_time_seconds=1,\n    )\n\n    workqueue.send_message(sqs_client, main_url, "poison message")\n\n    # Receive it, without deleting, across max_receive_count failed\n    # attempts -- each receive with no delete before VisibilityTimeout\n    # expires again is one failed processing attempt. ApproximateReceiveCount\n    # climbs 1, then 2 (== maxReceiveCount). The next delivery attempt after\n    # that is the one Floci redirects to the DLQ instead of the main queue,\n    # so this loop runs one attempt past max_receive_count to trigger it.\n    import time\n\n    receive_counts = []\n    for _ in range(3):\n        received = workqueue.receive_messages(\n            sqs_client, main_url, max_messages=1, wait_time_seconds=2\n        )\n        if received:\n            receive_counts.append(received[0]["approximate_receive_count"])\n        time.sleep(1.5)  # let VisibilityTimeout=1 expire before the next receive\n\n    assert receive_counts == [1, 2]\n\n    dlq_messages = workqueue.receive_messages(\n        sqs_client, dlq_url, max_messages=1, wait_time_seconds=2\n    )\n    assert len(dlq_messages) == 1\n    assert dlq_messages[0]["body"] == "poison message"\n\n    _teardown_queue(sqs_client, main_url)\n    _teardown_queue(sqs_client, dlq_url)\n\n\ndef test_get_queue_arn_returns_a_real_arn(sqs_client, queue_name):\n    queue_url = workqueue.ensure_queue(sqs_client, queue_name)\n\n    arn = workqueue.get_queue_arn(sqs_client, queue_url)\n\n    assert arn.startswith("arn:aws:sqs:")\n    assert queue_name in arn\n\n    _teardown_queue(sqs_client, queue_url)\n\n\n# ---------------------------------------------------------------------------\n# send_to_queue / receive_from_queue -- the CLI-facing wrappers. Each builds\n# its own client through get_aws_client(), ensures the queue, and turns AWS\n# errors into the same {"status": "error", ...} shape reportstore\'s wrapper\n# and cloudinventory.scan_inventory() already use.\n# ---------------------------------------------------------------------------\n\n\ndef test_send_to_queue_returns_an_ok_report(sqs_client, queue_name):\n    result = workqueue.send_to_queue(\n        "do the thing",\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n    )\n\n    assert result["status"] == "ok"\n    assert "message_id" in result\n\n    queue_url = sqs_client.get_queue_url(QueueName=queue_name)["QueueUrl"]\n    _teardown_queue(sqs_client, queue_url)\n\n\ndef test_receive_from_queue_returns_sent_messages(sqs_client, queue_name):\n    workqueue.send_to_queue(\n        "do the thing",\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n    )\n\n    result = workqueue.receive_from_queue(\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n        max_messages=1,\n        wait_time_seconds=2,\n    )\n\n    assert result["status"] == "ok"\n    assert len(result["messages"]) == 1\n    assert result["messages"][0]["body"] == "do the thing"\n\n    queue_url = sqs_client.get_queue_url(QueueName=queue_name)["QueueUrl"]\n    _teardown_queue(sqs_client, queue_url)\n\n\ndef test_receive_from_queue_with_delete_removes_the_message(sqs_client, queue_name):\n    workqueue.send_to_queue(\n        "do the thing",\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n    )\n\n    workqueue.receive_from_queue(\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n        max_messages=1,\n        wait_time_seconds=2,\n        delete=True,\n    )\n    result = workqueue.receive_from_queue(\n        queue_name=queue_name,\n        region=REGION,\n        endpoint_url=FLOCI_ENDPOINT,\n        max_messages=1,\n        wait_time_seconds=1,\n    )\n\n    assert result["messages"] == []\n\n    queue_url = sqs_client.get_queue_url(QueueName=queue_name)["QueueUrl"]\n    _teardown_queue(sqs_client, queue_url)\n\n\ndef test_send_to_queue_catches_client_error(monkeypatch):\n    class BoomClient:\n        def create_queue(self, **kwargs):\n            raise botocore.exceptions.ClientError(\n                {"Error": {"Code": "AccessDenied", "Message": "nope"}}, "CreateQueue"\n            )\n\n    monkeypatch.setattr(workqueue, "get_aws_client", lambda *a, **k: BoomClient())\n\n    result = workqueue.send_to_queue("hi", queue_name="q", region=REGION)\n\n    assert result["status"] == "error"\n    assert result["error"] == "AccessDenied"\n\n\ndef test_receive_from_queue_catches_no_credentials_error(monkeypatch):\n    class BoomClient:\n        def create_queue(self, **kwargs):\n            raise botocore.exceptions.NoCredentialsError()\n\n    monkeypatch.setattr(workqueue, "get_aws_client", lambda *a, **k: BoomClient())\n\n    result = workqueue.receive_from_queue(queue_name="q", region=REGION)\n\n    assert result["status"] == "error"\n    assert result["error"] == "no-credentials"\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2--watch-it-fail-2",children:"Step 2 \u2014 Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_workqueue.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_workqueue.py'.\n...\nE   ImportError: cannot import name 'workqueue' from 'platformops' (.../src/platformops/__init__.py)\n=========================== short test summary info ============================\nERROR tests/test_workqueue.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.07s\n"})}),"\n",(0,r.jsxs)(n.h3,{id:"step-3--write-workqueuepy",children:["Step 3 \u2014 Write ",(0,r.jsx)(n.code,{children:"workqueue.py"})]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > src/platformops/workqueue.py << \'PYEOF\'\n"""platformops.workqueue -- SQS queue automation with a dead-letter-queue pattern.\n\nA queue decouples "something needs doing" from "something is doing it right\nnow" -- a caller sends a message and moves on, a separate process receives\nit and does the work, on its own schedule. That separation only stays safe\nif a message that repeatedly fails to process has somewhere to go besides\n"retried forever" (a poison message wedges the queue for every message\nbehind it) or "silently dropped" (the failure disappears with no trace).\nThis module wires that somewhere-to-go: `ensure_dead_letter_queue()` creates\na DLQ, `ensure_queue()` points a main queue\'s `RedrivePolicy` at it with a\n`maxReceiveCount`, and SQS itself moves a message there once it has been\nreceived and left undeleted that many times.\n\n`VisibilityTimeout` is how long a received-but-not-yet-deleted message stays\nhidden from other receivers -- long enough for the receiver to finish\nprocessing it, or the message reappears and looks like a new delivery.\n`ReceiveMessageWaitTimeSeconds` enables long polling: a `receive_messages()`\ncall waits up to that many seconds for a message to arrive instead of\nreturning empty immediately, trading a slightly slower empty response for\nfar fewer wasted API calls against an often-empty queue.\n\nEvery client comes from `platformops.awsclient.get_aws_client()`.\n"""\n\nfrom __future__ import annotations\n\nimport json\nfrom typing import Any\n\nimport botocore.exceptions\n\nfrom platformops.awsclient import get_aws_client\n\nDEFAULT_VISIBILITY_TIMEOUT = 30\nDEFAULT_WAIT_TIME_SECONDS = 10\n\n\ndef ensure_queue(\n    client: Any,\n    queue_name: str,\n    *,\n    dead_letter_arn: str | None = None,\n    max_receive_count: int = 5,\n    visibility_timeout: int = DEFAULT_VISIBILITY_TIMEOUT,\n    wait_time_seconds: int = DEFAULT_WAIT_TIME_SECONDS,\n) -> str:\n    """Create `queue_name` if it does not exist, and return its URL.\n\n    SQS\'s own `create_queue` is already idempotent by name -- calling it\n    again with the exact same attributes returns the existing queue\'s URL\n    instead of erroring or creating a duplicate. This function relies on\n    that directly rather than pre-checking for the queue\'s existence itself.\n\n    `dead_letter_arn` and `max_receive_count` together set a `RedrivePolicy`\n    attribute -- leave `dead_letter_arn` unset for a queue with no DLQ at\n    all.\n    """\n    attributes: dict[str, str] = {\n        "VisibilityTimeout": str(visibility_timeout),\n        "ReceiveMessageWaitTimeSeconds": str(wait_time_seconds),\n    }\n    if dead_letter_arn is not None:\n        attributes["RedrivePolicy"] = json.dumps(\n            {\n                "deadLetterTargetArn": dead_letter_arn,\n                "maxReceiveCount": max_receive_count,\n            }\n        )\n    response = client.create_queue(QueueName=queue_name, Attributes=attributes)\n    queue_url: str = response["QueueUrl"]\n    return queue_url\n\n\ndef ensure_dead_letter_queue(client: Any, queue_name: str) -> tuple[str, str]:\n    """Create a plain queue meant to be used as a DLQ, and return `(queue_url, queue_arn)`.\n\n    A DLQ is not a special AWS resource type -- it is an ordinary queue that\n    a main queue\'s `RedrivePolicy` happens to point at. This function\'s only\n    job beyond `ensure_queue()` is fetching the ARN, since `RedrivePolicy`\n    needs the DLQ\'s ARN, not its URL.\n    """\n    queue_url = ensure_queue(client, queue_name)\n    return queue_url, get_queue_arn(client, queue_url)\n\n\ndef get_queue_arn(client: Any, queue_url: str) -> str:\n    """Fetch the ARN for a queue URL -- what `RedrivePolicy` needs to name a DLQ."""\n    response = client.get_queue_attributes(\n        QueueUrl=queue_url, AttributeNames=["QueueArn"]\n    )\n    arn: str = response["Attributes"]["QueueArn"]\n    return arn\n\n\ndef send_message(client: Any, queue_url: str, body: str) -> str:\n    """Send one message, and return its message ID."""\n    response = client.send_message(QueueUrl=queue_url, MessageBody=body)\n    message_id: str = response["MessageId"]\n    return message_id\n\n\ndef receive_messages(\n    client: Any,\n    queue_url: str,\n    *,\n    max_messages: int = 1,\n    wait_time_seconds: int | None = None,\n    visibility_timeout: int | None = None,\n) -> list[dict[str, Any]]:\n    """Receive up to `max_messages`, each with its body, receipt handle and receive count.\n\n    `ApproximateReceiveCount` is included on every message returned -- it is\n    SQS\'s own count of how many times this message has been received\n    without being deleted, the same number `RedrivePolicy.maxReceiveCount`\n    compares against to decide when a message moves to the DLQ.\n    """\n    kwargs: dict[str, Any] = {\n        "QueueUrl": queue_url,\n        "MaxNumberOfMessages": max_messages,\n        "AttributeNames": ["ApproximateReceiveCount"],\n    }\n    if wait_time_seconds is not None:\n        kwargs["WaitTimeSeconds"] = wait_time_seconds\n    if visibility_timeout is not None:\n        kwargs["VisibilityTimeout"] = visibility_timeout\n\n    response = client.receive_message(**kwargs)\n    return [\n        {\n            "message_id": message["MessageId"],\n            "receipt_handle": message["ReceiptHandle"],\n            "body": message["Body"],\n            "approximate_receive_count": int(\n                message.get("Attributes", {}).get("ApproximateReceiveCount", "0")\n            ),\n        }\n        for message in response.get("Messages", [])\n    ]\n\n\ndef delete_message(client: Any, queue_url: str, receipt_handle: str) -> None:\n    """Delete a message using the receipt handle from `receive_messages()`.\n\n    Deleting is how a receiver tells SQS "I finished processing this\n    successfully" -- a message that is never deleted is exactly the\n    "processing failed" case this module\'s DLQ pattern exists to handle.\n    """\n    client.delete_message(QueueUrl=queue_url, ReceiptHandle=receipt_handle)\n\n\ndef send_to_queue(\n    body: str,\n    *,\n    queue_name: str,\n    region: str,\n    profile: str | None = None,\n    endpoint_url: str | None = None,\n) -> dict[str, Any]:\n    """The one call a CLI command needs to send a message: build a client, ensure the queue, send.\n\n    Same `{"status": "error", ...}` convention as\n    `reportstore.upload_report_to_bucket()` and\n    `cloudinventory.scan_inventory()`.\n    """\n    try:\n        client = get_aws_client(\n            "sqs", region=region, profile=profile, endpoint_url=endpoint_url\n        )\n        queue_url = ensure_queue(client, queue_name)\n        message_id = send_message(client, queue_url, body)\n    except botocore.exceptions.NoCredentialsError as exc:\n        return {"status": "error", "error": "no-credentials", "message": str(exc)}\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "Unknown")\n        return {"status": "error", "error": code, "message": str(exc)}\n\n    return {"status": "ok", "queue": queue_name, "message_id": message_id}\n\n\ndef receive_from_queue(\n    *,\n    queue_name: str,\n    region: str,\n    profile: str | None = None,\n    endpoint_url: str | None = None,\n    max_messages: int = 1,\n    wait_time_seconds: int | None = None,\n    delete: bool = False,\n) -> dict[str, Any]:\n    """The one call a CLI command needs to receive messages: build a client, ensure the queue, receive.\n\n    `delete=True` deletes every message received before returning -- the\n    CLI\'s way of saying "I processed these successfully". Leaving it\n    `False` (the default) leaves each message in the queue, undeleted, so a\n    learner can watch `ApproximateReceiveCount` climb across repeated\n    receives, the same behavior the dead-letter-queue test exercises.\n    """\n    try:\n        client = get_aws_client(\n            "sqs", region=region, profile=profile, endpoint_url=endpoint_url\n        )\n        queue_url = ensure_queue(client, queue_name)\n        messages = receive_messages(\n            client,\n            queue_url,\n            max_messages=max_messages,\n            wait_time_seconds=wait_time_seconds,\n        )\n        if delete:\n            for message in messages:\n                delete_message(client, queue_url, message["receipt_handle"])\n    except botocore.exceptions.NoCredentialsError as exc:\n        return {"status": "error", "error": "no-credentials", "message": str(exc)}\n    except botocore.exceptions.ClientError as exc:\n        code = exc.response.get("Error", {}).get("Code", "Unknown")\n        return {"status": "error", "error": code, "message": str(exc)}\n\n    return {"status": "ok", "queue": queue_name, "messages": messages}\nPYEOF\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4--watch-it-pass-against-real-floci-1",children:"Step 4 \u2014 Watch it pass, against real Floci"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_workqueue.py -v\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"tests/test_workqueue.py::test_ensure_queue_is_idempotent_and_sets_attributes PASSED [ 11%]\ntests/test_workqueue.py::test_send_then_receive_then_delete_round_trip PASSED [ 22%]\ntests/test_workqueue.py::test_dead_letter_queue_receives_a_message_after_max_receive_count PASSED [ 33%]\ntests/test_workqueue.py::test_get_queue_arn_returns_a_real_arn PASSED    [ 44%]\ntests/test_workqueue.py::test_send_to_queue_returns_an_ok_report PASSED  [ 55%]\ntests/test_workqueue.py::test_receive_from_queue_returns_sent_messages PASSED [ 66%]\ntests/test_workqueue.py::test_receive_from_queue_with_delete_removes_the_message PASSED [ 77%]\ntests/test_workqueue.py::test_send_to_queue_catches_client_error PASSED  [ 88%]\ntests/test_workqueue.py::test_receive_from_queue_catches_no_credentials_error PASSED [100%]\n\n============================== 9 passed in 10.47s ===============================\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The DLQ test alone takes several seconds -- it really waits out a ",(0,r.jsx)(n.code,{children:"VisibilityTimeout"})," twice against\na real running queue, not a mock that returns instantly."]}),"\n",(0,r.jsx)(n.h3,{id:"step-5--wire-the-cli-and-see-it-work-live",children:"Step 5 \u2014 Wire the CLI, and see it work live"}),"\n",(0,r.jsxs)(n.p,{children:["Add ",(0,r.jsx)(n.code,{children:"queue-send"})," and ",(0,r.jsx)(n.code,{children:"queue-receive"})," commands to ",(0,r.jsx)(n.code,{children:"cli.py"})," (same pattern as ",(0,r.jsx)(n.code,{children:"report-upload"}),"). Then:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'uv run platformops queue-send work-queue "do the thing" --region us-east-1\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"sent to work-queue: 3c12fb68-7a2b-4918-8cc7-cb747034504d\n"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run platformops queue-receive work-queue --region us-east-1 --wait-time 3\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"1 message(s) from work-queue\n  [1] do the thing\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.code,{children:"[1]"})," is ",(0,r.jsx)(n.code,{children:"ApproximateReceiveCount"})," -- this message has been delivered once and was not\ndeleted, so a second ",(0,r.jsx)(n.code,{children:"queue-receive"})," (after the default 30-second ",(0,r.jsx)(n.code,{children:"VisibilityTimeout"})," expires)\nwould show ",(0,r.jsx)(n.code,{children:"[2]"}),"."]}),"\n",(0,r.jsx)(n.h3,{id:"step-6--see-the-dead-letter-queue-work-live-end-to-end",children:"Step 6 \u2014 See the dead-letter queue work, live, end to end"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m22_dlq_demo.py << \'PYEOF\'\nimport json\nimport time\n\nfrom platformops.awsclient import get_aws_client\nfrom platformops import workqueue\n\nclient = get_aws_client("sqs", region="us-east-1", endpoint_url="http://localhost:4566")\n\ndlq_url, dlq_arn = workqueue.ensure_dead_letter_queue(client, "demo-poison-dlq")\nmain_url = workqueue.ensure_queue(\n    client, "demo-poison-queue", dead_letter_arn=dlq_arn,\n    max_receive_count=2, visibility_timeout=1, wait_time_seconds=1,\n)\nworkqueue.send_message(client, main_url, "poison message")\nprint("sent \'poison message\' to the main queue\\n")\n\nfor attempt in range(1, 4):\n    received = workqueue.receive_messages(client, main_url, max_messages=1, wait_time_seconds=2)\n    if received:\n        msg = received[0]\n        print(f"attempt {attempt}: received on MAIN queue, "\n              f"ApproximateReceiveCount={msg[\'approximate_receive_count\']} "\n              f"(not deleting -- simulating a failed processing attempt)")\n    else:\n        print(f"attempt {attempt}: nothing on MAIN queue")\n    time.sleep(1.5)\n\nprint()\ndlq_messages = workqueue.receive_messages(client, dlq_url, max_messages=1, wait_time_seconds=2)\nprint(f"found on DLQ: {json.dumps(dlq_messages[0], indent=2)}")\n\nclient.delete_queue(QueueUrl=main_url)\nclient.delete_queue(QueueUrl=dlq_url)\nPYEOF\nuv run python /tmp/m22_dlq_demo.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:'sent \'poison message\' to the main queue\n\nattempt 1: received on MAIN queue, ApproximateReceiveCount=1 (not deleting -- simulating a failed processing attempt)\nattempt 2: received on MAIN queue, ApproximateReceiveCount=2 (not deleting -- simulating a failed processing attempt)\nattempt 3: nothing on MAIN queue\n\nfound on DLQ: {\n  "message_id": "24a8d025-5e95-433b-ba4a-466372c5225e",\n  "receipt_handle": "72554280-9db0-4046-8187-9c652d349836",\n  "body": "poison message",\n  "approximate_receive_count": 4\n}\n'})}),"\n",(0,r.jsx)(n.admonition,{title:"This output is point-in-time",type:"warning",children:(0,r.jsxs)(n.p,{children:["The message ID and receipt handle are generated fresh by Floci on every run -- yours will differ.\nThe shape (two deliveries on the main queue, then nothing, then the same message on the DLQ) is\nwhat stays the same every time. ",(0,r.jsx)(n.code,{children:"approximate_receive_count"})," on the DLQ (",(0,r.jsx)(n.code,{children:"4"}),", not ",(0,r.jsx)(n.code,{children:"2"}),") reflects one\nmore internal delivery attempt SQS made while deciding to redirect the message -- what matters is\nthat it landed in the DLQ at all, not that specific number."]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"phase-5--full-quality-gate-and-version-bump",children:"Phase 5 \u2014 Full quality gate and version bump"}),"\n",(0,r.jsxs)(n.p,{children:["Unset ",(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," before running the full suite. boto3 reads this variable natively, on\nevery client it builds -- including the ones Module 21's ",(0,r.jsx)(n.code,{children:"test_cloudinventory.py"})," builds inside\na ",(0,r.jsx)(n.code,{children:"@mock_aws"})," block. With the variable still exported from Phase 0, those ",(0,r.jsx)(n.code,{children:"moto"}),"-mocked EC2\ncalls get redirected to Floci's real container instead of moto's clean, isolated fixture, and\nthree of Module 21's tests fail with confusing, unrelated-looking assertion errors. This is\nexactly the \"one exported variable, invisible everywhere\" risk the lesson's section on\n",(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," warns about -- here it is showing up for real:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"unset AWS_ENDPOINT_URL\nuv run pytest -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 30%]\n........................................................................ [ 60%]\n........................................................................ [ 90%]\n.......................                                                  [100%]\n239 passed in 25.76s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Up from 198 tests at ",(0,r.jsx)(n.code,{children:"v1.6.0"})," -- 41 new tests across ",(0,r.jsx)(n.code,{children:"test_awsclient.py"}),", ",(0,r.jsx)(n.code,{children:"test_reportstore.py"}),",\n",(0,r.jsx)(n.code,{children:"test_findingsstore.py"}),", ",(0,r.jsx)(n.code,{children:"test_workqueue.py"})," and the new CLI wiring in ",(0,r.jsx)(n.code,{children:"test_cli.py"}),". None of the\ncommands for the rest of this phase (linting, type-checking, the version bump) touch AWS at all,\nso ",(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," stays unset from here on -- re-export it (Phase 0's command) only if you\ncome back to run a Floci-touching CLI command or test after this point."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'uv run pytest tests/test_cli.py -k "report_upload or queue_send or queue_receive" -q\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"..........                                                               [100%]\n10 passed, 46 deselected in 0.36s\n"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format --check src/ tests/\nuv run mypy src/\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"All checks passed!\n36 files already formatted\nSuccess: no issues found in 18 source files\n"})}),"\n",(0,r.jsx)(n.p,{children:"Bump the version and update the changelog:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run python -c \"\npath = 'pyproject.toml'\ntext = open(path).read()\ntext = text.replace('version = \\\"1.6.0\\\"', 'version = \\\"1.7.0\\\"', 1)\nopen(path, 'w').write(text)\n\"\nuv sync -q\nuv run platformops version\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 1.7.0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"## [1.7.0] - Local AWS Automation Environment"})," section to ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," above the previous\nrelease, following the same ",(0,r.jsx)(n.code,{children:"### Added"})," structure every prior release entry already uses."]}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"floci doctor"})," shows ",(0,r.jsx)(n.code,{children:"docker.daemon"})," failing."]})," Open Docker Desktop (or start your Docker\ndaemon) and re-run ",(0,r.jsx)(n.code,{children:"floci doctor"})," -- Floci cannot start a container without it. A ",(0,r.jsx)(n.code,{children:"docker.socket"}),"\nfailure alone, with ",(0,r.jsx)(n.code,{children:"docker.daemon"})," passing, is a known false negative on Rancher Desktop and\nsimilar setups -- safe to ignore."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"floci start"})," reports ",(0,r.jsx)(n.code,{children:"Error: Container 'floci' is already running"}),"."]})," This is not a\nfailure -- unlike some tools, ",(0,r.jsx)(n.code,{children:"floci start"})," does not silently reuse an already-running\ncontainer, it errors instead. Run ",(0,r.jsx)(n.code,{children:"floci doctor"})," to confirm the existing container is healthy\nand move on. If it is genuinely stuck in a bad state, ",(0,r.jsx)(n.code,{children:"floci stop"})," (which removes it) then\n",(0,r.jsx)(n.code,{children:"floci start"})," again creates a fresh one -- but see the warning above about what that costs."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"A test or CLI command hangs instead of failing fast."})," Confirm ",(0,r.jsx)(n.code,{children:"AWS_ENDPOINT_URL"})," is exported\ncorrectly to ",(0,r.jsx)(n.code,{children:"http://localhost:4566"})," (not ",(0,r.jsx)(n.code,{children:"https://"}),", no trailing slash) and that\n",(0,r.jsx)(n.code,{children:"floci doctor"})," shows ",(0,r.jsx)(n.code,{children:"endpoint.reachable \u2713"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"botocore.exceptions.EndpointConnectionError"})," or connection refused."]})," Floci is not running.\nRun ",(0,r.jsx)(n.code,{children:"floci start"}),' and wait for "Floci AWS is ready" before retrying.']}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:["A test file in Phase 2-4 reports ",(0,r.jsx)(n.code,{children:"SKIPPED"})," instead of running."]})," This is ",(0,r.jsx)(n.code,{children:"require_floci"}),"\nworking as designed -- it means Floci was not reachable when the test session started. Run\n",(0,r.jsx)(n.code,{children:"floci doctor"})," to confirm it is up, then re-run ",(0,r.jsx)(n.code,{children:"pytest"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"test_dead_letter_queue_receives_a_message_after_max_receive_count"})," fails intermittently."]}),"\nThis test relies on real wall-clock time (",(0,r.jsx)(n.code,{children:"VisibilityTimeout=1"})," second, with ",(0,r.jsx)(n.code,{children:"time.sleep(1.5)"}),"\nbetween receives). A heavily loaded laptop can occasionally make one iteration land early --\nre-run it on its own with ",(0,r.jsx)(n.code,{children:"uv run pytest tests/test_workqueue.py -k dead_letter -v"})," to confirm."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"uv run platformops version"})," still reports ",(0,r.jsx)(n.code,{children:"1.6.0"}),"."]})," Run ",(0,r.jsx)(n.code,{children:"uv sync"})," again after editing\n",(0,r.jsx)(n.code,{children:"pyproject.toml"})," -- the installed package's version comes from the last sync, not the file on\ndisk."]}),"\n"]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,r.jsxs)(n.p,{children:["This lab's demo resources are separate from the ones its automated tests create and clean up\n(",(0,r.jsx)(n.code,{children:"test_reportstore.py"}),", ",(0,r.jsx)(n.code,{children:"test_findingsstore.py"})," and ",(0,r.jsx)(n.code,{children:"test_workqueue.py"})," each delete their own\nbucket/table/queue in a fixture teardown already). Clean up what the manual steps above created:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -f /tmp/m22_readiness_report.json /tmp/m22_findings_demo.py /tmp/m22_dlq_demo.py\n\nuv run python -c \"\nfrom platformops.awsclient import get_aws_client\nfrom platformops import reportstore\n\nclient = get_aws_client('s3', region='us-east-1', endpoint_url='http://localhost:4566')\nreportstore.empty_bucket(client, 'platformops-reports-demo')\nclient.delete_bucket(Bucket='platformops-reports-demo')\n\"\n\nuv run python -c \"\nfrom platformops.awsclient import get_aws_client\n\nclient = get_aws_client('sqs', region='us-east-1', endpoint_url='http://localhost:4566')\nurl = client.get_queue_url(QueueName='work-queue')['QueueUrl']\nclient.delete_queue(QueueUrl=url)\n\"\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"src/platformops/awsclient.py"}),", ",(0,r.jsx)(n.code,{children:"reportstore.py"}),", ",(0,r.jsx)(n.code,{children:"findingsstore.py"}),", ",(0,r.jsx)(n.code,{children:"workqueue.py"}),", the three new\nCLI commands, ",(0,r.jsx)(n.code,{children:"tests/conftest.py"}),", the four new test files, the ",(0,r.jsx)(n.code,{children:"v1.7.0"})," version bump and the\n",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," entry all stay -- they are permanent additions to the project."]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsxs)(n.strong,{children:["Do not run ",(0,r.jsx)(n.code,{children:"floci stop"}),"."]})," Floci itself stays running -- later modules in this course use it\ntoo, and stopping it removes the container along with every bucket, table and queue it holds, not\njust this lab's own. If you need to free up resources on your laptop for another lab profile, ask\nin the module's Troubleshooting section for that specific module rather than stopping Floci here."]})]})}function u(e={}){let{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(d,{...e})}):d(e)}},8453(e,n,t){t.d(n,{R:()=>o,x:()=>a});var s=t(6540);let r={},i=s.createContext(r);function o(e){let n=s.useContext(i);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),s.createElement(i.Provider,{value:n},e.children)}}}]);