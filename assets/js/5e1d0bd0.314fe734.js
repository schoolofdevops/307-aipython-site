"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["2153"],{1577(e,n,t){t.r(n),t.d(n,{metadata:()=>s,default:()=>p,frontMatter:()=>o,contentTitle:()=>a,toc:()=>d,assets:()=>l});var s=JSON.parse('{"id":"m33-mlops-boundary/lab","title":"Lab: Python at the MLOps Boundary","description":"Lab intent","source":"@site/docs/m33-mlops-boundary/lab.md","sourceDirName":"m33-mlops-boundary","slug":"/m33-mlops-boundary/lab","permalink":"/307-aipython-site/docs/m33-mlops-boundary/lab","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":2,"frontMatter":{"sidebar_position":2,"title":"Lab: Python at the MLOps Boundary"},"sidebar":"courseSidebar","previous":{"title":"Lesson: Python at the MLOps Boundary","permalink":"/307-aipython-site/docs/m33-mlops-boundary/lesson"},"next":{"title":"Quiz: Python at the MLOps Boundary","permalink":"/307-aipython-site/docs/m33-mlops-boundary/quiz"}}'),r=t(4848),i=t(8453);let o={sidebar_position:2,title:"Lab: Python at the MLOps Boundary"},a="Lab \u2014 Python at the MLOps Boundary",l={},d=[{value:"Lab intent",id:"lab-intent",level:2},{value:"Phase 0 -- Why this lab talks to saved MLflow responses, not a live server",id:"phase-0----why-this-lab-talks-to-saved-mlflow-responses-not-a-live-server",level:2},{value:"Phase 1 -- TDD: <code>AIServiceDefinition</code>, the new <code>ai-service</code> type",id:"phase-1----tdd-aiservicedefinition-the-new-ai-service-type",level:2},{value:"Step 1 -- Write the failing tests",id:"step-1----write-the-failing-tests",level:3},{value:"Step 2 -- Watch it fail",id:"step-2----watch-it-fail",level:3},{value:"Step 3 -- Write the model",id:"step-3----write-the-model",level:3},{value:"Step 4 -- Watch it pass",id:"step-4----watch-it-pass",level:3},{value:"Phase 2 -- TDD: MLflow-shaped read-only endpoints in <code>httpclient.py</code>",id:"phase-2----tdd-mlflow-shaped-read-only-endpoints-in-httpclientpy",level:2},{value:"Step 1 -- Save three example MLflow responses as fixtures",id:"step-1----save-three-example-mlflow-responses-as-fixtures",level:3},{value:"Step 2 -- Write the failing tests",id:"step-2----write-the-failing-tests",level:3},{value:"Step 3 -- Watch it fail",id:"step-3----watch-it-fail",level:3},{value:"Step 4 -- Add the three functions to <code>httpclient.py</code>",id:"step-4----add-the-three-functions-to-httpclientpy",level:3},{value:"Step 5 -- Watch it pass",id:"step-5----watch-it-pass",level:3},{value:"Phase 3 -- TDD: <code>ai_inspect.py</code> -- fetch evidence, then judge it separately",id:"phase-3----tdd-ai_inspectpy----fetch-evidence-then-judge-it-separately",level:2},{value:"Step 1 -- Write the failing tests",id:"step-1----write-the-failing-tests-1",level:3},{value:"Step 2 -- Watch it fail",id:"step-2----watch-it-fail-1",level:3},{value:"Step 3 -- Write the module",id:"step-3----write-the-module",level:3},{value:"Step 4 -- Watch it pass",id:"step-4----watch-it-pass-1",level:3},{value:"Phase 4 -- Wire the CLI, then run it against three real scenarios",id:"phase-4----wire-the-cli-then-run-it-against-three-real-scenarios",level:2},{value:"Step 1 -- Add the <code>ai-inspect</code> command",id:"step-1----add-the-ai-inspect-command",level:3},{value:"Step 2 -- Create the service definition file",id:"step-2----create-the-service-definition-file",level:3},{value:"Step 3 -- Run it live: a healthy workload",id:"step-3----run-it-live-a-healthy-workload",level:3},{value:"Step 4 -- Run it live: a failed training run",id:"step-4----run-it-live-a-failed-training-run",level:3},{value:"Step 5 -- Run it live: a batch workload, no endpoint to check",id:"step-5----run-it-live-a-batch-workload-no-endpoint-to-check",level:3},{value:"Phase 5 -- Full quality gate and version bump",id:"phase-5----full-quality-gate-and-version-bump",level:2},{value:"Teardown",id:"teardown",level:2}];function c(e){let n={admonition:"admonition",code:"code",em:"em",h1:"h1",h2:"h2",h3:"h3",header:"header",hr:"hr",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,i.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"lab--python-at-the-mlops-boundary",children:"Lab \u2014 Python at the MLOps Boundary"})}),"\n",(0,r.jsx)(n.h2,{id:"lab-intent",children:"Lab intent"}),"\n",(0,r.jsxs)(n.p,{children:["Add a new kind of service definition, ",(0,r.jsx)(n.code,{children:"AIServiceDefinition"})," (model registry, model version, serving\nruntime, endpoint, inference mode), next to the ",(0,r.jsx)(n.code,{children:"ServiceDefinition"})," this project already has. Add a\nsmall, typed client against MLflow's own REST API shape. Build ",(0,r.jsx)(n.code,{children:"platformops.ai_inspect"}),", a\nfetch-then-judge inspector that reports whether a model version is ready, whether its training run\nfinished cleanly, and whether its serving endpoint is healthy. Wire it into the CLI as\n",(0,r.jsx)(n.code,{children:"platformops ai-inspect"}),". Release: ",(0,r.jsx)(n.strong,{children:"v2.8.0 -- AI Workload Inspector"}),"."]}),"\n",(0,r.jsxs)(n.admonition,{title:"Where this lab starts",type:"info",children:[(0,r.jsxs)(n.p,{children:["This lab assumes ",(0,r.jsx)(n.code,{children:"~/platformops"})," at ",(0,r.jsx)(n.code,{children:"v2.7.0"}),", from Module 32."]}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nuv run platformops version\n"})}),(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 2.7.0\n"})})]}),"\n",(0,r.jsx)(n.h2,{id:"phase-0----why-this-lab-talks-to-saved-mlflow-responses-not-a-live-server",children:"Phase 0 -- Why this lab talks to saved MLflow responses, not a live server"}),"\n",(0,r.jsxs)(n.p,{children:["This course's lab tools are fixed: git, Docker, ",(0,r.jsx)(n.code,{children:"uv"}),", ",(0,r.jsx)(n.code,{children:"kind"}),", ",(0,r.jsx)(n.code,{children:"kubectl"}),", ",(0,r.jsx)(n.code,{children:"claude"}),", Floci, ",(0,r.jsx)(n.code,{children:"syft"}),",\n",(0,r.jsx)(n.code,{children:"grype"})," -- see the Environment page. MLflow, the machine-learning tracking and registry tool this\nmodule's lesson introduced, is not one of them. This lab does not install or start one."]}),"\n",(0,r.jsxs)(n.p,{children:["Instead, this phase follows the same pattern Module 26 (",(0,r.jsx)(n.code,{children:"httpclient.py"}),"'s GitHub endpoints) and\nModule 30 (",(0,r.jsx)(n.code,{children:"httpclient.py"}),"'s Prometheus/Alertmanager-shaped endpoints) already used: write a small,\ntyped client against a real, documented REST API shape, and test it against saved example JSON\nresponses instead of a live server. ",(0,r.jsx)(n.code,{children:"tests/fixtures/mlflow/"})," holds five such responses -- a found\nregistered model, a ",(0,r.jsx)(n.code,{children:"READY"})," model version, a ",(0,r.jsx)(n.code,{children:"PENDING_REGISTRATION"})," model version, a ",(0,r.jsx)(n.code,{children:"FINISHED"})," run,\nand a ",(0,r.jsx)(n.code,{children:"FAILED"})," run -- shaped exactly the way MLflow's own ",(0,r.jsx)(n.code,{children:"/api/2.0/mlflow/..."})," endpoints respond.\nThe client code this lab writes is the same code that would work against a real MLflow server\nlater; only the tests stay offline."]}),"\n",(0,r.jsxs)(n.h2,{id:"phase-1----tdd-aiservicedefinition-the-new-ai-service-type",children:["Phase 1 -- TDD: ",(0,r.jsx)(n.code,{children:"AIServiceDefinition"}),", the new ",(0,r.jsx)(n.code,{children:"ai-service"})," type"]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"servicedef.py"})," (Module 5) validates a plain web service's operational identity. A model-serving\nworkload needs a few fields a web service never has -- which registry, which exact model version,\nwhich serving runtime, which inference mode. This phase adds ",(0,r.jsx)(n.code,{children:"AIServiceDefinition"})," as its own model,\nnext to ",(0,r.jsx)(n.code,{children:"ServiceDefinition"}),", not as new fields bolted onto it."]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.strong,{children:"Problem statement"})," -- ",(0,r.jsx)(n.code,{children:"AIServiceDefinition"})," needs to validate:"]}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"name"}),", ",(0,r.jsx)(n.code,{children:"team_owner"})," -- the same ownership fields every service definition already carries."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"model_registry"}),", ",(0,r.jsx)(n.code,{children:"registered_model_name"}),", ",(0,r.jsx)(n.code,{children:"model_version"})," -- which registry, which model, and\nwhich exact numbered version (a plain digit string, like MLflow's own version identifiers --\nnever ",(0,r.jsx)(n.code,{children:'"latest"'})," or ",(0,r.jsx)(n.code,{children:'"v4"'}),")."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"serving_runtime"})," -- the program that loads the model and answers requests (for example ",(0,r.jsx)(n.code,{children:"vllm"}),")."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"endpoint"})," -- the URL a live workload answers requests on."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"inference_mode"})," -- ",(0,r.jsx)(n.code,{children:'"online"'})," or ",(0,r.jsx)(n.code,{children:'"batch"'}),", nothing else."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.code,{children:"observability"})," -- the same ",(0,r.jsx)(n.code,{children:"ObservabilityRefs"})," (dashboard URL, alert channel) every service\ndefinition already has."]}),"\n"]}),"\n",(0,r.jsx)(n.h3,{id:"step-1----write-the-failing-tests",children:"Step 1 -- Write the failing tests"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_aiservice.py"',children:'import pytest\n\nfrom platformops.aiservice import (\n    GOOD_AI_EXAMPLE,\n    AIServiceDefinition,\n    validate_ai_service,\n)\n\n\ndef test_a_complete_valid_ai_service_definition_validates_successfully():\n    result = validate_ai_service(GOOD_AI_EXAMPLE)\n    assert isinstance(result, AIServiceDefinition)\n    assert result.name == "support-assistant"\n    assert result.inference_mode == "online"\n    assert result.registered_model_name == "support-assistant-intent-classifier"\n\n\ndef test_a_missing_required_field_fails_with_the_field_required_error():\n    data = {key: value for key, value in GOOD_AI_EXAMPLE.items() if key != "endpoint"}\n\n    errors = validate_ai_service(data)\n\n    assert isinstance(errors, list)\n    assert any(\n        error["loc"] == ("endpoint",) and error["type"] == "missing" for error in errors\n    )\n\n\ndef test_an_invalid_inference_mode_is_rejected_not_silently_accepted():\n    data = {**GOOD_AI_EXAMPLE, "inference_mode": "streaming"}\n\n    errors = validate_ai_service(data)\n\n    assert isinstance(errors, list)\n    assert any(error["loc"] == ("inference_mode",) for error in errors)\n\n\n@pytest.mark.parametrize("bad_version", ["v4", "4.0", "latest", ""])\ndef test_a_non_numeric_model_version_is_rejected(bad_version):\n    data = {**GOOD_AI_EXAMPLE, "model_version": bad_version}\n\n    errors = validate_ai_service(data)\n\n    assert isinstance(errors, list)\n    assert any(error["loc"] == ("model_version",) for error in errors)\n\n\ndef test_batch_inference_mode_is_accepted_with_no_extra_fields_required():\n    data = {**GOOD_AI_EXAMPLE, "inference_mode": "batch"}\n\n    result = validate_ai_service(data)\n\n    assert isinstance(result, AIServiceDefinition)\n    assert result.inference_mode == "batch"\n\n\ndef test_to_summary_reads_as_one_line_naming_the_model_version_and_runtime():\n    result = validate_ai_service(GOOD_AI_EXAMPLE)\n    assert isinstance(result, AIServiceDefinition)\n\n    summary = result.to_summary()\n\n    assert "support-assistant" in summary\n    assert "support-assistant-intent-classifier:4" in summary\n    assert "vllm" in summary\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2----watch-it-fail",children:"Step 2 -- Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_aiservice.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_aiservice.py'.\nHint: make sure your test modules/packages have valid Python names.\nTraceback:\n...\ntests/test_aiservice.py:3: in <module>\n    from platformops.aiservice import (\nE   ModuleNotFoundError: No module named 'platformops.aiservice'\n=========================== short test summary info ============================\nERROR tests/test_aiservice.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.08s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["You should see a real ",(0,r.jsx)(n.code,{children:"ModuleNotFoundError: No module named 'platformops.aiservice'"})," -- the module\ndoes not exist yet."]}),"\n",(0,r.jsx)(n.h3,{id:"step-3----write-the-model",children:"Step 3 -- Write the model"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/aiservice.py"',children:'"""AI service definitions -- PlatformOps v2.8.\n\n`servicedef.py` (Module 5) answers one question about a normal web service:\nwhat does its operational identity look like -- where does it run, who\nowns it, where do the alerts go. An AI-serving workload -- a model behind\nan inference endpoint -- carries the same ownership questions, plus a set\nthat a plain web service never needs answered: which model, which version\nof it, trained and tracked by which run, served by which runtime, and\nreachable how (a live endpoint that answers one request at a time, or a\nbatch job with no endpoint to poll at all).\n\nThink of it the way a pharmacy tracks a batch of medicine, not just the\nshelf it sits on. A shelf label ("aisle 4, cold storage") is what\n`ServiceDefinition` already captures -- where the thing lives. A medicine\nlabel also carries a batch number and an expiry date -- which exact batch\nthis is, and whether it is still the one that passed inspection. A model\nversion is that batch number: `support-assistant-intent-classifier:4` is\nnot the same as `:5`, even if both currently answer requests at the same\nURL, the same way two batches of the same drug are not interchangeable\njust because they sit on the same shelf.\n\n`AIServiceDefinition` is a new, separate model rather than new fields\nbolted onto `ServiceDefinition` -- the same choice `cloudaudit.py` (Module\n24) made when it wrapped `ResourceRecord` instead of extending it. A plain\nweb service will never have a `model_version` or a `serving_runtime`; giving\n`ServiceDefinition` those fields would mean every existing service record\nin this project carries fields that are always empty. `validate_ai_service()`\nmirrors `validate_service()` exactly: it returns the validated model on\nsuccess, or Pydantic\'s own list of error dicts on failure -- never raises,\nso a caller reports every problem in one file instead of stopping at the\nfirst one.\n"""\n\nfrom __future__ import annotations\n\nfrom typing import Any, Literal\n\nfrom pydantic import BaseModel, Field, ValidationError\n\nfrom platformops.servicedef import ObservabilityRefs\n\nInferenceMode = Literal["batch", "online"]\n\n# MLflow\'s own model version identifier is a plain, ever-increasing integer\n# rendered as a string ("1", "2", "3", ...) -- never "v3" or "latest". Only\n# an *exact* version reaches this toolkit: an inspector that let "latest"\n# through would be inspecting whatever the registry happens to point at\n# right now, not the version a deployment record actually claims to run.\n_MODEL_VERSION_PATTERN = r"^\\d+$"\n\n\nclass AIServiceDefinition(BaseModel):\n    """One AI-serving workload\'s operational identity: the model behind it, not just where it runs."""\n\n    name: str\n    team_owner: str\n    model_registry: str\n    registered_model_name: str\n    model_version: str = Field(pattern=_MODEL_VERSION_PATTERN)\n    serving_runtime: str\n    endpoint: str\n    inference_mode: InferenceMode\n    observability: ObservabilityRefs\n\n    def to_summary(self) -> str:\n        """One line an on-call engineer can scan -- names the exact model version, not just the service."""\n        return (\n            f"{self.name} [{self.inference_mode}] "\n            f"model={self.registered_model_name}:{self.model_version} "\n            f"runtime={self.serving_runtime} owner={self.team_owner}"\n        )\n\n\ndef validate_ai_service(\n    data: dict[str, Any],\n) -> AIServiceDefinition | list[dict[str, Any]]:\n    """Validate a raw dict against `AIServiceDefinition`.\n\n    Same contract as `platformops.servicedef.validate_service()`: the\n    validated model on success, or `ValidationError.errors()` -- a plain\n    list of dicts -- on failure, so a caller never has to wrap this in its\n    own try/except to report every problem at once.\n    """\n    try:\n        return AIServiceDefinition.model_validate(data)\n    except ValidationError as exc:\n        return [dict(error) for error in exc.errors()]\n\n\nGOOD_AI_EXAMPLE = {\n    "name": "support-assistant",\n    "team_owner": "ml-platform-team",\n    "model_registry": "mlflow",\n    "registered_model_name": "support-assistant-intent-classifier",\n    "model_version": "4",\n    "serving_runtime": "vllm",\n    "endpoint": "https://support-assistant.internal.example.com/v1/predict",\n    "inference_mode": "online",\n    "observability": {\n        "dashboard_url": "https://grafana.example.com/d/support-assistant",\n        "alert_channel": "#support-assistant-alerts",\n    },\n}\n\nBAD_AI_EXAMPLE = {\n    "name": "support-assistant",\n    "team_owner": "ml-platform-team",\n    "model_registry": "mlflow",\n    "registered_model_name": "support-assistant-intent-classifier",\n    "serving_runtime": "vllm",\n    "endpoint": "https://support-assistant.internal.example.com/v1/predict",\n    "inference_mode": "online",\n    "observability": {\n        "dashboard_url": "https://grafana.example.com/d/support-assistant",\n        "alert_channel": "#support-assistant-alerts",\n    },\n}\n\n\ndef _print_result(label: str, data: dict) -> None:\n    result = validate_ai_service(data)\n    print(f"{label}:")\n    if isinstance(result, AIServiceDefinition):\n        print(f"  OK -- {result.to_summary()}")\n    else:\n        for error in result:\n            location = ".".join(str(part) for part in error["loc"])\n            print(f"  FAIL -- {location}: {error[\'msg\']}")\n\n\nif __name__ == "__main__":\n    _print_result("Good AI service definition", GOOD_AI_EXAMPLE)\n    print()\n    _print_result("Bad AI service definition (missing model_version)", BAD_AI_EXAMPLE)\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4----watch-it-pass",children:"Step 4 -- Watch it pass"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_aiservice.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".........                                                                [100%]\n9 passed in 0.08s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["All nine tests should pass -- the base case, the missing-field case, the rejected inference mode,\nfour parametrized bad ",(0,r.jsx)(n.code,{children:"model_version"})," values, the batch-mode case, and ",(0,r.jsx)(n.code,{children:"to_summary()"}),"."]}),"\n",(0,r.jsxs)(n.h2,{id:"phase-2----tdd-mlflow-shaped-read-only-endpoints-in-httpclientpy",children:["Phase 2 -- TDD: MLflow-shaped read-only endpoints in ",(0,r.jsx)(n.code,{children:"httpclient.py"})]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"httpclient.py"})," already holds every HTTP integration this project has: GitHub (Module 9/26) and a\nPrometheus/Alertmanager-shaped observability backend (Module 30). This phase adds three more\nfunctions, following the exact retry-then-validate shape every earlier one already uses --\n",(0,r.jsx)(n.code,{children:"get_registered_model()"}),", ",(0,r.jsx)(n.code,{children:"get_model_version()"})," and ",(0,r.jsx)(n.code,{children:"get_run()"}),", each a single ",(0,r.jsx)(n.code,{children:"GET"})," against one of\nMLflow's own documented REST paths."]}),"\n",(0,r.jsx)(n.h3,{id:"step-1----save-three-example-mlflow-responses-as-fixtures",children:"Step 1 -- Save three example MLflow responses as fixtures"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"mkdir -p tests/fixtures/mlflow\n"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",metastring:'title="tests/fixtures/mlflow/registered_model_found.json"',children:'{\n  "registered_model": {\n    "name": "support-assistant-intent-classifier",\n    "creation_timestamp": 1722500000000,\n    "last_updated_timestamp": 1722900000000,\n    "latest_versions": [\n      {\n        "name": "support-assistant-intent-classifier",\n        "version": "4",\n        "current_stage": "Production",\n        "status": "READY",\n        "run_id": "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6",\n        "source": "s3://mlflow-artifacts/4/a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6/artifacts/model"\n      }\n    ]\n  }\n}\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",metastring:'title="tests/fixtures/mlflow/model_version_ready.json"',children:'{\n  "model_version": {\n    "name": "support-assistant-intent-classifier",\n    "version": "4",\n    "current_stage": "Production",\n    "status": "READY",\n    "run_id": "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6",\n    "source": "s3://mlflow-artifacts/4/a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6/artifacts/model"\n  }\n}\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",metastring:'title="tests/fixtures/mlflow/model_version_pending.json"',children:'{\n  "model_version": {\n    "name": "support-assistant-intent-classifier",\n    "version": "5",\n    "current_stage": "None",\n    "status": "PENDING_REGISTRATION",\n    "run_id": "c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8",\n    "source": "s3://mlflow-artifacts/5/c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8/artifacts/model"\n  }\n}\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",metastring:'title="tests/fixtures/mlflow/run_finished.json"',children:'{\n  "run": {\n    "info": {\n      "run_id": "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6",\n      "run_name": "intent-classifier-retrain-2026-08-01",\n      "status": "FINISHED",\n      "start_time": 1722500000000,\n      "end_time": 1722503600000\n    },\n    "data": {\n      "metrics": [\n        { "key": "accuracy", "value": 0.94, "timestamp": 1722503600000, "step": 0 },\n        { "key": "f1_score", "value": 0.91, "timestamp": 1722503600000, "step": 0 }\n      ]\n    }\n  }\n}\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-json",metastring:'title="tests/fixtures/mlflow/run_failed.json"',children:'{\n  "run": {\n    "info": {\n      "run_id": "b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7",\n      "run_name": "intent-classifier-retrain-2026-08-05",\n      "status": "FAILED",\n      "start_time": 1722900000000,\n      "end_time": 1722901200000\n    },\n    "data": {\n      "metrics": []\n    }\n  }\n}\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2----write-the-failing-tests",children:"Step 2 -- Write the failing tests"}),"\n",(0,r.jsxs)(n.p,{children:["Add the new import names to ",(0,r.jsx)(n.code,{children:"tests/test_httpclient.py"}),"'s existing ",(0,r.jsx)(n.code,{children:"from platformops.httpclient import (...)"})," block -- ",(0,r.jsx)(n.code,{children:"get_model_version"}),", ",(0,r.jsx)(n.code,{children:"get_registered_model"})," and ",(0,r.jsx)(n.code,{children:"get_run"}),", kept in\nalphabetical order with the imports already there."]}),"\n",(0,r.jsxs)(n.p,{children:["Then add one new line directly below the existing ",(0,r.jsx)(n.code,{children:'OBS_FIXTURES = Path(__file__).parent / "fixtures" / "observability"'})," line:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_httpclient.py (one new line, directly below the existing OBS_FIXTURES line)"',children:'MLFLOW_FIXTURES = Path(__file__).parent / "fixtures" / "mlflow"\n'})}),"\n",(0,r.jsxs)(n.p,{children:["And one new helper function directly below the existing ",(0,r.jsx)(n.code,{children:"_obs_fixture()"})," function, above ",(0,r.jsx)(n.code,{children:"_fixture()"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_httpclient.py (new helper, directly below the existing _obs_fixture() function)"',children:"def _mlflow_fixture(name: str) -> dict:\n    return json.loads((MLFLOW_FIXTURES / name).read_text())\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Then append this whole block to the end of ",(0,r.jsx)(n.code,{children:"tests/test_httpclient.py"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_httpclient.py (append at the end of the file)"',children:'\n\n# ---------------------------------------------------------------------------\n# get_registered_model / get_model_version / get_run -- MLflow\'s own REST API\n# shape (Model Registry + Tracking). Module 33 talks to these the same way\n# query_metrics() talks to Prometheus\'s shape: real endpoint paths, a real\n# response envelope, validated the moment it crosses into this program.\n# ---------------------------------------------------------------------------\n\nMLFLOW_BASE_URL = "http://localhost:5000"\n\n\n@respx.mock\ndef test_get_registered_model_returns_the_latest_version_summary():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/registered-models/get").mock(\n        return_value=httpx.Response(\n            200, json=_mlflow_fixture("registered_model_found.json")\n        )\n    )\n\n    model = get_registered_model(MLFLOW_BASE_URL, "support-assistant-intent-classifier")\n\n    assert model["name"] == "support-assistant-intent-classifier"\n    assert len(model["latest_versions"]) == 1\n    assert model["latest_versions"][0]["version"] == "4"\n    assert model["latest_versions"][0]["current_stage"] == "Production"\n\n\n@respx.mock\ndef test_get_registered_model_raises_endpoint_status_error_on_404(monkeypatch):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/registered-models/get").mock(\n        return_value=httpx.Response(404, json={"error_code": "RESOURCE_DOES_NOT_EXIST"})\n    )\n\n    with pytest.raises(EndpointStatusError) as excinfo:\n        get_registered_model(MLFLOW_BASE_URL, "no-such-model")\n    assert excinfo.value.status_code == 404\n\n\n@respx.mock\ndef test_get_registered_model_raises_response_format_error_on_malformed_body():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/registered-models/get").mock(\n        return_value=httpx.Response(200, json={"unexpected": "shape"})\n    )\n\n    with pytest.raises(ResponseFormatError):\n        get_registered_model(MLFLOW_BASE_URL, "support-assistant-intent-classifier")\n\n\n@respx.mock\ndef test_get_model_version_returns_status_and_run_id():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(\n            200, json=_mlflow_fixture("model_version_ready.json")\n        )\n    )\n\n    version = get_model_version(\n        MLFLOW_BASE_URL, "support-assistant-intent-classifier", "4"\n    )\n\n    assert version["status"] == "READY"\n    assert version["run_id"] == "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6"\n\n\n@respx.mock\ndef test_get_model_version_reports_a_pending_registration_honestly():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(\n            200, json=_mlflow_fixture("model_version_pending.json")\n        )\n    )\n\n    version = get_model_version(\n        MLFLOW_BASE_URL, "support-assistant-intent-classifier", "5"\n    )\n\n    assert version["status"] == "PENDING_REGISTRATION"\n\n\n@respx.mock\ndef test_get_run_returns_status_and_metrics():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_mlflow_fixture("run_finished.json"))\n    )\n\n    run = get_run(MLFLOW_BASE_URL, "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6")\n\n    assert run["status"] == "FINISHED"\n    assert {m["key"]: m["value"] for m in run["metrics"]} == {\n        "accuracy": 0.94,\n        "f1_score": 0.91,\n    }\n\n\n@respx.mock\ndef test_get_run_reports_a_failed_run_honestly_not_as_an_http_error():\n    respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_mlflow_fixture("run_failed.json"))\n    )\n\n    run = get_run(MLFLOW_BASE_URL, "b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7")\n\n    assert run["status"] == "FAILED"\n    assert run["metrics"] == []\n\n\n@respx.mock\ndef test_get_run_retries_a_503_and_succeeds_on_the_next_attempt(monkeypatch):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    route = respx.get(f"{MLFLOW_BASE_URL}/api/2.0/mlflow/runs/get")\n    route.side_effect = [\n        httpx.Response(503),\n        httpx.Response(200, json=_mlflow_fixture("run_finished.json")),\n    ]\n\n    run = get_run(MLFLOW_BASE_URL, "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6")\n\n    assert run["status"] == "FINISHED"\n    assert route.call_count == 2\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-3----watch-it-fail",children:"Step 3 -- Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'uv run pytest tests/test_httpclient.py -q -k "registered_model or model_version or (get_run and not retries)"\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_httpclient.py'.\nHint: make sure your test modules/packages have valid Python names.\nTraceback:\n...\ntests/test_httpclient.py:11: in <module>\n    from platformops.httpclient import (\nE   ImportError: cannot import name 'get_model_version' from 'platformops.httpclient'\n=========================== short test summary info ============================\nERROR tests/test_httpclient.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.18s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["You should see a real ",(0,r.jsx)(n.code,{children:"ImportError: cannot import name 'get_model_version' from 'platformops.httpclient'"})," -- Python raises for the first missing name it reaches while unpacking\nthe import tuple (the three new names were added in alphabetical order, so ",(0,r.jsx)(n.code,{children:"get_model_version"})," comes\nbefore ",(0,r.jsx)(n.code,{children:"get_registered_model"}),"); all three functions are still missing at this point."]}),"\n",(0,r.jsxs)(n.h3,{id:"step-4----add-the-three-functions-to-httpclientpy",children:["Step 4 -- Add the three functions to ",(0,r.jsx)(n.code,{children:"httpclient.py"})]}),"\n",(0,r.jsxs)(n.p,{children:["Append this whole block to the end of ",(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"}),", right after the existing\n",(0,r.jsx)(n.code,{children:"list_alerts()"})," function:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/httpclient.py (append at the end of the file)"',children:'\n\n# ---------------------------------------------------------------------------\n# MLflow\'s own REST API shape (Module 33). This course does not assume a\n# running MLflow tracking server -- it is not in this course\'s list of lab\n# tools -- so these functions\' tests never talk to a real one, the same\n# way `query_metrics()`/`search_logs()`/`list_alerts()` above never talk to a\n# real Prometheus, Loki or Alertmanager. What lives here is a small, typed\n# client against MLflow\'s documented, versioned REST paths\n# (`/api/2.0/mlflow/...`), so a learner who does connect this to a real\n# server later gets working code, not a guess. `platformops.ai_inspect`\n# (Module 33) is the only module that calls these three functions; every one\n# of them is a GET -- this file never registers a model, never transitions a\n# stage, and never creates or updates a run.\n# ---------------------------------------------------------------------------\n\n\nclass ModelVersionInfo(BaseModel):\n    """One registered model version, following MLflow\'s own `ModelVersion` shape.\n\n    `version` stays a string, exactly as MLflow\'s API returns it -- MLflow\n    itself treats a model version as an opaque, ever-increasing identifier,\n    not a number to do arithmetic on. `status` is MLflow\'s own lifecycle\n    value (`READY`, `PENDING_REGISTRATION`, `FAILED_REGISTRATION`); this\n    client reports it as-is and leaves judging it to\n    `platformops.ai_inspect`.\n    """\n\n    name: str\n    version: str\n    current_stage: str\n    status: str\n    run_id: str\n    source: str\n\n\nclass RegisteredModelInfo(BaseModel):\n    """A registered model\'s own record: its name plus its known versions.\n\n    `latest_versions` mirrors MLflow\'s registry response -- one entry per\n    stage that currently has a version assigned to it. An empty list is a\n    real, valid answer: a model registered with no version pushed to any\n    stage yet.\n    """\n\n    name: str\n    latest_versions: list[ModelVersionInfo] = Field(default_factory=list)\n\n\nclass RegisteredModelResponse(BaseModel):\n    """The envelope `GET /api/2.0/mlflow/registered-models/get` returns."""\n\n    registered_model: RegisteredModelInfo\n\n\ndef get_registered_model(\n    base_url: str,\n    name: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> dict[str, Any]:\n    """Fetch one registered model\'s record from MLflow\'s Model Registry API.\n\n    `GET {base_url}/api/2.0/mlflow/registered-models/get?name=...` -- a read\n    of the registry entry itself, not any one version\'s detail (see\n    `get_model_version()` for that). Raises `EndpointStatusError` for a\n    model MLflow has never heard of (a 404), the same honest-failure\n    contract every other function in this module already follows.\n    """\n    url = f"{base_url}/api/2.0/mlflow/registered-models/get"\n    params = {"name": name}\n    logger.debug("fetching registered model %s from %s", name, base_url)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        parsed = RegisteredModelResponse.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info(\n        "fetched registered model %s (%d version(s))",\n        name,\n        len(parsed.registered_model.latest_versions),\n    )\n    return parsed.registered_model.model_dump()\n\n\nclass ModelVersionResponse(BaseModel):\n    """The envelope `GET /api/2.0/mlflow/model-versions/get` returns."""\n\n    model_version: ModelVersionInfo\n\n\ndef get_model_version(\n    base_url: str,\n    name: str,\n    version: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> dict[str, Any]:\n    """Fetch one specific model version\'s status, stage and originating run.\n\n    `GET {base_url}/api/2.0/mlflow/model-versions/get?name=...&version=...`.\n    `status` can be `READY`, `PENDING_REGISTRATION` or\n    `FAILED_REGISTRATION` -- a version mid-registration is a real, known\n    state this function reports as-is; it never waits or retries a business\n    outcome, only a transient network failure (see `_request_with_retry()`).\n    """\n    url = f"{base_url}/api/2.0/mlflow/model-versions/get"\n    params = {"name": name, "version": version}\n    logger.debug("fetching model version %s:%s from %s", name, version, base_url)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        parsed = ModelVersionResponse.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info(\n        "fetched model version %s:%s (status=%s)",\n        name,\n        version,\n        parsed.model_version.status,\n    )\n    return parsed.model_version.model_dump()\n\n\nclass RunMetric(BaseModel):\n    """One logged metric value, following MLflow\'s own `Metric` shape (a metric can be logged more than once; this is one point)."""\n\n    key: str\n    value: float\n    timestamp: int\n    step: int = 0\n\n\nclass RunData(BaseModel):\n    """The `data` half of an MLflow run -- this client only reads `metrics`; params and tags are out of scope here."""\n\n    metrics: list[RunMetric] = Field(default_factory=list)\n\n\nclass RunInfo(BaseModel):\n    """The `info` half of an MLflow run: identity, status and timing.\n\n    `status` is MLflow\'s own run lifecycle value (`RUNNING`, `FINISHED`,\n    `FAILED`, `KILLED`) -- a training run failing is a normal, expected\n    outcome this function reports honestly, not an HTTP error. `end_time`\n    is `None` while a run is still in progress.\n    """\n\n    run_id: str\n    run_name: str = ""\n    status: str\n    start_time: int\n    end_time: int | None = None\n\n\nclass Run(BaseModel):\n    """One MLflow run, combining `info` and `data` exactly as the Tracking API returns them."""\n\n    info: RunInfo\n    data: RunData = Field(default_factory=RunData)\n\n\nclass GetRunResponse(BaseModel):\n    """The envelope `GET /api/2.0/mlflow/runs/get` returns."""\n\n    run: Run\n\n\ndef get_run(\n    base_url: str,\n    run_id: str,\n    *,\n    token: str | None = None,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n) -> dict[str, Any]:\n    """Fetch one training/evaluation run\'s status and logged metrics from MLflow\'s Tracking API.\n\n    `GET {base_url}/api/2.0/mlflow/runs/get?run_id=...`. A `FAILED` or\n    `KILLED` run is a real, valid response body (status 200) -- MLflow\n    tells you what happened to the run, it does not turn a failed\n    experiment into an HTTP error. This function flattens the response to\n    `{"run_id", "run_name", "status", "start_time", "end_time", "metrics"}`\n    for the caller; `platformops.ai_inspect` is the layer that decides\n    whether a given status counts as good news.\n    """\n    url = f"{base_url}/api/2.0/mlflow/runs/get"\n    params = {"run_id": run_id}\n    logger.debug("fetching run %s from %s", run_id, base_url)\n    with httpx.Client(timeout=timeout, headers=_auth_headers(token)) as client:\n        response = _request_with_retry(client, "GET", url, params=params)\n\n    _raise_for_status(response, url)\n\n    try:\n        payload = response.json()\n    except ValueError as exc:\n        raise ResponseFormatError(f"{url} did not return valid JSON") from exc\n\n    try:\n        parsed = GetRunResponse.model_validate(payload)\n    except ValidationError as exc:\n        raise ResponseFormatError(\n            f"{url} response did not match the expected shape: {exc}"\n        ) from exc\n\n    logger.info("fetched run %s (status=%s)", run_id, parsed.run.info.status)\n    return {\n        "run_id": parsed.run.info.run_id,\n        "run_name": parsed.run.info.run_name,\n        "status": parsed.run.info.status,\n        "start_time": parsed.run.info.start_time,\n        "end_time": parsed.run.info.end_time,\n        "metrics": [metric.model_dump() for metric in parsed.run.data.metrics],\n    }\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-5----watch-it-pass",children:"Step 5 -- Watch it pass"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'uv run pytest tests/test_httpclient.py -q -k "registered_model or model_version or (get_run and not retries)"\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".......                                                                  [100%]\n7 passed, 54 deselected in 0.19s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Seven of the eight new tests match this filter -- the ",(0,r.jsx)(n.code,{children:'-k "... and not retries"'})," clause deliberately\nexcludes ",(0,r.jsx)(n.code,{children:"test_get_run_retries_a_503_and_succeeds_on_the_next_attempt"}),", the same way it did in Step 3.\nRun the full ",(0,r.jsx)(n.code,{children:"test_httpclient.py"})," file once more to confirm that eighth test, and nothing else, broke:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_httpclient.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".............................................................            [100%]\n61 passed in 1.30s\n"})}),"\n",(0,r.jsxs)(n.h2,{id:"phase-3----tdd-ai_inspectpy----fetch-evidence-then-judge-it-separately",children:["Phase 3 -- TDD: ",(0,r.jsx)(n.code,{children:"ai_inspect.py"})," -- fetch evidence, then judge it separately"]}),"\n",(0,r.jsxs)(n.p,{children:["This is the fetch/aggregate split you have already used twice: ",(0,r.jsx)(n.code,{children:"releasecheck.py"})," (Module 26) and\n",(0,r.jsx)(n.code,{children:"observability.py"})," (Module 30). Three ",(0,r.jsx)(n.code,{children:"gather_*_evidence()"})," functions are the only functions here\nthat reach outside this module -- into the MLflow-shaped client you just wrote, or into\n",(0,r.jsx)(n.code,{children:"check_health()"})," (Module 9). ",(0,r.jsx)(n.code,{children:"evaluate_ai_workload()"})," is a pure function with no network call\nanywhere in its body."]}),"\n",(0,r.jsx)(n.h3,{id:"step-1----write-the-failing-tests-1",children:"Step 1 -- Write the failing tests"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_ai_inspect.py"',children:'import json\nfrom pathlib import Path\n\nimport httpx\nimport respx\n\nfrom platformops.ai_inspect import (\n    evaluate_ai_workload,\n    gather_endpoint_evidence,\n    gather_model_evidence,\n    gather_run_evidence,\n    inspect_ai_workload,\n)\nfrom platformops.aiservice import GOOD_AI_EXAMPLE, validate_ai_service\n\nFIXTURES = Path(__file__).parent / "fixtures" / "mlflow"\n\n\ndef _fixture(name: str) -> dict:\n    return json.loads((FIXTURES / name).read_text())\n\n\nMLFLOW_URL = "http://localhost:5000"\nMODEL_NAME = "support-assistant-intent-classifier"\nRUN_ID = "a1b2c3d4e5f647a8b9c0d1e2f3a4b5c6"\n\n\n# ---------------------------------------------------------------------------\n# gather_*_evidence -- respx/MockTransport-mocked, one per source.\n# ---------------------------------------------------------------------------\n\n\n@respx.mock\ndef test_gather_model_evidence_reports_a_ready_version():\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(200, json=_fixture("model_version_ready.json"))\n    )\n\n    evidence = gather_model_evidence(MLFLOW_URL, MODEL_NAME, "4")\n\n    assert evidence["fetched"] is True\n    assert evidence["status"] == "READY"\n    assert evidence["run_id"] == RUN_ID\n\n\n@respx.mock\ndef test_gather_model_evidence_degrades_honestly_when_unreachable(monkeypatch):\n    monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        side_effect=httpx.ConnectError("connection refused")\n    )\n\n    evidence = gather_model_evidence(MLFLOW_URL, MODEL_NAME, "4")\n\n    assert evidence["fetched"] is False\n    assert "unreachable" in evidence["error"]\n\n\n@respx.mock\ndef test_gather_run_evidence_reports_a_finished_run():\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_finished.json"))\n    )\n\n    evidence = gather_run_evidence(MLFLOW_URL, RUN_ID)\n\n    assert evidence["fetched"] is True\n    assert evidence["status"] == "FINISHED"\n    assert evidence["metrics"]["accuracy"] == 0.94\n\n\n@respx.mock\ndef test_gather_run_evidence_reports_a_failed_run_as_fetched_not_unreachable():\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_failed.json"))\n    )\n\n    evidence = gather_run_evidence(MLFLOW_URL, "b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7")\n\n    assert evidence["fetched"] is True\n    assert evidence["status"] == "FAILED"\n\n\ndef test_gather_endpoint_evidence_checks_a_reachable_online_endpoint():\n    transport = httpx.MockTransport(lambda request: httpx.Response(200))\n\n    evidence = gather_endpoint_evidence(\n        "https://support-assistant.internal.example.com/v1/predict",\n        "online",\n        transport=transport,\n    )\n\n    assert evidence["checked"] is True\n    assert evidence["ok"] is True\n    assert evidence["status_code"] == 200\n\n\ndef test_gather_endpoint_evidence_skips_a_batch_workload_with_no_standing_endpoint():\n    evidence = gather_endpoint_evidence(\n        "https://support-assistant.internal.example.com/v1/predict", "batch"\n    )\n\n    assert evidence["checked"] is False\n    assert "batch" in evidence["reason"]\n\n\n# ---------------------------------------------------------------------------\n# evaluate_ai_workload -- pure, hand-built evidence, no network.\n# ---------------------------------------------------------------------------\n\n_MODEL_READY = {\n    "fetched": True,\n    "status": "READY",\n    "current_stage": "Production",\n    "run_id": RUN_ID,\n}\n_MODEL_PENDING = {\n    "fetched": True,\n    "status": "PENDING_REGISTRATION",\n    "current_stage": "None",\n    "run_id": RUN_ID,\n}\n_MODEL_UNREACHABLE = {"fetched": False, "error": "boom"}\n\n_RUN_FINISHED = {"fetched": True, "status": "FINISHED", "metrics": {"accuracy": 0.94}}\n_RUN_FAILED = {"fetched": True, "status": "FAILED", "metrics": {}}\n\n_ENDPOINT_HEALTHY = {\n    "checked": True,\n    "ok": True,\n    "status_code": 200,\n    "latency_ms": 42.0,\n    "error": None,\n}\n_ENDPOINT_UNHEALTHY = {\n    "checked": True,\n    "ok": False,\n    "status_code": 503,\n    "latency_ms": 10.0,\n    "error": None,\n}\n_ENDPOINT_UNREACHABLE = {\n    "checked": True,\n    "ok": False,\n    "status_code": None,\n    "latency_ms": None,\n    "error": "connection failed: refused",\n}\n_ENDPOINT_NOT_APPLICABLE = {\n    "checked": False,\n    "reason": "batch inference has no standing endpoint to check",\n}\n\n\ndef test_evaluate_ai_workload_reports_healthy_when_everything_checks_out():\n    report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_READY,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_HEALTHY,\n    )\n\n    assert report.model["status"] == "PASS"\n    assert report.run["status"] == "PASS"\n    assert report.endpoint["status"] == "PASS"\n    assert report.verdict == "healthy"\n    assert report.sources_failed == []\n\n\ndef test_evaluate_ai_workload_reports_unhealthy_when_the_run_failed():\n    report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_READY,\n        run_evidence=_RUN_FAILED,\n        endpoint_evidence=_ENDPOINT_HEALTHY,\n    )\n\n    assert report.run["status"] == "FAIL"\n    assert report.verdict == "unhealthy"\n\n\ndef test_evaluate_ai_workload_marks_endpoint_not_applicable_for_batch_inference():\n    report = evaluate_ai_workload(\n        service="nightly-fraud-scorer",\n        inference_mode="batch",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="spark-batch",\n        model_evidence=_MODEL_READY,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_NOT_APPLICABLE,\n    )\n\n    assert report.endpoint["status"] == "NOT_APPLICABLE"\n    # a batch workload with no endpoint to check is still healthy overall --\n    # NOT_APPLICABLE is never treated as a failed gating section.\n    assert report.verdict == "healthy"\n    assert "endpoint" not in report.sources_ok\n    assert "endpoint" not in report.sources_failed\n\n\ndef test_evaluate_ai_workload_reports_unknown_model_status_honestly():\n    report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="5",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_PENDING,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_HEALTHY,\n    )\n\n    assert report.model["status"] == "UNKNOWN"\n    assert report.verdict == "unhealthy"\n\n\ndef test_evaluate_ai_workload_reports_a_source_it_could_not_fetch_at_all():\n    report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_UNREACHABLE,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_HEALTHY,\n    )\n\n    assert report.model["status"] == "UNKNOWN"\n    assert "model" in report.sources_failed\n    assert "model" not in report.sources_ok\n\n\ndef test_evaluate_ai_workload_distinguishes_unreachable_endpoint_from_unhealthy_endpoint():\n    unreachable_report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_READY,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_UNREACHABLE,\n    )\n    unhealthy_report = evaluate_ai_workload(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name=MODEL_NAME,\n        model_version="4",\n        serving_runtime="vllm",\n        model_evidence=_MODEL_READY,\n        run_evidence=_RUN_FINISHED,\n        endpoint_evidence=_ENDPOINT_UNHEALTHY,\n    )\n\n    assert unreachable_report.endpoint["status"] == "UNKNOWN"\n    assert unhealthy_report.endpoint["status"] == "FAIL"\n\n\n# ---------------------------------------------------------------------------\n# inspect_ai_workload -- the thin orchestrator, fetch chained through evaluate.\n# ---------------------------------------------------------------------------\n\n\n@respx.mock\ndef test_inspect_ai_workload_chains_the_models_run_id_into_the_run_lookup():\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(200, json=_fixture("model_version_ready.json"))\n    )\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_finished.json"))\n    )\n    service = validate_ai_service(GOOD_AI_EXAMPLE)\n\n    report = inspect_ai_workload(\n        service,\n        mlflow_base_url=MLFLOW_URL,\n        endpoint_transport=httpx.MockTransport(lambda request: httpx.Response(200)),\n    )\n\n    assert report.model["status"] == "PASS"\n    assert report.run["status"] == "PASS"\n    assert report.verdict == "healthy"\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-2----watch-it-fail-1",children:"Step 2 -- Watch it fail"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_ai_inspect.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"ImportError while importing test module '.../tests/test_ai_inspect.py'.\nHint: make sure your test modules/packages have valid Python names.\nTraceback:\n...\ntests/test_ai_inspect.py:7: in <module>\n    from platformops.ai_inspect import (\nE   ModuleNotFoundError: No module named 'platformops.ai_inspect'\n=========================== short test summary info ============================\nERROR tests/test_ai_inspect.py\n!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\n1 error in 0.08s\n"})}),"\n",(0,r.jsxs)(n.p,{children:["You should see a real ",(0,r.jsx)(n.code,{children:"ModuleNotFoundError: No module named 'platformops.ai_inspect'"}),"."]}),"\n",(0,r.jsx)(n.h3,{id:"step-3----write-the-module",children:"Step 3 -- Write the module"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="src/platformops/ai_inspect.py"',children:'"""platformops.ai_inspect -- read a model version\'s registry state, its training run, and its serving endpoint, honestly.\n\n`observability.py` (Module 30) already drew the line between "no data" and\n"could not check" for metrics, logs and alerts. An AI-serving workload needs\nthat same discipline applied to three different questions: is the exact\nmodel version this deployment claims to run actually registered and ready\n(`get_model_version()`), did the training run behind it finish cleanly\n(`get_run()`), and -- only for a workload with a live endpoint -- does that\nendpoint answer requests (`check_health()`, Module 9). None of these three\nquestions is "is the model any good." Judging accuracy, drift or bias is\ndata-science work this toolkit deliberately never does; this module answers\nthe operational questions an SRE or platform engineer is actually paged\nfor: is the thing we deployed the thing we meant to deploy, and is it up.\n\nThe fetch/aggregate split every prior read-only inspector in this project\nuses (`cloudaudit.py`, M24; `releasecheck.py`, M26; `observability.py`, M30)\ncarries over exactly. `gather_model_evidence()`, `gather_run_evidence()`\nand `gather_endpoint_evidence()` are the only functions here that reach\noutside this module -- into `platformops.httpclient`\'s MLflow-shaped\nfunctions or `check_health()`. Each one catches every way its call can\nfail and hands back a plain dict of raw facts, or an honest\n`"fetched": False`. `evaluate_ai_workload()` is the other half: a pure\nfunction with no network call anywhere in its body, that turns three\nalready-gathered evidence dicts into one `AIWorkloadReport`.\n\nOne judgment call is specific to this module: batch inference has no\nstanding endpoint to poll. A nightly scoring job that reads from a queue\nand writes results to a table is not "down" just because nothing answers\non port 443 right now -- there was never anything meant to answer there.\n`gather_endpoint_evidence()` reports that case as `"checked": False`, and\n`evaluate_ai_workload()` turns it into a `NOT_APPLICABLE` section, never a\n`FAIL` -- the same discipline `cloudremediate.py` (Module 25) already\napplies to a rule type that is not remediable by design: a question this\nmodule was never meant to answer honestly says so, instead of guessing.\n\nThis module makes no write call of any kind. It never registers a model\nversion, never transitions a stage, and never restarts a serving process --\nsee the Deep Dive for the mechanical proof.\n"""\n\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nfrom typing import Any\n\nimport httpx\n\nfrom platformops.aiservice import AIServiceDefinition\nfrom platformops.httpclient import (\n    DEFAULT_TIMEOUT,\n    EndpointStatusError,\n    EndpointUnreachableError,\n    HttpCheckError,\n    ResponseFormatError,\n    check_health,\n    get_model_version,\n    get_run,\n)\n\n_FETCHABLE_HTTP_ERRORS = (\n    EndpointStatusError,\n    EndpointUnreachableError,\n    ResponseFormatError,\n    HttpCheckError,\n)\n\n# MLflow\'s own model version lifecycle. A version can sit in\n# `PENDING_REGISTRATION` for a moment after being pushed -- a real, known\n# state, not a failure -- which is why it is not in either set below and\n# falls through to an honest `UNKNOWN` in `_evaluate_model()`.\n_READY_MODEL_STATUSES = {"READY"}\n_FAILED_MODEL_STATUSES = {"FAILED_REGISTRATION"}\n\n# MLflow\'s own run lifecycle. `RUNNING` and `SCHEDULED` are real, known,\n# in-progress states -- not a failure, and not proof of success either --\n# so they also fall through to `UNKNOWN`.\n_SUCCESSFUL_RUN_STATUSES = {"FINISHED"}\n_FAILED_RUN_STATUSES = {"FAILED", "KILLED"}\n\n\n# ---------------------------------------------------------------------------\n# Fetch -- one function per evidence source. These, and only these, call\n# into platformops.httpclient. Every one of them catches its own failures\n# and hands back a plain dict of raw facts -- never a verdict.\n# ---------------------------------------------------------------------------\n\n\ndef gather_model_evidence(\n    mlflow_base_url: str,\n    registered_model_name: str,\n    model_version: str,\n    *,\n    token: str | None = None,\n) -> dict[str, Any]:\n    """Fetch one model version\'s registry status and originating run id."""\n    try:\n        version = get_model_version(\n            mlflow_base_url, registered_model_name, model_version, token=token\n        )\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    return {\n        "fetched": True,\n        "status": version["status"],\n        "current_stage": version["current_stage"],\n        "run_id": version["run_id"],\n    }\n\n\ndef gather_run_evidence(\n    mlflow_base_url: str, run_id: str, *, token: str | None = None\n) -> dict[str, Any]:\n    """Fetch one training run\'s status and logged metrics. A `FAILED` run is a fetched, honest answer, not a failure to fetch."""\n    try:\n        run = get_run(mlflow_base_url, run_id, token=token)\n    except _FETCHABLE_HTTP_ERRORS as exc:\n        return {"fetched": False, "error": str(exc)}\n    return {\n        "fetched": True,\n        "status": run["status"],\n        "metrics": {metric["key"]: metric["value"] for metric in run["metrics"]},\n    }\n\n\ndef gather_endpoint_evidence(\n    endpoint: str,\n    inference_mode: str,\n    *,\n    timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n    transport: httpx.BaseTransport | None = None,\n) -> dict[str, Any]:\n    """Health-check a live serving endpoint -- skipped, honestly, for a batch workload with no endpoint to poll."""\n    if inference_mode == "batch":\n        return {\n            "checked": False,\n            "reason": "batch inference has no standing endpoint to check",\n        }\n    result = check_health(endpoint, timeout=timeout, transport=transport)\n    return {\n        "checked": True,\n        "ok": result.ok,\n        "status_code": result.status_code,\n        "latency_ms": result.latency_ms,\n        "error": result.error,\n    }\n\n\n# ---------------------------------------------------------------------------\n# Aggregate -- pure functions. Nothing below this line calls httpclient,\n# check_health, or a gather_* function.\n# ---------------------------------------------------------------------------\n\n\n@dataclass\nclass AIWorkloadReport:\n    """One AI workload\'s combined model/run/endpoint picture.\n\n    `endpoint` carries `status="NOT_APPLICABLE"` for a batch workload --\n    it is never folded into `sources_failed`, and it never drags the\n    overall `verdict` to `"unhealthy"` on its own. `sources_ok` and\n    `sources_failed` only ever name `"model"` and `"run"`, plus\n    `"endpoint"` when this workload actually has one to check.\n    """\n\n    service: str\n    inference_mode: str\n    registered_model_name: str\n    model_version: str\n    serving_runtime: str\n    model: dict[str, Any]\n    run: dict[str, Any]\n    endpoint: dict[str, Any]\n    verdict: str\n    sources_ok: list[str]\n    sources_failed: list[str]\n\n\ndef _evaluate_model(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "model version evidence unavailable"),\n        }\n    status = evidence["status"]\n    if status in _READY_MODEL_STATUSES:\n        return {\n            "status": "PASS",\n            "detail": f"model version is READY (stage={evidence[\'current_stage\']})",\n        }\n    if status in _FAILED_MODEL_STATUSES:\n        return {"status": "FAIL", "detail": f"model version registration {status}"}\n    return {"status": "UNKNOWN", "detail": f"model version status is {status}"}\n\n\ndef _evaluate_run(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("fetched"):\n        return {\n            "status": "UNKNOWN",\n            "detail": evidence.get("error", "run evidence unavailable"),\n        }\n    status = evidence["status"]\n    if status in _SUCCESSFUL_RUN_STATUSES:\n        return {"status": "PASS", "detail": "training run FINISHED"}\n    if status in _FAILED_RUN_STATUSES:\n        return {"status": "FAIL", "detail": f"training run {status}"}\n    return {"status": "UNKNOWN", "detail": f"training run status is {status}"}\n\n\ndef _evaluate_endpoint(evidence: dict[str, Any]) -> dict[str, Any]:\n    if not evidence.get("checked"):\n        return {\n            "status": "NOT_APPLICABLE",\n            "detail": evidence.get("reason", "endpoint not checked"),\n        }\n    if evidence["ok"]:\n        return {\n            "status": "PASS",\n            "detail": f"endpoint healthy -- {evidence[\'status_code\']} in {evidence[\'latency_ms\']}ms",\n        }\n    if evidence["error"]:\n        # No response came back at all -- a DNS failure, a refused\n        # connection, a timeout. This is "could not check", not "checked\n        # and it is down": the same UNKNOWN/FAIL split observability.py\n        # draws between an unreachable backend and one that answered.\n        return {\n            "status": "UNKNOWN",\n            "detail": f"endpoint unreachable -- {evidence[\'error\']}",\n        }\n    return {\n        "status": "FAIL",\n        "detail": f"endpoint answered {evidence[\'status_code\']} -- not healthy",\n    }\n\n\ndef evaluate_ai_workload(\n    *,\n    service: str,\n    inference_mode: str,\n    registered_model_name: str,\n    model_version: str,\n    serving_runtime: str,\n    model_evidence: dict[str, Any],\n    run_evidence: dict[str, Any],\n    endpoint_evidence: dict[str, Any],\n) -> AIWorkloadReport:\n    """Combine three already-gathered evidence dicts into one report. Pure -- no network call, ever.\n\n    `verdict` is `"healthy"` only when every gating section is `PASS`, or\n    -- for `endpoint` on a batch workload -- `NOT_APPLICABLE`. A source\n    that failed to fetch, or a gating section this run could not judge\n    (`UNKNOWN`), holds `verdict` at `"unhealthy"` the same way an `UNKNOWN`\n    section holds `releasecheck.py`\'s verdict at `"not_ready"` -- incomplete\n    evidence is never treated as good evidence.\n    """\n    model_section = _evaluate_model(model_evidence)\n    run_section = _evaluate_run(run_evidence)\n    endpoint_section = _evaluate_endpoint(endpoint_evidence)\n\n    sources = {"model": model_evidence, "run": run_evidence}\n    sources_ok = [name for name, ev in sources.items() if ev.get("fetched")]\n    sources_failed = [name for name, ev in sources.items() if not ev.get("fetched")]\n    if endpoint_evidence.get("checked"):\n        sources_ok.append("endpoint")\n\n    gating_sections = [model_section, run_section]\n    if endpoint_section["status"] != "NOT_APPLICABLE":\n        gating_sections.append(endpoint_section)\n    has_fail = any(section["status"] == "FAIL" for section in gating_sections)\n    has_unknown = any(section["status"] == "UNKNOWN" for section in gating_sections)\n    verdict = "healthy" if not has_fail and not has_unknown else "unhealthy"\n\n    return AIWorkloadReport(\n        service=service,\n        inference_mode=inference_mode,\n        registered_model_name=registered_model_name,\n        model_version=model_version,\n        serving_runtime=serving_runtime,\n        model=model_section,\n        run=run_section,\n        endpoint=endpoint_section,\n        verdict=verdict,\n        sources_ok=sources_ok,\n        sources_failed=sources_failed,\n    )\n\n\ndef inspect_ai_workload(\n    service: AIServiceDefinition,\n    *,\n    mlflow_base_url: str,\n    token: str | None = None,\n    endpoint_timeout: httpx.Timeout | float = DEFAULT_TIMEOUT,\n    endpoint_transport: httpx.BaseTransport | None = None,\n) -> AIWorkloadReport:\n    """Gather every evidence source for `service` and evaluate it -- the one call a CLI command or script needs.\n\n    The model version\'s evidence names the run that trained it\n    (`run_id`); this function reads that id and chains it straight into\n    `gather_run_evidence()` -- the same real MLflow workflow a person\n    follows by hand: look up the version, follow it to the run, check the\n    run\'s own outcome. When the model version could not be fetched at all,\n    there is no `run_id` to chain from -- the run section reports its own\n    honest `"fetched": False` rather than guessing which run to ask about.\n    This is intentionally a thin orchestrator, the same shape\n    `observability.inspect_observability()` and\n    `releasecheck.run_release_check()` already established.\n    """\n    model_evidence = gather_model_evidence(\n        mlflow_base_url,\n        service.registered_model_name,\n        service.model_version,\n        token=token,\n    )\n\n    run_id = model_evidence.get("run_id") if model_evidence.get("fetched") else None\n    if run_id is not None:\n        run_evidence = gather_run_evidence(mlflow_base_url, run_id, token=token)\n    else:\n        run_evidence = {\n            "fetched": False,\n            "error": "no run id available -- model version evidence unavailable",\n        }\n\n    endpoint_evidence = gather_endpoint_evidence(\n        service.endpoint,\n        service.inference_mode,\n        timeout=endpoint_timeout,\n        transport=endpoint_transport,\n    )\n\n    return evaluate_ai_workload(\n        service=service.name,\n        inference_mode=service.inference_mode,\n        registered_model_name=service.registered_model_name,\n        model_version=service.model_version,\n        serving_runtime=service.serving_runtime,\n        model_evidence=model_evidence,\n        run_evidence=run_evidence,\n        endpoint_evidence=endpoint_evidence,\n    )\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-4----watch-it-pass-1",children:"Step 4 -- Watch it pass"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_ai_inspect.py -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".............                                                            [100%]\n13 passed in 0.21s\n"})}),"\n",(0,r.jsx)(n.p,{children:"All 13 tests should pass -- six fetch tests, six pure-evaluation tests, and one that proves the\nrun id chains from the model lookup into the run lookup."}),"\n",(0,r.jsx)(n.h2,{id:"phase-4----wire-the-cli-then-run-it-against-three-real-scenarios",children:"Phase 4 -- Wire the CLI, then run it against three real scenarios"}),"\n",(0,r.jsxs)(n.h3,{id:"step-1----add-the-ai-inspect-command",children:["Step 1 -- Add the ",(0,r.jsx)(n.code,{children:"ai-inspect"})," command"]}),"\n",(0,r.jsxs)(n.p,{children:["Add the two new imports to ",(0,r.jsx)(n.code,{children:"cli.py"}),"'s existing ",(0,r.jsx)(n.code,{children:"from platformops import __version__, multiregion"}),"\nblock -- insert them directly below it, in alphabetical order with the imports that already follow:"]}),"\n",(0,r.jsxs)(n.p,{children:['```python title="src/platformops/cli.py (new imports, right after the existing ',(0,r.jsx)(n.code,{children:"from platformops import __version__, multiregion"}),' line)"\nfrom platformops.ai_inspect import inspect_ai_workload\nfrom platformops.aiservice import validate_ai_service']}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{children:'\nInsert this command in `cli.py` immediately above the existing `@app.command("api-serve")` block,\nwith two blank lines above it and two blank lines below it -- the same spacing every other pair of\ntop-level functions in this file already uses:\n\n```python title="src/platformops/cli.py (additions, added just above the `api-serve` command)"\n@app.command("ai-inspect")\ndef ai_inspect_cmd(\n    ctx: typer.Context,\n    path: Annotated[\n        Path, typer.Argument(help="path to an AI service definition YAML file")\n    ],\n    mlflow_url: Annotated[\n        str,\n        typer.Option(\n            "--mlflow-url",\n            help="MLflow tracking server base URL, e.g. http://localhost:5000",\n        ),\n    ] = "http://localhost:5000",\n    as_json: Annotated[\n        bool,\n        typer.Option(\n            "--json", help="print a machine-readable JSON report instead of plain text"\n        ),\n    ] = False,\n) -> None:\n    """Report a model version\'s registry state, its training run\'s outcome, and its serving endpoint\'s health.\n\n    Read-only: this command never registers a model version, never\n    transitions a stage, and never restarts a serving process -- see\n    `platformops.ai_inspect` for the fetch/aggregate split, and its Deep\n    Dive for the mechanical proof. An `online` service\'s endpoint is\n    health-checked with the same `check_health()` this toolkit already\n    uses (Module 9); a `batch` service\'s endpoint section reports\n    `NOT_APPLICABLE` -- a batch job with no standing endpoint is not "down".\n    Exit 0 for `healthy`, 1 for `unhealthy`, 2 when the file itself is not a\n    valid AI service definition.\n    """\n    quiet = ctx.obj["quiet"]\n\n    try:\n        data = load_yaml_dict(path)\n    except ConfigError as exc:\n        _fail(str(exc), as_json=as_json, exit_code=2)\n\n    result = validate_ai_service(data)\n    if isinstance(result, list):\n        _fail(\n            f"{path} is not a valid AI service definition -- {len(result)} field(s) failed",\n            as_json=as_json,\n            exit_code=2,\n        )\n        # unreachable -- _fail() always raises; this line only narrows `result` for mypy\n        raise typer.Exit(code=2)\n\n    report = inspect_ai_workload(\n        result,\n        mlflow_base_url=mlflow_url,\n        token=os.environ.get("MLFLOW_TRACKING_TOKEN"),\n    )\n\n    if as_json:\n        typer.echo(\n            json.dumps(\n                {\n                    "status": "ok",\n                    "service": report.service,\n                    "inference_mode": report.inference_mode,\n                    "registered_model_name": report.registered_model_name,\n                    "model_version": report.model_version,\n                    "serving_runtime": report.serving_runtime,\n                    "verdict": report.verdict,\n                    "model": report.model,\n                    "run": report.run,\n                    "endpoint": report.endpoint,\n                    "sources_ok": report.sources_ok,\n                    "sources_failed": report.sources_failed,\n                },\n                indent=2,\n            )\n        )\n        raise typer.Exit(code=0 if report.verdict == "healthy" else 1)\n\n    if not quiet or report.verdict != "healthy":\n        typer.echo(\n            f"{report.service} ({report.inference_mode}, "\n            f"{report.registered_model_name}:{report.model_version} on "\n            f"{report.serving_runtime}): {report.verdict.upper()}"\n        )\n    for label, section in (\n        ("model", report.model),\n        ("run", report.run),\n        ("endpoint", report.endpoint),\n    ):\n        typer.echo(f"  [{section[\'status\']}] {label}: {section[\'detail\']}")\n    if report.sources_failed:\n        typer.echo(f"  unreachable source(s): {\', \'.join(report.sources_failed)}")\n    raise typer.Exit(code=0 if report.verdict == "healthy" else 1)\n'})}),"\n",(0,r.jsxs)(n.p,{children:["Add matching tests. Append this block to the end of ",(0,r.jsx)(n.code,{children:"tests/test_cli.py"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-python",metastring:'title="tests/test_cli.py (append at the end of the file)"',children:'\n\nAI_SERVICE_YAML_TEXT = (\n    "name: support-assistant\\n"\n    "team_owner: ml-platform-team\\n"\n    "model_registry: mlflow\\n"\n    "registered_model_name: support-assistant-intent-classifier\\n"\n    \'model_version: "4"\\n\'\n    "serving_runtime: vllm\\n"\n    "endpoint: https://support-assistant.internal.example.com/v1/predict\\n"\n    "inference_mode: online\\n"\n    "observability:\\n"\n    "  dashboard_url: https://grafana.example.com/d/support-assistant\\n"\n    "  alert_channel: \'#support-assistant-alerts\'\\n"\n)\n\n\ndef _fake_ai_workload_report(*, verdict="healthy"):\n    from platformops.ai_inspect import AIWorkloadReport\n\n    model_status = "PASS" if verdict == "healthy" else "FAIL"\n    return AIWorkloadReport(\n        service="support-assistant",\n        inference_mode="online",\n        registered_model_name="support-assistant-intent-classifier",\n        model_version="4",\n        serving_runtime="vllm",\n        model={\n            "status": model_status,\n            "detail": "model version is READY (stage=Production)",\n        },\n        run={"status": "PASS", "detail": "training run FINISHED"},\n        endpoint={"status": "PASS", "detail": "endpoint healthy -- 200 in 42.0ms"},\n        verdict=verdict,\n        sources_ok=["model", "run", "endpoint"],\n        sources_failed=[] if verdict == "healthy" else ["model"],\n    )\n\n\ndef test_ai_inspect_exits_0_for_a_healthy_workload(tmp_path, monkeypatch):\n    service_path = tmp_path / "service-support-assistant.ai.yaml"\n    service_path.write_text(AI_SERVICE_YAML_TEXT)\n    monkeypatch.setattr(\n        cli_module, "inspect_ai_workload", lambda *a, **k: _fake_ai_workload_report()\n    )\n\n    result = runner.invoke(app, ["ai-inspect", str(service_path)])\n\n    assert result.exit_code == 0\n    assert "HEALTHY" in result.stdout\n\n\ndef test_ai_inspect_exits_1_for_an_unhealthy_workload(tmp_path, monkeypatch):\n    service_path = tmp_path / "service-support-assistant.ai.yaml"\n    service_path.write_text(AI_SERVICE_YAML_TEXT)\n    monkeypatch.setattr(\n        cli_module,\n        "inspect_ai_workload",\n        lambda *a, **k: _fake_ai_workload_report(verdict="unhealthy"),\n    )\n\n    result = runner.invoke(app, ["ai-inspect", str(service_path)])\n\n    assert result.exit_code == 1\n    assert "UNHEALTHY" in result.stdout\n\n\ndef test_ai_inspect_json_prints_the_full_report(tmp_path, monkeypatch):\n    service_path = tmp_path / "service-support-assistant.ai.yaml"\n    service_path.write_text(AI_SERVICE_YAML_TEXT)\n    monkeypatch.setattr(\n        cli_module, "inspect_ai_workload", lambda *a, **k: _fake_ai_workload_report()\n    )\n\n    result = runner.invoke(app, ["ai-inspect", str(service_path), "--json"])\n    payload = json.loads(result.stdout)\n\n    assert result.exit_code == 0\n    assert payload["verdict"] == "healthy"\n    assert payload["registered_model_name"] == "support-assistant-intent-classifier"\n    assert payload["model_version"] == "4"\n\n\ndef test_ai_inspect_on_an_invalid_service_definition_exits_2(tmp_path):\n    service_path = tmp_path / "service-bad.ai.yaml"\n    service_path.write_text("name: support-assistant\\n")\n\n    result = runner.invoke(app, ["ai-inspect", str(service_path)])\n\n    assert result.exit_code == 2\n    assert "not a valid AI service definition" in result.output\n\n\ndef test_ai_inspect_on_a_missing_file_exits_2():\n    result = runner.invoke(app, ["ai-inspect", "no-such-file.yaml"])\n\n    assert result.exit_code == 2\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest tests/test_cli.py -q -k ai_inspect\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:".....                                                                    [100%]\n5 passed, 104 deselected in 0.97s\n"})}),"\n",(0,r.jsx)(n.h3,{id:"step-2----create-the-service-definition-file",children:"Step 2 -- Create the service definition file"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-yaml",metastring:'title="service-support-assistant.ai.yaml"',children:'name: support-assistant\nteam_owner: ml-platform-team\nmodel_registry: mlflow\nregistered_model_name: support-assistant-intent-classifier\nmodel_version: "4"\nserving_runtime: vllm\nendpoint: https://support-assistant.internal.example.com/v1/predict\ninference_mode: online\nobservability:\n  dashboard_url: https://grafana.example.com/d/support-assistant\n  alert_channel: "#support-assistant-alerts"\n'})}),"\n",(0,r.jsx)(n.h3,{id:"step-3----run-it-live-a-healthy-workload",children:"Step 3 -- Run it live: a healthy workload"}),"\n",(0,r.jsxs)(n.p,{children:["This lab does not have a real MLflow server or a real ",(0,r.jsx)(n.code,{children:"support-assistant"})," endpoint to point at, so\nthis demo does exactly what a real MLflow server would answer -- through ",(0,r.jsx)(n.code,{children:"respx"}),", wrapped around a\nreal ",(0,r.jsx)(n.code,{children:"platformops ai-inspect"})," invocation, run through the CLI exactly the way a learner would type\nit in a terminal, not through a raw Python function call."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m33_demo_healthy.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/mlflow")\nMLFLOW_URL = "http://localhost:5000"\nENDPOINT_URL = "https://support-assistant.internal.example.com/v1/predict"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(200, json=_fixture("model_version_ready.json"))\n    )\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_finished.json"))\n    )\n    respx.get(ENDPOINT_URL).mock(return_value=httpx.Response(200))\n\n    result = runner.invoke(app, ["ai-inspect", "service-support-assistant.ai.yaml"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m33_demo_healthy.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"support-assistant (online, support-assistant-intent-classifier:4 on vllm): HEALTHY\n  [PASS] model: model version is READY (stage=Production)\n  [PASS] run: training run FINISHED\n  [PASS] endpoint: endpoint healthy -- 200 in 13.1ms\n\nexit code: 0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The exact latency number will differ on your machine -- it is a real, timed local call, not a fixed\nvalue. ",(0,r.jsx)(n.code,{children:"model"})," and ",(0,r.jsx)(n.code,{children:"run"})," are both ",(0,r.jsx)(n.code,{children:"PASS"}),"; ",(0,r.jsx)(n.code,{children:"endpoint"})," is ",(0,r.jsx)(n.code,{children:"PASS"})," too, since this is an ",(0,r.jsx)(n.code,{children:"online"})," workload.\nOverall verdict: ",(0,r.jsx)(n.code,{children:"HEALTHY"}),", exit code ",(0,r.jsx)(n.code,{children:"0"}),"."]}),"\n",(0,r.jsx)(n.h3,{id:"step-4----run-it-live-a-failed-training-run",children:"Step 4 -- Run it live: a failed training run"}),"\n",(0,r.jsx)(n.p,{children:"Same model version, same endpoint -- but this time the run that trained it failed."}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m33_demo_failed_run.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/mlflow")\nMLFLOW_URL = "http://localhost:5000"\nENDPOINT_URL = "https://support-assistant.internal.example.com/v1/predict"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(200, json=_fixture("model_version_ready.json"))\n    )\n    # The run this model version was trained from failed -- a real, honest\n    # answer, not a network problem.\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_failed.json"))\n    )\n    respx.get(ENDPOINT_URL).mock(return_value=httpx.Response(200))\n\n    result = runner.invoke(\n        app, ["ai-inspect", "service-support-assistant.ai.yaml", "--json"]\n    )\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m33_demo_failed_run.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:'{\n  "status": "ok",\n  "service": "support-assistant",\n  "inference_mode": "online",\n  "registered_model_name": "support-assistant-intent-classifier",\n  "model_version": "4",\n  "serving_runtime": "vllm",\n  "verdict": "unhealthy",\n  "model": {\n    "status": "PASS",\n    "detail": "model version is READY (stage=Production)"\n  },\n  "run": {\n    "status": "FAIL",\n    "detail": "training run FAILED"\n  },\n  "endpoint": {\n    "status": "PASS",\n    "detail": "endpoint healthy -- 200 in 12.7ms"\n  },\n  "sources_ok": [\n    "model",\n    "run",\n    "endpoint"\n  ],\n  "sources_failed": []\n}\n\nexit code: 1\n'})}),"\n",(0,r.jsxs)(n.p,{children:["The exact latency number will differ on your machine. ",(0,r.jsx)(n.code,{children:"model"})," is still ",(0,r.jsx)(n.code,{children:"PASS"})," -- the registry entry itself is fine. ",(0,r.jsx)(n.code,{children:"run"})," is ",(0,r.jsx)(n.code,{children:"FAIL"})," -- the training run\nbehind it did not finish cleanly. Overall verdict: ",(0,r.jsx)(n.code,{children:"unhealthy"}),", exit code ",(0,r.jsx)(n.code,{children:"1"}),". Notice ",(0,r.jsx)(n.code,{children:"model"})," being\n",(0,r.jsx)(n.code,{children:"PASS"})," does not save the overall verdict: every gating section has to pass."]}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"sources_failed"})," stays empty here even though ",(0,r.jsx)(n.code,{children:"run"})," reports ",(0,r.jsx)(n.code,{children:"FAIL"})," -- that field only tracks\nwhether each source could be ",(0,r.jsx)(n.em,{children:"reached"})," at all, not what verdict it returned. All three sources\nanswered here, so all three land in ",(0,r.jsx)(n.code,{children:"sources_ok"}),"; ",(0,r.jsx)(n.code,{children:"run"}),"'s ",(0,r.jsx)(n.code,{children:"FAIL"})," is a real, successfully-fetched\nresult, not a connection problem. A source only moves into ",(0,r.jsx)(n.code,{children:"sources_failed"})," when the call itself\ncould not complete -- ",(0,r.jsx)(n.code,{children:"test_evaluate_ai_workload_reports_a_source_it_could_not_fetch_at_all"})," in\nPhase 3 covers exactly that case."]}),"\n",(0,r.jsx)(n.h3,{id:"step-5----run-it-live-a-batch-workload-no-endpoint-to-check",children:"Step 5 -- Run it live: a batch workload, no endpoint to check"}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"nightly-fraud-scorer"})," is ",(0,r.jsx)(n.code,{children:"inference_mode: batch"})," -- no ",(0,r.jsx)(n.code,{children:"respx"})," route is registered for its\nendpoint at all, on purpose, to prove this command never even attempts that request."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > service-nightly-fraud-scorer.ai.yaml << \'YAMLEOF\'\nname: nightly-fraud-scorer\nteam_owner: ml-platform-team\nmodel_registry: mlflow\nregistered_model_name: support-assistant-intent-classifier\nmodel_version: "4"\nserving_runtime: spark-batch\nendpoint: https://support-assistant.internal.example.com/v1/predict\ninference_mode: batch\nobservability:\n  dashboard_url: https://grafana.example.com/d/nightly-fraud-scorer\n  alert_channel: "#fraud-scoring-alerts"\nYAMLEOF\n'})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'cat > /tmp/m33_demo_batch.py << \'PYEOF\'\nimport json\nfrom pathlib import Path\n\nimport httpx\nimport respx\nfrom typer.testing import CliRunner\n\nfrom platformops.cli import app\n\nrunner = CliRunner()\nFIXTURES = Path("tests/fixtures/mlflow")\nMLFLOW_URL = "http://localhost:5000"\n\n\ndef _fixture(name):\n    return json.loads((FIXTURES / name).read_text())\n\n\nwith respx.mock:\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/model-versions/get").mock(\n        return_value=httpx.Response(200, json=_fixture("model_version_ready.json"))\n    )\n    respx.get(f"{MLFLOW_URL}/api/2.0/mlflow/runs/get").mock(\n        return_value=httpx.Response(200, json=_fixture("run_finished.json"))\n    )\n    # No route registered for the endpoint at all -- if this command tried\n    # to reach it, respx would raise, and this demo would fail loudly.\n\n    result = runner.invoke(app, ["ai-inspect", "service-nightly-fraud-scorer.ai.yaml"])\n    print(result.stdout)\n    print("exit code:", result.exit_code)\nPYEOF\nuv run python /tmp/m33_demo_batch.py\n'})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"nightly-fraud-scorer (batch, support-assistant-intent-classifier:4 on spark-batch): HEALTHY\n  [PASS] model: model version is READY (stage=Production)\n  [PASS] run: training run FINISHED\n  [NOT_APPLICABLE] endpoint: batch inference has no standing endpoint to check\n\nexit code: 0\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"endpoint"})," reports ",(0,r.jsx)(n.code,{children:"NOT_APPLICABLE"}),", not ",(0,r.jsx)(n.code,{children:"FAIL"})," -- and the overall verdict is still ",(0,r.jsx)(n.code,{children:"HEALTHY"}),",\nbecause ",(0,r.jsx)(n.code,{children:"model"})," and ",(0,r.jsx)(n.code,{children:"run"})," are both ",(0,r.jsx)(n.code,{children:"PASS"})," and a ",(0,r.jsx)(n.code,{children:"NOT_APPLICABLE"})," endpoint never gates the verdict.\nNo ",(0,r.jsx)(n.code,{children:"respx"})," route existed for the endpoint URL, and no error was raised -- proof this command never\neven attempted that request for a batch workload."]}),"\n",(0,r.jsx)(n.admonition,{title:"Troubleshooting",type:"warning",children:(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"ModuleNotFoundError: No module named 'platformops.aiservice'"})," (or ",(0,r.jsx)(n.code,{children:".ai_inspect"}),")"]})," -- confirm\nyou created the file under ",(0,r.jsx)(n.code,{children:"src/platformops/"}),", not ",(0,r.jsx)(n.code,{children:"tests/"}),", and that the TDD step's file was\nsaved before you ran the test again."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"pydantic_core._pydantic_core.ValidationError"})," mentioning ",(0,r.jsx)(n.code,{children:"model_version"})]})," -- ",(0,r.jsx)(n.code,{children:"model_version"}),"\nonly accepts a plain digit string (",(0,r.jsx)(n.code,{children:'"4"'}),", not ",(0,r.jsx)(n.code,{children:'"v4"'})," or ",(0,r.jsx)(n.code,{children:"4"})," without quotes). Check the YAML file\nquotes it: ",(0,r.jsx)(n.code,{children:'model_version: "4"'}),"."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:["A demo script prints a real ",(0,r.jsx)(n.code,{children:"httpx.ConnectError"})," instead of the fixture data"]})," -- confirm the\n",(0,r.jsx)(n.code,{children:"with respx.mock:"})," block wraps the ",(0,r.jsx)(n.code,{children:"runner.invoke(...)"})," call itself, and that every URL the CLI\nwill actually call (both MLflow endpoints, plus the service's own endpoint for an ",(0,r.jsx)(n.code,{children:"online"}),"\nworkload) has a matching ",(0,r.jsx)(n.code,{children:"respx.get(...).mock(...)"})," line above it."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:["The batch-workload demo (Step 5) raises instead of printing ",(0,r.jsx)(n.code,{children:"NOT_APPLICABLE"})]})," -- if it raised a\nreal connection error, ",(0,r.jsx)(n.code,{children:"gather_endpoint_evidence()"})," is not checking ",(0,r.jsx)(n.code,{children:"inference_mode"})," correctly, or\n",(0,r.jsx)(n.code,{children:"service-nightly-fraud-scorer.ai.yaml"}),"'s ",(0,r.jsx)(n.code,{children:"inference_mode"})," field was not saved as ",(0,r.jsx)(n.code,{children:"batch"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsxs)(n.strong,{children:[(0,r.jsx)(n.code,{children:"ai-inspect"})," reports ",(0,r.jsx)(n.code,{children:"UNKNOWN"})," for ",(0,r.jsx)(n.code,{children:"model"})," or ",(0,r.jsx)(n.code,{children:"run"})," when you expected ",(0,r.jsx)(n.code,{children:"PASS"}),"/",(0,r.jsx)(n.code,{children:"FAIL"})]})," --\n",(0,r.jsx)(n.code,{children:"UNKNOWN"})," means the fetch itself failed (a connection error, a malformed response) or the status\nMLflow reported was not one this module recognizes as a clean pass or a clean failure (for\nexample, a version still ",(0,r.jsx)(n.code,{children:"PENDING_REGISTRATION"}),"). Check which fixture file the mocked route\nreturns."]}),"\n"]})}),"\n",(0,r.jsx)(n.h2,{id:"phase-5----full-quality-gate-and-version-bump",children:"Phase 5 -- Full quality gate and version bump"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run pytest -q\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"........................................................................ [ 13%]\n........................................................................ [ 27%]\n.............................sssss................................sss... [ 41%]\n........................ssssss.......................................... [ 54%]\n........................................................................ [ 68%]\n........................................................................ [ 82%]\n......................ssssssssss........................................ [ 96%]\n...........sssssssss                                                     [100%]\n491 passed, 33 skipped in 30.38s\n"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run ruff check src/ tests/\nuv run ruff format --check src/ tests/\nuv run mypy src/\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"All checks passed!\n61 files already formatted\nSuccess: no issues found in 31 source files\n"})}),"\n",(0,r.jsx)(n.p,{children:"Bump the version and update the changelog:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"uv run python -c \"\nimport re\npath = 'pyproject.toml'\ntext = open(path).read()\ntext = text.replace('version = \\\"2.7.0\\\"', 'version = \\\"2.8.0\\\"', 1)\nopen(path, 'w').write(text)\n\"\nuv sync\nuv run platformops version\n"})}),"\n",(0,r.jsx)(n.p,{children:(0,r.jsx)(n.strong,{children:"Expected output"})}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"platformops 2.8.0\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Add a ",(0,r.jsx)(n.code,{children:"## [2.8.0] - AI Workload Inspector"})," section to ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"}),", above the previous release,\nfollowing the same ",(0,r.jsx)(n.code,{children:"### Added"})," structure every prior release entry already uses."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:'git add -A\ngit commit -m "platformops v2.8.0 -- AI Workload Inspector"\ngit tag v2.8.0\n'})}),"\n",(0,r.jsx)(n.admonition,{title:"Lost your progress? Compare against the checkpoint",type:"info",children:(0,r.jsxs)(n.p,{children:["The instructor's copy of this exact ",(0,r.jsx)(n.code,{children:"v2.8.0"})," end state ships in the labs repository at\n",(0,r.jsx)(n.code,{children:"307-aipython-labs/labs/checkpoints/v2.8.0"}),". If your project stops passing the gates, clone or pull\nthat repository and compare ",(0,r.jsx)(n.code,{children:"src/platformops/aiservice.py"}),", ",(0,r.jsx)(n.code,{children:"src/platformops/ai_inspect.py"}),",\n",(0,r.jsx)(n.code,{children:"src/platformops/httpclient.py"})," and ",(0,r.jsx)(n.code,{children:"src/platformops/cli.py"})," against yours file by file."]})}),"\n",(0,r.jsx)(n.hr,{}),"\n",(0,r.jsx)(n.h2,{id:"teardown",children:"Teardown"}),"\n",(0,r.jsxs)(n.p,{children:["Every evidence source in this lab is a saved MLflow-shaped fixture or a ",(0,r.jsx)(n.code,{children:"respx"}),"-mocked endpoint,\nreplayed in-process -- there is no real MLflow server, no container, and no cluster this lab\nstarted. Remove the demo scripts and the batch-mode example file:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-bash",children:"cd ~/platformops\nrm -f /tmp/m33_demo_healthy.py /tmp/m33_demo_failed_run.py /tmp/m33_demo_batch.py\nrm -f service-nightly-fraud-scorer.ai.yaml\n"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.code,{children:"src/platformops/aiservice.py"}),", ",(0,r.jsx)(n.code,{children:"src/platformops/ai_inspect.py"}),", ",(0,r.jsx)(n.code,{children:"httpclient.py"}),"'s three new\nfunctions, the ",(0,r.jsx)(n.code,{children:"ai-inspect"})," CLI command, ",(0,r.jsx)(n.code,{children:"tests/fixtures/mlflow/"}),",\n",(0,r.jsx)(n.code,{children:"service-support-assistant.ai.yaml"}),", the ",(0,r.jsx)(n.code,{children:"v2.8.0"})," version bump and the ",(0,r.jsx)(n.code,{children:"CHANGELOG.md"})," entry all\nstay -- they are permanent additions to the project, and carry forward into every later module."]})]})}function p(e={}){let{wrapper:n}={...(0,i.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(c,{...e})}):c(e)}},8453(e,n,t){t.d(n,{R:()=>o,x:()=>a});var s=t(6540);let r={},i=s.createContext(r);function o(e){let n=s.useContext(i);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function a(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),s.createElement(i.Provider,{value:n},e.children)}}}]);