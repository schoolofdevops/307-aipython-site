"use strict";(self.webpackChunkcoursesmith_site=self.webpackChunkcoursesmith_site||[]).push([["5501"],{303(e,t,a){a.r(t),a.d(t,{metadata:()=>n,default:()=>u,frontMatter:()=>r,contentTitle:()=>l,toc:()=>c,assets:()=>h});var n=JSON.parse('{"id":"m9-http-apis/quiz","title":"Quiz: HTTP APIs and Integration Automation","description":"<Quiz questions={[","source":"@site/docs/m9-http-apis/quiz.mdx","sourceDirName":"m9-http-apis","slug":"/m9-http-apis/quiz","permalink":"/307-aipython-site/docs/m9-http-apis/quiz","draft":false,"unlisted":false,"tags":[],"version":"current","sidebarPosition":3,"frontMatter":{"sidebar_position":3,"title":"Quiz: HTTP APIs and Integration Automation"},"sidebar":"courseSidebar","previous":{"title":"Lab: HTTP APIs and Integration Automation","permalink":"/307-aipython-site/docs/m9-http-apis/lab"},"next":{"title":"Deep Dive (Part 2)","permalink":"/307-aipython-site/docs/m9-http-apis/deep-dive"}}'),o=a(4848),s=a(8453),i=a(7517);let r={sidebar_position:3,title:"Quiz: HTTP APIs and Integration Automation"},l="HTTP APIs and Integration Automation \u2014 Quiz",h={},c=[];function d(e){let t={h1:"h1",header:"header",...(0,s.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(t.header,{children:(0,o.jsx)(t.h1,{id:"http-apis-and-integration-automation--quiz",children:"HTTP APIs and Integration Automation \u2014 Quiz"})}),"\n",(0,o.jsx)(i.A,{questions:[{prompt:"A popular Python HTTP library, `requests`, has no default timeout at all unless you set one on every call. Why does this course use `httpx` instead, and set `DEFAULT_TIMEOUT` explicitly?",options:[{text:"Because a script with no timeout can hang forever on a server that never answers -- an explicit timeout policy fails fast on purpose",correct:!0,explanation:'This is the module\'s core timeout argument: a client with no timeout is not "safe by default", it is a script that can hang indefinitely the moment a downstream host stops answering. Setting one explicitly is a deliberate choice, not an extra step.'},{text:"Because `httpx` is faster than `requests` at making the same request",correct:!1,explanation:"Raw request speed is not why this module picks `httpx` over `requests` -- the timeout default and async-readiness are the stated reasons."},{text:"Because `requests` cannot make GET requests to a public API",correct:!1,explanation:"`requests` can make the exact same GET requests `httpx` can -- the difference this module cares about is the timeout default, not what requests are possible."},{text:"Because `httpx` does not require an internet connection",correct:!1,explanation:"Both libraries need a real network connection to reach a real API -- neither one works offline."}]},{prompt:"Which of the following are genuine reasons this module distinguishes `httpx.ConnectError`/`httpx.TimeoutException` from `httpx.HTTPStatusError`? (Select all that apply.)",multiSelect:!0,options:[{text:'They mean different things: one is "never got an answer," the other is "got an answer, and the answer is a problem"',correct:!0,explanation:"This is the phone-call analogy from the lesson -- a line that rings with nobody there is a different failure from someone answering and saying no. Collapsing both into one handler throws away exactly this distinction."},{text:"A caller during an incident needs to know whether the network is broken or the service is actively reporting trouble",correct:!0,explanation:'"Is the network down, or is the service up and unhealthy" is a real triage question -- `check_health()` keeps this distinction in `HealthResult.error` versus a bad `status_code` for exactly this reason.'},{text:"Collapsing both into one broad `except Exception` is always a syntax error in Python",correct:!1,explanation:"It is legal Python -- the problem is not that it fails to run, it is that it throws away useful information about which kind of failure actually happened."},{text:"`httpx` refuses to run if both exception types are not handled separately",correct:!1,explanation:"`httpx` does not enforce this -- nothing stops you from writing one broad `except Exception:`. The lesson argues you should not, not that the library forbids it."}]},{prompt:"In `platformops http-check`, what does exit code `2` mean, and how is it different from `1`?",options:[{text:"Exit `2` means the command itself was called wrong (e.g. a URL missing a scheme), caught before any network call; exit `1` means a real problem with the check itself (unhealthy or unreachable)",correct:!0,explanation:"This extends the same `0`/`1`/`2` contract M8 built for `validate` -- `2` is a usage error the command refuses to even attempt, `1` covers both an unhealthy status and a connection failure, since both are genuine problems with the check's outcome."},{text:"Exit `2` means the endpoint returned a 4xx status; exit `1` means it returned a 5xx status",correct:!1,explanation:"The HTTP status code is not what maps to the CLI's own exit code here -- `http-check` exits `1` for any unhealthy or unreachable result, regardless of whether the underlying status was 4xx or 5xx."},{text:'There is no real difference -- both simply mean "something went wrong"',correct:!1,explanation:'A script branching on the exit code needs the two to mean different things -- `2` says "you called this wrong," `1` says "the check itself found a problem."'},{text:"Exit `2` means the request timed out; exit `1` means the request was refused",correct:!1,explanation:"Both a timeout and a refused connection are reported as exit `1` (`error` in `HealthResult`) -- exit `2` is reserved for a usage error caught before any request is attempted at all."}]},{prompt:"GitHub's list endpoints (like the workflow runs this module's `list_workflow_runs()` fetches) cap each response at a fixed page size. What tells a client whether more results exist?",options:[{text:'The `Link` response header, which names a `rel="next"` URL when another page is available',correct:!0,explanation:"This is the RFC 5988 pattern the lesson's library-drawer analogy describes -- `_next_page_url()` parses exactly this header, and `list_workflow_runs()` keeps requesting the `next` URL until the header stops naming one."},{text:"The response body always includes a `total_count` field the client checks against how many it has already fetched",correct:!1,explanation:"GitHub's workflow-runs endpoint does include a total count, but this module's pagination logic follows the `Link` header's `next` relation, not a manual count comparison."},{text:"The client must guess a page count up front and request each one in a fixed loop",correct:!1,explanation:"Guessing a fixed page count is exactly what following the `Link` header avoids -- the server tells the client whether more pages exist, rather than the client assuming a number."},{text:"Every GitHub response includes every result regardless of how many there are",correct:!1,explanation:"This is the wrong assumption the lesson warns against -- reading only the first page silently under-reports the true count."}]},{prompt:"Why does `_request_with_retry()` retry a `429` or a `5xx` response, but never a `404` or a `401`?",options:[{text:"A `429`/`5xx` is often a transient condition that can resolve on its own; a `404`/`401` describes a permanent fact about the request that will not change on a second try",correct:!0,explanation:"This is the elevator-button analogy from the lesson -- pressing again after a brief stall can help, but a wrong floor button will not become the right one no matter how many times you press it. Retrying a permanent client error just wastes time and delays reporting the real problem."},{text:"`429` and `5xx` are numerically higher than `404` and `401`, and this module retries the larger numbers",correct:!1,explanation:"The numeric value is not the reason -- `RETRYABLE_STATUS_CODES` is chosen by what each code actually means (transient vs. permanent), not by comparing the numbers."},{text:"Retrying a `404` would raise a Python exception that cannot be caught",correct:!1,explanation:"A `404` raises `EndpointStatusError`, a normal, catchable exception -- the reason it is not retried is that retrying would not change the outcome, not that the exception is somehow special."},{text:"GitHub's API blocks any client that retries a `401`",correct:!1,explanation:"This module's own design choice not to retry a `401` is not a rule GitHub enforces -- it is a deliberate decision because a permanent auth failure will not resolve itself on a second try."}]},{prompt:"Which statements about this module's retry-with-backoff design are correct? (Select all that apply.)",multiSelect:!0,options:[{text:"The backoff delay grows exponentially with the attempt number",correct:!0,explanation:"`_sleep_backoff()` computes `BASE_BACKOFF_SECONDS * (2 ** attempt)` -- each retry waits roughly twice as long as the one before it."},{text:"A random jitter term is added on top of the exponential delay",correct:!0,explanation:"`random.uniform(0, BASE_BACKOFF_SECONDS)` is added to the exponential term -- this is what stops many clients retrying the same failure from all waking up and retrying in the exact same instant."},{text:"Retries only ever happen on a `GET` request in this module",correct:!0,explanation:'`_request_with_retry()` is only ever called with `"GET"` inside `httpclient.py` -- retries are wired only for the idempotent read path, never for a hypothetical write.'},{text:"Retries continue indefinitely until the request finally succeeds",correct:!1,explanation:"Retries stop at `MAX_RETRIES` -- after that, the function raises `EndpointUnreachableError` (for a connection failure) or lets the caller see the last bad status, rather than retrying forever."}]},{prompt:"Why is it unsafe to blindly wrap a `POST` request (for example, one that creates a deployment or opens an incident) in the same automatic retry loop this module uses for `GET`?",options:[{text:"A `POST` is often not idempotent -- if the first attempt actually succeeded on the server but the response was lost, a blind retry can create a second, duplicate resource",correct:!0,explanation:"This is the idempotency argument from both the lesson and the deep dive: `GET` is safe to repeat because asking twice has the same effect as asking once; a `POST` that creates something usually does not have that property."},{text:"httpx physically cannot send a POST request more than once in the same process",correct:!1,explanation:"httpx has no such restriction -- nothing stops code from retrying a POST; the reason not to is about what happens on the server, not a library limitation."},{text:"A POST request always returns a 429 status, which this module never retries",correct:!1,explanation:"A POST is not inherently rate-limited any more than a GET is -- the concern with retrying POST is about accidentally duplicating a create, not about which status code it happens to return."},{text:"POST requests do not support a `Content-Type` header, so retrying one would fail validation",correct:!1,explanation:"This is unrelated to idempotency -- POST requests commonly carry a body and a `Content-Type` header; the retry-safety concern is entirely about server-side side effects, not headers."}]},{prompt:"This lab's GitHub calls work without any authentication token. What is `_auth_headers()` for, if no token is required?",options:[{text:"It establishes the pattern every real, authenticated API integration needs -- adding a token to a header only when one is given, and never logging its value",correct:!0,explanation:"The lesson is explicit that this pattern matters even when this lab's calls do not need it -- the moment this client points at a private repo or an internal API, a token appears, and this is the one place that value is ever touched."},{text:"It is required by httpx -- every `httpx.Client()` must be constructed with an `Authorization` header",correct:!1,explanation:"httpx has no such requirement -- `check_health()` builds a client with no headers at all. `_auth_headers()` exists for this module's own credential-handling discipline, not a library rule."},{text:"It converts a GitHub username and password into a token automatically",correct:!1,explanation:"`_auth_headers()` takes an already-obtained token as a parameter -- it does not perform any username/password exchange."},{text:"It is only used by `check_health()`, never by `get_repo_info()`",correct:!1,explanation:"`check_health()` is a general-purpose probe against any URL and does not use `_auth_headers()` at all -- it is `get_repo_info()` and `list_workflow_runs()`, the GitHub-specific calls, that build their headers through it."}]},{prompt:"You are reviewing a teammate's change to `_auth_headers()`. Which version follows this module's credential-handling rule?",options:[{text:'`logger.debug("request will carry an authorization token (value withheld from logs)")` -- states that a token is present, never what it is',correct:!0,explanation:"This is exactly what the real code does -- the log line communicates the fact that authentication is happening, which is useful for debugging, without ever putting the secret itself into a log stream."},{text:'`logger.debug("using token %s", token)` -- logs the token so a future debugging session can see exactly what was sent',correct:!1,explanation:"This is the mistake the lesson and lab both warn against -- a token in a log line is a token that can leak into a file, a terminal a colleague is screen-sharing, or a log aggregator nobody meant to hand credentials to."},{text:'`logger.debug("headers: %s", headers)` after `Authorization` has been added to the dict',correct:!1,explanation:"Logging the whole `headers` dict after the `Authorization` key is set logs the token just as directly as logging it by name -- the rule is about what ends up in the log text, not which variable name it came from."},{text:"Never log anything about authentication at all, even that a token was supplied",correct:!1,explanation:"This is safe but overly cautious for no reason -- stating *that* a token is present (never its value) is useful, legitimate debugging information and is exactly what the real code does."}]},{prompt:"What does `RepoInfo.model_validate(...)` in `get_repo_info()` protect against, and which earlier module does this pattern come from?",options:[{text:"A GitHub response missing an expected field or holding an unexpected type -- the same discipline `ServiceDefinition.model_validate(...)` (M5) applies to a YAML file, applied here to a JSON response",correct:!0,explanation:"The lesson makes this comparison directly: never trust a raw dict past the point where it entered your program, whether that dict came from a YAML file or an HTTP response. A mismatch fails loudly right here, as `ResponseFormatError`, instead of surfacing as a confusing `KeyError` later."},{text:"A slow network connection -- it retries the request if validation takes too long",correct:!1,explanation:"Validation is not a network operation and has nothing to do with request speed -- it checks the *shape* of already-received data, after the response has already arrived."},{text:"An expired GitHub token -- it automatically refreshes authentication",correct:!1,explanation:"Token refresh is not something `RepoInfo.model_validate(...)` does -- it validates the JSON payload's shape, unrelated to authentication state."},{text:"A rate-limited response -- it converts a 429 into a successful result",correct:!1,explanation:"A 429 is handled by the retry logic in `_request_with_retry()`, before the response ever reaches JSON parsing or `RepoInfo.model_validate(...)`."}]},{prompt:"While reviewing your coding agent's diff for the Phase 2 extension, which of these would you flag as wrong, based on this module's design? (Select all that apply.)",multiSelect:!0,options:[{text:"`RETRYABLE_STATUS_CODES` includes `401`",correct:!0,explanation:"A `401` is a permanent client error, not a transient one -- including it in the retryable set means the client would waste time retrying a request that will never succeed as-is."},{text:"`max_retries` is bound as a parameter default (`def _request_with_retry(..., max_retries: int = MAX_RETRIES)`) instead of being read from the module constant inside the function body",correct:!0,explanation:"A default evaluated once at import time freezes the value at that moment -- a test (or later code) overriding the module-level `MAX_RETRIES` constant would have no effect, exactly the gotcha this module's own retry helper works around."},{text:"`respx` is added under `[dependency-groups] dev`, not the project's runtime `dependencies`",correct:!1,explanation:"This is correct, not a problem -- `respx` is only needed to run the mocked test suite, never at runtime when a learner actually runs `platformops`."},{text:"`list_workflow_runs()` builds one `httpx.Client()` before its pagination loop and reuses it for every page",correct:!1,explanation:"This is the correct pattern, not a bug -- building the client once lets keep-alive reuse the same connection across pages instead of paying a fresh handshake for every page."}]},{prompt:"A mocked test in `test_httpclient.py` needs to prove that a `429` followed by a `200` succeeds on the retry, without the test actually waiting for the real backoff delay. What does the test do to avoid a slow, flaky test?",options:[{text:"It monkeypatches `time.sleep` inside `platformops.httpclient` so the backoff delay is skipped, while still exercising the real retry logic and the real (short) mocked HTTP responses",correct:!0,explanation:'This is exactly what `monkeypatch.setattr("platformops.httpclient.time.sleep", lambda _: None)` does in the real test suite -- the retry *logic* still runs for real, only the actual waiting is skipped, so the test finishes in milliseconds instead of seconds.'},{text:"It reduces `BASE_BACKOFF_SECONDS` to `0` for the entire test file, changing the constant everyone else's code also uses",correct:!1,explanation:"Changing a shared module constant for every test (rather than monkeypatching just the sleep call for one test) risks masking a real bug in the backoff formula and affecting unrelated tests -- the real approach patches `time.sleep`, not the formula itself."},{text:"It skips testing the retry behavior entirely, since real timing cannot be tested",correct:!1,explanation:"The real test suite does test retry behavior directly (four scenarios: 429-then-success, 500-exhausts, 401-never-retried, connection-failure-exhausts) -- it is entirely testable once the actual waiting is removed from the test run."},{text:"It runs the test against the real GitHub API and waits for a real `429` to occur naturally",correct:!1,explanation:"Waiting for GitHub to actually rate-limit a test on demand is unreliable and slow -- this is exactly why the module moves from live-network tests in the lab's first pass to deterministic, mocked ones in Phase 2."}]},{prompt:"A junior teammate asks why `check_health()` opens a brand-new `httpx.Client()` for every call instead of reusing one client across many calls, the way `list_workflow_runs()` does across its pagination loop. What is the accurate answer?",options:[{text:"`check_health()` is invoked once per CLI process and exits -- there is no second request in that same process for a kept-alive connection to serve, so reuse has nothing to save",correct:!0,explanation:"Connection reuse only pays off when more than one request in the same process talks to the same host. A one-shot `platformops http-check` command makes exactly one request per process, so there is no second call in that process to benefit from keeping the connection open."},{text:"`httpx.Client()` cannot be reused across multiple calls -- each one is single-use by design",correct:!1,explanation:"An `httpx.Client()` is explicitly designed to be reused across many requests -- that is exactly what `list_workflow_runs()` does across its paginated calls, and what makes reuse worthwhile there."},{text:"Reusing a client would make `check_health()` slower because it has to check the connection is still valid first",correct:!1,explanation:"Reuse is the faster path, not the slower one -- it is what avoids paying a fresh TCP and TLS handshake on every request. `check_health()` simply never gets the chance to reuse anything, since it only ever makes one request per process."},{text:"Only GitHub API calls are allowed to reuse a client; arbitrary URLs are not",correct:!1,explanation:"There is no such restriction in httpx or in this module -- the choice is about how many requests happen in one process, not which host is being called."}]}]})]})}function u(e={}){let{wrapper:t}={...(0,s.R)(),...e.components};return t?(0,o.jsx)(t,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},7517(e,t,a){a.d(t,{A:()=>i});var n=a(4848),o=a(6540);function s({q:e,state:t,onToggle:a}){return(0,n.jsxs)("div",{className:"question_QpiT",children:[(0,n.jsx)("p",{className:"prompt_TqRJ",children:e.prompt}),e.multiSelect&&(0,n.jsx)("p",{className:"hint_e_F7",children:"(select all that apply)"}),(0,n.jsx)("ul",{className:"options_X9pq",children:e.options.map((e,o)=>{let s=t.selected.has(o),i="option_qGol";return t.checked?e.correct?i+=" correct_LYA5":s&&(i+=" incorrect_kRFc"):s&&(i+=" chosen_H3WG"),(0,n.jsxs)("li",{children:[(0,n.jsxs)("button",{type:"button",className:i,disabled:t.checked,onClick:()=>a(o),children:[(0,n.jsx)("span",{className:"marker_vN_w",children:s?"\u25CF":"\u25CB"})," ",e.text]}),t.checked&&(s||e.correct)&&e.explanation&&(0,n.jsx)("p",{className:"explanation_HBZ0",children:e.explanation})]},o)})})]})}function i({questions:e}){let[t,a]=(0,o.useState)(e.map(()=>({selected:new Set,checked:!1}))),r=t.every(e=>e.checked),l=t.filter((t,a)=>{var n,o;let s;return n=e[a],o=t.selected,s=n.options.map((e,t)=>e.correct?t:-1).filter(e=>e>=0),n.multiSelect?o.size===s.length&&[...o].every(e=>n.options[e].correct):1===o.size&&s.includes([...o][0])}).length;return(0,n.jsxs)("div",{className:"quiz_I014",children:[e.map((o,i)=>(0,n.jsx)(s,{q:o,state:t[i],onToggle:t=>a(a=>a.map((a,n)=>{if(n!==i)return a;let o=new Set(a.selected);return e[i].multiSelect?o.has(t)?o.delete(t):o.add(t):(o.clear(),o.add(t)),{...a,selected:o}}))},i)),(0,n.jsxs)("div",{className:"actions_XUwt",children:[(0,n.jsx)("button",{type:"button",className:"check_wZ2j",onClick:()=>a(e=>e.map(e=>({...e,checked:!0}))),children:"Check answers"}),(0,n.jsx)("button",{type:"button",className:"resetBtn_JHSa",onClick:()=>a(e.map(()=>({selected:new Set,checked:!1}))),children:"Reset"}),r&&(0,n.jsxs)("span",{className:"score_AN7p",children:["Score: ",l," / ",e.length]})]})]})}},8453(e,t,a){a.d(t,{R:()=>i,x:()=>r});var n=a(6540);let o={},s=n.createContext(o);function i(e){let t=n.useContext(s);return n.useMemo(function(){return"function"==typeof e?e(t):{...t,...e}},[t,e])}function r(e){let t;return t=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:i(e.components),n.createElement(s.Provider,{value:t},e.children)}}}]);